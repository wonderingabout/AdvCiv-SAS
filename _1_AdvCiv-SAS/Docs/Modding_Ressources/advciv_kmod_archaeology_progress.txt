AdvCiv / K-Mod / AdvCiv-SAS historical bug archaeology progress
Created 2026-08-19
Maintained by wonderingabout + LLM/AI helpers

Purpose
-------
Systematically review the historical AdvCiv and then K-Mod commit diffs for inherited bugs, regressions, suspicious behavior changes, and useful fixes that may still matter to AdvCiv-SAS. After completing those inherited-history sweeps, separately review AdvCiv-SAS branch history for surviving regressions and flaws in executable source code.

Primary sources
---------------
For an ordinary clone/Codex/IDE session, use the tracked greppable mirror:
  LLM_Helpers/commit_diffs/INDEX.txt
  LLM_Helpers/commit_diffs/PATH_HISTORY_INDEX.txt
  LLM_Helpers/commit_diffs/<segment>_<practical-count>_<short-sha>.diff

For a light-source handoff, `_SNAPSHOT_CONTEXT/commit_diffs/` is generated fresh from that snapshot's current Git ancestry with the same renderer/filtering policy. Current source and Git remain authoritative; the tracked mirror can lag HEAD until `python LLM_Helpers/sync_tracked_commit_diffs.py` is run.

Both this progress file and the tracked commit-diff mirror are kept in Git for future clone/LLM archaeology but export-ignored from player/release archives. The tracked mirror is excluded from its own generated historical patches to prevent recursive history-of-history growth.

History inventory at tracker creation
-------------------------------------
AdvCivPreSAS: 3,094 commits, practical 4343 -> 1250
  full=2,737, partial=202, summary-only=155
KMod: 1,249 commits, practical 1249 -> 1
  full=1,249
Total historical commits in planned systematic sweep: 4,343

Default sweep order
-------------------
1. AdvCivPreSAS newest -> oldest: practical 4343 down to 1250.
2. KMod newest -> oldest: practical 1249 down to 1.
3. When a finding points across a lineage boundary, trace that specific code farther immediately rather than waiting for the broad sweep to reach it.
4. Do not "fix" a merely suspicious historical choice without understanding current semantics and gameplay impact.
5. After the K-Mod sweep reaches practical 1, run a separate newest-to-oldest systematic sweep over AdvCiv-SAS branch history with the same evidence standard. The SAS phase has its own source-touching-SHA denominator and is not included in the completed 4,343 inherited-history total.
6. The SAS phase is intentionally source-focused: review runtime/game C++ (`*.cpp`, `*.h`) and Python (`*.py`) changes, including map scripts, while normally ignoring XML, Markdown/text documentation, art/binary assets and generated logs. Mixed commits are still reviewed for their executable-source hunks. Developer/CI helper Python can be audited separately but does not consume the primary runtime-source denominator.
7. SAS diffs can contain large CRLF/EOL-only churn, often alongside work produced through Codex/ChatGPT or other tooling. Do not skip such commits or discount them by authorship: treat practical number + SHA as identity and compare the before/after runtime source after normalizing line endings/whitespace noise where necessary. The goal is to reveal the substantive code hidden inside an enormous EOL patch, not to exclude the commit from review.
8. Freeze each SAS sweep inventory at its starting snapshot HEAD so bug-fix commits made during the sweep do not continuously move the denominator. Later commits can be reviewed as a small follow-up delta.

Counters
--------
Systematic historical commits reviewed: 4,343 / 4,343
Targeted lineage traces completed: 5 (KI#205, KI#206, KI#237.2, S002, KI#285)
Confirmed defects tracked here: 132
  inherited/pre-SAS defects: 43
  SAS-specific regressions: 89
Confirmed defects fixed: 97
Confirmed defects with fix prepared/staged, pending user runtime validation: 0
Confirmed defects with broader repair/refactor intentionally pending: 1 (KI#213)
Confirmed defects with surgical repair identified, implementation/documentation deferred: 34 (KI#243, KI#244, KI#246, KI#248, KI#249, KI#250, KI#251, KI#252, KI#253, KI#256, KI#257, KI#258, KI#259, KI#260, KI#261, KI#267, KI#285, KI#286, KI#296, KI#300, KI#304, KI#305, KI#306, KI#307, KI#309, KI#310, KI#311, KI#312, KI#313, KI#314, KI#319, KI#321, KI#322, KI#325)
Resolved non-defect/currently-safe audit candidates: 3 (including rejected KI#324 display-only diagnosis)
Open current-tree suspects awaiting archaeology/semantic decision: 0
Unfinished K-Mod final-block candidates to recheck before closure: 0
Confirmed defects newly found by the current XML-cost audit: 1 (KI#206)
Confirmed previously-unfixed defects newly found by the systematic historical sweep: 6 (KI#207, KI#208, KI#209, KI#210, KI#211, KI#212)
Prior reference defects already found before this tracker: 2 (the two independent KI#205 defects)

AdvCiv-SAS runtime-source sweep (phase 2)
-----------------------------------------
Starting snapshot boundary: SAS practical 6168, SHA cf1272d7fed57ee154d84305c2d1bb8937ae4e58.
SASBranch SHAs reachable in that snapshot: 1,825.
Primary runtime-source SHAs (`CvGameCoreDLL` C++/headers, runtime `Assets/Python`, `PrivateMaps` and equivalent game-executed Python): 1,292.
Additional SHAs touching only developer/CI/helper Python: 20; excluded from the primary denominator unless separately audited.
SHAs with no primary runtime C++/Python change and not helper-only: 513; intentionally skipped by this source-focused phase.
Runtime-source SHAs systematically reviewed: 1,292 / 1,292.
Sweep order: newest -> oldest.
SAS primary runtime-source sweep status: COMPLETE.
Current certified SAS source cursor: practical 4345, SHA e490c254d0.
Next runtime-source SHA / current OPEN audit: none; primary runtime-source inventory exhausted.
New surviving defects found by SAS source sweep so far, grouped in five-number ranges:
  KI#213-KI#217: KI#213 is the one confirmed SAS-specific defect with a broader refactor pending; KI#214 fixed in commit 962e912da; KI#215, KI#216 and KI#217 fixed in SAS practical 6170 / 79410b2dba.
  KI#218-KI#222: KI#218, KI#219 and KI#220 fixed in SAS practical 6171 / 53ee909e99; KI#221 and KI#222 fixed in SAS practical 6172 / c36032deb5.
  KI#223-KI#227: KI#223 fixed in commit c8903d21a; KI#224 fixed in commit fa4a1468d; KI#225 fixed in SAS practical 6176 / b8a7805fe; KI#226 and KI#227 fixed in commit c8903d21a.
  KI#228-KI#232: KI#228 fixed/validated in SAS practical 6177 / c99ab79e5b; KI#229 and KI#230 fixed/validated in commit 0a96cdaa7; KI#231 and KI#232 fixed/validated in commit 31becb027.
  KI#233-KI#237: KI#233 fixed/validated in commit 31becb027; KI#234 fixed/validated in commit fbd121d7f; KI#235 fixed/validated in commit f2ecb1334; KI#236 fixed/validated in commit 26b2ac788; KI#237 and inherited Base AdvCiv KI#237.2 fixed/validated in commit df3da8780.
  KI#238-KI#242: KI#238 and KI#242 fixed/validated in commit 49732aa36; KI#239 and KI#240 fixed/validated in commit 9fb1f179a; KI#241 fixed/validated in commit 256402817.
  KI#243-KI#247: KI#243 and KI#244 are SAS-specific Water map state/topology bugs; KI#245 fixed/validated in commit 49732aa36; KI#246 is a SAS-specific Longworld duplicate-start fallback; SAS-specific BTG Grid distinct-region capacity KI#247 is fixed/validated together with its inherited KI#274 root in commit cbe0dcc89.
  KI#248-KI#252: KI#248, KI#250, KI#251 and KI#252 are SAS-specific BTG Grid template/option/fallback bugs; KI#249 is an inherited third-party/Firaxis-style missing isFreshWater() call bug across several map scripts.
  KI#253-KI#257: KI#253 is a SAS-specific BTG Grid per-player-hub capacity mismatch; inherited third-party Peirce KI#254 and KI#255 are fixed/validated in the current batch; KI#256 is a SAS-specific imported Equal Islands Ice-cloning bug; KI#257 is an inherited Planet/C2C CyMap.sPlot() alias and wrap-aware river-direction bug.
  KI#258-KI#262: KI#258 is a SAS-specific Planet Smart Selection area-unit regression; KI#259 is an inherited Planet latitude-contract bug affecting AdvCiv True Starts; KI#260 is a SAS-specific Planet low-grid-percent failure; KI#261 is an inherited Planet/C2C builder-pruning index bug; inherited tiny-island latitude/longitude copy/paste KI#262 is fixed/validated.
  KI#263-KI#267: inherited Medium and Small eastern-continent grain KI#263, SAS-specific 48-civilization/player-slot compatibility regressions KI#264 and KI#265, and inherited Custom Continents six-continent shift-axis KI#266 are fixed/validated. Inherited compact-player-ID/custom-start mapping KI#267 spans BTG Lagoon/Cross, BTS/Ruff Team Battleground, Ring, Hub and Inland Sea; Lagoon/Cross and Team Battleground are fixed/validated, while Ring/Hub/Inland Sea remain deferred.
  KI#268-KI#272: inherited BTG Lagoon Top-v-Bottom unequal-team unbounded-retry KI#268, SAS-specific BTG Lagoon stale raw world-size-index KI#269, inherited BTG Lagoon wrapped-plot shift/desynchronized fixed-coordinate follow-up KI#270, SAS-specific BTG Cross modulo-region/positional-mirroring duplicate-start KI#271 and dormant Crossed-positioning option exposure KI#272 are fixed/validated.
  KI#273-KI#277: SAS-specific BTG Cross unequal-region source-mirroring dimension KI#273, inherited BTG Grid/Cross source-region double-generation/ranking/bookkeeping KI#274, inherited Refar Random Script Chooser Coastal-on-Biggest-Land start-validator exception KI#275 and inherited BTS label mismatch KI#275.2, embedded Medium-and-Small vertical-shift comparison/assignment typo KI#276 and Terra randomized-quadrant regional-bound overflow/corruption KI#277 are fixed/validated.
  KI#278-KI#282: SAS-specific Rainforest forced-resource world-size-table adaptation KI#278 and undersized Arena grid KI#278.2, Global Highlands Mountain Pattern world-size-table adaptation KI#279, Earth2 middle-stage start predicate KI#280, later fixed-Huge-policy grid-calibration regression KI#281, missing-Arena regional-grain table KI#282, Pangaea Arena grain-table KeyError KI#282.2 and five-script missing-Arena percentage/Terra grain follow-up KI#282.3 are fixed/validated. KI#278.3 records the resolved prepared-fix calibration follow-up and is not counted as another surviving defect.
  KI#283-KI#287: inherited Firaxis Donut wrapped-plot seam shift/fixed-center terrain desynchronization KI#283, inherited Firaxis CvMapGeneratorUtil Y-seam strip-size guard dimension typo KI#284, SAS-specific map-generator subclass/base-context compatibility regression KI#284.2 and inherited Firaxis Hub/Ring/Wheel asymmetric forest-variety transition-boundary KI#287 are fixed/validated. Inherited/pre-SAS AdvCiv negative minStartingDistanceModifier clamp regression KI#285 and SAS-specific Wheel >18-player default-start-fallback omission KI#286 remain deferred.
  KI#288-KI#292: SAS-specific Team Battleground scalar world-size-calibration helper misuse KI#288, inherited BTS/Ruff Team Battleground Start Separated/Start Anywhere option-collapse KI#289, inherited BTS/Ruff Python-2 integer-angle circle-spacing KI#290, SAS-specific high-player small-circle duplicate-start regression KI#291 and inherited Firaxis Hub/Ring/Wheel nine-player buffered-region boundary typo KI#292 are fixed/validated.
  KI#293-KI#297: SAS-specific Oasis compact-grid fixed-river-lane bounds KI#293 and undersizing KI#293.2, inherited Firaxis/Warlords Mirror two-team hardcoded TeamTypes 0/1 start-assignment KI#294 and SAS-specific undersizing/NULL-start follow-up KI#294.2, inherited Firaxis/Warlords Inland Sea 17-player small-world empty-start-region KI#295 and SAS-specific Highlands lost-global climate-flip KI#297 are fixed/validated; SAS-specific worker stale partial-Farm-progress/future-food commitment KI#296 remains deferred.
  KI#298-KI#302: SAS-specific Highlands 48-civilization Arena start-capacity regression KI#298 and broader undersizing KI#298.2 are fixed/validated. Sevopedia Movie-to-Music No-Movies graphics-option state leak KI#299 and Terrain Units (Any Build) incomplete improvement-validity bug KI#302 are fixed/validated. The Build-to-standard-Civilopedia namespace bug KI#301 is fixed by source review; runtime follow-up confirmed that the inherited BUG option reaches the standard Civilopedia after the required restart, correcting the first attempted test that had remained in the already-created Sevopedia instance. Info Screen Timeline hidden-holy-city-name spoiler KI#300 remains deferred.
  KI#303-KI#307: KI#303's SAS-specific Specialist Extra Yields omission of Building SpecialistExtraCommerce effects is fixed/validated. KI#304's SAS-specific partial found-value short-to-int migration, KI#305's SAS-specific World Size Chart terrain-cell-versus-playable-tile unit bug, KI#306's SAS-specific stale per-turn player-wide maintenance-ranking cache and KI#307's SAS-specific Main Interface localization-cache invalidation remain deferred.
  KI#308-KI#312: KI#308's SAS-specific City Screen Specialist Breakdown GP-modifier display bug is fixed/validated by using the exposed exact total modifier instead of reverse-engineering the integer-truncated final rate. Validation exposed and then confirmed sibling KI#308.2: the Culture Breakdown similarly inferred its modifier through an integer-truncated base rate despite an exact exposed getter; after its fix, runtime review across cities showed the expected 25-point increments, including screenshot 0180's +75%. KI#309's SAS-specific AI_religionValue team-power duplication, KI#310's SAS-specific water-heavy no-production safety-net hole, KI#311's inherited/pre-SAS PlayerTypes-as-TeamTypes diplomacy valuation bug and KI#312's SAS-specific master/vassal technology-availability root remain deferred.
  KI#313-KI#317: KI#313 is a SAS-specific human BFC-Artist cleanup ownership bug: practical 5128 promises not to conflict with human choices but stores no provenance for the automatically forced Artist, so during its post-BFC cleanup window it decrements any positive forced-Artist count, including Artists the human deliberately forced; KI#314 is a SAS-specific Earth Day event adaptation mismatch where practical 5121 mechanically retargets the still-explicit Earth Day event from Environmentalism to the unrelated Trade Bloc economic-integration civic without retheming the event. KI#315's SAS-specific active unlimited-specialist civic-value sign inversion, KI#316's independent subtraction of the normal 100% Great Person multiplier baseline, and KI#317's SAS-specific pre-change happiness filter that made its happy-city branch unreachable and missed cities that would become unhappy after dropping the civic are fixed and runtime-smoke-tested.
  KI#318-KI#319: KI#318's SAS-specific practical-5092 WonderConstructRand zero-range regression is fixed and runtime-smoke-tested: the three fixed-base opportunistic-Wonder calculations again accept valid zero-valued leaders while retaining the negative-range guards that addressed the inherited CvRandom debug-assert failure. KI#319 remains deferred: its CvSelectionGroupAI::AI_update no-progress tripwire snapshots only head coordinates/moves and mission-queue length, not head identity; legitimate AI_setUnitAIType transitions detach the acting head via joinGroup(NULL), and when another unit remains on the same tile with the same moves the changed-head group is misclassified as unchanged and MISSION_SKIP is pushed onto the remaining stack, consuming its turn.
  KI#320-KI#324.2: KI#320's SAS-specific AI_yieldValue order-of-operations defect is fixed and runtime-smoke-tested by applying the existing high-happiness hammer divisor before production value is normalized and consumed. KI#321's exact-mapping implementation was rejected after runtime testing because it replaced the useful compact `All` summary with a redundant long civilization-icon list, so the underlying misinformation finding remains deferred; KI#322 is a SAS-specific Python-2.4/Python-3 half-tie rounding mismatch in the shared AIP predump/runtime normalizer; KI#323 adds the missing chance-first-strike information to the inherited RFC/DoC Unit Chart and is runtime-validated. KI#324's display-only diagnosis was reviewed and rejected because removing zero-base limit/max rows would hide intentional SAS promotion-capacity information. The deeper SAS-specific KI#324.2 is fixed by removing the inherited base-damage promotion veto while retaining limit/max, UnitCombat and other eligibility guards.
  KI#325: inherited BtS city-name identity defect, broadened by practical 4345. Vanilla BtS already translates each canonical civilization city-name key into the current language before `isCityNameValid` compares that display string against living cities' stored `getName()` strings. Current SAS retains that contract and supports live language changes, so one canonical generated city name can become a different comparison string after a language switch and evade the living-city duplicate check. Practical 4345 extends the same translated-string identity contract to the new all-past-city-name registry, so former-name reuse prevention is language-sensitive too. Real unchanged BtS data proves reachability: `TXT_KEY_CITY_NAME_AACHEN` is `Aachen` in English and `Aix-la-Chapelle` in French. Preferred repair direction: preserve a stable canonical identity for generated city names separately from literal/player-entered display names instead of using a translated string as both identity and presentation.

Post-C156 reconciliation against current HEAD and final SAS-source completion
----------------------------------------------------------------------------
ChatGPT-5.6-Sol's C122-C138 continuation started from the C121 handoff state (90 defects = 37 inherited + 53 SAS-specific) and added exactly 27 findings, reaching its internally correct snapshot total of 117 = 40 inherited + 77 SAS-specific. Current HEAD also contained five findings added during the parallel repair/testing work (KI#275.2, KI#278.2, KI#282.2, KI#282.3 and KI#284.2), producing the C138 combined state of 122 = 41 inherited + 81 SAS-specific. The parallel commits fixed/validated KI#247; KI#263-KI#284.2 except KI#267; and KI#287-KI#292, while KI#285 and KI#286 remained deferred.
Runtime testing of KI#293-KI#298 then established three additional SAS-specific follow-ups (KI#293.2, KI#294.2 and KI#298.2), producing 125 = 41 inherited + 84 SAS-specific before the final archaeology continuation. The connected repairs fixed/validated all eight KI#293-KI#295/KI#297-KI#298.2 surfaces: Oasis river bounds and sizing; Mirror actual-team IDs, sizing and NULL-start fallback; Inland Sea 17-player regions; and Highlands climate orientation, sizing and 48-player Arena capacity.
Arena Oasis generated at 20x12 with good spacing through turn 11 (screenshot 0143). Arena Mirror generated at 20x12 with both default teams and actual TeamTypes 2/3 under Start Together; both players spawned normally and the game continued. A 17-player Duel Inland Sea map generated with players appearing normally distributed at a glance (screenshot 0140). Highlands generated at 32x20 on Duel and 24x16 on Arena; repeated Arena generation visibly produced both north-cold and south-cold orientations. A 48-player Arena Highlands map also generated successfully and completed an 11-turn autoplay with all starts appearing valid at a glance (screenshot 0147). Python AST parsing and focused bounds/accounting checks pass. Fixed with GPT-5.6-Sol after ChatGPT-5.6-Sol's archaeology and wonderingabout's runtime testing.
The C139-C156 continuation initially identified six candidate defects: SAS-specific KI#320, KI#321, KI#322 and KI#324, plus inherited/pre-SAS KI#323 and KI#325. Runtime validation rejected KI#321's first exact-mapping UI implementation and rejected KI#324's display-only diagnosis because its proposed row removal hid intentional SAS capability information. That review exposed the deeper SAS-specific KI#324.2 integration mismatch: SAS broadened collateral eligibility without adapting the inherited zero-base promotion veto. KI#323 is fixed and runtime-validated; KI#324.2 and KI#315-KI#317 are fixed, compiled and runtime-smoke-tested. The current combined archaeology total remains 131 = 43 inherited + 88 SAS-specific, with status accounting of 89 fixed / 0 prepared / 1 broader repair intentionally pending (KI#213) / 41 surgical repairs deferred. Older checkpoint-local counters remain historical annotations of what was known at each checkpoint and are intentionally not rewritten.
The primary SAS runtime-source inventory was also mechanically reconciled before completion: the original 1,317 denominator is superseded by 1,292 game-executed C++/header/Python SHAs under the tracker's written scope. C156 reaches the corrected 1,292 / 1,292 endpoint.

Recording rules
---------------
- Record clean progress as ranges rather than one line per harmless commit.
- For dense K-Mod/SAS archaeology, persist each completed checkpoint immediately instead of waiting for the whole segment or chat turn to finish. Use roughly 200 commits as a maximum checkpoint size and keep smaller live cursors while working when practical.
- Tooling/methodology note: this sweep was initially carried out primarily with ChatGPT in an interactive chat/tool environment (among other practical reasons, Codex usage availability was limited at the time). Interactive or otherwise non-persistent environments can sometimes interrupt a long review before a natural stopping point; more persistent coding-agent environments such as Codex or Claude Code may not share the same practical limitation.
- In an interactive/non-persistent environment, if a large commit or unresolved investigation is still in progress after roughly 20 minutes, save an OPEN checkpoint before continuing: preserve the last certified cursor, current candidate(s), partial findings/dispositions and exact next step. The ~20-minute interval is an empirical safety margin learned during this sweep, not a review-time limit or correctness rule; very large commits may span as many OPEN checkpoints as needed without advancing the certified cursor until the commit is genuinely closed.
- Never advance the certified cursor past an unfinished checkpoint merely because lower-numbered commits received exploratory spot checks.
- For every suspect/finding worth preserving, record segment, practical commit number, SHA, title, relevant file/function, classification and final disposition.
- Keep separate counts for open suspects, confirmed defects, actual fixes, inherited/pre-SAS bugs and SAS-specific regressions.
- A summary-only or partial archived diff is not automatically "clean"; if its title/path is relevant, inspect adjacent history/current source or obtain fuller source before closing it.
- Update Known Issues only for findings that deserve durable player/developer documentation; this tracker can hold weaker suspects and dead ends.
- Prefer accuracy over finding a bug in every commit. Most commits should be harmless.

SAS source checkpoints
----------------------
SAS-C001 - newest source batch through practical 6123 - COMPLETE
  Reviewed 12 runtime-source SHAs: practical 6168, 6167, 6166, 6165, 6164, 6163, 6162, 6127, 6126, 6125, 6124 and 6123.
  Practical-number gaps are expected because the denominator counts actual runtime-source SHAs rather than every branch commit or integer label.
  Practical 6168 -> 6163 are the recent archaeology fixes KI#211, KI#210, KI#209, KI#208, KI#207 and KI#206. Their surgical source changes were re-read against current descendants; no follow-up regression was found.
  Practical 6162 is the large upstream/1.14 merge. Reviewed all 12 runtime-source files rather than treating the merge title as sufficient. Particular checks included the experience-sorted five-pass AI unit-upgrade loop and iterator erasure, worker stale-build target guard with route exemption, `AI_ejectBestDefender` rounded divisions, founding-border cleanup, UWAI peace-demand scaling, and PerfectMongoose smoothing rewrite. No surviving defect established. The optimized PerfectMongoose disk traversal matches its own `getFilledCirclePoints` Euclidean-disk geometry for ordinary radii; its documented full-wrap semantic difference is outside normal map/radius use.
  Practical 6127 rechecked both KI#205 fixes (World-Wonder production-rank direction and game-speed-adjusted cheap-Wonder cap); current semantics remain consistent.
  Practical 6126's Military Advisor adaptive leader-button layout uses Python-2 integer arithmetic intentionally and retains a one-column floor; no overflow/crash path established for supported layouts.
  Practical 6125's Great-General immediate Instructor join exception applies only when already at the candidate city, so it removes a travel-danger asymmetry without exposing a movement path.
  Practical 6124 changes production-overflow plumbing/logging only; output parameters are initialized on entry, all production completion call sites receive the exact `handleOverflow` result, and no gameplay overflow calculation was altered.
  Practical 6123's Great-General safety/birth/retreat changes and logging hooks were checked for inverted safety predicates, stale path-end references and replay/log null handling; no new surviving defect established.
  Next: practical 6121 / 24104e1ce9.

SAS-C002 - practical 6121 -> 6114 source batch - COMPLETE
  Reviewed 7 runtime-source SHAs: practical 6121, 6119, 6118, 6117, 6116, 6115 and 6114.
  Practical 6121 only adds the builder base-XML work-rate line to basic unit help; the display is gated on a positive base work rate and does not change gameplay state.
  Practical 6119's AI Auto Play / SASGameRecord completion-cause plumbing was traced through scheduled countdown completion, explicit Python user/victory/defeat endings, active-player handoff and recorder reset state. The scheduled old=1 -> new=0 transition is logged once with its explicit cause, later zero-state calls do not duplicate the end, invalid Python cause values fall back to OTHER, and no surviving state/cause mismatch was established.
  Practical 6118 is recorder/map-log settings only; its layer-enable array, starting-player overlay and static/dynamic layer gating were checked for index/count mismatches without finding a behavioral defect.
  Practical 6117's CITYBILLBOARD_SCALE updater correctly extends its regex over intervening XML comments, and the OneArea resource second-pass `areas_tried.clear()` is the intended KI#202 repair.
  Practical 6116 replaces one offensive-production floor with a seven-era table. XML values are clamped to 0..100 and the current era is clamped to the configured array bounds before indexing; no era overflow or stale single-value path survives.
  Practical 6115 makes AdvCiv's relative-strength tech-trade discount independently tunable. A 0% effect algebraically returns neutral multiplier 1, 100% returns the inherited multiplier, and the independent SAS military-trade valuation remains active; no inverted interpolation was found.
  Practical 6114 is the larger map-generation tunability commit and received the bulk of this checkpoint. Both Python shared generators and C++ map-element paths were checked branch-by-branch for uninitialized optional state, divide-by-zero, random-index initialization and AdvCiv/BTS fallback asymmetry. `CvRandom::shuffle(std::vector<int>&)` initializes its caller-sized vector with 0..N-1 before shuffling, so the optional resource tie-randomization path is safe. `CvRandom::get(0)` deterministically returns 0, so a zero river-direction randomness value disables that term rather than causing a modulo/divide failure. The OneArea `iTarget` division is reached only while current bonus count is below a positive target; the BTS fallback ignores the AdvCiv-only sorted/second-pass machinery correctly. The desert-lake relative-percent define is documented as positive and the current 25% default keeps the compensated roll range positive. Optional river shuffled indices are allocated only for the AdvCiv path and safely deleted when NULL for BTS. No surviving map-generation defect was established in this commit.
  Next: practical 6113 / 86b81c25b7.

SAS-C003 - practical 6113 -> 6112 source batch - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 6113 and 6112.
  Practical 6113 is a very large civ-specific-building/art/XML commit, but its runtime C++/Python hunks only tidy authorship/source comments in existing SevoPedia and DLL code; no executable runtime statement changes in those files, so the large non-source payload is intentionally outside this phase.
  Practical 6112 splits the primary-area Worker signed reserve into independently tunable one-city, two-city and three-plus-city values. The selection executes only inside the existing primary-area `iCities > 0` guard, the percentage contribution is nonnegative, and the final result remains clamped to at least one Worker per city. No zero-city fallthrough, indexing or floor-regression defect was established.
  Next: practical 6111 / 8ab3703280.

SAS-C004 - practical 6111 -> 6100 source batch - COMPLETE
  Reviewed 12 runtime-source SHAs: practical 6111, 6110, 6109, 6108, 6107, 6106, 6105, 6104, 6103, 6102, 6101 and 6100. Practical 6099 and 6098 have no runtime C++/Python change and therefore do not consume the source denominator.
  This range is dominated by SASGameRecord expansion, so the audit emphasized state-transition hooks, array/vector bounds, nullable gameplay objects, format/varargs contracts and whether diagnostic bookkeeping could alter gameplay. A structural pass over the current recorder found 168 literal `logSASGameRecord(...)` calls and no format-argument-count mismatch; semantic argument types and the changed call sites were still reviewed individually because count matching alone cannot catch object-vs-integer varargs mistakes.
  Practical 6111 terrain-case food quality uses a reset-per-cell terrain-food accumulator and only indexes the selected dominant terrain after establishing a positive count. Derived uppercase symbols are checked against other terrain symbols. No zero-count or stale-static-state defect was established.
  Practical 6110 river-edge change logging tracks the south/east boundary bits that the ASCII river layer itself renders. A pure stored flow-direction change with unchanged boundary bits does not alter that map representation, so the boundary-change gate is consistent with the recorder semantics.
  Practical 6109/6108/6107 add bonus/feature/terrain/geography/political text-map layers and aspect-preserving resampling. Palette symbols remain restricted to printable ASCII; symbol-count indexing uses an unsigned-char cast into a 127-entry array; bonus resampling deliberately prefers any sparse bonus and marks multiple bonus types distinctly. Map width/height and horizontal-character divisors are clamped before use. No live indexing/division/resampling defect was established.
  Practical 6106 adds explicit Barbarian fog/goody spawns and compact Barbarian pressure snapshots. Created-unit pointers are null-checked by the recorder; unit/unit-AI buckets are sized from runtime counts and guarded before indexing; ordinary civilization flow buckets intentionally remain separate from the Barbarian summary.
  Practical 6105/6104 add calendar/color/final-score and Win32 performance context. Player-color access is guarded by NO_PLAYERCOLOR/NO_COLOR, final normalized score uses Civ4's existing final/victory calculation, optional process-memory APIs are resolved dynamically, and the monotonic `timeGetTime` subtraction uses unsigned wrap semantics. No current crash or gameplay-state mutation was found.
  Practical 6103 synthetic war summaries normalize team pairs, guard invalid pairs in executed release code, reconcile only tracked wars, snapshot final war success before AdvCiv resets it, and scope battle/city aggregates to the two participating teams. The commit itself already contains the release-safe executed guard noted by its turn-110 reconciliation comment; no committed survivor was established.
  Practical 6102 only centralizes diagnostic enum-name mappings and session metadata.
  Practical 6101 threads technology-acquisition provenance through `setHasTech`/research-progress helpers. All substantive DLL acquisition paths in the current descendant were rechecked: trade, free choice, goody, ordinary research, Great Person, espionage, random event, tech share, team merge/inherited progress, Advanced Start, Barbarian research, game setup and debug paths either pass an explicit cause or deliberately retain the UNKNOWN default for external/Python compatibility. The metadata does not change technology rules.
  Practical 6100 adds production/military/policy flow aggregates and completion hooks. Flow storage covers MAX_PLAYERS while periodic civilization rows intentionally iterate MAX_CIV_PLAYERS; exact level-3 Barbarian production/combat evidence and the separate Barbarian summary cover the special player. Completion hooks are placed on actual city-production/conscript paths rather than generic creation events, avoiding duplicate ordinary production accounting. No surviving source defect established.
  Next: practical 6097 / c7bfe6cc18.

SAS-C005 - practical 6097 -> 6095 source batch - COMPLETE
  Reviewed 3 runtime-source SHAs: practical 6097, 6096 and 6095.
  Practical 6097 refines inherited risky-stack sacrifice selection so an already-selected current/non-obsolete sacrifice can be replaced by a sufficiently valuable obsolete expendable. The replacement loop preserves the inherited movement, attack and no-blitz eligibility and deliberately uses the same `canMoveInto(..., true)` form as `AI_getBestGroupSacrifice`; it only substitutes after the inherited sacrifice path has produced a candidate and does not broaden ordinary attack eligibility. The added combat/GameRecord hooks are diagnostic only. No surviving defect established.
  Practical 6096 audits and makes turn-valued SAS define names explicit about Normal-speed scaling versus intentionally unscaled literal/path turns. Exact-key checks found no superseded runtime/XML define name left behind and all renamed keys resolve in the current descendant. Existing production/build/disband gates retain their established TrainPercent scaling; path-turn/contact limits are deliberately unscaled. New victory-denial countdown gates use `getSASVictoryDelayTurnsFromNormalGameSpeed`, which reads the same `VictoryDelayPercent` field as `CvGame::victoryDelay`; the current 20-Normal-turn thresholds scale consistently across the eight SAS game speeds. No stale define, zero-default, double-scaling or wrong game-speed field was established.
  Practical 6095 adds generic cross-city allocation for fresh non-combat food-production units. The helper only considers safe same-area cities that can train the exact unit and finish no slower than the current city; it includes the current city, so lack of a better candidate naturally resolves to KEEP rather than a waiting deadlock. The three passes first establish maximum eligible population, then whether a suitably close stagnant candidate exists, then select the largest qualifying candidate with production turns only as an equal-population tie-break. `foodDifference(true, false)` intentionally reports a city already producing a food-production unit as non-growing/stagnant, matching the documented pump behavior. Existing invested production and Settlers are exempt at the caller. No null, zero-turn, cross-area or unintended slower-delegation defect was established.
  Next: practical 6094 / 8227c7d328.

SAS-C006 - practical 6094 -> 6093 - CERTIFIED; KI#213 broader repair deferred
  Certified practical 6094 / SHA 8227c7d328 as the 37th reviewed runtime-source SHA and practical 6093 / SHA b3b83cf78f as the 38th. The 6093 review found KI#213; the commit can now be certified because the defect and preferred remediation are understood even though the broader repair is intentionally deferred.
  Practical 6094 adds the first empire offensive-production-capacity floor, changes the primary-area Worker reserve from unsigned +1 to signed -1, widens UWAI target-contact tuning through XML, and adds war/attack diagnostics. The offensive-capacity helper excludes zero-production cities, includes every productive city in the denominator and only current ATTACK/ATTACK_CITY unit production in the numerator; an empty-production empire returns 0 rather than dividing. The choosing city therefore contributes capacity without falsely counting itself as already committed. `AI_chooseLeastRepresentedUnit` may fail without changing production, and the branch returns only after a unit was actually selected. The signed Worker reserve is still bounded by the ordinary one-Worker-per-city floor and the separate one-city early-growth cap in `AI_neededWorkers`. The `CvSelectionGroupAI` additions are WAR-level-3 diagnostics after attacker selection; nullable oldest-unit reporting is guarded. UWAI's scheme-gate refactor evaluates both `canSchemeAgainst` booleans before updating the hireability cache, but `canSchemeAgainst` does not consume that cache, so the order change is behaviorally neutral. No surviving defect was established in 6094.
  Practical 6093 introduced the KI#198 worked-land Worker-demand fallback and a stronger shared primary-area Worker floor. The floor/minimum/scrap synchronization itself is internally consistent and is later retuned by 6094/6112. Its worked-land fallback nevertheless exposed the deeper split described below between the intentionally-empty legacy city land-build cache and the authoritative custom Worker build chooser.
  KI#213 confirmed: `CvCityAI::AI_updateWorkersHaveAndNeeded` counts a worked unimproved land plot despite an empty legacy `AI_getBestBuild` cache only when `!kPlot.isFeature()`. SAS's `AI_getImprovementValue` deliberately returns 0/`NO_BUILD` immediately for every land plot so custom `CvUnitAI::AI_bestCityBuild` owns ordinary land improvements. Current `AI_bestCityBuild` explicitly has a no-bonus Flood Plains branch (normally Cottage; Cottage/Farm under low-food pressure), so an unimproved worked Flood Plains tile is a real custom Worker job. Flood Plains are nevertheless a feature and their ordinary improvement does not pass through the legacy land cache; `AI_updateWorkersHaveAndNeeded` therefore classifies such a tile as `iWorkedUnimprovableCount` instead of `iUnimprovedWorkedPlotCount`. The exact 6093 fallback line survives unchanged in current SAS.
  The consequence is not merely naming: local `AI_getWorkersNeeded` can remain lower or even zero despite a valid Flood Plains job. `CvPlayerAI::AI_neededWorkers` sums that city demand, Worker redistribution/ferry logic uses it to rank destinations, and KI#192 intentionally permits a zero-demand secondary area to evacuate its last Worker. A sufficiently otherwise-complete secondary landmass can therefore be treated as finished while a worked Flood Plains tile is still validly improvable by the custom Worker AI. Primary-area standing floors may mask the global Worker-count symptom but do not repair the local demand contract.
  The initially-considered narrow Flood-Plains/feature-preserving fallback was deliberately rejected as a partial duplicate of Worker policy. Root architectural cause: `CvCityAI::AI_getBestBuild` / `AI_updateBestBuild` remain a legacy city-side cache whose ordinary land results SAS intentionally disables, while `CvUnitAI::AI_bestCityBuild` has become the authoritative land-improvement policy; several non-unit systems still consume the old cache. Preferred pending repair: extract the city/plot build-policy evaluation from the unit-bound Worker pipeline into a shared city/player-side evaluator, let the city best-build cache and Worker selector consume the same policy, and keep unit-only legality/pathing/reservations/mission execution in `CvUnitAI`. Directly calling `CvUnitAI::AI_bestCityBuild` from city demand is unsuitable because that function depends on an actual Worker, and Worker demand must work when no Worker exists. Do not hardcode Flood Plains unless the broader unification proves impractical.
  Next: practical 6092 / c30e4e1ef0.

SAS-C007 - practical 6092 -> 6086 - CERTIFIED
  Reviewed seven runtime-source SHAs as one closely-related military-production / GameRecord block: 6092 / c30e4e1ef0, 6091 / 63eef1e111, 6090 / 1a80ca7cff, 6089 / 2be4fd2642, 6088 / a9690eb86b, 6087 / 6c8d14b04e, and 6086 / bff4444562. No new surviving defect was established.
  Practical 6092 extends the power-per-city catch-up with an affordability-braked absolute BuildUnitProb component and adds detailed air/bombard GameRecord rows. The deficit division is reached only with a strictly-positive relevant-rival average; the later `r.decreaseTo(1)` still caps final BuildUnitProb. The buffered CITY_BOMBARD logger has an explicit recursion guard when it flushes through the generic writer, is reset/flushed at the existing turn-state boundaries, and its air/land call sites snapshot live pointers/state before mutation. No varargs count/type or pending-sequence defect was established.
  Practical 6091 adds broad military-production diagnostics and hardens `CvCity::getProductionBarPercentages` by snapshotting the validated queue order and using typed production accessors. For train/build/project orders, the typed `getProductionDifference(iNeeded, iStored, iModifier, false/bFoodProduction, true)` calls reproduce the old `getCurrentProductionDifference` calculation while avoiding repeated current-queue reads; Process orders already returned success without a production bar. The log-gated air-production rewrite preserves the inherited `|| SyncRandOneChanceIn(3)` short-circuit exactly: no synchronized RNG call occurs when land war/assault/free-air-XP already makes the branch relevant, and exactly one occurs otherwise. The additional log-only `AI_bestCityUnitAIValue` / `AI_processValue` reads checked here do not introduce an extra synchronized RNG draw or gameplay mutation.
  Practical 6090 fixes the inherited naval-trade sea-explorer fallback and adds geography GameRecord output. The corrected cap counts instantiated sea-domain units adjacent/inside the target water area plus queued sea-domain UnitAIs; queued units are not instantiated and therefore are not double-counted. Docked units use the same `waterArea()` relation as the coastal city being evaluated, and a special explorer is now attempted only while the area still has an unrevealed tile. Landmass percentage divisions use real nonempty land areas; era-indexed vectors are sized from the era table and guarded before indexing. No source survivor established.
  Practical 6089 adds spending gates to the peaceful generic city-hunting stack and late floating-defender retry while retaining wartime/severe-shortage exceptions, and prevents the strict-defender anti-overproduction rewrite from suppressing an explicit CITY_DEFENSE request while the city/empire is below its documented minimum. The severe-shortage expression matches the earlier floating-defender pass; the new conditions only narrow previously-unbudgeted branches.
  Practical 6088 scales final building short-circuit odds down by bounded BuildUnitProb pressure only while the normal +15 military-spending headroom remains. Both XML weight and BuildUnitProb contribution are bounded to at most 100 percent before forming `100 - reduction`, so no negative-odds inversion was established.
  Practical 6087 adds the bounded power-per-city under-strength multiplier. Relevant-rival city count guards the average, the boost executes only when that average is strictly above our own per-city power, total-power throttling takes precedence through `bGreatlyReduced`, and the final inherited BuildUnitProb cap remains in place. Practical 6092 later refines rather than repairs this logic.
  Practical 6086 correctly stops treating an intentionally-selected Process as 'no production' in the `CvCity::doTurn` emergency fallback; empty or invalid queues remain covered. Its GameRecord military count split distinguishes actual combat-capable units from Civ4's `bMilitarySupport` counter and updates previous-state deltas symmetrically.
  Next: practical 6085 / f651eeade3.


SAS-C008 - practical 6085 -> 6077 certified; practical 6076 OPEN on KI#214 - PARTIAL / DURABLE
  Reviewed and certified five runtime-source SHAs: 6085 / f651eeade3, 6084 / b67e621063, 6083 / cac3fa4d2b, 6081 / 38afe787cd, and 6077 / b80afdad17. Practical 6082/6080/6079/6078 contain no primary runtime C++/Python change and do not consume the denominator. Practical 6076 / 2cc4ccaf5d was then opened and produced KI#214; it is deliberately not yet certified.
  Practical 6085 is a large military-production diagnostics commit. The audit compared the rewritten RNG-bearing branches against their parent forms rather than assuming logging was inert. Project and building short-circuit calls still execute exactly once; the generic-unit gate explicitly preserves the old `||` short-circuit so synchronized RNG is skipped under land-war/early-game/human-founding forcing and when `bUnitExempt` blocks the roll; `AI_chooseUnit` likewise preserves the old `iOdds < 0 || RNG` short-circuit. The final BuildUnitProb extraction is algebraically unchanged. No diagnostic-side gameplay/RNG change was established.
  Practical 6084 extends GameRecord espionage/city output. Rival spending is only queried for met teams, the added AI-strategy reads are cached/read-only state, and the extended literal format rows are covered by the current format/argument-count audit. No gameplay mutation or logging contract defect established.
  Practical 6083 is the large `SASGameSummary` -> `SASGameRecord` rename. Its apparent ~4,000-line runtime create/delete was normalized by the rename vocabulary and compared structurally: the main C++ logger's executable skeleton is identical, and the many call-site changes are symbol/header/row-name renames rather than new behavior. The one apparent token mismatch in `CvUnit.cpp` is merely the unchanged local variable name `szSummaryPhase` passed to the newly-renamed recorder helper.
  Practical 6081 changes only a SevoPedia explanatory comment in the primary runtime Python scope; no executable Python statement changes.
  Practical 6077 expands GameRecord territory/victory/runtime context. Its new calculations are recorder-only: percentages guard nonpositive totals, AI victory-stage access is a direct cached-state getter, and `AI_getLowestVictoryCountdown` is a const scan of victory countdowns. No synchronized RNG or gameplay mutation established.
  Practical 6076 introduces the deterministic irrigation-chain search and remains OPEN. The current descendant stores only one `aiBestCost` per plot while separately imposing `iMaxPlots`; because overwrite penalties and route length are independent cost dimensions, that state representation can discard a more-expensive shorter route needed to satisfy the step limit. See F011 / KI#214.
  Next certified-cursor task: resolve/record practical 6076 sufficiently to certify it, then continue to practical 6075 / 22dc2f0abd.


SAS-C009 - practical 6076 -> 6074 - CERTIFIED; KI#214 broader repair deferred
  Certified three runtime-source SHAs: 6076 / 2cc4ccaf5d, 6075 / 22dc2f0abd, and 6074 / f03333ae15. KI#214 remains a confirmed current defect with a broader search-state repair pending; as with KI#213, the historical commit is nevertheless certified because its defect, survival and preferred remediation are understood.
  Practical 6076 received a full review of the new deterministic irrigation-chain route search plus the target-selection/value/protection changes around it. The surrounding dry-Farm / low-food target gates, first source-side step value, city-carrier handling, bonus/automation/buildability rejection and post-selection Farm protection are internally consistent. The surviving defect is the constrained-search state model recorded as F011: one cost per plot is insufficient when a separate plot-count constraint exists. No second independent 6076 defect was established.
  Practical 6075 primarily adds research/irrigation/GameRecord diagnostics. `bWasResearchingAcquiredTech` only snapshots the pre-existing `isResearchingTech` condition before the same `popResearch`; queue invalidation remains unchanged. Worker irrigation source scanning is entirely behind level-3 logging and was superseded immediately by 6076. Added research/building/pollution/production-overflow recorder reads do not alter gameplay state.
  Practical 6074 fixes Barbarian Work Boat demand. The shared seafood predicate requires an owned, non-city, non-connected bonus that the player can currently improve and reach. The Barbarian branch scans the full assigned city radius but still requires `getWorkingCity()==this`, preserving city-local demand. Existing capacity combines boats already in the relevant water area with Worker-Sea units docked on the current land city plot and this city's queued Worker-Sea production; those categories do not overlap because a docked unit is not in the water area and a queued unit is not instantiated. No surviving over/undercount defect established in this commit.
  Next: practical 6073 / 85b3818317.

SAS-C010 - practical 6073 -> 6070 - CERTIFIED
  Certified four runtime-source SHAs: 6073 / 85b3818317, 6072 / 1a66d77923, 6071 / 659606e4e9, and 6070 / 9efdd416d9. No new surviving defect was established in this range.
  Practical 6073 centralizes the UWAI victory-denial direct-war policy and lets imminent-victory emergencies bypass the ordinary contact-distance rejection only after the same shared power, land-target/naval and victory-timing gates pass. The two distance inputs intentionally come from different contexts: direct candidate selection uses nearest-city plot distance, while the contact hard-cap path uses its cached path-turn estimate so unreachable/no-lift contacts retain the stronger transport-aware rejection. No accidental unit conversion or unconditional distance bypass was established.
  Practical 6072 adds crowded-peaceful-city `AI_attackMove` diagnostics only. The entry predicate, alternative-city scan and return-site logging use read-only or const-cache forms (`AI_neededDefenders(..., bConstCache=true)` where relevant), guard all null/zero cases, and do not introduce synchronized RNG or mission changes. Current literal-format auditing also covers the added BBAI rows. No diagnostic-side gameplay defect was established.
  Practical 6071 fixes inherited upgrade-city routing by returning the exact selected upgrade type and applying the real affordability/SAS ROI gates before friendly-territory travel or waiting. The new `bUpgradeCityKnown` flag bypasses only the redundant second city search: the candidate already came from `getUpgradeCity(..., &eUpgradeUnit)`, while gold and economic gates still execute. Foreign-territory/no-move cases deliberately retain inherited travel because `canUpgrade` would fail there for positional reasons; on the next ordinary friendly movable turn rejected candidates are released. No false upgrade-eligibility path was established.
  Practical 6070 completes the targetless peacetime city-assault aggregate cap across multiple groups. `SAS_countPlotAttackCityGroupUnits` counts units by their live group head role, and `AI_setUnitAIType` detaches a reassigned unit through `joinGroup(NULL)`, so reassigning the old head decreases the aggregate by the actual released unit while the remaining group receives a new head and stays counted if still city-assault-led. The preventive `AI_omniGroup` gate likewise adds the incoming whole group only when it is not already represented in the target-plot aggregate. No hidden whole-group undercount or merge/release mismatch was established.
  Next: practical 6069 / 9be0cd0f3c.

SAS-C011 - practical 6069 -> 6068 - CERTIFIED
  Certified two runtime-source SHAs: 6069 / 9be0cd0f3c and 6068 / bf3cb1a389. No new surviving defect was established.
  Practical 6069 stabilizes UWAI's pre-war distance gate. It separately tracks nearest land-reachable and nearest-any target-city contacts, uses the land cap whenever a land route exists even if the current scenario is tagged naval, and uses the wider sea cap plus cargo-capacity requirement only when naval transport is actually required. This can intentionally reject a nearer naval alternative when a farther land route exists, but that is the explicit anti-flapping policy rather than an accidental min-variable/unit mismatch. Existing-plan tolerance is added only after a war plan exists; peace-scenario and already-at-war evaluations remain outside the hard reject.
  Practical 6068 adds aggregate military/upgrade-city diagnostics. Plot military units are counted once per unit while group IDs are deduplicated for group totals; percentage denominators are clamped. The added checks and logs are gated diagnostics and do not push missions, alter UnitAI roles, consume synchronized RNG or otherwise change gameplay state. Later 6071 expands the same upgrade-city logging while preserving its diagnostic nature.
  Next: practical 6067 / 875b071bcb.

SAS-C012 - practical 6067 - CERTIFIED; inherited KI#215 fix prepared
  Practical 6067 / 875b071bcb is certified. Its SAS-specific `AI_scrapSettlerWithNoValidSite` exception rechecks AI/Settler/absolute-scrap-disable/fighting state, is reached only after `AI_settleMove` has established an old Settler with no reachable valid site and no pending pickup, and detaches the Settler from land escorts before specialized deletion. No defect was established in the new 6067 bypass itself.
  The audit did confirm inherited KI#215 in the surrounding no-site cargo cleanup: the Settler called `getTransportUnit()->unloadAll()`, which can disembark unrelated passengers. `CvUnit::unload()` is the exact passenger-local operation needed: it leaves the Settler aboard when unloading is illegal (therefore preserving the existing no-scrap gate) and detaches only this Settler when unloading is legal. The one-line behavioral fix is prepared with detailed Known-Issues documentation; user compile/run/commit validation is pending.
  Certified cursor advances to practical 6067 / 875b071bcb.
  Next: practical 6066 / 6210495792.

SAS-C013 - practical 6066 -> 6060 - CERTIFIED
  Certified seven runtime-source SHAs: 6066 / 6210495792, 6065 / c54b256ece, 6064 / cd0fd0a2c6, 6063 / e746edc894, 6062 / 5910459ff5, 6061 / 08cb9d272e, and 6060 / 71ff2a0ba7. No additional surviving defect was established in this range.
  Practical 6066 consolidates the relevant-water-area fallback used by Settler production/site gating and relaxes the unescorted-founding block only when a Settler is already on its valid site and the plot is not actually dangerous. The inland fallback still requires an existing Settler transport on the chosen main sea, so it does not create speculative overseas demand; movement toward exposed sites remains guarded.
  Practical 6065 adds target-backed opportunistic assault-transport production. Target candidates must be known/deduced (Barbarians excepted), genuinely overseas through the selected water area, long-term beneficial, close enough to an owned city, positively valued and supported by a minimum attacker-to-defender force. Existing and queued cargo capacity is then compared with the bounded target requirement before bypassing the broad military-spending/invasion-state gates. A rare two-water/canal-city edge was explicitly reviewed: `AI_totalWaterAreaUnitAIs` and 6065's docked-transport capacity loop associate a docked ship with the port's primary `waterArea`, not `secondWaterArea`. SAS already handles primary+second seas explicitly at callers where both are semantically interchangeable; here the opportunistic production city itself deliberately selects only its primary sea, and a transport staged at another canal city's secondary sea is not unambiguously committed to this target sea. This is retained as a helper/policy cleanup edge, not classified as a defect without stronger behavioral evidence.
  Practical 6064 expands overseas-transport diagnostics and rewrites the shortage-random gate without changing synchronized-RNG consumption: the random roll is still evaluated only when the spending condition does not already allow the branch. No diagnostic side effect or changed short-circuit behavior was found.
  Practical 6063 permits a partially loaded assault transport to launch below the inherited broad reinforcement threshold and applies the shared distant-disposable-Barbarian-target rejection. The exception still requires cargo and delegates to the existing `AI_assaultSeaTransport` target/path logic; no bypass of combat/path legality was established.
  Practical 6062 separates overseas-transport diagnostics and repairs Barbarian island Worker demand by retaining a reserve even though the Barbarian land best-build cache is empty until a Worker exists. The behavioral additions around assault pickup/launch were checked for mission/RNG changes; no new defect was established.
  Practical 6061 allows a completed civilization secondary area to reach zero Worker demand and adds deterministic evacuation/pickup of a now-unneeded Worker. Its destination-aware Settler-cargo helper computes the exact Worker deficit, subtracts cargo Workers from the destination-area existing count once the transport is physically on that area, unloads only the needed Workers at destination, retains excess aboard, and can leave excess at home only before a FOUND mission has begun. The known possibility that the underlying demand itself misses valid custom Worker jobs is KI#213 and is not a separate logistics defect here.
  Practical 6060 adds central area-usefulness production gates and the first destination-aware Settler/Worker transport safeguards. `AI_ferryWorkers` now treats area demand as a hard capacity before pathfinding. The Worker-reload rejection does not include Workers already aboard the same Settler transport in destination supply and can therefore permit a redundant boarding in some sequences, but later exact-deficit unload/retain behavior prevents a demonstrated wrong delivery or stranded-Worker failure; retained as an optimization edge rather than a separate KI. The initial destination helper's coarser all/useful behavior is immediately refined by 6061 and does not survive current SAS.
  Certified cursor advances to practical 6060 / 71ff2a0ba7.
  Next: practical 6059 / f24055b1f1.

SAS-C014 - practical 6059 - CERTIFIED
  Certified practical 6059 / f24055b1f1 as the 68th runtime-source SHA. No surviving defect was established.
  The commit fixes the Base AdvCiv/K-Mod mismatch that treated a stack as Barbarian-only during a real-war preparation period: `bHuntOnlyBarbs` now also requires `!bAnyWarPlan`. `CvTeamAI::AI_isAnyWarPlan` deliberately excludes the Barbarian team and minor/vassal pseudo-targets in its cached count, so the new condition distinguishes real major-civilization war planning rather than accidentally suppressing ordinary Barbarian cleanup.
  The new distant-disposable-Barbarian-city filter is a rejection layer, not an attack-legality bypass. The pathable-Barbarian picker applies it only after the city has passed existing target-value/path generation; normal `AI_pickTargetCity` candidates are likewise filtered and then retried with `bHuntBarbs=false` so a real war city may replace the discarded Barbarian objective. Quick raids remain allowed through the path-turn threshold, and long-term-beneficial Barbarian cities remain eligible. No path/attack legality or mission-state regression was established.
  Certified cursor advances to practical 6059 / f24055b1f1.
  Next: practical 6058 / 0b7a1566d7.

SAS-C015 - practical 6058 -> 5951 - CERTIFIED; inherited Work Boat demand defect found
  Certified 96 runtime-source SHAs from practical 6058 / 0b7a1566d7 through practical 5951 / 856ac747bc. A mechanical recount of the commit-diff source hunks gives 96 source-bearing SHAs in this contiguous reviewed range; together with the prior 68, the certified SAS counter is therefore 164 / 1,317. This corrects an earlier conversational estimate of 173 before the durable checkpoint was written.
  Practical 6058's war-cause/GameRecord propagation was traced through declarations, queued war cascades, sponsored wars, random events, votes and nukes. The added cause queue stays aligned with the existing war queues and untouched callers retain defaults; no diagnostics-driven gameplay state change was established.
  Practical 6054's decisive-peace margin initially looked capable of suppressing emergency peace. Deeper arithmetic rejected the candidate: emergency evaluation first forces utility at least 100 points below the peace threshold, which under the intended/default margin still enters the decisive-peace branch directly.
  Practical 6050/6049's large-army regrouping/defender-ejection changes were checked against group lifetime semantics. `AI_setUnitAIType` detaches the unit through `joinGroup(NULL)`, and the multi-defender recall leaves at least one unit in the original group, so the retained group pointer remains valid while defenders are ejected. No surviving group/state bug was established.
  The first-Settler sequence around practical 6015 -> 6008 was traced forward rather than judging intermediate heuristics in isolation. Later commits intentionally supersede earlier return-site choices; the surviving current path remained coherent under the reviewed mission/transport invariants.
  The citizen/culture block around practical 5997 -> 5994 was checked for sign/gate mistakes. The citizen fallback still requires the full city-aware `AI_jobChangeValue` to be positive, and the culture shortcut suppresses serious culture only when the team has a genuinely shorter active countdown. No surviving defect was established.
  A new inherited defect was then confirmed while auditing practical 5951's Work Boat demand/availability lineage. `CvCityAI::AI_neededSeaWorkers` chooses its primary area through `waterArea(true)` but unconditionally adds `secondWaterArea()`. `CvPlot::secondWaterArea`, however, defines the excluded primary through plain `waterArea()` while itself ignoring impassable water. With an adjacent impassable-Ice area plus another accessible sea, `waterArea(true)` and `secondWaterArea()` can therefore resolve to the same accessible area and its unimproved sea bonuses are counted twice.
  Current SAS availability accounting already guards `pSecondSeaWorkerArea != pPrimarySeaWorkerArea`, including the KI#157-era production logic, so this duplicate need can produce a false local deficit such as needed=2 versus available=1 for one actionable seafood target. The defect is geometric/edge-case rather than a general explanation for excess Work Boats, but the duplicate count is deterministic when the area identity collision occurs.
  Lineage cross-check: the asymmetric `waterArea(true)` + `secondWaterArea()` demand pattern is already present at K-Mod practical 3 / 2414225e64 and remains present in the user-supplied Base AdvCiv 1.14 source. Current SAS retains it. This is therefore classified as an inherited Better BTS AI/K-Mod/AdvCiv defect rather than a SAS-introduced regression.
  Surgical repair identified for the later KI#216 batch: in `AI_neededSeaWorkers`, add the same pointer-identity guard already used by availability accounting before adding the second area's demand. Do not globally redefine `secondWaterArea`, whose established semantics have unrelated callers. Implementation/compile testing is intentionally deferred to the user's Codex pass.
  Certified cursor advances to practical 5951 / 856ac747bc.
  Next: practical 5950 / 1f82d3ca4f, "Fix Base AdvCiv bug: Minor AI Work Boat overproduction/target mismatches...".

SAS-C016 - practical 5950 -> 5948 - CERTIFIED; ready-attack upgrade-count defect found
  Certified 3 runtime-source SHAs: practical 5950 / 1f82d3ca4f, 5949 / 42cbc76648 and 5948 / a9515bf013. Together with SAS-C015, the certified SAS counter is now 167 / 1,317.
  Practical 5950's KI#157 Work Boat repair was checked independently rather than treating the historical KI label as proof. Its availability-side primary/second-water-area pointer guard is correct and is exactly the asymmetry that made KI#216 visible on the still-unguarded demand side. The target-selection changes skip already-connected seafood and allow owned disconnected sea bonuses outside a city BFC without establishing a second defect.
  Practical 5949's lower-food-improvement oscillation guard applies only to non-resource plots after the configured era and compares the current/proposed improvement's food yield for the same plot/owner. It prevents strictly lower-food replacements while leaving equal/higher-food changes and resource plots available; no surviving correctness defect was established.
  Practical 5948 introduced `SAS_getBestUpgradeForLog` and uses it behaviorally to count `iCanUpgradeNowUnits` before deciding whether a ready city-attack stack may bypass Base AdvCiv's upgrade wait. The intended condition, documented in the commit itself, is that zero group units can actually upgrade now. However, the loop passes `AI_getUnitAIType()` from the attack-stack unit executing `AI_attackCityMove` for every `pUpgradeLogUnit` in the group instead of using each member's own UnitAI. Current `CvUnitAI::AI_upgrade` explicitly evaluates each unit with that unit's own `AI_getUnitAIType()`.
  A concrete current-SAS counterexample proves the mismatch can change the result: SAM Infantry and Mobile SAM are both naturally `UNITAI_COUNTER`, SAM Infantry upgrades to Mobile SAM, and both XML entries explicitly mark `UNITAI_ATTACK_CITY` as a forbidden UnitAI. Mixed city-attack expeditions are real in current SAS; later KI#188 diagnostics record grouping decisions into attack-city expeditions from 113 `UNITAI_COUNTER` units (plus other roles). For a Counter SAM Infantry inside an attack-city-headed group, the practical-5948 helper therefore obtains zero `AI_unitValue` for both current and upgraded units under `UNITAI_ATTACK_CITY` and reports no candidate, while normal `AI_upgrade()` evaluates the same upgrade under `UNITAI_COUNTER` and can take it when currently available. The group-wide `iCanUpgradeNowUnits` count can thus become 0 when at least one member really can upgrade now, falsely enabling the no-upgrade-wait bypass.
  Surgical repair identified for the later KI#217 batch: evaluate every group member with its own UnitAI (for example through `pUpgradeLogUnit->AI().AI_getUnitAIType()` / deriving the role inside the helper) rather than passing the head's UnitAI. Preserve the existing deterministic 90% estimate and all other bypass gates unless separate testing justifies changing them. Implementation/compile testing is intentionally deferred to the user's Codex pass.
  Certified cursor advances to practical 5948 / a9515bf013.
  Next: practical 5947 / c5b1443fb2, "Fix Base AdvCiv issue: Remote captured-city attack stacks waiting on upgrades...".

SAS-C017 - practical 5947 -> 5912 - CERTIFIED; no new surviving defect
  Certified 26 runtime-source SHAs in the next contiguous source-bearing range, from practical 5947 / c5b1443fb2 through practical 5912 / 82f042a44f. The certified SAS counter advances from 167 to 193 / 1,317. Numerical gaps in this range are commits with no game-executed C++/Python hunks and therefore do not consume the runtime-source denominator.
  Practical 5947's remote-captured-city attack-stack bypass was checked against its city/core-distance classification and the original upgrade-wait loop. It only breaks out of the wait-count loop after a ready, non-overstrong target and the configured remote/large-stack test; subsequent normal attack-city logic remains responsible for movement. The added parking diagnostics do not mutate missions except at the pre-existing wait sites.
  Practical 5946's Great Person Golden Age wait cap normalizes unit age by game-speed percent and only stops the Golden-Age reservation from raising the score threshold after the cap; the existing sorted mission list still chooses among discover/join/trade/culture actions. No sign, zero-divisor or mission-suppression defect was established.
  Practical 5945's obsolete-only AI scrapping gate was traced through the global scrap kill switch, obsolete-tech requirement, cargo preservation and era-delay check. It narrows `canScrap` rather than creating a new kill path; no live correctness defect was established. Practical 5943's map-heaviness upgrade multiplier likewise changes only the AI ROI effective price, leaves the actual gold price untouched and uses bounded positive XML percentages in current configuration.
  Worker commits 5944, 5942, 5938 and 5926 -> 5912 were treated as one evolving lineage rather than judging intermediate heuristics in isolation. A genuine intermediate weakness in practical 5938 (`bHasUnimprovedBFCImprovementCandidate` could suppress replacements merely because an unreachable/reserved blank candidate existed) is explicitly repaired by practical 5944, which requires a pathable, unreserved blank candidate before using that suppression. The later current Worker implementation has undergone further KI#213/KI#214-era rewrites; no additional surviving defect from this reviewed range was established.
  Practical 5941's forest-chop change replaces a hard large-city health skip with a tunable penalty while retaining normal candidate value/path/reservation competition; its plot-hover coordinate addition is display-only. Practical 5940's first-city food/coastal corrections and strict Python XML lookup helper were checked for control-flow/lookup regressions; required static XML tags exist in the current asset set, and later first-city commits supersede the intermediate roaming choices without leaving a proven current defect.
  Practical 5929's unowned-resource diversity bonus applies once per distinct BFC bonus and asks the player's cities whether the resource is already owned; it is an explicit strategic nudge rather than replacement of normal tile/resource value. Practical 5928's naval-heavy coastal bonus is similarly a bounded additive site preference. Practical 5927's severe-unhealth gate consistently suppresses the SAS happy-growth bonuses in both yield and job-change valuation without affecting humans/food-production cases unintentionally.
  Practical 5925's timestamped BBAI filename helper is diagnostic-only; 5924's Military Advisor selection persistence filters dead/unmet players before restoring selection. The first-city sequence 5923 -> 5920 was followed through its later 5940 corrections rather than freezing an intermediate heuristic as a defect. Practical 5919's Port emergency building is gated to AI, mature ocean-coastal, non-danger cities and uses the existing emergency-building legality helper. Practical 5917 only disables the pre-existing outside-BFC fort/airbase path through a define. Practical 5916 synchronizes a minimum area-Worker production floor with both worker scrap guards; area worker counts include units in training, preventing every city from independently forcing the same deficit. Practical 5915 removes the disabled `AI_getBestBuild` gate for custom land Worker logic and repairs bonus re-improvement through the bonus-specific build table. No new surviving defect was established in these paths.
  Certified cursor advances to practical 5912 / 82f042a44f.
  Next runtime-source SHA: practical 5911 / cfd9cb9112, the Donut.py world-size grid-size adjustment.

SAS-C018 - practical 5911 -> 5892 source batch - COMPLETE
  Reviewed 19 runtime-source SHAs: practical 5911, 5910, 5909, 5908, 5907, 5906, 5905, 5904, 5903, 5902, 5900, 5899, 5898, 5897, 5896, 5895, 5894, 5893 and 5892. Practical 5901 is documentation-only and therefore does not consume the runtime-source denominator.
  Practical 5911 only retunes Donut Large/Huge grid dimensions. Practical 5910 converts two legacy TechWindow files to valid UTF-8 without changing executable logic. Practical 5909 repairs BUG/configobj lookup and Python-2.4-compatible exception handling; the new `sys.exc_info()[1]` uses were checked against their modules (including the tempting BugPath error path, which already imports `sys` at module scope), and no replacement NameError or lost exception branch survives.
  Practical 5908/5907 and 5894 are the Sevopedia 3D-audio lineage. The DLL reverse lookup introduced in 5894 reads `Audio3DScripts.xml` once and exposes the script-name mapping to Python; 5907 changes playback to a normal-volume 3D path that retains a `Destroy3DSound`-compatible handle. These changes are media-preview/UI behavior and no new state or namespace mismatch was established in current descendants.
  Practical 5906 replaces base XML farm/workshop food assumptions with plot/player-adjusted yield calculations and delays food-cost workshops. The in-progress-farm pre-credit remains a heuristic estimate rather than a complete before-vs-after improvement delta, but no deterministic correctness failure or surviving bad-state transition was established; later Worker logic also continues to evolve substantially. Practical 5905 is an Info Screen ID-column width/UI scaling fix.
  Practical 5904 adds explicit runtime log-level guards around BBAI/found-value logging passes. The guarded methods return the same nonlogging evaluation result and the player/city diagnostic guards do not gate their associated gameplay calculations. Practical 5903 introduces XML-tunable BBAI logging plus an early-expansion Settler war-plan exception; the first-Settler descendants were already traced through the later 5920-5940 and 6008-6015 sequences, and no additional surviving defect from this older stage was established. Practical 5902/5900 are Foreign/Military Advisor UI fixes with no gameplay-state mutation found.
  Practical 5899 retunes inherited map scripts and, importantly, maps SAS's Arena-shifted runtime world-size IDs back to the original Duel..Huge buckets where legacy arrays require them. Practical 5897 moves shared world-size helpers out of `PrivateMaps` so `import *` can no longer leak a helper `getGridSize` callback into playable scripts; imports are consistently redirected to `Assets/Python/SAS_WorldSizeUtils.py`. Practical 5893 then makes BTG_Cross base-size rows explicit. These are coherent map-sizing/helper repairs rather than a surviving index-offset regression.
  Practical 5898 collapses flat Python literals mechanically. Practical 5896 and 5892 repair legacy mixed/tab indentation in map scripts; the changed nested blocks were checked as block-structure repairs rather than assumed safe from their titles. Practical 5895's executable-source hunks are comment/format cleanup while its substantive Yeoman balance changes are XML-side and outside this source-focused denominator. No new surviving defect was established in the 5911 -> 5892 range.
  Certified cursor advances to practical 5892 / 0e6854abcd.
  Next runtime-source SHA: practical 5891 / e4893dee90, the broad Python parenthesis/indentation cleanup. Treat it as a fresh checkpoint-sized audit rather than waving it through as mechanical because it touches many runtime Python files.

SAS-C019 - practical 5891 -> 5865 source cleanup batch - COMPLETE
  Reviewed 23 runtime-source SHAs: practical 5891, 5890, 5887, 5884, 5883, 5882, 5881, 5880, 5879, 5878, 5877, 5876, 5875, 5874, 5873, 5872, 5871, 5870, 5869, 5868, 5867, 5866 and 5865. Practical 5889, 5888, 5886 and 5885 do not touch primary runtime C++/Python and therefore do not consume the denominator.
  Practical 5891 was treated as a fresh large audit despite its cleanup title: it touches 47 runtime Python files in 251 runtime hunks. Every runtime hunk is character-for-character identical after removing whitespace; there are no token/content substitutions. The changes collapse multiline parenthesized expressions, and the few changed lines containing `#` retain comments after the expression has already closed. This supports the helper's own significant-token safety checks and establishes the runtime pass as semantics-preserving rather than merely assuming it. Practical 5890 only splits comma-separated imports. Practical 5887 removes excess blank lines.
  Practical 5882 normalizes mixed indentation only on continuation lines inside multiline matrix/data assignments and verifies significant tokens. Practical 5881/5880 similarly collapse selected multiline function definitions/calls while preserving significant token streams and skipping comments/multiline strings/backslash continuations. No executable token change or block-membership change was found in those generated passes. Practical 5879 removes unnecessary trailing semicolons.
  Practical 5878/5877 split same-line semicolon-separated statements onto separate lines. The changed statements retain their original suite indentation, including conditional map-generation branches, so both operations remain in the same control-flow block. Practical 5876 -> 5869 expand one-line `if`/`elif`/`else` suites containing `break`, `continue`, `pass` or small assignments; the new body lines are indented one level under the original header, preserving the old one-line-suite semantics. Practical 5868 rewrites `not x in y` to the grammar-equivalent `x not in y`.
  Practical 5867 rewrites equality/inequality comparisons with `None` to identity checks. The runtime occurrences were checked as ordinary None sentinels (Python objects/callables/strings/numeric-or-None values/CyPlot-or-None holders), not logic relying on an overloaded equality comparison. Practical 5866 rewrites old Python-2 `raise E, value` syntax to constructor-call form; the changed values are ordinary strings/normal exception constructor values, so Python 2.4 exception meaning is retained.
  Practical 5865 wraps simple Python-2 print statements for modern lint parsing. Single-expression prints keep the same evaluated expression; the few multi-argument cases were manually converted to one formatted expression (e.g. coordinates, player/pass diagnostics, `debugString/eComment`) rather than naively creating a tuple-print expression. These are diagnostic output paths and no gameplay-state side effect was introduced.
  No new surviving defect was established in the 5891 -> 5865 cleanup sequence.
  Certified cursor advances to practical 5865 / fa6b78670d.
  Next runtime-source SHA: practical 5864 / 8e39ab6e0a, the substantive centralization of Python magic numbers into `SASMagicNumbers.py`; audit it separately from this mechanics-heavy cleanup layer.

SAS-C020 - practical 5864 Python magic-number centralization - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5864 / 8e39ab6e0a. Practical 5863, 5862 and 5861 do not touch primary runtime C++/Python and therefore do not consume the denominator.
  This substantive refactor moves world-size runtime indices, Sevopedia WIDGET_PYTHON routing IDs and Movie/Music packed-key type IDs into a new import-free `Assets/Python/SASMagicNumbers.py`. It was audited as an import/value/dispatch migration rather than assumed safe from the refactor title.
  All 51 removed local/duplicated constant definitions map to the exact same centralized values; the new module has 45 unique constants because several old definitions were duplicates. This includes world-size indices Arena=0 through SAS48=10, Sevopedia routing IDs 6798 through 6818, Movie types 1 through 6 and Music types 1 through 6. No value drift was found.
  Every Sevopedia consumer, `_sevopedia_helpers.py`, `CvTechChooser.py` and `CvMapGeneratorUtil.py` that begins using `SAS_MAGIC_*` receives a direct `from SASMagicNumbers import *`. The PrivateMaps scripts intentionally receive the constants transitively through `from SAS_WorldSizes import *`; later practical 5897 independently shows all 26 affected map scripts replacing that exact import with `from SAS_WorldSizeUtils import *`, proving the dependency was present across the centralization interval rather than inferred from current source. `SAS_WorldSizes.py` itself imports `SASMagicNumbers` and re-exports the prefixed names through its import-star surface. No missing-name path was established.
  The non-mechanical chart-log change in `_sevopedia_helpers.chart_add_csv_log_button` removes an old `hasattr(top, "SAS_PEDIA_PYTHON_CHART_LOG")` fallback and always uses the centralized WIDGET_PYTHON ID. Its runtime callers are the Sevopedia Era/GameSpeed/Handicap/WorldSize chart pages and pass the Sevopedia main object; after centralization the helper owns the constant directly, so the removed instance-attribute fallback is no longer needed and does not alter the intended dispatch route. Leader attitude/action/era fallback routing in SevoPediaMain is likewise changed from constants imported through the SevoPediaLeader module to the same centralized numeric IDs.
  `SASMagicNumbers.py` is import-free as intended, so importing it from generic map/UI modules does not create a CvPythonExtensions/SASUtils circular dependency. No stale added-side use of the removed `self.SAS_PEDIA_*`, `SAS_WORLDSIZE_*` or `MAPGEN_SAS_WORLDSIZE_*` names was found in the practical-5864 patch.
  No new surviving defect was established in practical 5864.
  Certified cursor advances to practical 5864 / 8e39ab6e0a.
  Next runtime-source SHA: practical 5860 / 1548510c78.

SAS-C021 - practical 5860 through 5853 UI/cache and world-size transition - COMPLETE
  Reviewed 8 runtime-source SHAs: practical 5860 / 1548510c78, 5859 / f2ca08ee13, 5858 / c0af31da65, 5857 / dee1d9c5ad, 5856 / de057d10e8, 5855 / 63884cdbf7, 5854 / 05174d6206 and 5853 / f96ddafd75. Practical 5852 does not touch primary runtime C++/Python and therefore does not consume the denominator.
  Practical 5859 temporarily makes the Military Advisor screen persistent to preserve expensive Battles-table widgets across full close/reopen. Practical 5860 explicitly reverts that exact `screen.setPersistent(True)` experiment to `screen.setPersistent(False)` after testing showed Civ4's native minimap could reopen as an empty blue panel and remain broken through debug-mode transitions until save reload. Current SAS retains only the safe same-open Battles cache/tab-switch optimization. This is a real historical intermediate regression, but it was repaired by the immediately newer descendant and is not a surviving finding.
  The new Timeline/Battles caches were checked beyond that persistence experiment. The Timeline key covers active player, reveal-all state, replay-message count and game turn, and reuse also validates the existing table widget/row state. The Battles key covers the selected perspective/history entries plus table layout state; current SAS intentionally limits its effective lifetime to the open Military Advisor instance after practical 5860 restores non-persistence. No stale-current-state path was established.
  Practical 5860 centralizes footer-tab drawing through `SASUtils.drawAdvisorFooterTabRows` / `drawAdvisorFooterTabs`. Calls from the Domestic, Foreign, Info, Military, Policy, Victory and World advisors were checked against each screen's active-page IDs and inactive widget/data routing; the helper preserves the active yellow/inert tab and each advisor's pre-existing navigation semantics. The three DLL hunks in practical 5860 are comment/spelling-only rather than gameplay changes.
  Practical 5857 adds click-to-Sevopedia routing for city-screen Religion and Corporation icons while retaining their help widgets. Its holy-city and corporation-headquarters overlay DDS widgets could intercept the click; practical 5858 explicitly sets those overlays to `HITTEST_NOHIT` and extends the handler to the overlay names, so the underlying redirect works in the current descendant. This is another historical intermediate UI defect already repaired before the certified cursor, not a new surviving KI.
  Practical 5856's Military Advisor Summary expansion and `summaryBuildingHasTech` prerequisite display were checked against the all-AND-tech loop and AdvCiv's `isHasTech(NO_TECH)` semantics. The helper is display gating rather than build legality and no false gameplay state was established. Its DLL change is non-runtime comment/metadata support for the advisor rather than AI/game-state behavior.
  Practical 5855 replaces hardcoded world-size integers with named runtime constants while preserving the existing values Arena=0, Duel=1, Tiny=2, Small=3, Standard=4, Large=5 and Huge=6. Map scripts already import the shared world-size helper across this interval; `CvMapGeneratorUtil` intentionally keeps local `MAPGEN_SAS_*` constants to avoid PrivateMaps load-order coupling. No missing-name or index drift was found.
  Practicals 5854 and 5853 are the associated map-size correction/calibration layer. The direct grid dictionaries are adjusted for the Arena-shifted runtime indices; BTG_Cross no longer maps every base size through Large to one Huge-sized grid, Global_Highlands/PerfectMongoose/Water receive calibrated or corrected dimensions, SAS_Longworld's direct lookup is corrected, and the Victory/Sevopedia world-size displays follow the same runtime dimensions. The surviving current mappings are monotonic/calibrated where intended; no separate runtime defect was established.
  One current city-bonus display limitation documented in practical 5860 was deliberately not promoted to a KI: the optional frameless DDS bonus mode empirically supports the existing right-click pedia path but not a Python-only left-click redirect. The commit itself records that constraint, standard pedia access remains available, and no simulation/state correctness failure was established.
  No new surviving defect was established in practical 5860 through 5853.
  Certified cursor advances to practical 5853 / f96ddafd75.
  Next runtime-source SHA: practical 5851 / 4a2c6394d1.

SAS-C022 - practical 5851 upstream AdvCiv 1.12 merge / PerfectMongoose meteor pairing - COMPLETE / FINDING
  Reviewed 1 runtime-source SHA: practical 5851 / 4a2c6394d1, the large merge of upstream AdvCiv 1.12 into the SAS branch. Practical-number gap 5850 through 5818 contains no reachable branch commits in this mirror; the next runtime-source SHA is practical 5817.
  The merge's DLL changes were audited individually rather than accepted as an upstream-fix bundle. The meaningful hunks are overwhelmingly genuine fixes or deliberate upstream changes: ArmamentForecast uses the analyst team's reveal state and avoids a zero-revealed coast division path; City AI corrects rival-victory/religion-building variable mixups and restores the worked-plot value multiplier; specialist-commerce tech valuation stops overwriting its discovery flag on later commerce types; assault-sea grouping iterates the candidate group rather than this unit's current group; invasion/nuclear-war analysis fixes multiplicative and enemy/self variable mistakes; namespace/comment/test changes are mechanical. These corrected forms survive current SAS and, where present in the supplied AdvCiv 1.14 source, match the later upstream descendant.
  PerfectMongoose's filled-disk changes to `GetAverageInHex` / `GetStdDevInHex` address a real ring-versus-disk averaging error; their later performance rewrite was separately audited at SAS-C001. The distinct surviving problem is in the same practical's `PangaeaBreaker.getCirclePoints` change from an ordered list to `set()`.
  `CirclePoint` defines only `__init__(x, y)` and no coordinate equality/hash. Under Civ4's Python object semantics, separately constructed `CirclePoint` instances remain distinct set elements even when their `(x, y)` coordinates are equal. Therefore the new set does not accomplish its stated duplicate removal.
  The change is not merely ineffective. Active map generation calls `PangaeaBreaker.breakPangaeas` when Pangaeas are disallowed; a meteor strike calls `castMeteorUponTheEarth`, which obtains `getCirclePoints`, sorts the returned list only by `y`, then consumes entries two at a time as the left/right endpoints passed to `drawCraterLine`. The pre-5851 ordered list intentionally produced opposite-side pairs after the stable y-sort. Passing through an unordered identity set destroys that same-y ordering while retaining all coordinate duplicates, so the endpoint pairing no longer has a correctness invariant.
  Concrete radius-4 row proof: the midpoint-circle generator emits the y=0 boundary sequence `(+4, -4, +4, -4)` as four distinct objects. With the old ordered list, the stable y-sort preserves those four equal-y entries and pairs `(+4,-4)` twice, drawing the full x=-4..+4 crater row. With the practical-5851 identity set, all four objects still survive but their iteration order is unspecified; an allowed equal-y order such as `(-4,-4,+4,+4)` pairs same-side endpoints and draws only degenerate x=-4 and x=+4 lines for that row. Other radii contain the same duplicate-endpoint structure. Thus crater shape can depend on unordered set iteration rather than the circle geometry.
  This path is live rather than a dead helper: current `generatePlotTypes` invokes `pb.breakPangaeas()` when `maximumMeteorCount > 0`, and `breakPangaeas` repeatedly calls `castMeteorUponTheEarth` while a disallowed Pangaea remains. The supplied Base AdvCiv 1.14 `PerfectMongoose.py` still contains the same identity-set `getCirclePoints` plus y-only endpoint pairing, and current SAS also retains it unchanged. No existing KI/tracker entry was found for this defect.
  Preferred surgical repair for the present architecture: restore an ordered list/`append` in this boundary-point helper rather than trying to make the set truly coordinate-deduplicating. The active meteor rasterizer expects paired boundary points, and the filled-disk averaging callers introduced in the same upstream patch no longer rely on this boundary helper. A broader alternative would be to rewrite `castMeteorUponTheEarth` to derive min/max x per y before deduplicating, but that is unnecessary for the minimal correctness repair. Code/KI implementation is intentionally deferred to the user's Codex pass.
  No second surviving defect was established in the other practical-5851 runtime hunks.
  Certified cursor advances to practical 5851 / 4a2c6394d1.
  Next runtime-source SHA: practical 5817 / 814b748d66.

SAS-C023 - practical 5817 through 5808 Sevopedia/UI cleanup, Military summary and Glance pagination - COMPLETE
  Reviewed 9 runtime-source SHAs: practical 5817 / 814b748d66, 5816 / a18e92afb7, 5814 / f3bb231d54, 5813 / fea2f98c7e, 5812 / c81ee60580, 5811 / 478cf7ecea, 5810 / 5be8e021c1, 5809 / 65336cddcb and 5808 / b6192c987b. Practical 5815 is helper/docs only and does not consume the denominator.
  Practical 5817 is mostly display cleanup plus the Sevopedia Build/Feature feature-production panel conversion to a MultiList with the actual per-tech production modifier shown under each tech button. The list still iterates the same `(iTech, iModifier)` data and preserves pedia-jump widget data; no missing modifier/button path was established. Other runtime hunks are formatting/comment compaction and small layout spacing changes.
  Practical 5816's repo-wide Python blank-line trim was verified mechanically rather than accepted from its title. Across its runtime `.py` hunks, removing whitespace from executable/non-comment lines yields the same token stream before and after; the apparent non-whitespace differences are comment/doc compaction. No runtime semantic change was found.
  Practical 5814 moves the end-turn/status text away from the diplomacy overlay and repairs Military Advisor Summary partition percentages. `_partPct` operates only on exhaustive partitions (military/civilian, land/sea/air, health bands and battle outcomes), keeps zero-count buckets at 0, then assigns the integer-rounding remainder to a nonzero largest bucket so each displayed partition reaches 100%. Independent/overlapping statistics continue to use `_withPct`; no denominator or category-mixing defect was found.
  Practicals 5813, 5812 and 5811 polish the Foreign Advisor / city tables and change the optional extra commerce-slider pair from hardcoded +/-100 to an XML-tunable fast increment. The map-view-only second pair continues to use `WIDGET_CHANGE_PERCENT`, signs remain correct, and the slider cluster reserves the matching two button columns. The later 5811 value change is tuning, not a correctness change.
  Practical 5810 adds scoreboard background-style cycling. The style index is seeded from the documented SAS define and later cycles modulo the six-element style list; redraw recreates only the background panel when the style changes. No state leak or navigation/game-state effect was established.
  Practical 5809 adds fast/fastest scoreboard scroll controls. Up/down signs match the existing offset convention, `updateScoreStrings` remains the central clamp for the upper bound, explicit `max(0, ...)` protects downward overshoot, and visibility follows whether movement in that direction is possible. The other touched Python files are formatting/comment cleanup rather than behavior changes.
  Practical 5808 fixes the 24+ rival Glance-tab horizontal overflow by paginating rival columns. The page list excludes the active leader, page count uses Python-2 integer ceiling arithmetic, page index is clamped when the known-player set changes, header/rows use the same page membership, and arrows clamp to valid page bounds. Its temporary `set` is only a set of integer player IDs used for membership while the actual drawing loop remains ordered by player ID; it does not create the ordering hazard found in KI#218. Replay-screen changes are label positioning only.
  No new surviving defect was established in practical 5817 through 5808.
  Certified cursor advances to practical 5808 / b6192c987b.
  Next runtime-source SHA: practical 5807 / e65191a992.

SAS-C024 - practical 5807 through 5800 Python collapse / strict XML lookup / Military Summary audit - COMPLETE / 2 FINDINGS
  Reviewed 7 runtime-source SHAs: practical 5807, 5805, 5804, 5803, 5802, 5801 and 5800. Practical 5806 is developer-helper-only and therefore does not consume the primary runtime-source denominator.
  Practical 5807's broad Claude-generated multiline-to-single-line Python collapse was not accepted from its title. Its 132 runtime hunks were mechanically narrowed to 34 apparent non-whitespace cases; each of those was then inspected. The differences are comment relocation/rewriting or parenthesized tuple/call/signature compaction with preserved values, argument order and operators. In particular, the 27-field Military battle-log format argument order and `RectLayout` floor-division expression remain intact. No executable semantic change was established.
  Practicals 5805 -> 5803 migrate `gc.getInfoTypeForString` lookups to the fail-loud `SASUtils.getInfoTypeOrFail` helper and hoist repeated static lookups. The pass-3 variable cases were audited individually rather than assuming every dynamic-looking key was mandatory. Runtime-generated colors/unit classes come from fixed valid tables; GP, trait, hurry, TechChooser and strategy-overlay keys resolve from fixed lists; the uprising random-event tech/unit-class arguments are hard-coded existing XML tags; Customizable Domestic Advisor specialist names and BUG option/config keys are configuration contracts where a missing XML name is exactly the malformed-data condition the new strict helper is intended to expose. The old `< 0` fallback lines in a few third-party paths become redundant, but no valid-game optional-key contract was found that now raises unexpectedly. Hoisted static color/process/terrain/feature IDs preserve their values and lifetime.
  Practical 5802 is Summary-tab polish: terminology, icons, legend link and positive-only Best City/Best Defense display gates. Its runtime changes do not repair or introduce the two arithmetic/state defects below.
  Practical 5801 expands the Summary's army/city/battle statistics and replaces one aggregate upgrade-cost total with min/avg/max/total statistics. This introduces KI#220 candidate: `PROMOTION_LEADER` has `iUpgradeDiscount=100`, so Great-General-led units can have a real zero-gold upgrade, but the code appends an upgrade price only when `iPrice > 0`. A free-upgrade unit is therefore omitted from the min/average sample. With one free upgrade and one 75-gold upgrade, the panel reports Min/Avg 75/75 instead of 0/37 (Python-2 integer mean). Total is accidentally unaffected because adding zero changes nothing, and all-free samples also display zero by coincidence; current SAS retains the defect.
  Practical 5800 introduces the Summary tab and its exact DLL support-cost breakdown bindings. This introduces KI#219 candidate: the new `CyPlayer::calculateUnitCostBreakdown` and `calculateUnitSupplyBreakdown` call the internal reference-output `CvPlayer` overloads directly. Those overloads deliberately omit the public wrappers' `if (isAnarchy()) return 0` exemption because they are also used for hypothetical/intermediate calculations. The Summary then inflates and displays their nonzero subtotals as actual Unit Cost, Unit Supply and Total Cost + Supply. During anarchy the ordinary engine-facing `calculateUnitCost()` / `calculateUnitSupply()` return zero, so the Summary can display gold costs the player is not actually being charged. No later SAS diff changes these bindings or special-cases anarchy; current SAS retains the mismatch.
  The remaining 5800 Summary state/navigation and Python binding plumbing was checked: the new page IDs/tab arrays stay aligned, perspective validation is performed before drawing, tuple declarations/bindings match the Python unpack order, free/paid counts use the same internal calculations, and the existing Finance/Domestic-Advisor-style inflation display convention is preserved. No third independent survivor was established in this checkpoint.
  Certified cursor advances to practical 5800 / 9e84f1482c.
  Next runtime-source SHA: practical 5799 / 58f4d842f6.

SAS-C025 - practical 5799 through 5786 Sevopedia navigation / no-multiline / main-interface icon audit - COMPLETE
  Reviewed 12 runtime-source SHAs: practical 5799, 5798, 5797, 5796, 5795, 5793, 5791, 5790, 5789, 5788, 5787 and 5786. Practicals 5794 and 5792 have no runtime C++/Python change and therefore do not consume the primary denominator.
  Practical 5799 removes Index-only `TXT_KEY_*` prefix stripping and English-only `"The X"` comma-flipping from sort keys. The flattened Index now uses the same displayed names as type-specific pages; the intentionally separate two-character Trait icon-prefix strip remains. Search and placement still consume the same resulting `self.index` entries, so no data/widget mismatch was introduced.
  Practicals 5798 -> 5795 add and then refine Sevopedia keyboard navigation. Practical 5797 immediately fixes the first Index/ordinary-list mismatch from 5798, and current code registers a page-specific navigator (`SAS_navigateIndexTable` for Index, `SAS_navigateItemList` for ordinary lists). The Index cursor records cells in the same sorted loop/order in which they are drawn, clamps at both ends, and restores/re-highlights only the previously/currently recorded cell. Left/Right Back/Next uses a debounce dictionary separate from search typing so category redraw/search-state resets cannot create the earlier double-history jump. No surviving navigation-state or current-page dispatch defect was established.
  Practical 5793's Foreign Advisor change keeps rival GPT visible when trade-network connectivity is absent and shifts the pre-existing Not Connected overlay away from the GPT column; the GPT visibility remains gated by the existing gold-trading/Currency rules. The companion Sevopedia/MainInterface changes only swap lock/unlock art keys. No gameplay-state or data-disclosure defect was established.
  Practicals 5787 -> 5790 are the no-multiline branch. Every runtime hunk in 5788/5789/5790 has an identical executable token stream before/after when comments/layout tokens are ignored. All 204 practical-5787 runtime hunks likewise preserve the token stream; the 30 fragments that Python's tokenizer cannot parse in isolation because the hunk starts/ends inside an indented suite also match character-for-character after whitespace removal. Practical 5791 is the merge from the same practical-5786 base and contains the cumulative formatting/comment relocation; manual checks of its apparent mismatches found comment placement changes rather than executable expression changes. No semantic survivor was established in this mechanical series.
  Practical 5786 increases research/production bar icons from 20 to 25 scaled pixels and computes explicit vertical offsets from the bar height/text offset. Production switches from an `<img>` text tag to the raw button path so `setSASCenteredImageButtonAtLeftOfTextRow` uses its image-button branch, matching the already-used research path and the helper's declared contract. The centering arithmetic remains bounded by the existing row/bar geometry; no state or click-target regression was established.
  Certified cursor advances to practical 5786 / ce48dab4e4.
  Next runtime-source SHA: practical 5785 / 6e6bca43e4.

SAS-C026 - practical 5785 through 5780 define caching / Military Advisor unit-help audit - COMPLETE
  Reviewed 6 runtime-source SHAs: practical 5785, 5784, 5783, 5782, 5781 and 5780.
  Practical 5785 expands lazy one-time caching of vanilla and SAS GlobalDefines across advisors, Main Interface, Tech Chooser and Sevopedia. This was checked against practical 5770's explicit cache contract: hot XML edits are intentionally unsupported for already-created Python/UI objects and should require a Civ4 restart, because live define mutation had produced crashy mixed widget/layout state. The new grouped caches initialize all members to `None` together and populate them together under a shared sentinel; singleton caches use their own sentinel. All cached values are direct static define lookups, not player/game-state results. No partial-cache or supported-hot-reload regression was established.
  Practical 5784 threads a configurable promotion-icon size through `CvGameTextMgr::setUnitHelp` and the Python omit-owner wrapper. The core function keeps a default size of 16 for every existing C++ caller; the only Python `getSpecificUnitHelpOmitOwner` caller is updated to pass the advisor's initialized inline-icon size. Boolean argument order remains `bColorAllegiance=false, bOmitOwner=true, bIndicator=false`, so the new integer does not shift an existing flag.
  Practicals 5783 and 5780 resize the Military Advisor Map-tab leader/unit/minimap layout and refactor minimap visibility. Runtime geometry keeps a positive full-row floor, derives minimap height from the existing bottom anchors, and restores `CyInterface().getShowInterface()` after the temporary minimap-only visibility update. The newly clickable unit-detail label calls the same toggle path as the +/- button. No stale interface mode or row-overflow correctness defect was established.
  Practical 5782 only inserts the missing space before live Worker build-turn timers in `setUnitHelp`; suspended and ordinary timer branches are changed symmetrically. Practical 5781 exposes the existing omit-owner `setUnitHelp` path to Python for Military Advisor rows; it changes display ownership text only and preserves the unit/build help source. No gameplay-state change or wrapper-signature mismatch survives current descendants.
  Certified cursor advances to practical 5780 / d8d69be689.
  Next runtime-source SHA: practical 5779 / 0cc73aa368.

SAS-C027 - practical 5779 through 5774 Military Advisor Map/Composition / icon-sort audit - COMPLETE
  Reviewed 6 runtime-source SHAs: practical 5779, 5778, 5777, 5776, 5775 and 5774.
  Practical 5779 renames the Military Advisor Units page to Map and disambiguates map/minimap geometry symbols. Current descendants contain no surviving `PAGE_UNITS` references, and all renamed map/minimap geometry consumers use the corresponding new symbols. The other advisor/chooser runtime hunks in this commit are matching no-multiline/comment cleanup rather than behavioral changes.
  Practical 5778 adds optional text-color controls to the Sevopedia expanded-text playground only. Practical 5777 adjusts Map-tab leader/unit geometry and art choices; the later 5780->5784 layout refinements reviewed in C026 supersede its fixed two-row thresholds without leaving a stale branch.
  Practical 5776 is the KI#129 minimap lifecycle repair: tab/debug switches rebuild page-owned widgets in place while deliberately retaining the native minimap frame, initialize that frame once per real advisor opening, and re-front/update it when returning to Map. Later practical 5780 factors the temporary interface-visibility update into a helper that restores the prior interface mode. Current descendants no longer use the pre-fix hideScreen()+interfaceScreen() tab-switch path, so the historical disappearance regression is repaired rather than surviving.
  Practical 5775 adds the current Composition snapshot. Perspective selection passes through the shared active/debug/vassal validator; the collector iterates only that player's units, counts `canFight()` units by type, counts UnitCombat only when nonnegative, and counts only promotions actually held. The three sortable tables use the matching info getter/widget for each dictionary. No hidden-player leak, invalid info index or iteration-state defect was established.
  Practical 5774 adds invisible sort keys to icon-only table cells. All reviewed callers offset nullable IDs by +1 so no-value maps to group 0; result/role/terrain/feature/civ/leader/unit groups remain well below the four-base-5-digit group capacity in current SAS, while the six-digit row suffix exceeds any realistic advisor row count. The zero-width/Bidi control characters are used only as invisible text sort keys and the existing button/widget data are unchanged. No current display-data or sort-key collision defect was established.
  Certified cursor advances to practical 5774 / 1c7dd567dc.
  Next runtime-source SHA: practical 5773 / dfaf0ab340.

Confirmed findings / lineage references
---------------------------------------
SAS-C028 - practical 5773 through 5772 Battles XP/GG and city-capture context audit - COMPLETE / KI#221 DISCOVERED (fixed later in 6172)
  Reviewed 2 runtime-source SHAs: practical 5773 and 5772.
  Practical 5773 adds pre-fight XP and Great-General attachment context to Battle-history rows. The generic combat-details event is emitted synchronously from `CvUnit::resolveCombat` before player-facing `combatLogCalc` and before either lethal `combatResult` or the explicit retreat event. The same-owner duplicate `combatLogCalc` path intentionally preserves the richer pending XP/GG tuple; lethal and retreat recorders then consume and clear both pending contexts. Old save rows remain length-guarded, and the plot-context rewriter preserves any appended unit-context tail. No stale-context or tuple-offset defect was established.
  Practical 5772 adds defended/captured-city plot context. Its attacker-wins coordinate correction correctly records the defender/city plot rather than the post-combat attacker location, and `onCityAcquired` necessarily runs after the battle row has been recorded. However, the later city-capture patch matches rows only by current turn plus winner/new-owner and loser/previous-owner. An undefended enemy city can be conquered by ordinary movement with `acquireCity(..., true, ...)` and therefore fires `cityAcquired(..., bConquest=true)` without any preceding combat result. If those players had an unrelated battle earlier in the same turn, `_patchCityCapturedEntry` rewrites that unrelated newest row to the empty city's X/Y and marks it captured. This is KI#221 candidate.
  Preferred surgical repair for KI#221: require the candidate row's stored battle coordinates to equal the captured city's coordinates (and reject retreat/non-lethal rows for defensive robustness) in addition to the existing turn/owner-direction match. A defended-city capture already stores the losing defender's plot coordinates, so this strengthens attribution without needing a new global pending-capture state.
  Partial inspection of practical 5771/5770 below this hit is orientation only and does not advance the certified cursor; resume them independently.
  Certified cursor advances to practical 5772 / fc82d93007.
  Next: practical 5771 / dd03caa3fa.

SAS-C029 - practical 5771 through 5765 Battles/UI lineage and unit-capture audit - COMPLETE / KI#222 DISCOVERED (fixed later in 6172)
  Reviewed 7 runtime-source SHAs: practical 5771, 5770, 5769, 5768, 5767, 5766 and 5765.
  Practical 5771's Battles `Est%` is explicitly a simple starting-effective-strength share rather than exact Civ4 odds; perspective swapping keeps the inspected player's strength in the numerator. Its shared maximized-advisor layout is algebraically the same Domestic layout it replaces, while the Sevopedia style/background playground is an opt-in developer tool with lazy caches and DDS-only background filtering. No surviving gameplay/UI correctness defect was established there.
  Practical 5770 moves Military Advisor/UI define reads to a lazy one-session cache. Its own documented contract is intentionally restart-stable (runtime XML changes have no effect until restart), so the later broad define caching does not violate a supported hot-reload path. Battle terrain pseudo-info IDs are likewise lazily cached without changing their values.
  Practical 5769 initially snapshots battle terrain/feature context at `pWinner` coordinates. That is historically wrong for an attacking winner before plot advance, but practical 5772 later repairs the surviving path by using the loser's pre-death plot when the pending attacker wins. Practical 5768's non-lethal withdrawal/combat-limit row and 5766's end-effective-strength data were traced through the current pending-context/event ordering; no independent survivor was found. Practical 5767 is display-column reordering only.
  Practical 5765 introduces actual `unitCaptured` reporting and the Battle-history capture matcher. This produces KI#222 candidate: `_matchesCapturedBattle` requires `entry[4] == iOldUnitType`, i.e. the captured unit's old type must equal the lethal combat loser's type. In the current SAS unit XML, every unit with a non-`NONE` `<Capture>` tag is a Settler or Worker variant with `iCombat=0`; `CvUnit::canFight()` is `baseCombatStr() > 0`, so these capturable units cannot be the combat defender/loser represented by that row. Captures instead occur when a fighting unit enters the plot and `CvUnit::setXY` iterates the non-defending enemy units, sets their capturing player and kills/converts them. The matcher therefore cannot associate the standard captured civilians with the preceding escort-killing battle, leaving Cap#/Cap blank.
  Preferred surgical repair for KI#222: now that practical 5772 stores attacker-win battle coordinates on the defeated defender's plot, match the capture event to the newest same-turn lethal row with the same capturing/old-owner direction and the captured unit's plot coordinates, rather than requiring the captured civilian type to equal the combat loser type. `pNewUnit` already supplies the capture-plot X/Y. Reject retreat/non-lethal rows. All current capturable unit classes convert to `UNITCLASS_WORKER`, so the existing same-result-type count accumulator remains compatible with multiple civilian captures on one cleared plot.
  Certified cursor advances to practical 5765 / 254194766d.
  Next: practical 5764 / 1763ecf094.

SAS-C030 - practical 5764 through 5755 Battles presentation / Sevopedia search / advisor-perspective audit - COMPLETE
  Reviewed 9 runtime-source SHAs: practical 5764, 5763, 5762, 5761, 5760, 5759, 5758, 5757 and 5755. Practical 5756 is non-runtime.
  Practical 5764/5763 reorganize Battle rows from attacker/defender to inspected-player "Our/Their" presentation and add strength/result/plot interaction polish. Perspective conversion keeps attacker/defender identity from the stored pending context; later descendants preserve the same owner mapping. No surviving tuple-position or role inversion defect was established.
  Practical 5762 merges the 5759-5761 special-character search branch. Search strings remain raw for actual filtering while only font-tagged display text escapes renderer-sensitive `&`, `<` and `>`; typed and clickable punctuation both converge on `SAS_appendSearchCharacter`. The KEYS visibility define hides only the clickable punctuation overlay and intentionally leaves physical-key support active. Debounce keys include the added punctuation codes. No current search-state or escaping defect was established.
  Practical 5758 adds Battles vassal/debug perspective selection; practical 5755 provides the shared active-player/vassal permission helper. Outside debug, the selectable set is the active player plus alive players whose team is a vassal of the active team, and every retained perspective is revalidated through `getAdvisorValidPerspectivePlayer`; debug mode uses only alive players in the requested player range. No unrelated-player perspective path was established.
  Practical 5757 is the initial save-persistent Battles implementation. Its active-player-only combat-details limitation is repaired by practical 5766's generic all-combat details event, its attacker-win plot ambiguity is repaired by practical 5772, and its later capture extensions are tracked separately as KI#221/KI#222. No additional survivor from the initial storage/sanitization path was established.
  Certified cursor advances to practical 5755 / 1fabcaa557.
  Next: practical 5754 / 05e1ed7cbe.

SAS-C031 - practical 5754 through 5749 Domestic Advisor perspective/overview audit - COMPLETE
  Reviewed 6 runtime-source SHAs: practical 5754, 5753, 5752, 5751, 5750 and 5749.
  Practical 5754 centralizes repeated advisor debug-dropdown creation. The helper preserves each caller's barbarian/max-player choice and selected-player semantics; outside debug its later vassal mode remains separately permission-gated. No player-range or dropdown-data mismatch was established.
  Practical 5753 adds the Overview legend link only. Practical 5751 is the Overview -> Overview 1 naming migration; the page IDs, table IDs and refresh routing move consistently. Practical 5752/5750 tune columns/widths and deliberately remove or relocate low-value columns rather than altering game state.
  Practical 5749 is the substantive vassal/debug Domestic perspective expansion. Rechecked its current descendant for stale direct `CyGame().getActivePlayer()` data reads: apart from initialization, an old commented reference, and the deliberate guards that disable active-player-only specialist/city-selection interaction while inspecting another player, the current Overview data paths use `self.iActivePlayer`. Culture, Great-Person thresholds, city iteration and table refresh therefore stay on the selected perspective rather than mixing the active player's values into vassal rows.
  No surviving correctness defect established in this batch.
  Certified cursor advances to practical 5749 / de2bf3716e.
  Next: practical 5748 / ee29cc6134.

SAS-C032 - practical 5748 through 5738 Domestic Overview/font/DLL-capacity/Sevopedia-search audit - COMPLETE
  Reviewed 11 runtime-source SHAs: practical 5748, 5747, 5746, 5745, 5744, 5743, 5742, 5741, 5740, 5739 and 5738.
  Practical 5748 adds Overview 4 and initially reads the active player directly in its new city/Great-Person paths. That is a genuine intermediate perspective limitation, but newer practical 5749 is exactly the vassal/debug perspective repair already certified in SAS-C031: current `drawOverview4Contents`, Great-Person threshold reads and the other Overview data paths use `self.iActivePlayer`. The city-screen specialist Y adjustment is geometry only.
  Practical 5747 adds only the unlocked scoreboard annotation glyph. Practical 5746/5745 consolidate dynamic font tags and replace separate `*Bold` globals with each normal tag's `.bold` companion. Current runtime Python was searched for the removed bold/uppercase font-tag symbols and has no stale callers; `_DynamicFontTag` constructs `.bold` for every non-bold instance and retains the same configured size. Practical 5744 removes fallback font sizes and makes invalid/out-of-range XML font defines fail fast; valid configured values 1..4 preserve the prior tag values, so this is an intentional configuration-contract tightening rather than a valid-game regression.
  Practical 5743 makes the 48-civilization DLL capacity the default by changing `MAX_CIV_PLAYERS` from 18 to 48 while retaining `BARBARIAN_PLAYER = MAX_CIV_PLAYERS` and the following `MAX_PLAYERS` enum relationship. The matching chart/document changes do not alter simulation logic. Practical 5742 adds the KI#127 startup warning only when selected-world default players exceed the active DLL's `getMAX_CIV_PLAYERS()` value; the once-per-session flag is set only after a warning is actually emitted, and the popup does not modify game setup/state.
  Practical 5741 is comment-only in runtime source. Practical 5740 unifies the Index and ordinary-list Sevopedia search state/refresher; practical 5739 moves the shared search panel to the header and fixes the clear widget's input routing. Current descendants retain exactly the intended ordinary-list and Index refresher registrations, reset the active refresher on category/special-page transitions, and route search clear/keypress refresh through the registered view; no stale separate Index-search state survives. Practical 5738 is a mechanical multiline-call/bracket collapse in the touched Sevopedia files; changed executable calls retain argument/order/indentation semantics.
  No surviving defect established in this range.
  Certified cursor advances to practical 5738 / 7868026fd4.
  Next: practical 5737 / 6bdf60426c.

SAS-C033 - practical 5737 through 5728 Sevopedia helper/refactor/footer-navigation audit - COMPLETE
  Reviewed 9 runtime-source SHAs: practical 5737, 5736, 5735, 5734, 5733, 5732, 5731, 5729 and 5728. Practical 5730 is documentation/file-renaming only and is excluded from the runtime-source denominator.
  Practical 5737 is a one-line/code-ordering cleanup around the expandable-panel helpers. Practical 5736 replaces repeated empty-panel text boilerplate with `draw_none_text`; the helper preserves the old widget type, centered-Y calculation, horizontal/vertical padding and custom `NONE`/`ALWAYS`/`NEVER` text keys. Practical 5735/5734 consolidate expandable history/text-panel calls. The removed EventTrigger/Vote explicit collapsed-body offsets are intentionally replaced by the shared title-aware default (`H_ADJUST_Y_AFTER_ANIMATION_NO_HEADER`) rather than dropping header clearance, and current callers preserve each page's title, body, expanded-state flag and Python widget-data route. No argument-order or expanded/collapsed-state regression was established.
  Practical 5733 changes Improvement/Specialist yield/slot displays to shared attached-row panel templates. The refactor retains the old button art and widget/data tuples for irrigation, terrain, river concepts, tech/civic/route/building/vote-source links; the new `bAnyFound`/entries emptiness checks are equivalent to the prior `iY == 6` sentinel for deciding whether to show the no-content message. The layout/scroll behavior changes are deliberate presentation changes rather than altered game-data selection.
  Practical 5732 adds current-game Leader/Civilization prev/next footer navigation. Targets are rebuilt from alive major players, hide unmet opponents outside debug, and are intentionally deduplicated by pedia info ID because the destination page is a Leader/Civilization info rather than a player-instance page. Click handling validates the requested info against a freshly rebuilt target set before `pediaJump`, so stale/dead/unmet target state cannot be followed. Practical 5731 centralizes footer rendering and greys Back/Next/Clear according to actual history/future state.
  Practical 5729 fixes KI#125 by preserving `pediaHistory`/`pediaFuture` across closing/reopening Sevopedia and adds a manual Clear command. Reopening selects the existing current history tail and `pediaJump` does not append a duplicate when the tail already equals that page; it also leaves the forward list intact. Clear drops the future list and retains exactly the current/last page as the sole history node. Current descendants refresh the footer immediately after Clear. Practical 5728's first-category keyboard-focus repair is guarded by the presence of a normal item-list widget and catches unsupported focus calls; it does not activate on special/no-list pages.
  No surviving correctness defect established in this range.
  Certified cursor advances to practical 5728 / c008723793.
  Next: practical 5727 / 554f98b019.

SAS-C034 - practical 5727 through 5723 map-HUD geometry / annotation-state audit - COMPLETE
  Reviewed 5 runtime-source SHAs: practical 5727, 5726, 5725, 5724 and 5723.
  Practical 5727 makes map-view `PlotListPanel` X/Y/width tunable and fixes unit-bar overflow by deriving the per-row column count from the same adjusted available width later used to size each panel. Signed X/Y/help-area XML adjustments dispatch positive/negative values through HLEN/VLEN without passing negatives into those scaling helpers. The default width reduction remains safely positive in ordinary UI geometry; deliberately extreme XML tuning is explicitly documented as unsupported. Practical 5726 is the matching help-area width tunable and changes presentation geometry only.
  Practical 5725 replaces temporary custom annotation-toggle DDS art with an existing HUD style and reshuffles existing HUD icons. Current runtime/XML was searched for the removed `SAS_EMOJI_CROSS_MARK`, `SAS_EMOJI_CROSS_MARK_BLACK` and `SAS_EMOJI_PENCIL` art keys and has no stale references. The annotation widget remains a custom widget while the visual control uses the standard styled-check-box path.
  Practical 5724 is titled `update docs` but touches runtime Python/C++/XML. Every executable-source hunk was checked: the Python and C++ changes are comment wording/spacing only, and the XML value-bearing nodes retain their values; no gameplay/AI code change is hidden behind the title.
  Practical 5723 adds save-persistent map-annotation visibility. `hideMapAnnotationSigns` first snapshots every visible sign/landmark tuple before removing any, avoiding sign-index mutation while enumerating. Hidden state and the sign cache are persisted separately; on load, hidden saves restore cached captions and explicitly disable/clear DotMap layers, while visible saves clear the hidden cache and explicitly re-enable/show DotMap. The later practical-5725 art refactor preserves this state path. No current annotation-state loss or simulation-state defect was established.
  No surviving correctness defect established in this range.
  Certified cursor advances to practical 5723 / 5e3ef99bf4.
  Next: practical 5722 / 7de906fcdd.

SAS-C035 - practical 5722 Domestic Advisor multi-overview / sortable-date audit - COMPLETE / NEW SURVIVING DEFECT
  Reviewed 1 runtime-source SHA: practical 5722 / 7de906fcdd (`enhance Overview 1 tab; add Overview 2 and Overview 3 tabs in Domestic advisor`).
  The new page-ID/table lifecycle, per-city Overview 2/3 calculations, specialist-header construction, shared numeric/text scaling helpers, Info-screen date/status presentation and World-Advisor icon-header refactors were checked against current descendants. The active-player-specific culture/Great-Person reads originally introduced in Overview 3 are historical-only: newer practical 5749 converts the Domestic overview paths to `self.iActivePlayer` for the vassal/debug perspective feature already certified in SAS-C031.
  Confirmed surviving SAS-specific UI correctness defect / KI#223 candidate: the new Domestic `Founded` column is placed in a sortable Overview table but stores `CyGameTextMgr().getTimeStr(pLoopCity.getGameTurnFounded(), false)` through `screen.setTableText`. Practical 5722 itself documents the relevant engine behavior in `SASTextScale`: `setTableInt` sorts cells numerically whereas `setTableText` sorts lexically. Formatted founding dates such as `4000 BC`, `500 BC`, `100 AD` and `800 AD` therefore sort by their display strings rather than chronology. Current SAS retains the same representation after practical 5748 moved the column from Overview 3 to Overview 4.
  This is distinct from the Info-screen Wonders date/status column that practical 5722 intentionally converts to textual sorting because that cell can contain mixed formatted date/status/production content. Every Domestic Founded row is a real city founding date, and the same commit/current World Advisor already demonstrates the suitable sortable convention: `iFoundYear = CyGame().getTurnYear(pCity.getGameTurnFounded())` displayed through numeric `setTableInt`/`SASTextScale.setTableIntLabel`. Preferred surgical repair is to give Founded a monotonic numeric representation (the existing signed-year convention is the simplest/current precedent; game-turn value would be even more strictly monotonic if desired) instead of the BC/AD formatted string.
  Certified cursor advances to practical 5722 / 7de906fcdd.
  Next: practical 5721 / cd76f9cd37.

SAS-C036 - practical 5721 through 5717 advisor live-toggle / World Advisor geography audit - COMPLETE
  Reviewed 5 runtime-source SHAs: practical 5721 / cd76f9cd37, 5720 / 87e5d7097a, 5719 / bd7c56a32b, 5718 / 46c7c90784 and 5717 / deda3602d0.
  Practical 5721 is the KI#123 live BUG-advisor variant repair. The Domestic and Military advisor factories are re-run on each F1/F5 open, return immediately when the selected variant is unchanged, replace both the cached object and `HandleInputMap` when it changes, and reset the Custom Domestic Advisor's module-level position-cache flag before rebuilding that object. The current Military path calls `interfaceScreen()` for both variants; only BUG-specific `IconGridActive` setup is conditional. No surviving regression was established.
  Practicals 5720 and 5719 were audited together as the World Advisor geography split. After practical 5720 removes the overlapping Land row, Water/Peak/Hill/Flat form an exhaustive, mutually-exclusive plot partition while River/Lake/Coastal remain intentionally overlapping properties. `TERRAIN_PEAK` and `TERRAIN_HILL` are excluded only from the Terrain table; both are XML `bGraphicalOnly=1` pseudo-terrain entries, while real plot terrain continues to be counted through `getTerrainType()`. Practical 5719's Fort removal moves that information to the BFC2/Improvements presentation rather than dropping it, and its River/Lake/Coastal predicates are applied consistently in the city-BFC and empire-territory collectors.
  Practical 5718 changes only the World Advisor class docstring in runtime source. Practical 5717 reorders the World Advisor tabs/layout declarations so BFC1 becomes the default and Environment moves last; its large diff mainly relocates the Environment draw function and related initialization while preserving the calculations. No current data-selection or state defect was established.
  Certified cursor advances to practical 5717 / deda3602d0.
  Next: practical 5716 / 117cd6da20.

SAS-C037 - practical 5716 through 5712 World Advisor migration/BFC origin audit + retrospective vassal-observer interaction - COMPLETE / DEFECT LATER FIXED AS KI#224
  Reviewed 5 runtime-source SHAs: practical 5716 / 117cd6da20, 5715 / ec3d2640ec, 5714 / 9abe202a3c, 5713 / 4919f62e5f and 5712 / b5ca948b8e.
  Practical 5716 removes the old Info-screen `Improvements Current` snapshot while retaining the save-persistent Units/Buildings `CyStatistics` counters. The current-map improvement information had already moved into the World Advisor BFC/Territory collectors, where it is more exhaustive; no lifetime counter was silently deleted. Practical 5715's advisor-button art/World-Advisor route reshuffle is presentation/data relocation only.
  Practical 5714 introduces Territory plus the graphical-only/special-map Improvement Sevopedia grouping. Its BFC-vs-Suburbs split forms a unique set of the selected player's city-BFC plot indices and then buckets only plots owned by that player; graphical-only/special-map classification changes documentation/grouping rather than simulation. Practical 5713 adds BFC2 and the shared lazy legend helper; practical 5712 adds BFC1 and the World Advisor reopen/zoom path. Under their original invariant, `self.iActivePlayer`/`self.iActiveTeam` are the real active player/team, so their reveal and bonus filters respect the human observer's knowledge.
  Retrospective cross-generation defect / KI#224 confirmed while tracing those collectors forward: newer practical 5755 adds non-debug vassal perspectives and changes World Advisor so `self.iActiveTeam` becomes the selected vassal's team. BFC1, BFC2 and Territory then used that selected team for `pPlot.isRevealed(...)`; BFC2/Territory additionally used it for `pPlot.getBonusType(...)`, while improvement/route collectors read raw live plot state. The shared vassal-perspective feature explicitly promises that it does not reveal hidden map data. The DLL does not make master/vassal visibility identical: `CvPlot::updateSight` gives a master sight around vassal cities, but vassal-owned plots and vassal units grant ordinary sight to the vassal team, so plots can be revealed to the vassal and not to the active master's team. Teams can also differ in bonus-reveal technologies. Selecting a vassal could therefore make World Advisor aggregate geography/resources that the actual active player did not know.
  Fixed in commit fa4a1468d by keeping the selected vassal as the data-owner/player perspective while deriving a separate observer team from the real active player outside debug mode. Reveal and bonus checks now use that observer; improvement and route counts use its revealed-state APIs, while debug mode retains its reveal bypass and selected-team resource behavior. This restores the documented vassal-view contract without removing the vassal perspective itself.
  Certified cursor advances to practical 5712 / b5ca948b8e.
  Next: practical 5711 / 3ad8ca1d07.

SAS-C038 - practical 5711 through 5706 World Advisor extraction / Hall of Fame identity / minor Sevopedia naming audit - COMPLETE
  Reviewed 6 runtime-source SHAs: practical 5711 / 3ad8ca1d07, 5710 / 3ae737e057, 5709 / 6574be4005, 5708 / db2912d8db, 5707 / a118eeebd5 and 5706 / f8d1cda84b.
  Practical 5711 completes the Environment-tab extraction from `CvInfoScreen` into the standalone `CvWorldAdvisorScreen`: the pollution/global-warming formulas, help widgets and player/team reset state are moved with the new advisor shell rather than replaced by a second calculation path. Practical 5710 introduces that shell and the F7 World Advisor control consistently across `CIV4ControlInfos.xml`, the C++ `ControlTypes` enum / `canDoControl` / `doControl`, the Python enum and `CvScreensInterface`; `WORLD_ADVISOR = 31` is unique in the Python screen-id table and the screen dispatch remains live in current descendants. No control/screen-id mismatch or simulation change was established.
  Practical 5709 is the small `CvInfoScreen.py` diff-reset cleanup. Practical 5708 moves the Timeline LOG-button font-size lookup out of early module/screen initialization and into runtime-layout calculation after game defines are initialized; the same 48/28 versus 66/32 sizing rule is preserved, while the false `SAS_UI_FONT_LABEL=0` startup warning path is avoided. Its other runtime hunks are comment-header cleanup only.
  Practical 5707 adds Hall-of-Fame leader/civilization/color/trait presentation and changes Info Score-tab leader/civilization cells from contact widgets to Sevopedia jumps. Hall-of-Fame reconstruction is deliberately best-effort because replay data stores leader/color directly but civilization only through description strings; unmatched/custom civ names explicitly produce no civ icon, and trait icons intentionally reflect current XML. Stored replay scores/results are not modified. The Info Score tab already supplied the actual leader/civilization buttons before this practical, so the widget-target change does not newly expose an identity that the row had hidden. No current sorting/index mismatch or replay-state mutation was established.
  Practical 5706 only renames the Sevopedia `Un.List`/`Combat` labels to `Units`/`Unit Chart` and corresponding documentation/text references; runtime category IDs/handlers remain unchanged.
  Certified cursor advances to practical 5706 / f8d1cda84b.
  Next: practical 5705 / 964cd251f7.

SAS-C039 - practical 5705 through 5700 Sevopedia EventTrigger effects/prereqs/grouping audit - COMPLETE / DEFECT LATER FIXED AS KI#225
  Reviewed 6 runtime-source SHAs: practical 5705 / 964cd251f7, 5704 / f592e66e53, 5703 / 298f4e5250, 5702 / 72e780b452, 5701 / de5c11cc2c and 5700 / db46a59ae5.
  Practical 5705 is presentation/layout polish for `SevoPediaEventTrigger`. Practical 5704 expands EventInfo effect cards with previously omitted flags/scalars/script hooks and plot changes; spot checks against `CvPlot::canApplyEvent/applyEvent` confirm that negative Improvement/Route changes really do remove the current improvement/route even when the XML type is `NONE`, while positive changes require the concrete type. No wrong-sign or field/API mismatch was established in those additions. Practical 5703 adds more trigger prerequisite fields/buttons plus prerequisite-event era chaining; feature/terrain/improvement/bonus/route lists are correctly rendered as alternatives, matching the DLL's plot-trigger loops. Its prerequisite-event link chooses the first trigger containing a shared EventInfo; duplicate EventInfos do exist, but the link is necessarily a best-effort bridge because EventInfo has no standalone Sevopedia page, and no gameplay/pedia state corruption was established.
  Practical 5702 adds direct/indirect EventTrigger groupings and shared multilist connector helpers; practical 5701 moves the generic pedia-link wrapper into `_sevopedia_helpers` and removes temporary debug dumps. Their widget targets and helper argument order survive in current descendants.
  Practical 5700 introduced the earliest-era inference and KI#225. `_SAS_getEventTriggerEarliestEra` / later `_SAS_getEventTriggerEarliestEraAndSource` treated every `BuildingsRequired`, `UnitsRequired`, `ReligionsRequired` and `CorporationsRequired` entry as an AND-like hard constraint and took the maximum era across the full list. The DLL instead treats these lists as eligibility pools governed by the corresponding `iNum*` requirement: `CvUnit::getTriggerValue` accepts any listed unit class and `pickTriggerUnit` only needs one valid unit; player/city building, religion and corporation checks count/accept qualifying alternatives rather than requiring every listed member.
  Concrete current-XML proofs: `EVENTTRIGGER_MAN_NAMED_JED` has `iNumUnits=1` with Scout (Ancient/Hunting) OR Explorer (Renaissance/Astronomy), so it can satisfy its unit gate in Ancient while the old pedia helper took the maximum and grouped it as Renaissance (indirect). `EVENTTRIGGER_BLESSED_SEA` has `iNumReligions=1` with Paganism (Ancient/Polytheism) plus Buddhism/Daoism (Classical), so one Ancient religion suffices but the old grouping was pushed to Classical. `EVENTTRIGGER_EXPERIENCED_CAPTAIN` requires one Drydock (Medieval/Machinery) and one eligible naval unit; Galley is Ancient/Sailing, but later alternatives extend through Modern, causing the old max-all-alternatives inference to group the trigger in Modern even though the combined building+unit gates can already be met in Medieval.
  This was a grouping/UI correctness defect, not simulation behavior: the helper explicitly claimed to compute the earliest era at which a trigger can fire, but its aggregation semantics disagreed with the engine and deterministically placed some triggers too late. Fixed in the current implementation batch by using the earliest eligible building/unit class, the Nth-earliest distinct religion/corporation type for player-level counts, and the total-count plus one-listed-type semantics for city triggers, while still taking the maximum against truly AND-gated direct/indirect constraints.
  The helper also intentionally skips route-derived eras and can therefore leave route-only triggers in the Any-era bucket. That limitation is explicit in the code and is not promoted to a separate KI in this checkpoint; it can be reconsidered as an accuracy enhancement when repairing the broader grouping helper.
  Certified cursor advances to practical 5700 / db46a59ae5.
  Next: practical 5699 / 823694fffa.

SAS-C040 - practical 5699 through 5698 Sevopedia EventTrigger link/effect-semantics audit - COMPLETE / NEW SURVIVING DEFECT
  Reviewed 2 runtime-source SHAs: practical 5699 / 823694fffa and 5698 / 218460e3fb.
  Practical 5699 wraps EventTrigger asset descriptions in the existing Sevopedia literal-link mechanism. The wrapper is presentation-only and current link dispatch still reverse-matches the same info descriptions before jumping to a pedia page; no simulation state or list-index change was introduced. Practical 5698 expands the EventInfo effect summary. Its `bGoldToPlayer` wording is imprecise in the generic comment because the transfer direction also depends on the sign of the event gold, but current XML has only the negative-gold `bGoldToPlayer=1` case, for which the displayed "Gift Gold to Other Player" direction is correct; no current defect is counted for that comment.
  Practical 5698 does contain KI#226 candidate. It labels `CvEventInfo::getMinPillage()/getMaxPillage()` as a "Pillage Gold" range and appends the gold-commerce symbol. The DLL uses those fields as a count of improvements to destroy, not an amount of gold: `CvPlayer::applyEvent` and `CvCity::applyEvent` choose a pillage count in that range and remove that many eligible improvements, while `CvGameTextMgr`'s native event help renders the same values as pillage counts. Current XML contains live examples such as `EVENT_LOOTERS_1` with 1/1 and other events with 2-4 and 3-5 pillage ranges. The current Sevopedia therefore reports an objectively wrong effect/unit for those events.
  Preferred surgical repair for KI#226: rename/render this effect as an improvement-pillage/destruction count (singular value or range as appropriate) and remove the gold icon. Reuse the engine's existing pillage wording where practical so city/global variants remain semantically aligned. Code/KI implementation is intentionally deferred to the user's Codex pass.
  Certified cursor advances to practical 5698 / 218460e3fb.
  Next: practical 5697 / e991959c53.

SAS-C041 - practical 5697 through 5692 Event/Vote Sevopedia bindings/load/refactor audit - COMPLETE / NEW SURVIVING DEFECT
  Reviewed 6 runtime-source SHAs: practical 5697 / e991959c53, 5696 / 7e3ae7fe1f, 5695 / 24e206d39d, 5694 / 0fe53a9cba, 5693 / d6c6e0bbb5 and 5692 / fb18bad9c4.
  Practical 5697 fixes three real inherited Python-binding mistakes (`getReligionRequired`, `isPickOtherPlayerCity`, and `CvEventInfo.getNumUnits`) and exposes additional Event/EventTrigger fields through correctly typed wrappers/getters. The current declarations and Boost.Python targets agree with the owning info classes; no new binding-class/signature mismatch survived review. Practical 5696 moves Event/EventTrigger XML loading to the end of pre-menu globals so Sevopedia works from the main menu; all referenced runtime info domains are already loaded there and `bEventsLoaded` prevents the former optional-game load from repeating it. Practical 5695 adds optional Vote Civilopedia text through the existing info-base path.
  Practicals 5694/5693 centralize Sevopedia margins, pane-icon sizes and animation rotations. The moved constants retain the exact former values (15/10 margins, 64/164 icon/frame, -20/30 rotations), and consumers preserve the old formulas/targets; no layout/state semantic change was established.
  Practical 5692 introduces the still-current Vote Sevopedia and contains KI#227 candidate. `placeRequirements()` reads `voteInfo.getTradeRoutes()` and displays it as `Trade Routes: N`, while `placeSpecial()` reads the same field and correctly displays it as the all-cities trade-route effect. The DLL confirms `CvVoteInfo::getTradeRoutes()` is an enacted-resolution effect: `CvGame::processVote` calls `changeTradeRoutes(kVote.getTradeRoutes() * iChange)` when applying/removing the passed resolution. It is not a vote prerequisite. Current `VOTE_SINGLE_CURRENCY` has `iTradeRoutes=1`, so its page deterministically displays the +1 reward twice, once falsely under Requirements and once correctly under Effects.
  Preferred surgical repair for KI#227: remove `getTradeRoutes()` from the Requirements panel and retain the existing Effects entry. The adjacent Population Threshold / Minimum Voters / State Religion Vote Percent fields describe voting availability/threshold/weight rules rather than this enacted effect; no broader panel redesign is required for the concrete bug. Code/KI implementation is intentionally deferred to the user's Codex pass.
  Certified cursor advances to practical 5692 / fb18bad9c4.
  Next: practical 5691 / f3d0345919.

SAS-C042 - practical 5691 through 5683 TechChooser/city-HUD geometry and per-era leader-art lineage audit - COMPLETE
  Reviewed 9 runtime-source SHAs: practical 5691 / f3d0345919, 5690 / bdd90ecbc3, 5689 / cea9de8907, 5688 / 7e8e968117, 5687 / 30003b69d5, 5686 / 85e356f7ed, 5685 / 56fe019d7e, 5684 / 8f6e1c5329 and 5683 / ad844aa896.
  Practical 5691 right-aligns scoreboard leader names only while the Tech Chooser screen is active and dirties the scoreboard on both open and close. The same local alignment flag controls vassal prefix/postfix placement and label justification, so the temporary alignment does not leave the vassal marker on the wrong side; the normal BUG option is restored on the next redraw after close. Practical 5690 separates the city-screen decorative center ribbon lift from the functional `CenterBottom` bar height and adds cached restart-stable geometry defines. Both center rectangles are derived from the same saved base geometry and are restored from that base when leaving city view; no accumulating move/resize drift or uninitialized-define path survived review. Practical 5689 replaces the inherited fixed 0.23/0.03 city-camera displacement with restart-stable percentage defines; the calculation, selected-city plot/null checks and north-neighbor interpolation remain otherwise unchanged.
  Practical 5688 is the follow-up overlay-order repair for expanded leader history: expanded content returns through the leaderhead overlay, expanded history draws only its overlay, and normal history is drawn last so it remains top-most. No panel-state recursion or history-state mutation was introduced.
  Practicals 5683-5687 are the divergent-branch/merge lineage for optional per-era leaderhead art. The XML parser allocates one default-constructed `CvString` per era, validates parsed era IDs before indexing and leaves omitted eras empty; the copy constructor explicitly deep-copies that `CvString` array after AdvCiv's raw member memcpy, and the destructor frees it. Practical 5684 merely moves the common no-era-art case ahead of the player scan. Practical 5685 removes the temporary Cyrus/De Gaulle test XML and gates parsing behind a restart-stable SAS define. Practical 5686 exposes the era tag to Python and adds preview buttons; preview IDs come only from known era entries and `-1` is handled as the default-art reset. Practical 5687 merges the feature branch and retains the same resulting Python/C++ content rather than introducing a merge-conflict semantic change.
  The only established limitation in this family remains already-documented KI#120: `CvLeaderHeadInfo::getArtInfo()` has no player-instance argument and therefore chooses the lowest-index ever-alive player using that leader, so duplicate leader instances can share the wrong era art. Current XML contains no `EraArtDefineTags` entries, so the optional feature is presently dormant unless such data is reintroduced; that does not make the implementation itself a new defect.
  Two loose audit observations from the preceding cutoff were explicitly closed before certification. First, current practical 6173 has exactly one `pediaJump(...)` in `SevoPediaLeader.applyLeaderEra()`, and the tracked SAS diff mirror contains no commit adding two consecutive calls there; the earlier apparent duplicate is treated as an inspection/transient false positive and no already-certified commit is reopened. Second, `CvEventInfo.getMaxNumReligions()` is an applicability constraint on religion-conversion events (the DLL uses it to restrict eligible cities), but showing that qualifier alongside the conversion effect is not objectively false in the same way as KI#226's pillage-gold label; no separate defect is counted.
  Certified cursor advances to practical 5683 / ad844aa896.
  Next: practical 5682 / e57d95292b.


SAS-C043 - practical 5682 through 5672 chart sizing, category auto-jump and expanded-panel lineage audit - COMPLETE
  Reviewed 10 runtime-source SHAs: practical 5682 / e57d95292b, 5681 / e9d0f2aab2, 5680 / 25259dc3e3, 5678 / 6318cdd190, 5677 / efd952b607, 5676 / 1b8089910d, 5675 / cc72dc546c, 5674 / c0e3bfca20, 5673 / 2dc47f2c38 and 5672 / b0e2758dc4. Practical 5679 changes XML/docs only and is therefore intentionally skipped by the frozen runtime-source denominator.
  Practical 5682 is presentation-only in the reviewed runtime paths: it widens Era/Handicap/World Size chart field columns, adjusts the corresponding table-fill percentages, renames the base-DLL recommendation from the ambiguous "Base" to "18 civs", and wraps the main-interface waiting strings in the existing label-font tags. It does not alter chart values, turn-state logic or event dispatch.
  Practical 5681 preserves expanded Sevopedia panels while browsing to another item in the same category. The code captures the previous page object's history/content expansion flags before the new item's `interfaceScreen` call resets them, draws the new item once, then deliberately clears first-pass widgets and redraws with the preserved overlay flag. Same-item reloads keep the existing flag directly, and category changes do not carry expansion state across unrelated page objects. No history-stack or item-ID corruption survived review.
  Practical 5680 changes Religion list ordering only when grouping by era: it requests the underlying XML order inside each era group rather than applying a second alphabetical sort. The remaining runtime-adjacent changes are text/shortcut presentation. Practical 5678 is the already-documented KI#119 repair: `pediaJump(PEDIA_MAIN, category)` first calls `showContents`, so the active category list is rebuilt before `SAS_getFirstSelectableItemIdFromCurrentList()` scans it; the redirect runs only on fresh category clicks (`bRemoveFwdList`) with a real item-list widget, skips no-item categories and removes only the transient category-history node it just created. Current descendants retain that corrected flow.
  Practicals 5672-5677 form one expanded-panel lineage. Practical 5672 introduces the Unit history overlay and the reusable expandable-text helper, while also prepending de-duplicated Unit unique names to the same history text. Practical 5673 generalizes the helper across Bonus, Build, Building, Civic, Civilization, Corporation, Feature, Improvement, Leader, Project, Religion, Specialist, Tech, Terrain and Trait pages. Every converted page initializes `bHistoryExpanded`, resets it only when its item ID changes, exposes `setHistoryExpanded`, and routes the active page through the shared WIDGET_PYTHON dispatcher. Header-bearing pages retain the shared header/text offset; formerly headerless pages remain headerless and use the corresponding top text region. No copy/paste wrong page, wrong widget target, missing reset/setter or content-source substitution was found.
  Practical 5674 adds the analogous expanded non-text content container for Unit animation and an independent `bContentExpanded` state. Practical 5675 adds RELOAD by redrawing the same current category/item without changing that state. Practical 5676 extends the same helper to Bonus, Building and Improvement animations. Practical 5677 adds the Leaderhead-specific expanded container plus attitude/action controls; its expanded attitude refresh redraws the current item, while the normal path refreshes the leaderhead widget in place. History/content overlays deliberately suppress the underlying animation where necessary for widget layering; item changes reset both flags, and the central dispatcher calls a content/history setter only when the active page actually implements it.
  Current descendants were checked for routing/ID and state symmetry rather than judging only the introducing patches. The expanded-text/content/leader helpers still use separate Python action IDs, item changes reset the relevant per-page state, and reload/close/expand operations target the current pedia item rather than creating a category/item jump. No additional surviving correctness defect was established in this family.
  Certified cursor advances to practical 5672 / b0e2758dc4.
  Next: practical 5671 / f1d93d53e6.



SAS-C044 - practical 5671 through 5670 scoreboard traversal simplification and lock-hover toggle audit - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 5671 / f1d93d53e6 and 5670 / 2835bc63ff.
  Practical 5671 replaces duplicated nested ranked-team/ranked-player traversals in `CvMainInterface.updateScoreStrings` with one ordered `aeVisiblePlayers` list. The same `isShowTeamScore` / `isShowPlayerScore` predicates and team-equality test remain in force. In the aligned BUG path, each visible team/player is still registered with `scores.addTeam` / `scores.addPlayer` and `playerScoreString(..., True)` is still called to populate the per-column Scoreboard fields before `Scoreboard.draw`. In the non-aligned path, the visible list is sliced by the same scroll offset/max-row window before drawing. Width measurement, dead-player contact widgets, flash reset and background sizing remain tied to the actually drawn slice. No rank-order or aligned/non-aligned data-loss regression was established.
  Practical 5670 adds the scoreboard hover-lock toggle. `Scoreboard.draw` gains only an explicit `bForceExpanded` parameter that bypasses the normal hover-dependent column removal when true. The main interface registers dedicated WidgetUtil widget IDs/help at module load, creates the lock checkbox once, stores the state in `bScoreAlwaysExpand`, and redraws the current scoreboard when toggled. The feature changes scoreboard presentation only and does not alter the visibility predicates or game/player/team state.
  The lower practical-5667 scroll implementation has an OPEN interaction candidate involving BUG's pre-existing positive `Max Players` cap versus the new outer scroll-window count. It is intentionally not classified or counted here because both practical-5668 SHAs lie between the certified cursor and that introducing commit and must be audited first. Certified progress therefore stops before practical 5668.
  Certified cursor advances to practical 5670 / 2835bc63ff.
  Next: practical 5668 / 57e2cacb59 (`Merge pull request #20 from wonderingabout/ui-font-scaling`).

SAS-C045 - practical 5668 merge/sibling scoreboard-layer audit - COMPLETE
  Reviewed 2 runtime-source SHAs sharing practical 5668: 57e2cacb59 (`Merge pull request #20 from wonderingabout/ui-font-scaling`) and 858784dc33 (`add scoreboard toggle lock hover button`).
  The large-looking 57e2cacb59 GitHub merge was resolved structurally rather than treating its first-parent 24k-line diff as 24k lines of unique implementation. GitHub metadata confirms parents 1dbafbf25dfa80ada5ee5f564b3b236885c1c563 and af7f9f3532ebe2e3920b6002a8e688987a4cd7c3; direct compare from second parent af7f9f3532 to merge 57e2cacb59 reports zero changed files. Thus the merge tree is byte-identical to practical 5667's tree and the merge contributes history only, with no merge-conflict/runtime implementation of its own to regress.
  The sibling 858784dc33 is exactly practical 5667 plus the scoreboard hover-lock feature: runtime changes are limited to `Assets/Python/BUG/Scoreboard.py` and `Assets/Python/Screens/CvMainInterface.py` (with lock art/XML and docs alongside). `Scoreboard.draw` gains the `bForceExpanded` presentation override; the interface registers the up/down/toggle WidgetUtil actions, stores `bScoreAlwaysExpand`, draws a lock checkbox and passes the flag into `Scoreboard.draw`. This is the same feature lineage independently certified in the later practical-5670 merge result; no additional player/team visibility, rank, game-state or hidden UI mutation was introduced by this sibling commit.
  The practical-5667 positive-`Max Players` versus scroll-window mismatch remains OPEN and is now the exact next commit rather than an out-of-order look-ahead. No defect is counted in C045 itself.
  Certified cursor advances through the second practical-5668 runtime SHA / 858784dc33.
  Next: practical 5667 / af7f9f3532 (`avoid scoreboard vertical overflow, and add +/- scroll buttons`).

SAS-C046 - practical 5667 aligned-scoreboard scroll / BUG Max Players interaction audit - COMPLETE / NEW SURVIVING DEFECT
  Reviewed 1 runtime-source SHA: practical 5667 / af7f9f3532 (`avoid scoreboard vertical overflow, and add +/- scroll buttons`).
  The new scroll implementation correctly prevents the scoreboard from running above the available HUD height in the ordinary/default `Max Players = 0` case and supplies independent aligned/non-aligned drawing paths. The surviving defect is narrower: in the aligned BUG path (default `Align Icons = True`), the outer interface computes/clamps `iScoreScrollOffset` and shows +/- buttons from the full visible-player count, then `Scoreboard.draw()` calls the pre-existing `Scoreboard.sort()`, whose `Max Players` option can truncate `_playerScores` before the new scroll slice is applied. `draw()` can only re-clamp its local copy of the offset; the caller retains the uncapped offset/count and uses them for button state.
  The BUG option is live and supported in `Assets/Config/Advanced Scoreboard.xml`: `Align Icons` defaults true and `Max Players` supports 0 through 50 (default 0). A concrete case with 40 visible players, room for 30 scoreboard rows and `Max Players = 10` proves the mismatch: the caller sees 40 and exposes scrolling; `Scoreboard.sort()` reduces the drawable list to 10; `draw()` re-clamps every requested offset to 0 because 10 <= 30; therefore the same 10 players remain visible while the external offset and +/- button state change. The default zero cap masks the regression.
  The non-aligned path is not classified as the same defect: it does not use the BUG `Scoreboard.sort()` / `Max Players` machinery and practical 5667 did not newly remove that cap there. KI#228 is specifically the new aligned-scoreboard scrolling feature consuming a different effective list length than the aligned BUG renderer.
  Current practical 6176 retains the mismatch: `_scoreboardVisiblePlayers(scores)`/`iTotalCount` include all visible players, while `Scoreboard.sort()` still applies positive `ScoreOpt.getMaxPlayers()` before `Scoreboard.draw()` slices. Later active-player centering/button additions also consume the uncapped outer list, so the correct modern repair is to make count, centering, button state and drawing consume one shared post-sort/post-`Max Players` effective player list rather than adding another independent clamp.
  Certified cursor advances to practical 5667 / af7f9f3532.
  Next: practical 5666 / be1f2fad48.


SAS-C047 - practical 5666 through 5657 UI-font-scaling / imported-screen audit - COMPLETE / NEW SURVIVING DEFECT
  Reviewed 10 runtime-source SHAs: practical 5666 / be1f2fad48, 5665 / 3745247826, 5664 / 5977fad88c, 5663 / f4b3071f8e, 5662 / a344736ece, 5661 / d295469939, 5660 / a121ffba58, 5659 / e43ac9a5a3, 5658 / ffa01b3373 and 5657 / e51f6d39ad.
  Practical 5666, 5663 and 5662 are font/button/geometry scaling changes in Wonder Movie, Tech Splash, Victory/SpaceShip and Sevopedia/Timeline UI; the changed values and wrapper calls preserve the existing screen/widget identities and information-selection paths. Practical 5665's Info Graph legend widens the legend region and runtime-fits row/icon height to crowded player sets without changing which graph series or players are selected.
  Practical 5664 adds the legacy Wonder Movie screen and practicals 5659 -> 5660 form the analogous BtS Tech Splash import -> SAS expansion lineage. Current screen dispatch still reaches these screens through their established IDs/options; the subsequent sizing/font changes preserve callback and argument ordering. Practical 5661's era/media changes are presentation-only (including removing the literal ` Era` suffix from the already-localized era description), and its Tech Splash multiline collapse preserves the original call arguments.
  Practical 5658 adds font scaling around event/combat messages. The DLL `addMessage` wrapper receives its `CvWString` by value and wraps that local display string only; the Python combat helper likewise scales log text without changing the event/combat state being reported. No simulation or event-routing regression was established.
  Practical 5657, however, changes the Hall of Fame Date column from the dedicated sortable `screen.setTableDate(...)` cell to `SASTextScale.setTableTextLabel(...)` while retaining `screen.enableSort(self.TABLE_ID)`. It also converts the displayed replay date to a formatted `getDateStr(...)` string. Base AdvCiv 1.14 still uses `setTableDate` for this column, and the current SAS 6176 descendant still uses generic text. The separate Sort-by-Date dropdown remains chronological because it sorts replay rows by numeric `getFinalTurn()`, but clicking the table Date header now asks the engine to sort a text cell rather than a date cell. This is KI#229.
  Certified cursor advances through practical 5657 / e51f6d39ad.
  Next: practical 5656 / 7d69f98142 (`upscale most of diplomacy screen (when it was possible)`).


SAS-C048 - practical 5656 through 5647 diplomacy / Score tab / Dawn-Replay / Timeline-rename audit - COMPLETE
  Reviewed 10 runtime-source SHAs: practical 5656 / 7d69f98142, 5655 / 905a5090a8, 5654 / e987566be4, 5653 / 33f32dd81f, 5652 / c8726554aa, 5651 / e916c28266, 5650 / a689059534, 5649 / 727f3bc0a3, 5648 / 6218188952 and 5647 / d1fb5d675b.
  Practical 5656's diplomacy scaling preserves the source contracts on both sides. Python diplomacy responses originate as XML text keys; the helper localizes once and clears the argument tuple only on successful localization, while unresolved key-shaped strings retain the original args for EXE fallback. The DLL wrapper mutates only the local display `CvWString` returned for trade rows; trade headings remain intentionally unwrapped because their EXE widget can display font markup literally. No trade-state, deal or diplomacy-decision mutation was introduced.
  Practicals 5655 -> 5653 alter the Info Screen Score tab. The always-expanded layout changes geometry only. The new team technology count is a deliberate team-level reference field; sensitive live intelligence remains independently gated (research through `canSeeResearch`, power through demographics visibility, city count through revealed-city logic). Trait icons derive from the admitted scoreboard player rows. No established spoiler/visibility contract violation or sort regression was proved in this tranche.
  Practicals 5652 -> 5648 are Dawn of Man / Replay / Dan Quayle / PLE presentation changes. The 5651 Dawn-of-Man rewrite collects all non-default unit/building overrides but renders only the first pair; current SAS XML was checked against class defaults and no civilization has more than one non-default unit override or more than one non-default building override, so this is future-fragile rather than a live omission. Replay/player metadata selection and widget identities remain intact through the fullscreen/font changes.
  Practical 5647 consistently renames the Info Screen History tab/cache/define/text plumbing to Timeline. Current runtime Python contains no stale `iHistoryID`, `drawHistoryTab`, `buildHistoryCache`, `historyCacheEntries`, History table-coordinate or old History define/text-key references from this renamed system. No rename-induced dead path was established.
  Certified cursor advances through practical 5647 / d1fb5d675b.
  Next: practical 5646 / d01fbb4c76 (`cache in military advisor`).


SAS-C049 - practical 5646 through 5640 Military Advisor cache/layout/icon and Foreign-tab repair audit - COMPLETE
  Reviewed 6 runtime-source SHAs: practical 5646 / d01fbb4c76, 5645 / 88cda735b8, 5644 / b9bebfca30, 5643 / ad18001b54, 5642 / deba579c9d and 5640 / 3a0a232bd3. Practical 5641 / 4d872a762b changes art/font/XML/docs only and is intentionally outside the runtime-source denominator.
  Practical 5646's advertised Military Advisor cache is pass-local hoisting: active player/team/debug/threshold and unit-info objects are stored only in local variables during the draw/refresh call, so there is no persistent cache to become stale when the player/perspective/game state changes.
  Practical 5645 replaces a hardcoded 1920x1080 shell with shared runtime advisor bounds. `updateRuntimeLayout(screen)` runs on `interfaceScreen`, recomputes leader columns, map/text sizes, Great-General bar anchors and resolution-dependent inline-icon size before widgets are built. No old-resolution state is retained across opens. Practical 5644 makes the inline icon sizes XML-tunable by current vertical resolution without changing row data or selection semantics.
  Practical 5643 fixes the older Military Advisor overlay-button approach by embedding each combat/unit/detail icon into the list-row text itself. The existing row widget/data1/data2 values are passed unchanged into the new row helper, preserving click-to-select/map behavior while making icon and text scroll together. Art-file renames alongside it are reflected in XML. Practical 5642 adds only an explanatory comment to runtime Python.
  Practical 5640 is a correct historical Foreign Advisor navigation repair. An already-active tab label previously retained `WIDGET_FOREIGN_ADVISOR` with data `-1`, which could be routed by the shared shell machinery into the other F3/F4 advisor. The fix uses inert `WIDGET_GENERAL` only for the active label; inactive tabs keep their original routing widget/data. Current descendants retain that invariant.
  Certified cursor advances through practical 5640 / 3a0a232bd3.
  Next: practical 5639 / 2128dee8c7 (`refactor move shared safe font upscaling to label to helper file`).


SAS-C050 - practical 5639 through 5632 shared text / Glance / Game Speed / Leader-AIP layout audit - COMPLETE / NEW SURVIVING DEFECT
  Reviewed 8 runtime-source SHAs: practical 5639 / 2128dee8c7, 5638 / 7f14d9f875, 5637 / d272086f76, 5636 / 6c23de1a02, 5635 / 6ae1dc83d0, 5634 / 42dba7265e, 5633 / 04696f26f6 and 5632 / 6bc421f184.
  Practical 5639 centralizes the existing safe label-font wrapping into `SASTextScale`; the helper retains the old no-double-wrap behavior when text already contains a font tag, and the migrated table/list/scoreboard callers preserve their original widget/data arguments and cell types. Practical 5638 is the already-documented KI#116 workaround: only Glance rows with a plain >=2-glyph trailing status-icon run keep that suffix at font 2; the numeric/head text retains the selected label font.
  Practical 5637 correctly changes F3 opening to persisted-tab state and scales city building/trade table text with the label font; active-tab routing had already been fixed in practical 5640. Practical 5636 changes Game Speed chart label abbreviations/first-column widths and adds a legend; the font-specific widths are validated positive and the high-resolution override has an explicit positive threshold gate.
  Practicals 5633 -> 5635 form one AI Personality category-layout lineage. Practical 5633 moves category definitions out of the predumped cache into UI-owned tuples; practical 5634 reorganizes them into named categories and restores the low-resolution compact ordering; practical 5635 fixes the remaining 1440p boundary by using `>= 1440`, moves `No War At` to the left at 1440p+, and skips category construction when the AIP is disabled. Current descendants retain one complete intended category assignment per column/resolution; the intermediate layout mistakes are already repaired.
  Practical 5632 introduces `SAS_SEVOPEDIA_LEADER_ITEMS_NO_REDUCE_MIN_WIDTH`, documented so `<=0` keeps the custom leader item-list width always enabled. `SevoPediaLeader` implements that contract correctly with `(iNoReduceMinWidth > 0) and screenWidth >= threshold`, but `SevoPediaMain.SAS_getLeaderItemsWidthByCurrentUIFont()` immediately returns the base width whenever `screenWidth >= self.SAS_SEVOPEDIA_LEADER_ITEMS_NO_REDUCE_MIN_WIDTH` without checking that the threshold is positive. Therefore setting the define to 0 or -1 makes every ordinary positive resolution force the base width—the exact opposite of the documented disable semantics. Current practical 6176 retains this mismatch. This is KI#230.
  Certified cursor advances through practical 5632 / 6bc421f184.
  Next: practical 5631 / b2c5c93edd (`minor comment upscaling changes`).


SAS-C051 - practical 5631 through 5630 comment/upscaled Domestic Advisor audit - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 5631 / b2c5c93edd and 5630 / e27ca537c7.
  Practical 5631 adds explanatory comments only across Scoreboard/advisor runtime Python; no executable expression changes.
  Practical 5630 upscales the Domestic and Customizable Domestic Advisors. CDA culture/Great-Person labels and table-row font sizes change presentation only. Base Domestic Advisor replaces init-time font-wrapped header constants with raw localized text/symbols plus a draw-time `getOverviewHeaderLabel` wrapper, so runtime label scaling is applied when the table is built rather than freezing a prewrapped body tag. Existing table column order, widths' proportional formulas, data getters, row widgets and cell-value semantics are otherwise preserved. No sorting, selection, perspective or city-state regression was established.
  Certified cursor advances through practical 5630 / e27ca537c7.
  Next: practical 5629 / b6a5681256 (`increase font from 2 to 3 or body to label in most places (to improve readability)`).


SAS-C052 - practical 5629 broad BODY-to-LABEL readability pass - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5629 / b6a5681256.
  This large-looking readability commit was checked by separating the repetitive font substitutions from every other changed token. Across Scoreboard, Foreign Advisor, Info Screen, Tech Chooser and Victory Screen runtime Python, the executable changes are the intended `SAS_FONT_TAG_BODY` -> `SAS_FONT_TAG_LABEL` substitutions (plus trailing-newline churn). Existing helper calls, table cell types, widget/data arguments, player/team visibility predicates, sorting inputs and control flow are unchanged.
  The commit also retunes hover/billboard XML values, but those XML-only changes do not consume the runtime-source denominator and no hidden Python expression change was bundled with them. No surviving correctness defect was established.
  Certified cursor advances through practical 5629 / b6a5681256.
  Next: practical 5628 / 79b078dff6 (`increase base billboard from 100 to 120 percent, increase base hover text size from 2 to 3`).

SAS-C053 - practical 5628 through 5623 billboard / main-interface readability lineage - COMPLETE
  Reviewed 5 runtime-source SHAs: practical 5627 / 3f7bf6f5f8, 5626 / 71065af0f9, 5625 / b32e95eb5c, 5624 / e6c0ad69a8 and 5623 / b8f2aab775. Practical 5628 / 79b078dff6 changes XML/docs only and therefore advances the historical cursor without consuming the runtime-source denominator.
  Practicals 5623 -> 5625 form one city-billboard implementation lineage rather than independent features. Practical 5623 removes the earlier DLL-side `CITY_BILLBOARDS` fScale mutation and billboard-name/production character trimming experiment. Practical 5624 replaces it with `SASBillboardScale.py`, which derives the `CITYBILLBOARD_SCALE` fader keys from the configured body-font multiplier and runs from BUG initialization; practical 5625 keeps the same generated values but avoids rewriting `CIV4DetailManager.xml` when the existing key block already matches. No current stale-DLL path or double-application survives.
  A later real weakness in this Python design was traced rather than misclassified here: after archived XML comments were inserted between the fader `<Name>` and its `<Key>` lines, the original 5624 regex no longer matched. Practical 6117 / 91454e007c explicitly extends that regex over intervening comments; that repair was already certified in SAS-C002 and current 6176 retains it. Thus the lineage contains a historical later-fixed failure, not a new surviving defect from this range.
  Practical 5626 changes the main interface's intended base text from BODY to LABEL and relocates the Specialists Breakdown above Culture while retaining the existing data getters, widget identities and bar/text update paths. Practical 5627 transfers top-bar width from Research to the GP bar and adjusts city food/production text insets using the same target rectangles; the changes are layout accommodations for the larger label font rather than changes to research/GP/yield state. Practical 5628 only retunes XML hover/billboard defaults.
  Certified cursor advances through practical 5623 / b8f2aab775.
  Next runtime-source SHA: practical 5622 / 527e44b2a3 (`add Inverted info in sevopedia ai personality panel legend, fix sevopedia media player text not being bold`).


SAS-C054 - practical 5622 through 5620 Sevopedia polish / Foreign Advisor split lineage - COMPLETE
  Reviewed 3 runtime-source SHAs: practical 5622 / 527e44b2a3, 5621 / 91467a1817 and 5620 / bb7b7eb7d8.
  Practical 5622 changes one shortened AI Personality label and makes the currently selected Media Player queue row explicitly bold; it does not alter cached values, queue identity or playback routing.
  Practicals 5620 -> 5621 form the Foreign Advisor split/control lineage. Practical 5620 creates a lightweight Foreign Diplomacy shell over the canonical Foreign Advisor implementation, partitions the trade and diplomacy/intel tab-key sets and routes known tab IDs to the appropriate shell. Practical 5621 replaces the old Finance-control slot with the explicit Foreign-Diplomacy F3 control and updates Python screen IDs, main-interface button control IDs, C++ control enums/dispatch and hotkey text consistently. Current descendants retain one canonical implementation with separate screen IDs/shell tab sets.
  The initial split did contain a real historical navigation flaw, but it is already repaired: an active F3 tab label still dispatched `WIDGET_FOREIGN_ADVISOR` with data `-1`, which could route a repeat click into the positionally corresponding F4 tab. Practical 5640 / 3a0a232bd3 changes active labels to inert `WIDGET_GENERAL`; that repair was independently certified in SAS-C049. The later KI#123 advisor-object/input-routing rebuild repair is likewise already present in current descendants. No additional surviving defect from the 5620/5621 split was established.
  Certified cursor advances through practical 5620 / bb7b7eb7d8.
  Next: practical 5619 / 862ba215e8 (`move screen indepdendent geometry computation to init in foreign advisor`).


SAS-C055 - practical 5619 through 5617 advisor cache-lifetime audit - COMPLETE
  Reviewed 3 runtime-source SHAs: practical 5619 / 862ba215e8, 5618 / 11d21ee951 and 5617 / f21276c11a.
  Practical 5619 moves only screen-independent Espionage geometry constants into `CvForeignAdvisor.__init__`; resolution/advisor-bound values remain recomputed in `updateRuntimeLayout`. Literal-to-constant substitutions preserve the old values and formulas, including the former `ESP_ROW_Y_AMOUNT` alias (same value as `ESP_ROW_Y_WEIGHT`), and tab-link widths remain recomputed from runtime `X_EXIT`.
  Practical 5618 further caches deterministic color IDs, art paths and static/localized labels in Foreign, Info, Policy and Victory advisors. Language-dependent values remain inside the pre-existing per-language `initText` paths; gameplay/player/team state is still read at draw/update time. No stale perspective, game-state or resolution-derived value was moved into persistent storage.
  Practical 5617 performs the broader Domestic/Foreign/Info/Military/Policy/Tech/Victory cache pass. Domestic, Foreign, Info, Policy and Victory cache translated labels only through language-aware initialization and continue to invoke `initText` on screen-open/update paths. Military adds the same language guard and caches only static text/art/color IDs; live unit/player values remain recomputed. TechChooser explicitly separates language refresh from one-time art caching and retains its special pre-final-initialization path so early access does not leave required fields unset. Across all seven screens, cached art/color/info IDs are deterministic XML lookups, while active-player/team/debug/game-state and resolution-dependent values remain runtime.
  No surviving cache invalidation, language, perspective or layout regression was established in this tranche.
  Certified cursor advances through practical 5617 / f21276c11a.
  Next: practical 5616 / aa02dc468f (`remove multiline Nationality in corporations tab and improve spacing`).


SAS-C056 - practical 5616 through 5614 Policy Advisor corporation presentation audit - COMPLETE / NEW SURVIVING DEFECT
  Reviewed 3 runtime-source SHAs: practical 5616 / aa02dc468f, 5615 / a839d7bd88 and 5614 / 516fdce425.
  Practical 5616 removes the old multiline nationality/HQ-owner presentation and increases Religion/Corporation row spacing. The resulting one-line city/owner labels preserve the same reveal gates and selected-player data sources; no independent presentation-state regression was established in this commit.
  Practical 5615 adds the corporation founding Great-Person icon beside the already-derived founding-unit text. Current SAS has seven corporations and seven corresponding `FoundsCorporation` HQ buildings, so the founding-building loop does not fall through to an unrelated final building under current data; the added icon uses the same unit found by the text path. No current-data defect was established there.
  Practical 5614 adds `Consumes`, generated-bonus and generated-yield/commerce rows to the integrated Corporation tab. `Consumes` maps the prerequisite-bonus list directly and generated bonus maps `getBonusProduced()` correctly. The new `getCorporationGeneratedText()`, however, prints `CvCorporationInfo.getYieldProduced()` / `getCommerceProduced()` raw integer values. Those APIs expose Civ4's hundredths-based corporation rates. Current XML contains values such as 100 food, 50 food, 150 culture and 300 commerce; native `CvGameTextMgr::setCorporationHelp()` applies the world-size corporation percent and then formats those stored rates as +1, +0.50, +1.50, +3, etc. Gameplay likewise multiplies the stored hundredths by resource count/world-size scaling before dividing to yield/commerce units. The Policy tab therefore can display `100` where the actual/native per-resource effect is `+1`, `150` where it is `+1.50`, and additionally ignores world-size scaling. Current practical 6177 retains this helper unchanged. This is KI#231.
  A lower out-of-order look-ahead while checking the 5610/5611 reveal-sensitive Religion/Corporation rows found a strong cross-generation Policy-vassal observer-team candidate related to practical 5755. It is intentionally not counted yet: certified descent stops at 5614, and practicals 5613 -> 5610 will be re-audited in order before classification.
  Certified cursor advances through practical 5614 / 516fdce425.
  Next: practical 5613 / 31de1f5929 (`remove multiline corporation bonuses + show corporations in Sevopedia Building Required For`).


SAS-C057 - practical 5613 through 5610 Policy Religion/Corporation integration lineage audit - COMPLETE / NEW RETROSPECTIVE SURVIVING DEFECT
  Reviewed 4 runtime-source SHAs: practical 5613 / 31de1f5929, 5612 / 2dfd631d16, 5611 / a742ffbf05 and 5610 / 662f1d53c0.
  Practical 5613 adds a corporation jump to a Building page's `Required For` row from that building's own `getFoundsCorporation()` value; widget type/data point to the same corporation ID. Its corporation prerequisite-bonus flattening changes row layout only. No current semantic defect was established.
  Practical 5612 fixes Corporation-tab widget persistence by explicitly deleting the child multiline widgets and per-corporation dynamic labels when changing tabs. Its `NUM_CORPORATION_PREREQ_BONUSES` cache is initialized on first screen entry rather than early module construction and represents a restart-stable XML define; no stale player/game value is cached.
  Practical 5611 integrates the old Corporation screen into the Policy advisor and redirects the legacy `CORPORATION_SCREEN` dispatcher to the new tab. Under the then-valid invariant that `iActivePlayer` is always the real active player, its HQ reveal check is information-safe. The founding-unit display searches the building whose `FoundsCorporation` matches the current corporation and then the unit that can build it; current SAS XML has seven corporations and exactly seven corresponding founding/HQ buildings, so the loop does not fall through to an unrelated final building. The commit also repairs practical 5610's transient Exit-handler mistake by routing the Religion Exit and Corporation Exit through `closePolicyScreen` while leaving the explicit Convert button on the conversion path.
  Practical 5610 integrates the legacy Religion screen into Policy. Its holy-city reveal check still uses the real active player's team, and its BUG/advc religion-city help/convert logic remains tied to the selected active player as before. The new separate Exit control was initially handled by `ReligionConvert`, but practical 5611 immediately corrects that transition bug; current descendants are safe from that handler issue.
  Retrospective cross-generation defect / KI#232: practical 5755 / 1fabcaa557 later adds non-debug vassal perspectives to `CvPolicyAdvisorScreen`, replacing the old real-active-player reset with `getAdvisorValidPerspectivePlayer(..., bAllowVassalPerspective=True)` in both Religion and Corporation refresh paths. The pre-existing global holy-city and headquarters rows continue to test `isRevealed(gc.getPlayer(self.iActivePlayer).getTeam(), ...)`, so after 5755 the observer becomes the selected vassal team rather than the human/master team. The shared vassal-perspective contract explicitly promises that this UI access does not reveal hidden map data. Master/vassal map knowledge is not identical: vassalization updates city sight for the master, but ordinary exploration/unit sight remains team-specific; a vassal can therefore have revealed an unrelated third party's holy city/HQ that its master has not. Selecting that vassal can expose the foreign city name/owner through Policy. KI#224's later fix is World-Advisor-specific and does not touch these Policy reveal paths; current practical 6177 still uses the selected vassal team here.
  Preferred KI#232 repair: preserve the selected vassal as the Policy data subject, but outside debug derive a separate map-observer team from `CyGame().getActivePlayer()` for global holy-city/HQ reveal checks. Debug mode may retain the selected-team behavior. This mirrors the already-validated KI#224 subject-vs-observer split without changing religion/corporation gameplay or vassal permissions.
  Certified cursor advances through practical 5610 / 662f1d53c0.
  Next: practical 5609 / 685511e46f (`fix last visited religion tab selected on reopen; refactor`).


SAS-C058 - practical 5609 through 5604 early Policy/Religion integration and canonical rename audit - COMPLETE
  Reviewed 6 runtime-source SHAs: practical 5609 / 685511e46f, 5608 / 0bb222d640, 5607 / 6ab44b0f61, 5606 / d7073ea436, 5605 / 236c73f2bc and 5604 / aaa800431b.
  Practicals 5608 -> 5609 form the first Religion-to-Policy tab integration/refactor. The initial merge redirects the legacy Religion screen entry/input mapping into Policy and adds page-state/data/layout plumbing; 5609 then preserves the last visited Policy tab on ordinary reopen, removes unused legacy members and derives Religion layout from the shared runtime advisor bounds. Later practicals 5610-5611 complete the Religion/Corporation transition and repair the transient Exit-handler issue already documented in C057. No additional current survivor was established from the earlier merge.
  Practical 5607 only aligns the Policy advisor title with the shared advisor anchor.
  Practicals 5604 -> 5606 canonicalize the old Civics/Policy screen name across Python and the small DLL caller surface. Practical 5605 keeps the screen's numeric Python ID unchanged at 9 while renaming the symbol to `POLICY_ADVISOR_SCREEN`; `CvScreensInterface` maps that same ID to `policyAdvisorScreen`, `CvGame::doControl(CONTROL_CIVICS_SCREEN)` and the civic popup both call the new `PolicyAdvisorScreen` Python entrypoint, and the screen object's name/input-map identifiers are renamed consistently. Practical 5606 then performs a 100%-similarity file rename from `CvCivicsScreen.py` to `CvPolicyAdvisorScreen.py` and updates the import. Practical 5604's preceding rename changes the screen/UI identifiers consistently without changing gameplay civic selection. No C++/Python enum, string-entrypoint or input-map mismatch survives.
  Certified cursor advances through practical 5604 / aaa800431b.
  Next: practical 5603 / c6ab80c208 (`update advisor buttons order and other related changes`).


SAS-C059 - practical 5603 advisor-control removal/hotkey-order audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5603 / c6ab80c208 (`update advisor buttons order and other related changes`). The archived diff is partial overall, so the executable control changes were reconstructed across the embedded runtime hunks, adjacent history and current descendants.
  The commit removes dedicated Religion/Corporation controls consistently from `CvEnums.h`, `CyEnumsInterface.cpp`, `CIV4ControlInfos.xml`, `CvGame::canDoControl/doControl` and the main-interface HUD button create/show/hide/front paths. The standalone Religion/Corporation Python entrypoints still exist at this point, so removing only their dedicated keyboard/HUD controls in preparation for the later Policy-tab merge does not break unrelated screen dispatch.
  The Civics/Finance F2/F3 swap changes only their XML hotkeys and matching visible labels/button ordering; the underlying control enum identities and `CvGame::doControl` cases remain intact. Removing two enum entries is mirrored by removing the two XML `ControlInfo` entries and Python enum exports, so action-info/control indexing stays aligned. No surviving control-ID, hotkey, HUD-button or Python-entrypoint mismatch was established.
  Certified cursor advances through practical 5603 / c6ab80c208.
  Next: practical 5602 / 0532e0fd8b (`Move victory screen to f9 and info screen to f8`).


SAS-C060 - practical 5602 through 5600 advisor hotkey / Environment-to-Info / Finance-to-Domestic migration audit - COMPLETE
  Reviewed 3 runtime-source SHAs: practical 5602 / 0532e0fd8b, 5601 / 9953b9f0ad and 5600 / d25484e2bb.
  Practical 5602 swaps the Info and Victory shortcut assignments to F8/F9 and reorders their main-interface advisor buttons accordingly. Their control/screen identities are not exchanged; XML hotkeys, labels and HUD ordering agree, so no input-routing mismatch was established.
  Practical 5601 moves the Environment page out of the deleted `EconomicsAdvisor.py` and into `CvInfoScreen.py`. The migrated data path preserves the old semantics: pollution source/total values use the selected Info player, player-dependent sustainability/land-defence calculations use the same player ID, and global-warming index/chances/event tally/severity remain game-global. The tab list/index and runtime geometry are extended consistently, and no environment calculation was silently dropped or redirected to another player/team.
  Practical 5600 aliases the legacy Finance advisor entry/input mapping to the Domestic advisor and adds a Finance tab. The migrated financial calculations retain the old public player APIs and arithmetic, including public `calculateUnitCost()` / `calculateUnitSupply()` (therefore retaining the engine's anarchy-zero behavior rather than the later KI#219 internal-overload mistake), maintenance/civic inflation handling, treasury/GPT, commerce-source and expense breakdowns. The standalone Economics-advisor header/debug chrome is intentionally not migrated; the financial data itself is redrawn inside the Domestic shell. No surviving finance arithmetic or routing regression was established.
  Certified cursor advances through practical 5600 / d25484e2bb.
  Next: practical 5599 / b8253247d2 (`mirror the other advisors' structure in domestic advisor ...`).


SAS-C061 - practical 5599 Domestic Advisor shell / Free Colony control audit - COMPLETE / KI#233 FIXED LATER
  Reviewed 1 runtime-source SHA: practical 5599 / b8253247d2.
  Practical 5599 replaces the default `CvDomesticAdvisor` floating-table presentation with the shared advisor shell. The table geometry, title/background panels and specialist footer remain internally consistent, but the commit also deletes the independent `DomesticSplit` block while doing that layout work: the old screen scanned `getLiberationPlayer(false)` / `canSplitEmpire()`, created a `WIDGET_ACTION` button bound to `CONTROL_FREE_COLONY`, and closed the advisor after that button was clicked. Current `CvDomesticAdvisor.py` still has no corresponding Free Colony/Liberate button.
  This is not an intentional removal of the underlying colony mechanic. Base AdvCiv 1.14 retains the conditional `DomesticSplit` button, current `CONTROL_FREE_COLONY` still opens `BUTTONPOPUP_FREE_COLONY`, and the optional BUG `CvCustomizableDomesticAdvisor` still exposes the same button. However, `CustDomAdv.Enabled` defaults to false, so the standard SAS F1 path uses the button-less `CvDomesticAdvisor`. Alt+F1 remains an XML hotkey for `CONTROL_FREE_COLONY`, which means the mechanic is not completely inaccessible, but the default Domestic Advisor has lost its visible/contextual control without the 5599 commit message or docs identifying that gameplay-action affordance as intentionally removed. This is KI#233.
  Fixed and runtime-validated in the current implementation batch by restoring the conditional native action in a runtime-derived footer slot without restoring the superseded floating background/old exit layout; see F030 and KI#233 for implementation and screenshot evidence.
  Certified cursor advances through practical 5599 / b8253247d2.
  Next: practical 5598 / 2934fc73cf (`add alternative top spacing above tech tree...`).


SAS-C062 - practical 5598 through 5590 TechChooser / Victory / Civics / Domestic advisor layout-cache audit - COMPLETE
  Reviewed 9 runtime-source SHAs: practical 5598 / 2934fc73cf, 5597 / 270b91c183, 5596 / 81f966e929, 5595 / 9d31bbb851, 5594 / 16f52c5916, 5593 / 0ace3d709e, 5592 / b36211565d, 5591 / 08c4bdf9e3 and 5590 / 09a4bc35a6.
  Practical 5598 adds a paired low-resolution TechChooser right-space/top-space override under the same positive width threshold; negative configured spaces are clamped to zero and runtime resolution remains read on screen open. Practical 5597 centralizes the advisor title Y anchor without changing screen-specific data/dispatch.
  Practicals 5594 -> 5596 form the Victory-screen expansion lineage. Practical 5594 intentionally makes the Resolutions and Members tabs exhaustive even before a vote source is active and supplies a required-wonder fallback table. Under Rise & Fall, AdvCiv still appends member rows to Resolutions and the separate Members tab is therefore redundant, but the 5594 commit message explicitly states that all tabs should always be shown; this was treated as an intentional navigation choice rather than a correctness defect. The fallback's owner-name visibility retains the existing own-team/debug/met-team rule. Practical 5595 replaces fixed 1024x768 bounds with shared runtime advisor bounds. Practical 5596 scales the Victory table columns to the runtime content width, keeps each column positive, and replaces runtime trimming of the localized vote-requirement text with a dedicated translation key; no vote arithmetic or eligibility rule changes.
  Practical 5593 caches only Civics static/translated labels behind the existing current-language/final-initialization guard. Practical 5592 recomputes Civics geometry from runtime advisor bounds and expands civic panels proportionally; civic choice/revolution state remains runtime. Practical 5591 similarly caches Domestic static headers only per language. Practical 5590 moves Domestic screen dimensions to the shared runtime-bounds helpers while preserving its city-table contents, selection widgets and specialist calculations. No stale player/game state, table-cell semantic change, input-routing mismatch or surviving advisor correctness defect was established in this tranche.
  Certified cursor advances through practical 5590 / 09a4bc35a6.
  Next runtime-source SHA: practical 5589 / c5b7209bf5.


F038 - KI#241: Foreign Advisor Treaties rows can hide resource terms while canceling the whole mixed deal - FIXED / VALIDATED
  Origin: SAS practical 5478 / 2078e7e8e9 (`Rename Foreign Advisor's Active Tab to Treaties Tab, beautify and enhance with icons`) replaces `CyGameTextMgr().getDealString(deal, ...)` in `CvExoticForeignAdvisor.drawActive` with per-item formatting and explicitly skips every `TRADE_RESOURCES` item. The row remains `WIDGET_DEAL_KILL` keyed only by `deal.getID()`.
  Confirmed current survivor: a `CvDeal` can and does contain resources together with other annual terms. `CvDeal::startTrade` returns `bSave=true` for both `TRADE_RESOURCES` and `TRADE_GOLD_PER_TURN`, and `CvDeal::addTradeItems` stores all saved items from both offer lists in the same deal object. More strongly, AdvCiv's ordinary end-of-round `CvPlayerAI::AI_foldDeals` deliberately folds opposite AI GPT-for-resource deals into one replacement `CvDeal` containing resource(s) plus the net GPT delta through `implementAndReturnDeal`. This is therefore a normal reachable game state, not merely an engine-permitted synthetic bundle.
  The cancellation widget operates on that complete object. `CvDLLWidgetData::doDealKill` resolves only `GC.getGame().getDeal(widgetDataStruct.m_iData1)` and opens cancellation for the deal ID; it has no trade-item sub-ID. Its hover/help path likewise calls `GAMETEXT.getDealString` for the full deal. Consequently a mixed deal such as resource + 10 GPT can be rendered by the Treaties tab only as `10 GPT` while clicking that row cancels the resource too. Before practical 5478 the row used the full deal string, so visible terms and cancellation scope matched. Current SAS practical 6182 retains the resource-skipping row construction.
  This is distinct from the intentional decision not to clutter Treaties with resource-only agreements: a resource-only deal is skipped entirely and therefore offers no misleading cancellation row. The defect occurs when hidden resource terms share a `CvDeal` with at least one displayed term.
  Surgical direction: preserve the reduced Treaties presentation only if the clickable row still makes whole-deal scope explicit. The simplest correctness repair is to render the full deal whenever a displayed deal also contains one or more resource terms (optionally with the new icon formatter for all terms), or otherwise remove/replace whole-deal cancellation from a partial row. Do not keep a partial term list attached to `WIDGET_DEAL_KILL` without indicating the hidden terms.
  Fixed in the current Foreign Advisor batch: resource-only deals remain omitted and deals without resources retain the current icon-rich formatter, but a visible mixed deal containing any resource now formats every item from both sides with resource/gold/treaty icons and Civ4's localized perspective grammar. A compact leader button beside the localized player name makes direction easier to scan. Displayed terms therefore match the complete deal canceled by `WIDGET_DEAL_KILL`; turn suffixes and one-row-per-deal behavior remain unchanged. Save file 478 first confirmed the complete plain-text form in screenshot 0071, then the final icon-enhanced form rendered correctly in-game; the refreshed `PythonErr.log` is empty. Static Python syntax and branch checks also pass.
  Classification: SAS-specific Foreign Advisor UI/action-scope regression introduced at practical 5478 and retained through the supplied practical-6182 source snapshot.

F037 - KI#240: Medieval+ Copper hard exclusion overrides live strategic and corporation value - FIXED / VALIDATED
  Origin: SAS practical 5484 / 923840f152 (`(Tremendously Improved) Do not buy dominated or equivalent strategic bonuses (era and bonus-aware)`) adds an early hard-denial block to `CvPlayerAI::AI_bonusTrade`. For Copper it marks every Medieval-or-later buy as dominated, regardless of whether the recipient has Iron: `eBonus == B_COPPER && (bHaveIron || bMedievalPlus)`.
  Confirmed current survivor: Copper is not universally dominated in that era range. Base AdvCiv 1.14's generic Privateer already accepts `BONUS_COPPER` OR `BONUS_IRON`; current SAS retains that resource alternative and places `UNIT_PRIVATEER` at Medieval `TECH_PROFESSIONAL_ARMY`. Thus a Medieval AI with no Iron can have a currently trainable strategic unit unlocked by importing Copper, yet practical 5484 returns `DENIAL_JOKING` before ordinary recipient value can preserve the offer. The hard rule therefore contradicts its own "dominated/equivalent" premise for a generic, reachable unit path rather than merely missing a rare future possibility.
  Corporation valuation independently proves the early hard return is too broad. Current `CORPORATION_4` and `CORPORATION_5` both consume Copper and produce positive yields/commerce from their prerequisite bonuses. `AI_corporationBonusVal` assigns positive marginal value when such a corporation is present, and the pre-existing `AI_bonusTrade` duplicate-resource filter deliberately hides additional copies only when `AI_corporationBonusVal(eBonus, true) <= 0`. The new 5484 block runs before that safeguard and can deny even a first Copper import, or later corporation-useful copies, throughout the corporation eras.
  The issue existed in substance at introduction, not only through later SAS drift: Copper-or-Iron Privateers are inherited from Base AdvCiv, and the hard `bMedievalPlus` branch was introduced by 5484 itself. Current SAS practical 6182 retains the same unconditional branch.
  Surgical direction: do not treat era alone as proof that Copper has zero recipient value. At minimum remove the unconditional `|| bMedievalPlus` Copper exclusion so a Copper-less/Iron-less AI reaches the normal value logic; additionally exempt resources with positive `kPlayer.AI_corporationBonusVal(eBonus, true)` from the hard dominated-resource gate so the existing corporation-aware contract is preserved. The other explicit substitute rules can remain independently tunable.
  Fixed in commit 9fb1f179a: Copper is now hard-dominated only when the recipient actually has Iron, and the hard denial is skipped when `kPlayer.AI_corporationBonusVal(eBonus, true)` is positive. Corporation valuation is queried only after a resource matches a dominated rule. This preserves the remaining substitute rules while allowing live Privateer and corporation value to reach ordinary recipient valuation. The DLL built successfully and a general in-game smoke run completed with an empty refreshed `PythonErr.log`; current Privateer/corporation XML and the source paths validate the corrected behavior without claiming a separately constructed diplomacy-save reproduction.
  Classification: SAS-specific AI trade-logic regression introduced at practical 5484 and retained through the supplied practical-6182 source snapshot.

F036 - KI#239: dominated-strategic AI-buy hard block leaks onto human recipients - FIXED / VALIDATED
  Origin: SAS practical 5484 / 923840f152 inserts the dominated/equivalent strategic-resource block near the start of `CvPlayerAI::AI_bonusTrade` under `iChange >= 0`, but does not restrict it to an AI recipient even though the commit title, KI#109 documentation, Main Changes guide and define all describe preventing the AI from buying inefficient resources.
  Confirmed current survivor: `kPlayer = GET_PLAYER(eToPlayer)` is the recipient/buyer and `bPlayerHuman = kPlayer.isHuman()`. The new block executes before AdvCiv's later `if(!bPlayerHuman)` value gate whose explicit contract says "Allow human to import resources that they probably don't need, but make sure that the AI doesn't." Consequently an AI seller can return `DENIAL_JOKING` for a human recipient solely from the AI-buy heuristic; e.g. a first-copy Copper import is hard-denied to a human in Medieval+ even though the later human-recipient path would otherwise let the player decide whether it is useful.
  This denial is not merely advisory text. `CvPlayer::buildTradeTable` calls the resource owner's `getTradeDenial`, which routes resources to `AI_bonusTrade(..., eWhoTo, 1)`, and when either participant is human it omits resources whose denial is `DENIAL_JOKING`. The affected AI-owned resource therefore disappears from the human diplomacy trade inventory. `AI_checkCancel` also calls `AI_bonusTrade(..., iChange=0)` for existing resource exports, so the same scope leak can cancel an AI-to-human resource deal after the heuristic becomes true.
  Surgical direction: scope the hard dominated-buy block to a non-human recipient, e.g. require `!bPlayerHuman` together with `iChange >= 0`. This still suppresses inefficient purchases whenever the buyer is AI, including AI purchases from a human seller, while restoring the pre-existing human-recipient trade/value contract and preventing heuristic-only cancellation of human imports.
  Fixed in commit 9fb1f179a by requiring `!bPlayerHuman` at the hard dominated-resource gate. AI recipients retain the efficiency rule for both AI and human sellers, while human recipients return to the pre-existing human-controlled import path. The successful DLL build and general in-game smoke run with an empty refreshed `PythonErr.log` validate integration; the explicit recipient direction and trade-table/cancellation call paths validate the scope correction without claiming a separately constructed diplomacy-save reproduction.
  Classification: SAS-specific diplomacy/trade regression introduced at practical 5484 and retained through the supplied practical-6182 source snapshot.

F035 - KI#238: SAS_Parallel_Lines extra players reuse the final custom starting slot - FIXED / VALIDATED
  Origin: SAS practical 5486 / 0870e0adc8 (`Add SAS_Parallel_Lines.py, update 48 civs DLL - Add quotes in Assets rebalancing`) introduces `PrivateMaps/SAS_Parallel_Lines.py`. Its layout size and `line_slots` capacity are derived from `CvWorldInfo.getDefaultPlayers()` for the selected world size, while `findStartingPlot` independently enumerates the actual alive civ players.
  Confirmed current survivor: the sum of `line_slots` is exactly the world default-player count. For any actual alive-player index beyond that capacity, the slot-selection loop exhausts without a match but leaves `iLine`/`iPosInLine` at the last line's last slot. Thus every excess player resolves to the same final custom start used by the last in-capacity player. Example: Standard / 3 Players Per Line builds `[3, 3, 2]`; player index 8 (the ninth player) falls through to line 2, position 1, duplicating player index 7.
  This is a reachable Custom Game/setup state rather than malformed input. In current DLL code, `CvGame::getRecommendedPlayers()` uses world `getDefaultPlayers()` as a recommendation, while actual player life is independently initialized from `SS_TAKEN` / `SS_COMPUTER` slots across `MAX_CIV_PLAYERS`. `SAS_Parallel_Lines.findStartingPlot` itself enumerates those actual alive players rather than enforcing the world default.
  The engine does not repair the duplicate: `CvPlayer::findStartingPlot` immediately returns any non-null Python starting plot, and `CvPlayer::setStartingPlot` records it without a uniqueness test. Consequently the normal C++ taken-start search is bypassed for the duplicated Python result.
  Sibling SAS maps already show the appropriate defensive contract: `SAS_Spiky_Avenues` and `SAS_Large_Facing_Islands` call `CyPythonMgr().allowDefaultImpl()` when the actual player index exceeds their custom slot list instead of reusing a slot.
  Surgical direction: before mapping `iIndex` into `line_slots`, compare it with the total custom slot capacity (or otherwise detect that no slot was matched); if out of range, call `CyPythonMgr().allowDefaultImpl()` and return. This preserves the designed custom starts for in-capacity players and lets the existing DLL choose a non-taken fallback for extras. A separate used-start set could further harden custom fallback searches, but is not required to fix this proven overflow path.
  Fixed and validated in commit 49732aa36 by delegating actual player indexes beyond the summed custom capacity to the DLL before the old final-slot fallthrough. Screenshot 0065 confirms a Standard nine-player / 3 Players Per Line game generated three populated lines with distinct positions; the refreshed `PythonErr.log` remained empty.
  Classification: SAS-specific regression/design bug introduced at practical 5486.

F034.2 - KI#237.2: runtime-sized city Great Person bar retains a fixed 230px text budget - FIXED / VALIDATED
  Classification: inherited Base AdvCiv scalable-HUD regression introduced by AdvCiv practical 3784 / e491d6d576 when the scalable-hud branch was merged into 1.06; retained by Base AdvCiv 1.14 and the audited pre-fix AdvCiv-SAS source.
  K-Mod/BUG passed 230px to the city-screen `getGreatPeopleText()` caller for its fixed layout. Practical 3784 introduced the runtime-scaled `CityRightPanelContents` and full-width `GreatPeopleBar`, but retained 230 in the relocated caller. At wider resolutions the rendered bar therefore has room for more GP types than the fitting helper is allowed to consider.
  Save file 478 screenshots 0060 and 0061 expose the mismatch in Poverty Point: the wider map GP bar shows all 53% / 42% / 5% types, while the city GP bar omits the fitting 5% Scientist because its caller still reports only 230px. Fixed in the current implementation batch by passing `gRect("GreatPeopleBar").width()`, matching the map caller's runtime-width contract. Post-fix screenshots 0062 and 0063 confirm that the map and city bars now both render 53% / 42% / 5%; the refreshed `PythonErr.log` remained empty.


F034 - KI#237: Maximum-types Great Person progress text accepts one candidate beyond its width limit - FIXED / VALIDATED
  Classification: inherited K-Mod/BUG-era Main Interface display bug retained by Base AdvCiv 1.14 and the audited pre-fix AdvCiv-SAS source; discovered while auditing practical 5505.
  `GPUtil.getGreatPeopleText()` builds `szNewTypes` by appending the next Great Person icon/percentage, but measures `szText + szTypes`, the previously accepted list, before assigning `szTypes = szNewTypes`. The fit test is therefore one candidate late: the first item that pushes the rendered text beyond `iGPBarWidth - 10` is accepted. Maximum types is the current default, mixed-Great-Person-point cities provide multiple candidates, and the top-bar form also includes the variable city name.
  Fixed in the current implementation batch by measuring the candidate string containing `szNewTypes` before accepting it, preserving the current probability order and stop-on-first-nonfit behavior. Static syntax and a narrow regression fixture confirm that the old control flow accepted an over-limit candidate while the corrected flow rejects it. Save file 478 screenshots 0062 and 0063 provide the in-game Maximum-types smoke test after the fix, with identical 53% / 42% / 5% data in both GP bars and an empty refreshed `PythonErr.log`. The separate fixed 230px city-caller mismatch exposed during testing is tracked as KI#237.2 / F034.2.


F033 - KI#236: Executive Sevopedia pages omit the corporation required for city training - FIXED / VALIDATED
  Classification: inherited Base AdvCiv Sevopedia requirement-display omission; current XML makes it visible for all seven Executive units.
  `CvUnitInfo.getPrereqCorporation()` is exposed to Python, `CvPlot::canTrain` requires that corporation to be active in the city, and inherited BUG unit-build logic reads the same prerequisite. Before the fix, Base AdvCiv 1.14 and SAS `SevoPediaUnit.placeRequires()` displayed unit tech, bonus, religion and building requirements but never read the corporation prerequisite.
  Fixed in commit 26b2ac788. Screenshot 0049 confirms that the AlumCo Executive now shows its technology and Aluminum Co as two separate Requires icons; screenshot 0047 confirms the new icon's correct Aluminum Co hover, and runtime testing confirmed that clicking it opens the Corporation page. The refreshed `PythonErr.log` remained empty.


F032 - KI#235: actual-unit hover suppresses functional Bomber collateral information - FIXED / VALIDATED
  Classification: AdvCiv-SAS DLL unit-hover information regression introduced on the merged branch in practical 5506 / c9be31986e and brought into the audited mainline by practical 5544 / f1e860eb99.
  `CvGameTextMgr` emitted actual-unit collateral lines only when collateral damage/limit was positive and `pUnit->canFight()`, which tests base combat strength only. Current Bomber and Stealth Bomber units instead have positive air combat and functional air collateral.
  Fixed and validated in commit f2ecb1334 by accepting `pUnit->canFight() || pUnit->airBaseCombatStr() > 0` while preserving civilian suppression. The DLL compiled successfully. Screenshot 0054 confirms that an actual Stealth Bomber hover now shows `Coll. D. (base 100%, lim. 200%) to 6 Defenders`; screenshot 0055 confirms that an actual Settler hover remains free of collateral noise.


F031 - KI#234: persistent Foreign Advisor can retain an invalid Espionage target/city/mission - FIXED / VALIDATED
  Classification: AdvCiv-SAS Foreign Advisor/Espionage state regression introduced by practical 5583 / e138ad4f82.
  Fixed in commit fbd121d7f by validating the retained target after rebuilding the alive/met list, preserving valid selection state while clearing invalid target/city/mission state together. Runtime testing confirmed persistence for a valid Lincoln target and correct replacement by Julius Caesar after a debug-perspective change made Lincoln ineligible; the refreshed `PythonErr.log` remained empty.


F030 - KI#233: default Domestic Advisor shell refactor removed the visible Free Colony / Liberate action - FIXED / VALIDATED
  Classification: AdvCiv-SAS default-Domestic-Advisor UI regression introduced by practical 5599 / b8253247d2; survives current practical 6177.
  Before practical 5599, normal `CvDomesticAdvisor.interfaceScreen()` checks whether any city can be liberated or whether the player can split the empire and, when applicable, creates `DomesticSplit` as a `WIDGET_ACTION` using `CONTROL_FREE_COLONY`'s action-info ID. Its click handler closes the advisor while the control opens the native free-colony/liberation popup. Base AdvCiv 1.14 still contains this path.
  Practical 5599's shared-shell refactor deletes the entire scan/button/handler block together with the old floating-background layout. The commit and corresponding Main Changes Guide text describe the new top/bottom-bar shell, contrast and removal of old shell chrome; they do not identify the separate colony/liberation gameplay-action button as intentionally removed. Current standard `CvDomesticAdvisor.py` still lacks any `CONTROL_FREE_COLONY` button.
  The optional BUG `CvCustomizableDomesticAdvisor` still creates `DomesticSplit`, but `CustDomAdv.Enabled` defaults to false, so that does not preserve the default F1 experience. `CONTROL_FREE_COLONY` also still has Alt+F1 in `CIV4ControlInfos.xml` and the DLL still dispatches it to `BUTTONPOPUP_FREE_COLONY`, so the game mechanic itself is not unreachable; the surviving defect is loss of the visible/context-sensitive action from the default Domestic Advisor.
  Fixed in the current implementation batch by restoring the conditional native `WIDGET_ACTION` plus `CONTROL_FREE_COLONY` action-info binding for the real active-player perspective. The button retains the original HUD advisor style and close-screen behavior, while a runtime-derived footer slot prevents overlap with the newer five-tab navigation and the current Exit-label width. Screenshot 0044 confirms the icon and native Alt+F1 tooltip render cleanly beside Exit; screenshot 0045 confirms clicking it closes the advisor and opens the correct native popup with Alexandria and Memphis as liberation choices. Alt+F1, the optional CDA path and the modern shell remain unchanged; the refreshed `PythonErr.log` was empty.


F029 - KI#232: Policy Advisor vassal perspective can use the vassal team's hidden holy-city/HQ knowledge - FIXED / VALIDATED
  Classification: AdvCiv-SAS Policy Advisor information-visibility regression introduced by practical 5755 / 1fabcaa557 over reveal-sensitive Religion/Corporation rows integrated in practicals 5610-5611; survives current practical 6177.
  The Religion tab's global holy-city row and Corporation tab's global headquarters row intentionally hide the city while it is unrevealed. In practicals 5610-5611 this is safe because `iActivePlayer` is reset to the real active player before drawing, so `pHolyCity.isRevealed(gc.getPlayer(self.iActivePlayer).getTeam(), ...)` / the corresponding HQ check use the human observer's team.
  Practical 5755 adds non-debug vassal perspectives and changes Policy's screen-open plus Religion/Corporation refresh paths to preserve a selected vassal through `getAdvisorValidPerspectivePlayer(..., bAllowVassalPerspective=True)`. It does not alter the old reveal gates. Consequently those global rows now ask what the selected vassal team has revealed. If that vassal has explored an unrelated player's holy city or corporate HQ while the human/master has not, the human can select the vassal and learn the hidden foreign city name/owner.
  This is reachable because a vassal relationship does not merge team map knowledge. `CvTeam::setVassal` updates city sight, and `CvPlot::updateSight` grants the master adjacent sight around vassal cities, but ordinary plot/unit exploration remains team-specific. The shared advisor helper and Main Changes Guide explicitly state that vassal advisor perspectives may expose subordinate domestic/world/policy state but must not reveal unrelated players or hidden map data.
  KI#224 fixed the same subject-vs-observer class of mistake only in `CvWorldAdvisorScreen` by introducing a real-active-player observer team; `CvPolicyAdvisorScreen` was not changed and current practical 6177 still uses the selected `self.iActivePlayer` team for both holy-city and HQ reveal tests. Keep KI#232 separate so the already-fixed World-Advisor issue remains accurately scoped.
  Fixed in the current implementation batch with a Policy Advisor observer-team helper. Outside debug, Religion Holy City and Corporation Headquarters `isRevealed` checks now use the real active player's team while `self.iActivePlayer` remains the selected vassal for permitted subject data; debug mode retains selected-team reveal behavior. Screenshot 0043 confirms that the Corporation tab renders normally for the selected Hatshepsut vassal perspective after the change. That save does not provide a controlled human-hidden/vassal-revealed city contrast, so the information boundary is validated directly by the observer-team code path while runtime testing supplies the vassal-perspective smoke test; the refreshed `PythonErr.log` was empty.


F028 - KI#231: Policy Advisor corporation generated yield/commerce row prints stored hundredths as whole units - FIXED / VALIDATED
  Classification: AdvCiv-SAS Policy Advisor corporation-display correctness regression introduced by practical 5614 / 516fdce425; survives current practical 6177.
  Practical 5614 adds `getCorporationGeneratedText(kCorpInfo)` and writes each nonzero `getYieldProduced(iYield)` / `getCommerceProduced(iCommerce)` directly with `%d` plus the corresponding GameFont symbol. These `CvCorporationInfo` getters do not return display units: the XML values are stored in hundredths. For example, current corporation XML contains `iYieldProduced=100` for a +1 yield effect, `50` for +0.50 and `iCommerceProduced=150` for +1.50.
  The DLL's authoritative `CvGameTextMgr::setCorporationHelp()` first scales each stored rate by the current world size's `getCorporationMaintenancePercent()`, then divides/formats by 100, including two decimal places when needed. `CvCity::getCorporationYieldByCorporation` / `getCorporationCommerceByCorporation` use the same stored hundredths and world-size scaling in gameplay. Thus the Policy tab currently presents raw storage values such as `100` or `150` as if they were +100/+150 yield or commerce per resource and can also disagree with non-100% world-size scaling.
  Fixed in the current implementation batch by applying the active map's world-size corporation percentage with C++-matching truncation toward zero, then formatting the scaled hundredths as exact signed integer or two-decimal display units before the existing yield/commerce symbols. Values scaled to zero remain omitted, and corporation gameplay/XML values are unchanged. Screenshot 0043 directly confirms correct fractional/signed output such as `+0.50`, `+0.25`, `+0.75`, `+1`, `+1.50` and `+1.75` across the seven Corporation columns; the refreshed `PythonErr.log` was empty.


F027 - KI#230: nonpositive Leader item-width cutoff reversed its documented disable behavior - FIXED
  Classification: AdvCiv-SAS Sevopedia Leader/UI configuration regression introduced by practical 5632 / 6bc421f184.
  `SAS_SEVOPEDIA_LEADER_ITEMS_NO_REDUCE_MIN_WIDTH` is documented in `GlobalDefines_advciv_sas.xml` as the screen-width threshold at/above which Leader pages stop reducing the item-list width, with `<=0` meaning the custom/font-dependent width stays enabled regardless of resolution. The Leader page's panel-width calculation follows that semantics: it applies the base-width override only when `iNoReduceMinWidth > 0` and the screen reaches the threshold.
  The same practical adds a separate main-list helper, `SevoPediaMain.SAS_getLeaderItemsWidthByCurrentUIFont()`, but its first branch is simply `if screenWidth >= self.SAS_SEVOPEDIA_LEADER_ITEMS_NO_REDUCE_MIN_WIDTH: return self.SAS_W_ITEMS_BASE`. For a configured value of 0 or -1 every normal positive screen width satisfies the branch, so the main list is forced to the unreduced base width rather than keeping the custom width. The two consumers can therefore disagree and the documented hard-disable value behaves backwards.
  Fixed in commit 0a96cdaa7 by mirroring the already-correct Leader-side condition in `SevoPediaMain`: the high-resolution base-width override now applies only when the threshold is `> 0` and the current resolution reaches it. The XML comment was also corrected to describe the existing at-or-above (`>=`) threshold exactly. With the cutoff set nonpositive and label font 3, runtime testing showed the Leader item list retained its 200px custom width and remained visibly narrower than the Civilization list's 230px base width, directly validating the documented disable behavior.


F026 - KI#229: Hall of Fame Date header lost date-typed sorting during font upscaling - FIXED
  Classification: AdvCiv-SAS Hall-of-Fame UI correctness regression introduced by practical 5657 / e51f6d39ad.
  Before practical 5657, the sortable Hall of Fame table writes the replay Date column with Civ4's dedicated `screen.setTableDate(...)`. The same screen independently provides a Sort-by-Date dropdown whose row ordering uses numeric `replayInfo.getFinalTurn()`. Practical 5657's text-upscaling pass replaces the Date cell with `SASTextScale.setTableTextLabel(...)` and stores a formatted `CyGameTextMgr().getDateStr(...)` display string, while leaving `screen.enableSort(self.TABLE_ID)` active. The table-header sort therefore no longer receives the date-typed cell that the original implementation intentionally supplied.
  The distinction is not merely historical style. Base AdvCiv 1.14 still has `enableSort` plus `setTableDate` for the Date column, and Civ4 exposes separate `setTableDate`, `setTableInt` and `setTableText` table APIs; SAS's own text helpers document text cells as lexical and integer cells as numeric. Current practical 6176 still writes the Date column through `setTableTextLabel`. Thus the dropdown can remain chronologically correct while a user click on the Date column header sorts the formatted BC/AD/date strings as text rather than using date semantics.
  Fixed in commit 0a96cdaa7 by adding shared `SASTextScale.setTableDateLabel`, which applies the configurable label font through Civ4's dedicated `setTableDate` cell type, and using it for the Hall of Fame Date column. The existing Sort-by-Date dropdown remains unchanged. Runtime testing confirmed that the Hall of Fame renders and the clickable Date header changes between ascending/descending sort states without an error; the available Hall of Fame contained only one entry, so multi-row chronological order could not be visually exercised in that test.


F025 - KI#228: aligned scoreboard scrolling disagreed with BUG Max Players cap - FIXED
  Classification: AdvCiv-SAS main-interface/BUG scoreboard regression introduced by practical 5667 / af7f9f3532.
  Practical 5667 adds a caller-owned scoreboard scroll offset and screen-height row cap. In the default aligned BUG path, `CvMainInterface` computes `iTotalCount` from every currently visible player and clamps/shows the new scroll controls from that total, then calls `Scoreboard.draw(screen, offset, maxRows)`. Inside `draw`, the pre-existing `Scoreboard.sort()` still honors the configurable positive `ScoreOpt.getMaxPlayers()` by truncating `_playerScores`; only after that does the new code clamp its local `iScrollOffset` to the truncated length and slice the rows.
  These two counts can disagree. With 40 visible players, 30 available rows and BUG `Max Players = 10`, the outer interface exposes scrolling over 40, while `Scoreboard.draw` has only 10 rows and locally forces every offset back to zero. The user can therefore change the external offset/button states without changing the displayed ten-player window. `Align Icons` defaults true; `Max Players` is a supported 0..50 option whose default 0 explains why the problem is normally hidden.
  Current descendants retain the split and later scoreboard autocenter controls also use the uncapped outer list. Preferred repair: centralize the aligned scoreboard's effective sorted/capped player list (or expose its effective count/order) and derive scroll clamp, active-player centering, +/- availability and drawing from that same post-`Scoreboard.sort`/post-`Max Players` population. Do not paper over only the button condition with a separate min() that could leave centering and drawing on different orders.
  This is UI correctness only; player/team visibility and game simulation are not changed.
  Fixed/validated/committed in SAS practical 6177 / c99ab79e5b by exposing and reusing the aligned BUG scoreboard's post-sort/post-`Max Players` player-ID order for total count, active-player centering, scroll clamping, +/- availability and drawing. The non-aligned path remains unchanged. Quick new-game and late-game save-file testing passed and `PythonErr.log` remained empty.

F024 - KI#227: Vote Sevopedia duplicated the trade-route resolution effect as a requirement - FIXED
  Classification: AdvCiv-SAS Sevopedia Vote-page correctness regression introduced by practical 5692 / fb18bad9c4.
  The initial Vote page adds `voteInfo.getTradeRoutes()` to `placeRequirements()` as `TXT_KEY_PEDIA_SAS_VOTE_TRADE_ROUTES`, while the same page also adds the exact field to `placeSpecial()` as `TXT_KEY_PEDIA_SAS_VOTE_EFFECT_TRADE_ROUTES_ALL_CITIES`. The latter is the correct semantic location. In the DLL, `CvGame::processVote` applies/removes `kVote.getTradeRoutes()` through the global `changeTradeRoutes(...)` only when processing the vote resolution; the field is not consulted by `canDoResolution` as a prerequisite.
  Current XML gives a direct visible case: `VOTE_SINGLE_CURRENCY` has `iTradeRoutes=1`. Before the fix, its Sevopedia page could therefore say `Trade Routes: 1` under Requirements and also `All Cities Trade Routes: +1` under Effects, falsely implying the reward must already exist before the resolution can be used.
  Fixed in commit c8903d21a by deleting the Requirements-side `getTradeRoutes()` line and keeping the existing Effects-side rendering, with a comment documenting `CvGame::processVote` semantics. Vote simulation remains unchanged.

F023 - KI#226: EventTrigger effect summary mislabeled improvement-pillage counts as gold - FIXED
  Classification: AdvCiv-SAS Sevopedia EventInfo correctness regression introduced by practical 5698 / 218460e3fb.
  The practical adds a "pillage gold range" renderer that reads `eventInfo.getMinPillage()` / `getMaxPillage()` and displays `TXT_KEY_PEDIA_SAS_EVENT_EFFECT_PILLAGE_GOLD` plus the gold-commerce symbol. Those fields do not represent gold. In `CvPlayer::applyEvent`, when `getMaxPillage() > 0`, the DLL computes `iPillage` from the min/max range, searches eligible owned improved plots and removes improvements until the requested count is reached. `CvCity::applyEvent` uses the same fields as a number of city-radius improvements to pillage. The native `CvGameTextMgr` event help likewise formats the values through the `TXT_KEY_EVENT_PILLAGE*` strings as counts, never as treasury gold.
  Current XML made the error directly visible before the fix: `EVENT_LOOTERS_1` uses `iMinPillage=1`, `iMaxPillage=1`, and other live events use ranges such as 2-4 and 3-5. Their Sevopedia choice summaries could therefore show "Pillage Gold: 2-4" with a gold icon even though the actual effect is to destroy/pillage 2-4 improvements.
  Fixed in commit c8903d21a by displaying `Improvements Pillaged` without a gold-commerce glyph, using one count for equal bounds and a range only when the bounds differ. Event simulation remains unchanged.

F022 - KI#225: EventTrigger earliest-era grouping maxed alternative asset/religion requirements as if every option were mandatory - FIXED
  Classification: AdvCiv-SAS Sevopedia correctness regression introduced by practical 5700 / db46a59ae5 in `_SAS_getEventTriggerEarliestEra`; later practicals 5702/5703 extended the presentation/chaining but preserved the same max-all-alternatives inference in `_SAS_getEventTriggerEarliestEraAndSource`.
  The old helper correctly handled direct OR-techs with a minimum and direct AND-techs with a maximum, but then called `_bumpIndirect` for every required building class, unit class, religion and corporation, retaining the maximum era across the entire list. Its comment called these constraints all "AND-ish." The engine semantics are different. `CvUnit::getTriggerValue` accepts a unit when its class matches any `UnitsRequired` entry, and `CvPlayer::pickTriggerUnit` needs only one valid unit when `iNumUnits > 0`. Player-level building checks sum counts across listed building classes and compare the total with `iNumBuildings`; religion/corporation checks likewise count qualifying listed alternatives against `iNumReligions` / `iNumCorporations`. City-level trigger checks accept any listed eligible building/religion/corporation once the relevant count gate is met.
  Current XML made the error directly observable. `EVENTTRIGGER_MAN_NAMED_JED` requires one unit and lists Scout (Ancient) plus Explorer (Renaissance); a Scout can trigger it in Ancient, but the old pedia grouping took Explorer's later era and reported Renaissance. `EVENTTRIGGER_BLESSED_SEA` requires one of Paganism/Buddhism/Daoism; Paganism is Ancient while the other two are Classical, so the earliest possible religion gate is Ancient but the old grouping reported Classical. `EVENTTRIGGER_EXPERIENCED_CAPTAIN` requires one Drydock (Medieval) and one unit from a large alternative naval list beginning with Ancient Galley and extending through Modern ships; the actual combined gates can be met in Medieval, while maxing all unit alternatives pushed the old pedia grouping to Modern.
  Fixed in the current implementation batch by aggregating each family according to its threshold semantics: earliest eligible building/unit class, Nth-earliest distinct religion/corporation type at player level, and total-count plus one-listed-type for city triggers. Count requirements without an explicit religion/corporation list now use all corresponding XML types, and types without a technology prerequisite remain valid earliest alternatives. True AND constraints and prerequisite-event chains still use the outer maximum; the separate route-only approximation remains explicit and unchanged.

F021 - KI#224: World Advisor vassal perspective could use the vassal team's hidden map/resource knowledge - FIXED
  Classification: AdvCiv-SAS World Advisor information-visibility regression introduced by practical 5755 / 1fabcaa557 when non-debug vassal perspectives were added to collectors originally introduced in practicals 5712-5714.
  Before practical 5755, the World Advisor's `self.iActiveTeam` was the real active player's team, so BFC/Territory calls such as `pPlot.isRevealed(self.iActiveTeam, False)` and `pPlot.getBonusType(self.iActiveTeam)` naturally used the human observer's knowledge. Practical 5755 allowed the active player to select an alive vassal and set `self.iActiveTeam` from that selected player. BFC1/BFC2/Territory therefore gated plots by the vassal team's reveal state; BFC2/Territory also revealed bonuses according to the vassal team's technologies.
  This was not harmless because master/vassal teams do not share full map sight. `CvPlot::updateSight` explicitly extends a vassal city's sight to its master, but the ordinary owned-plot and unit branches grant sight to the owning/unit team, not automatically to the master. A vassal can therefore have territory/exploration revealed that the active master's team has not revealed. `CvPlot::getBonusType(eTeam)` separately returns a bonus only when that observer team has the reveal technology, so a technologically different vassal could expose a resource the active team could not yet identify. The BFC2/Territory code also read raw `getImprovementType()` / `getRouteType()` after using the vassal reveal filter, which could bypass the active observer's last-known improvement/route state.
  The intended contract is explicit in both the practical-5755 shared-helper comment and the current Main Changes Guide: vassal advisor perspective may expose the vassal's relevant domestic/world/policy state but "does not reveal unrelated players or hidden map data." The World Advisor violated that contract.
  Fixed in commit fa4a1468d by separating subject from observer. `self.iActivePlayer` remains the selected vassal for cities, ownership and state, while outside debug the fog/resource observer team comes from `CyGame().getActivePlayer()`. Reveal and bonus checks use that team, improvements/routes use its revealed-state APIs, and debug mode preserves its reveal bypass and selected-team resource behavior.

F020 - KI#223: Domestic Advisor Founded column sorted formatted BC/AD strings lexically instead of chronologically - FIXED
  Classification: AdvCiv-SAS Domestic Advisor UI correctness regression introduced by practical 5722 / 7de906fcdd.
  Practical 5722 enables sorting on the new Overview tables and adds a Founded column whose cell is `CyGameTextMgr().getTimeStr(pLoopCity.getGameTurnFounded(), false)` written with `screen.setTableText`. The same practical explicitly documents in `SASTextScale.setTableIntLabel` that Civ4 sorts `setTableInt` cells numerically while `setTableText` cells sort lexically. Thus clicking Founded orders the human-readable strings, not founding time; e.g. strings spanning BC/AD cannot maintain chronological order.
  Before the fix, SAS still wrote the Founded cell with `setTableText` in `updateTable4`; practical 5748 only moved the column from Overview 3 to Overview 4, and no later historical source commit supplied a numeric sort representation. This is not the mixed-content Info-screen Wonders case that practical 5722 deliberately makes textual: Domestic Founded always represents a real city founding date.
  Fixed in commit c8903d21a by following the existing World Advisor convention: derive the raw signed founding year through `CyGame().getTurnYear` and render it with the shared numeric-table helper. The column now sorts chronologically; Civ4 exposes no separate numeric sort key that would preserve the localized BC/AD display.

F019 - KI#222: Battle-history capture matcher could not associate current capturable civilians with their preceding battle - FIXED
  Classification: AdvCiv-SAS Military Advisor Battles regression introduced by practical 5765 / 254194766d; survives the current source snapshot.
  Practical 5765 adds a DLL `unitCaptured` event after a captured replacement unit is successfully created, and Python searches newest-first for the battle row to decorate with Cap#/captured-unit data. `_matchesCapturedBattle` requires current turn, capturing player as row winner, old owner as row loser, and crucially `entry[4] == iOldUnitType`. `entry[4]` is the defeated combat unit type; `iOldUnitType` is the non-defending unit that was actually captured.
  Current-SAS XML makes that equality impossible for the standard capture mechanic. The only non-`NONE` `<Capture>` units are `UNIT_SETTLER`, `UNIT_WORKER`, `UNIT_INDIAN_FAST_WORKER`, `UNIT_NETHERLANDS_SWIFT_WORKER` and `UNIT_MAYA_QUICK_WORKER`; all have `iCombat=0` and all capture/convert to `UNITCLASS_WORKER`. `CvUnit::canFight()` returns `baseCombatStr() > 0`, and `canDefend()` immediately rejects units that cannot fight, so none of those five units can be the lethal defender/loser stored in a combatResult row.
  The engine capture path confirms the intended association is plot-based instead. In `CvUnit::setXY`, after the fighting unit enters a plot it iterates existing enemy units; non-defending/capturable units can receive `setCapturingPlayer(getOwner())` and are then killed/converted. The `unitCaptured` event is emitted from that unit's `kill` after `initUnit` creates the replacement. Thus a Worker/Settler capture normally follows the escort-clearing battle on the same plot, but its type is necessarily different from that battle's defeated fighting unit.
  Consequence: under current unit data the Battle-history capture matcher cannot patch the ordinary Worker/Settler captures it was introduced to show, so Cap#/Cap can remain blank even though the capture event itself fires correctly. This also explains why practical 5765 had to chase a separate BUG-event-manager shadowing problem without addressing the deeper association predicate.
  Preferred surgical repair: use `pNewUnit.getX()/getY()` and match the newest same-turn lethal battle row at those coordinates with the same capturing-player/old-owner direction; remove the `entry[4] == iOldUnitType` requirement and reject retreat/non-lethal rows. Practical 5772 already changed attacker-win battle coordinates to the loser's plot, eliminating the pre-advance-coordinate reason that practical 5765 originally gave for avoiding plot equality. Multiple captured civilians remain compatible with `_getEntryWithCapture`: every current capturable source unit converts through `UNITCLASS_WORKER`, so repeated capture events for a cleared plot resolve to the same capturing civilization's Worker unit type and increment the existing count. Fixed in SAS practical 6172 / c36032deb5 with the plot-based lethal-row association; Known Issues documentation was added in the same commit.

F018 - KI#221: undefended city conquest could relabel an unrelated same-turn battle as a city capture - FIXED
  Classification: AdvCiv-SAS battle-history regression introduced by practical 5772 / fc82d93007 in `SASBattleHistory.recordCityCaptured`; survives the current source snapshot.
  Practical 5772 patches the just-recorded combat row after `onCityAcquired` so the Military Advisor can distinguish a city that actually fell from a city merely defended in combat. `_matchesCityCapturedBattle` currently requires only `entry[0] == current game turn`, `entry[1] == new owner` and `entry[2] == previous owner`; `_patchCityCapturedEntry` searches newest-first and then rewrites that row's X/Y plus city-context flag.
  The engine does not require a combat to conquer a city. In `CvUnit::setXY`, a fighting unit that moves onto an enemy city plot calls `GET_PLAYER(eNewOwner).acquireCity(pNewCity, true, false, true)` even when the city has no defender; `CvPlayer::acquireCity` then reports `cityAcquired(..., bConquest=true)`. Such an undefended-city conquest therefore reaches `recordCityCaptured` with no new `combatResult` row to patch.
  Concrete corruption case: Player A defeats a Player B unit elsewhere earlier in turn T, then later walks into an undefended B city. The conquest event searches A/B history, finds that earlier same-turn A-won/B-lost battle, and `_setEntryCityContext` overwrites its stored coordinates with the empty city's coordinates and sets `_PLOT_CONTEXT_CITY_CAPTURED`. The Battles tab consequently claims the unrelated earlier fight captured that city. If no earlier same-turn A/B battle exists, nothing is patched, which helps hide the defect.
  Defended captures remain compatible with a stronger match: practical 5772 already corrects attacker-wins battle coordinates to the loser's pre-death plot, i.e. the captured city plot. Preferred surgical repair is therefore to pass `iX/iY` into the matcher and require `entry[5] == iX && entry[6] == iY`, while also rejecting `isRetreatEntry(entry)` / non-lethal rows for defensive robustness. This avoids global pending state and prevents an empty-city conquest from borrowing an unrelated battle. Fixed in SAS practical 6172 / c36032deb5 with the coordinate + non-retreat match; Known Issues documentation was added in the same commit.

F017 - KI#220: Military Summary excludes free upgrades from min/average upgrade-cost statistics - FIXED
  Classification: AdvCiv-SAS UI correctness regression introduced by practical 5801 / 1127ae3c5e in `CvMilitaryAdvisor.collectSummaryData`; survives the current snapshot.
  Practical 5801 changes the Summary from a running upgrade-cost total to an `aUpgradeCosts` list used for min/avg/max/total. For each upgradeable unit it finds the cheapest candidate only when `iPrice > 0`, then appends only when `iCheapest > 0`. Current `CvUnit::upgradePrice` can validly return zero: `PROMOTION_LEADER` has `iUpgradeDiscount=100`, so a normal combat unit that has absorbed a Great General can receive a free upgrade. The Summary itself already tracks such GG-led units.
  A zero-price upgrade therefore contributes nothing to the sample instead of contributing a real 0 observation. If the army has one free GG-led upgrade and one ordinary 75-gold cheapest upgrade, the correct cheapest-cost sample is `[0,75]` (Min 0, Avg 37, Max 75, Total 75 under Python-2 integer averaging), but the current code builds `[75]` and reports Min 75 / Avg 75 / Max 75 / Total 75. Total happens to remain correct because adding zero has no effect; an all-free-upgrade sample also happens to show all zeros because the empty-list defaults are zero. Those masking cases help explain why the bug is easy to miss.
  Preferred minimal repair: treat `iPrice >= 0` as a valid candidate and use a sentinel that distinguishes "no candidate" from price 0, then append `iCheapest` whenever a candidate exists. Keep negative only for the internal no-candidate sentinel.
  Fixed in SAS practical 6171 / 53ee909e99 with that sentinel-preserving nonnegative-price repair; Known Issues documentation was added in the same commit.

F016 - KI#219: Military Summary support breakdown bypasses the anarchy zero-cost rule - FIXED
  Classification: AdvCiv-SAS UI correctness regression introduced by practical 5800 / 9e84f1482c in the new Military Advisor Summary / `CyPlayer` breakdown bindings; survives the current snapshot.
  Civ4's public `CvPlayer::calculateUnitCost(int iExtraPop, int iExtraUnits)` and `calculateUnitSupply(int iExtraOutsideUnits)` wrappers return 0 while `isAnarchy()`. Their reference-output overloads intentionally do not contain that wrapper-level exemption; they compute the underlying free/paid counts and normal support subtotals.
  Practical 5800's `CyPlayer::calculateUnitCostBreakdown` and `calculateUnitSupplyBreakdown` call those internal reference-output overloads directly, then Python `collectSummaryData` applies inflation to the returned `iRawUnitCost` / `iRawUnitSupply` and uses those values as the displayed Unit Cost, Unit Supply and Total Cost + Supply. Consequently, opening the Summary during anarchy can show positive support gold even though the engine-facing cost functions explicitly return zero and the player is not charged those costs.
  This is not merely an intermediate-formula difference: the Summary legend describes the rows as the actual DLL formulas and the final line as the bottom-line gold/turn. Search of later SAS diffs found no anarchy special case or replacement of these bindings, and current source retains the direct internal-overload calls.
  Preferred repair direction: preserve useful free/paid counters if desired, but make the Summary's displayed cost components/final totals obey the same anarchy exemption as the public engine-facing cost functions. A Python-side explicit anarchy presentation may be clearer than silently zeroing individual formula operands, because the underlying paid-unit counts remain real while the final charged cost is waived.
  Fixed in SAS practical 6171 / 53ee909e99: the Summary preserves the breakdown counters but zeroes displayed charged support components/totals during anarchy and labels the waived totals accordingly.

F015 - KI#218: PerfectMongoose meteor boundary points pass through an identity set and lose required pairing order - FIXED
  Classification: inherited Base AdvCiv PerfectMongoose regression born upstream at practical 4373 / a61a76dc22, later integrated into SAS ancestry at practical 5851 / 4a2c6394d1; it survived current SAS and the supplied Base AdvCiv 1.14 source until the SAS fix.
  Upstream practical 4373 changes `PangaeaBreaker.getCirclePoints` from an ordered list to `set()` and changes the boundary-point `append(CirclePoint(...))` calls to `add(CirclePoint(...))`, with the stated goal of removing duplicate circle points. The branch-side practical 4381 occurrence has a byte-identical primary-runtime PerfectMongoose patch after diff metadata is stripped, and practical 5851 is the later SAS integration point rather than the absolute birth. `CirclePoint` has no coordinate equality/hash implementation, so separately allocated objects with identical x/y values remain distinct set members. The intended deduplication therefore does not occur.
  `castMeteorUponTheEarth` actively uses this helper during Pangaea breaking. It sorts the returned points only by y and then consumes them two at a time as horizontal crater-line endpoints. The pre-5851 insertion sequence places opposite x endpoints next to each other within each y row; an unordered set discards that ordering while retaining the duplicate identity objects. For radius 4, y=0 is generated as x `+4,-4,+4,-4`; the old stable y-sort produces two full (-4,+4) row pairs, whereas a legal set order `-4,-4,+4,+4` produces two degenerate endpoint lines and leaves the interior of that crater row untouched. The algorithm therefore no longer guarantees the intended rasterized disc.
  The path is live whenever PerfectMongoose is configured to break disallowed Pangaeas: `generatePlotTypes` calls `pb.breakPangaeas`, which calls `castMeteorUponTheEarth` for each strike. Current SAS and Base AdvCiv 1.14 retain the same set plus y-only pairing code.
  Preferred minimal repair: restore list/append ordering for this boundary helper. Do not merely add value equality/hash to `CirclePoint`, because true coordinate deduplication can leave more than two unique midpoint-circle boundary points on a y row while the current rasterizer still blindly pairs consecutive entries. A min/max-x-per-y rasterizer would also be correct but is a broader rewrite.
  Fixed in SAS practical 6171 / 53ee909e99 by restoring the ordered list/append boundary helper while leaving `getFilledCirclePoints` as the separate unique filled-disk helper.

F014 - KI#217: ready-attack upgrade-now count can use the wrong UnitAI for mixed stacks - FIXED
  Classification: AdvCiv-SAS regression introduced by practical 5948 / a9515bf013 in `CvUnitAI::AI_attackCityMove` / `SAS_getBestUpgradeForLog`; the surgical source/KI repair is staged in the current working tree and awaits user commit validation.
  Practical 5948 added a behavioral bypass for Base AdvCiv upgrade waiting when a ready city-attack stack has a valid non-strong target and `iCanUpgradeNowUnits <= 0`. The commit documentation explicitly describes the condition as "0 units can upgrade now". To compute the group count, it iterates every `pUpgradeLogUnit` but calls `SAS_getBestUpgradeForLog(*pUpgradeLogUnit, AI_getUnitAIType())`; `AI_getUnitAIType()` is the role of the attack-stack unit executing `AI_attackCityMove`, not the iterated member.
  This differs from actual `CvUnitAI::AI_upgrade`, which stores `UnitAITypes const eUnitAI = AI_getUnitAIType()` on the unit being upgraded and evaluates the current/candidate unit values with that role. `CvPlayerAI::AI_unitValue` returns zero immediately for a unit whose XML `NotUnitAI` contains the supplied role, so using the head role is not a harmless scoring approximation.
  Concrete current-SAS proof: `UNIT_SAM_INFANTRY` has default/allowed `UNITAI_COUNTER`, upgrades to `UNIT_MOBILE_SAM`, and both SAM Infantry and Mobile SAM explicitly list `UNITAI_ATTACK_CITY` in `NotUnitAIs`. Current SAS attack-city groups can contain Counter units; later KI#188 diagnostics record 113 `UNITAI_COUNTER` grouping decisions into attack-city expeditions. A Counter SAM Infantry in such a group is therefore evaluated by the practical-5948 helper as Attack City: both current and upgrade values are zero, so no upgrade candidate is reported. The same unit's real `AI_upgrade()` evaluates Mobile SAM as Counter and can upgrade it when Rocketry/gold/other normal requirements are satisfied.
  Consequence: `iCanUpgradeNowUnits` can be understated, including 0 when a mixed-role group member can truly upgrade now. That makes `bSkipUpgradeWaitForReadyAttack` true in a state where practical 5948 intended the stack to keep waiting for a real immediate upgrade, changing attack timing rather than only diagnostics. The defect is role/mixed-stack dependent and does not imply every bypass decision is wrong.
  Fixed in SAS practical 6170 / 79410b2dba: `SAS_getBestUpgradeForLog` now derives `kUnit.AI().AI_getUnitAIType()` from each iterated member, and both behavior and diagnostic-cost callers use that per-member result. The existing deterministic estimate and separate `canUpgrade(eBestUpgradeForLog)` availability check are otherwise preserved.

F013 - KI#216: Work Boat demand can double-count one accessible water area - FIXED
  Classification: inherited Better BTS AI/K-Mod/Base AdvCiv sea-Worker demand defect; surfaced during the SAS source sweep at practical 5951 / 856ac747bc.
  `CvCityAI::AI_neededSeaWorkers` obtains its first demand area with `waterArea(true)`, which excludes adjacent impassable water, then obtains its second through `secondWaterArea()`. `CvPlot::secondWaterArea()` determines the area to exclude from plain `waterArea()` but rejects impassable adjacent plots while finding its replacement. If plain `waterArea()` selects a larger impassable-Ice-side area A while `waterArea(true)` selects accessible area B, `secondWaterArea()` can also select B because it excludes A. The same accessible area is then passed to `AI_countUnimprovedBonuses` twice.
  Concrete consequence: one currently buildable/reachable seafood target in area B can contribute +1 as the primary and +1 again as the second area, giving `AI_neededSeaWorkers()==2` for one real target. Current SAS availability accounting already deduplicates equal primary/second area pointers, so the false demand can appear as needed 2 versus available 1 and trigger an unnecessary extra Work Boat. Once the real target is improved, demand collapses; the issue is therefore a rare topology-driven overproduction defect rather than persistent global Work Boat inflation.
  The topology is valid in current SAS because Ice is impassable and area calculation distinguishes water connectivity around such tiles. The matching Base AdvCiv 1.14 source supplied during this audit still contains the same demand pairing, and K-Mod practical 3 / 2414225e64 already contains the Better BTS AI ancestry.
  This is distinct from KI#157: that repair deduplicates available primary/second-area Work Boats and fixes availability/target scope, but `AI_neededSeaWorkers` itself still lacks the equality guard.
  Fixed in SAS practical 6170 / 79410b2dba: preserve both helper semantics and require `pSecondWaterArea != pPrimaryWaterArea` before adding second-area demand, mirroring the already-proven availability guard.

F012 - KI#215: no-site Settler cleanup can unload unrelated transport cargo - FIXED
  Classification: inherited K-Mod/Base AdvCiv cargo-cleanup defect surfaced while auditing SAS practical 6067; earliest available source-bearing boundary is KMod practical 3 / 2414225e64.
  In `CvUnitAI::AI_settleMove`, an AI Settler older than 20 turns with no reachable valid city site first executes `getTransportUnit()->unloadAll()` if carried. `CvUnit::unloadAll` unloads every unloadable passenger on that transport. The existing passenger-side `CvUnit::unload()` instead calls this unit's `canUnload()` and clears only this passenger's transport pointer.
  Practical consequence: at a valid unloading plot, unrelated military/Worker/Settler cargo can be disembarked solely because this Settler is being cleaned up. At sea or another invalid unloading location, `unload()` refuses and the existing `getTransportUnit()==NULL` condition still prevents scrapping, so the safety behavior is preserved. No group-wide unload is required by `setTransportUnit(NULL)` semantics.
  Lineage: exact `getTransportUnit()->unloadAll()` is present in KMod practical 3 and preserved by AdvCiv practical 2268 / d300ec8f97 and practical 1825 / 981e5c8d64; it survives current SAS. SAS practical 6067 did not introduce this line, but its specialized no-site Settler cleanup caused the surrounding inherited path to be re-audited.
  Fixed in SAS practical 6170 / 79410b2dba: call this Settler's `unload()` instead of the transport's `unloadAll()`, retain the existing `getTransportUnit()==NULL` gate, and document the passenger-local intent.

F011 - KI#214: irrigation-chain constrained search could discard the only route that satisfied the plot limit - FIXED / VALIDATED
  Classification: AdvCiv-SAS path-search/state-model defect introduced by SAS practical 6076, SHA 2cc4ccaf5d, in `SAS_findWorkerIrrigationChainStep`.
  The search is Dijkstra-like and ranks nodes by `iCost`, but memoizes only one `aiBestCost[ePlot]` for each plot while also enforcing a separate maximum route length `iMaxPlots` (current default 12). Search cost is not a monotonic proxy for step count: each extra route step after the first costs 350, while overwriting a growth improvement costs 400 per growth level and a generic other improvement costs 2200. A longer detour through empty plots can therefore be cheaper than a shorter route through an existing improvement.
  Concrete counterexample under current defaults: a 2-step route to convergence plot X that crosses one generic improvement costs about 2200 + 350 = 2550; a 7-step empty detour to the same X costs 6 * 350 = 2100. The 7-step state wins `aiBestCost[X]`, so the valid 2-step state is discarded. If an irrigation source lies six additional steps beyond X, the retained cheap route needs 13 plots and is stopped by the 12-plot cap, while the discarded route would reach the source in only 8. The function can therefore return no route even though a legal route exists within `SAS_WORKER_AI_IRRIGATION_CHAIN_MAX_PLOTS`. Equal-cost routes with different step counts can produce the same structural problem because `>= aiBestCost` also discards the alternate state.
  Gameplay impact: the 6076 feature exists specifically to connect dry BFC Farms/food-pressure targets to available irrigation. In a layout where the cheapest detour exceeds the route cap but a more-expensive shorter chain fits, Workers can fail to discover the intended chain and leave a dry Farm unresolved despite an admissible route. The bug is geometric/state-dependent rather than guaranteed every game.
  Fixed in the current validated implementation batch by storing best cost for every bounded `(plot, steps)` state. This preserves least-cost routing subject to the hard step constraint without a heuristic special case. Save file 478 then completed a compiled-DLL Huge Pangaea autoplay through turn 497: Worker level-3 BBAI logging recorded 12,089 selected routes and exercised the exact depth-12 boundary in 344 failed searches without a route invariant failure, assertion or crash; SASGameRecord confirmed the normal Space Race victory endpoint and showed safe 32-bit process-memory headroom.

F010 - KI#213: split city/Worker best-build policies can hide valid custom land jobs from Worker demand - CONFIRMED / BROADER REFACTOR PENDING
  Classification: AdvCiv-SAS regression/partial-fix gap in local Worker-demand accounting.
  Introduction: SAS practical 6093, SHA b3b83cf78f, in `CvCityAI::AI_updateWorkersHaveAndNeeded`.
  The 6093 repair correctly recognized that the legacy city best-build cache is intentionally empty for ordinary SAS land improvements, but its fallback was limited to worked, unimproved, featureless land: `!kPlot.isWater() && !kPlot.isFeature()`.
  Current `CvCityAI::AI_getImprovementValue` exits immediately for every land plot with `NO_BUILD`, while current `CvUnitAI::AI_bestCityBuild` independently handles ordinary land improvements and contains an explicit Flood Plains no-bonus branch. Current XML gives that branch `BUILD_COTTAGE` normally and `BUILD_COTTAGE`/`BUILD_FARM` under low-food pressure. Thus a worked unimproved Flood Plains tile can have a valid SAS Worker build while the legacy cache remains `NO_BUILD`.
  Because Flood Plains are a Feature, the 6093 fallback rejects the tile and increments `iWorkedUnimprovableCount` rather than `iUnimprovedWorkedPlotCount`. The exact fallback survives unchanged at the current snapshot boundary.
  Downstream impact: local `AI_getWorkersNeeded` is understated; area demand, worker city selection/ferrying and the KI#192 completed-secondary-area evacuation logic consume that demand. A secondary area with no other counted work can therefore be considered zero-demand/complete while a worked Flood Plains tile is still improvable. Strong primary-area floors can hide the empire-wide Worker-count symptom but do not correct local assignment semantics.
  Initially considered fixes included an explicit Flood Plains exception and a generic feature-preserving-build test. Both were rejected as additional approximations beside the authoritative Worker policy. Preferred pending architecture: extract the city/plot policy/evaluation portion of `CvUnitAI::AI_bestCityBuild` into shared city/player-side logic; populate/derive `CvCityAI::AI_getBestBuild` from that same policy; then keep Worker-instance concerns (unit `canBuild`, current position/pathfinding, group/mission reservations and execution) in `CvUnitAI`. This should repair KI#213 at the root and reduce future mismatches at other legacy-cache consumers. The refactor is intentionally deferred because it is substantial and deserves dedicated compile/autoplay/log testing rather than being smuggled into the historical sweep. Practical 6093 is therefore certified with a known pending repair.


F001 - KI#205 K-Mod World-Wonder production-rank bug - FIXED
  Classification: inherited K-Mod/Base AdvCiv bug
  Historical origin traced to KMod practical 1030, SHA 418581f6bd:
    "Made some large style adjustments to CvCityAI::AI_buildingValue"
  The World-Wonder focus gate used the inverted production-rank condition and rejected the strongest production cities.
  Fixed in SAS practical 6127, SHA 8951d71560.

F002 - KI#205 cheap-Wonder game-speed cap regression - FIXED
  Classification: AdvCiv-SAS regression
  Regression traced to SAS practical 5477, SHA b33174f828.
  The cheap-Wonder gate compared effective getProductionNeeded(Building) against a Normal-speed/raw-style cap without scaling the cap by ConstructPercent.
  Fixed in SAS practical 6127, SHA 8951d71560.

F003 - KI#206 AI random-event building-change percent normalization - FIXED
  Classification: inherited K-Mod/Base AdvCiv bug
  Discovery route: found by ChatGPT-5.6-Sol in the current-tree XML/raw-cost semantic audit after KI#205, before the systematic historical sweep started.
  Current Base AdvCiv 1.14 still multiplies iBuildingValue by ConstructPercent without the required /100.
  KMod practical 3, SHA 2414225e64 (initial source-bearing K-Mod commit, 27 Dec 2010), already contains the missing normalization in AI_eventValue.
  KMod practical 478, SHA bfc3c3cca7:
    "Added some missing components to the AI's evaluation of event options."
  added /=100 to both analogous unit-event paths but left the building-event path unnormalized.
  Practical 478 is therefore a revealing partial correction, not claimed as the origin of the bug.
  This proves the issue is pre-SAS. The available history does not establish whether the original omission was introduced by K-Mod or inherited from BtS/BBAI before K-Mod practical 3.
  The K-Mod trace was done locally from the light-source ZIP commit-diff archive without Internet lookup.
  Fixed in SAS practical 6163, SHA 179e5e1f3e.

F004 - KI#207 UWAI random team-member selection index mismatch - FIXED
  Classification: inherited Base AdvCiv bug
  Discovery route: first previously-unfixed defect found by the systematic newest-to-oldest historical sweep.
  Introduced in AdvCivPreSAS practical 2945, SHA f016996eb6:
    "Let UWAI negotiate with a random team member"
  CvTeam::getRandomMemberAlive generated a 0-based random eligible-member index but compared it with MemberIter::nextIndex(), whose current-element-visible value starts at 1 and whose sequence includes ineligible AI members during human-only selection.
  For three all-AI members the old distribution was 2/3, 1/3, 0 rather than 1/3 each; for two members it was 100%, 0%. Mixed human/AI teams additionally compared a human-only random range against all-member iterator positions.
  The fix keeps the same one synchronized RNG roll and counts eligible members explicitly with a zero-based index.
  AdvCiv practical 3209 later modernized the RNG helper but preserved the faulty comparison. AgentIterator::nextIndex() already had the relevant semantics before practical 2945, so the defect exists from introduction rather than from a later iterator change.
  The sweep had earlier re-encountered the real ExtraFreeOutsideUnits bug at practical 3956, but SAS had already fixed/documented it as KI#92; KI#207 is therefore the systematic sweep's first previously-unfixed surviving defect.
  Fixed in SAS practical 6164, SHA d3b1b89822 (the tracked-mirror infrastructure was amended into the same commit).

F005 - KI#208 worst-enemy trade-memory recalculation flag overwritten - FIXED
  Classification: inherited Base AdvCiv bug
  Discovery route: second previously-unfixed defect found by the systematic newest-to-oldest historical sweep.
  Introduced in AdvCivPreSAS practical 2833, SHA 98b7a50fac:
    "Minor optimization in update of worst enemy"
  CvTeamAI::AI_updateWorstEnemy tried to avoid an unnecessary second worst-enemy pass by remembering whether trade/grant-memory decay had modified the tentative new worst enemy.
  The loop used bUpdateWorstEnemyAgain = (eOther == eBestTeam), so a later unrelated team with nonzero memory could overwrite an earlier true back to false. The needed recalculation therefore depended on team iteration order and could commit a stale m_eWorstEnemy.
  The fix makes the flag sticky: once eBestTeam is actually modified, later teams cannot clear the required recalculation.
  AdvCiv practical 2998, SHA 8c36049d43 ("Merge branch 0.99 into master"), carried the same branch change into master; no later historical correction was found and the assignment survived into current SAS.
  Fixed in SAS practical 6165, SHA 6b2c738295.

F006 - KI#209 random-leader reassignment state copied from random-civilization state - FIXED
  Classification: inherited Base AdvCiv bug
  Discovery route: third previously-unfixed defect found by the systematic newest-to-oldest historical sweep.
  Introduced in AdvCivPreSAS practical 2532, SHA 60a15e6434:
    "Handle assignment of random civs and leaders in the DLL"
  CvInitCore::reassignPlayer saved both bRandomCiv and bRandomLeader from m_abCivChosenRandomly, then later restored bRandomLeader into m_abLeaderChosenRandomly. A slot whose civilization and leader random-choice state differed therefore had its saved leader provenance forced to equal its civilization provenance.
  The distinction can matter when choices are independent, notably with GAMEOPTION_LEAD_ANY_CIV; wasLeaderRandomlyChosen and the later setup/re-randomization paths use the leader-specific flag.
  The fix changes the one incorrect read to m_abLeaderChosenRandomly.get(eNewID), preserving both flags independently without changing RNG or assignment policy.
  No later historical correction was found and the exact typo survived into current SAS.
  Fixed in SAS practical 6166, SHA eaf4c8ac19.

F007 - KI#210 vote-selection iterator refactor allowed voting-team self-contact - FIXED
  Classification: inherited Base AdvCiv bug
  Discovery route: fourth previously-unfixed defect found by the systematic newest-to-oldest historical sweep.
  Introduced in AdvCivPreSAS practical 2502, SHA cbc25c22bd:
    "Misc. refactoring"
  CvGame::doVoteSelection previously iterated unordered team pairs with iTeam2 = iTeam1 + 1. Practical 2502 replaced the inner numeric loop with a fresh full TeamIter but did not restore the self-exclusion, so a voting team could be paired with itself.
  Because isHasMet(self) starts false, the self-pair can call meet(self). CvTeam::makeHasMet checks eOther == getID() only after setting seen/met state, updating tech share and firing the Python firstContact event, so that late guard does not prevent the bogus self-contact state/event.
  The fix skips equal team IDs directly in doVoteSelection before attempting contact, restoring the lost caller invariant without changing generic meet semantics or foreign-team voting contact.
  No later historical correction was found and the iterator loop survived into current SAS.
  Fixed in SAS practical 6167, SHA 0e7098a6a6.

Resolved audit candidates
-------------------------
S001 - CvUnit::gift / AI_processPeacetimeGrantValue game-speed semantics - NO FIX
  Classification: currently safe under AdvCiv-SAS game-speed data; no SAS defect found.
  Unit gifts feed raw XML unit cost into a common peacetime trade/grant normalization path that divides by ResearchPercent.
  Every current SAS game speed uses the same TrainPercent and ResearchPercent (and also the same ConstructPercent and BuildPercent): 33, 50, 67, 100, 150, 200, 300 and 400 respectively across the eight speeds.
  Therefore the production-vs-research percentage distinction does not create game-speed drift in current SAS. Revisit only if those speed percentages are deliberately decoupled in the future.

S002 - CvPlayerAI::AI_espionageVal mixed production-cost semantics - NO FIX
  Classification: inherited K-Mod rough valuation design; mixed cost APIs alone do not establish a bug.
  KMod practical 161, SHA f9e8a3c4ce, deliberately increased project-destruction valuation while retaining getProductionNeeded(Project).
  KMod practical 960, SHA c4a6ecbc7a, deliberately rewrote destroy-building valuation around raw building cost plus production turns and AI_buildingValue.
  KMod practical 973, SHA 2bbcecb994, refined that formula again and explicitly described AI_espionageVal as a rough commerce-value heuristic; it also replaced the disabled buy-unit mission's relative AI_unitValue component with raw unit production cost.
  The current building formula's important time/utility term already contains production-turn scaling, while projects intentionally use their effective production cost and special strategic multipliers. The buy-unit espionage mission remains disabled.
  Conclusion: the formulas are heterogeneous by design and no current SAS correction is justified merely to make their cost APIs syntactically uniform. Revisit if espionage valuation itself is redesigned, not as an XMLCost bug.

F008 - KI#211 terrain attack sign reversal in unknown-defender AI strength estimates - FIXED
  Classification: inherited K-Mod/Base AdvCiv bug; exact pre-K-Mod authorship unknown.
  Discovery route: fifth previously-unfixed defect found by the systematic newest-to-oldest historical sweep, and the first surviving defect found after entering the K-Mod segment.
  Present in KMod practical 3, SHA 2414225e64 (initial source-bearing K-Mod snapshot, 27 Dec 2010).
  CvUnit::maxCombatStr has a special pAttacker == this mode for estimating an attacker against an unknown defender. City/hills/feature attack modifiers enter iTempModifier and are reversed at the end, but terrain attack was added directly to iModifier and therefore kept the wrong sign.
  Live AI_sumStrength/AI_currEffectiveStr callers reach this mode. GroupPathFinder uses the resulting strength in defended-plot attack-ratio/path costing, AI_cityTargetStrengthByPath uses it for reachable city attack strength, and current SAS low-power attacker ordering also calls the same effective-strength mode.
  Current SAS XML contains 30 unit types with nonzero terrain-attack modifiers. A simple +20% terrain attack case could be estimated as about 83.3% of base rather than the intended 120% before later effective-strength scaling.
  AdvCiv practical 1733, SHA 0d648df6db, touched/preserved the same asymmetric branch. No later historical correction was found before current SAS.
  Fix: change only the terrain branch accumulator from iModifier to iTempModifier. Known-defender semantics stay unchanged; unknown-defender mode now reverses terrain attack together with its sibling attack modifiers.
  Fixed in SAS practical 6168, SHA cf1272d7fe; user compile/run validation succeeded and CI passed. Practical 3 has since been fully certified; the systematic sweep continues at practical 2.


F009 - KI#212 AI-Barbarian CombatDetails attacker/defender field copy-paste - FIXED
  Classification: minor inherited K-Mod/Base AdvCiv semantic/API bug; exact pre-K-Mod authorship unknown.
  Discovery route: sixth previously-unfixed defect found by the systematic historical sweep, during the continuing deep audit of KMod practical 3.
  Present in KMod practical 3, SHA 2414225e64, and still present in current SAS.
  In CvUnit::maxCombatStr, the AI-controlled defender branch for a Barbarian attacker correctly applies the AI-Barbarian strength modifier but stores the reporting value in CombatDetails::iAIBarbarianCombatModifierTB (defender/"This is Barbarian") instead of the corresponding attacker field ...AB. The AB field is separately declared, zeroed, exported to Python and read by CvUtil.py, but was never assigned.
  Current built-in combat logging masks the defect because TB and AB use the same text key/sign handling, and combat strength itself is already computed correctly before the reporting assignment. The definite impact is therefore minor exposed-data/API semantics rather than combat odds or current AI behavior.
  Prepared fix: change only the one CombatDetails write from ...TB to ...AB. No strength arithmetic, RNG, handicap value, text key or balance data changes.
  Fixed in SAS practical 6169 / 9968f03601. The repair changes only the one CombatDetails write from `...TB` to `...AB`; practical 3 remains fully certified.

Systematic sweep progress
-------------------------
AdvCivPreSAS: COMPLETE
  Reviewed: practical 4343 -> 1250; all 3,094 archived AdvCivPreSAS commits have been systematically reviewed.
  First real historical defect encountered: practical 3956, already fixed independently by SAS as KI#92.
  First previously-unfixed surviving defect found by the systematic sweep: practical 2945, now KI#207.
  Second previously-unfixed surviving defect: practical 2833, now KI#208.
  Third previously-unfixed surviving defect: practical 2532, now KI#209.
  Fourth previously-unfixed surviving defect: practical 2502, now KI#210.
  Practical 2944 -> 2834 contained no additional surviving defect after suspicious candidates were traced against current semantics/history.
  Practical 2832 -> 2533 likewise contained no additional surviving defect after suspicious candidates were traced.
  Practical 2531 -> 2503 likewise contained no additional surviving defect after suspicious candidates were traced.
  Practical 2501 -> 1250 contained no additional previously-unfixed surviving defect. Multiple genuine historical bugs were encountered but were already repaired by later AdvCiv commits before current SAS; these were not counted as new findings.
  Examples include the practical-2379 Free Trade relation-iterator anchor omission later corrected by practical 2492, and practical 1343 dropping several UWAI cache-array = -1 initializers later restored by practical 1395.
  AdvCivPreSAS systematic result: 4 previously-unfixed surviving defects found and fixed (KI#207 through KI#210).

KMod: IN PROGRESS
  Contiguous systematic review completed: practical 1249 -> 4 inclusive (1,246 / 1,249 K-Mod commits).
  New previously-unfixed surviving K-Mod defects found in the practical-3 OPEN audit so far: 2 (KI#211 fixed; KI#212 fix prepared).
  The absence of a finding is not treated as a problem; suspicious code is closed only after checking later K-Mod/AdvCiv history and current SAS semantics.
  Practical 1127 and 1126 introduced espionage/culture AI_commerceWeight caches and were reviewed together as a semantic checkpoint. Human slider changes refresh immediately, Great Works refresh culture weight, AI turn-start refresh is deliberate, and later AdvCiv split espionage calculation to fix the strategy-ordering issue that actually mattered; no additional surviving cache defect was established in current SAS.

  Certified K-Mod checkpoints after practical 1126:
    C001: practical 1125 -> 926 inclusive (200 unique commits), ending SHA cab7f149ba.
      No new surviving defect. Notable checks included the practical-1041 World-Wonder filter interaction, which is safe because practical 1030 had already defined BUILDINGFOCUS_WORLDWONDER to imply BUILDINGFOCUS_WONDEROK, plus the already-known KI#205 World-Wonder production-rank lineage at practical 1030.
    C002: practical 925 -> 726 inclusive (200 unique commits), ending SHA f52e4c54de.
      No new surviving defect. The practical-861 AI_vassalTrade boolean rewrite was genuinely wrong historically but was later eliminated by AdvCiv relation-filtered iterators; practical-765's partial-air-mission code is safe because selection groups cannot mix unit domains.
    C003: practical 725 -> 526 inclusive (200 unique commits), ending SHA 843d4fa887.
      No new surviving defect. Practical 643's Great-Person ternary-precedence bug was later fixed by practical 876; practical 571's coastal-guard calculation received its later practical-616 correction; practical 532's movement-priority rewrite received its missing-return fix at practical 705; the practical 576 -> 579 -> 583 bonus-connection sequence is safe for TechCityTrade=NONE because isHasTech(NO_TECH) deliberately returns true.
    C004: practical 525 -> 326 inclusive (200 unique commits), ending SHA 0059ba37ac.
      No new surviving defect. This range independently re-encountered the revealing practical-478 partial event-normalization correction behind already-fixed KI#206; other pathfinder, movement-flag, stranded-unit, disband/hurry, permanent-alliance and attack-stack candidates did not survive later K-Mod/AdvCiv/current-SAS checks.
    C005: practical 325 -> 126 inclusive (199 unique SHAs; practical 168 and 215 are absent while practical 196 occurs twice), ending SHA 37c76118c0.
      No new surviving defect. This historically bug-dense range contains later-fixed strength-memory, sea-assault, pathfinder, citizen-assignment, espionage and arithmetic mistakes. Practical 238's `int iExperienceNeeded = iExperienceNeeded = ...` is redundant/ugly rather than a correctness failure and was later cleaned up by practical 434.
    C006: practical 125 -> 76 inclusive (50 unique commits), ending SHA 6f0a698a53.
      No new surviving defect. Practical 121's `eNewCivic`/old-array-slot check was re-opened and remains protected by the civic-availability invariant; later K-Mod practical 514 removes that stale guard entirely while relying on the same invariant. Practical 118 genuinely reversed BBAI's bombard-turn scaling (`iBombardTurns - 20`), but practical 137 explicitly restores `20 - iBombardTurns`; current SAS is therefore safe. Other surviving plot-improvement, espionage, limited-building, force-peace, Great-Person and victory-strategy expressions in this range were checked against later K-Mod/AdvCiv changes and current invariants without establishing another live defect.
    C007: practical 75 -> 26 inclusive (51 unique SHAs because practical 73 occurs twice), ending SHA c5160fe82d.
      No new surviving defect. Practical 71 introduced an unguarded counterespionage-value division by our espionage points against the target; practical 84 later replaced that formula with a denominator guarded by std::max(1, ...), so current SAS is safe. Practical 66's rewritten AI_corporationValue applied city population/maintenance-modifier scaling to `iMaintenance` only after that value had already been added to `iTempValue`, making the intended city-specific adjustment a no-op; practical 77 superseded the sequence with a different explicit maintenance approximation. Practical 56 introduced a genuine compile-valid shadowing bug in updateGwPercentAnger by redeclaring `int iAngerPercent` inside the positive-GW branch; practical 59 rearranged the code but still retained the shadow, and practical 145 finally removed the inner declaration. Current SAS contains the corrected assignment. The large practical-41 patch was mostly newly imported audio XML; its comparatively small gameplay-code changes, plus the city-site, civic, corporation, building-cache, espionage, defensive-power, global-warming and event changes throughout this checkpoint, were checked against later K-Mod/AdvCiv history and current SAS without establishing another live defect.
    C008: practical 25 -> 5 inclusive (21 unique SHAs), ending SHA d63edcad21.
      No new surviving defect. Practical 23 squared a potentially negative global-warming responsibility factor, which could turn a net-negative contribution positive; practical 35 later replaced/clamped that transformation. Practical 13 divided global-warming chance scaling by getMaxTurns(), creating a zero-divisor path when Time Victory was disabled; practical 20 later fixed that case. Practical 9 tried to avoid overflow in ROUND_DIVIDE by using `a / b > 0` as its sign test, but integer truncation misclassifies same-sign fractions with magnitude below 1 (for example 2/3); AdvCiv practical 1968 (`Bugfix in K-Mod's ROUND_DIVIDE`) replaced it with the corrected XOR-sign test used by current SAS. Practical 7 could leave its global-warming terrain selector uninitialized after exhausting the random search; practical 9 fixed that path. Practical 25's experimental KAI_techValue has no live caller in the surviving lineage; practical 27 comments out its declaration and practical 84 removes it. The current AI_civicValue descendant of practical-5 UnhealthyPopulationModifier logic was also checked for a possible zero/negative denominator; current SAS civic XML uses 0 for that modifier, so no current defect is established.
    C009: practical 4, SHA 593c549b51 (`assets to go with the initial commit`) (1 unique SHA).
      No new surviving defect. This is the companion assets/Python/XML import for the initial K-Mod source snapshot rather than a normal compact algorithm patch. All 102 imported XML files parse structurally. K-Mod-specific Python/XML customizations and their current descendants were checked separately: the three `Assets/Python/K-Mod/*` utility scripts from this import no longer exist in current SAS; the finance-advisor replacement is initialized through CvScreensInterface.init before use; current SAS's surviving trade-culture city-screen display is additionally gated by USE_KMOD_TRADE_CULTURE. The practical-4 Religion Advisor customization did contain a genuine historical UI defect: its subset selection populated `self.RELIGIONS`, but drawReligionInfo still iterated every religion. K-Mod practical 777 (`Fixed the subset options for BUG religious advisor.`) changes the drawing loop to iterate the selected subset; current SAS no longer contains that old screen implementation. The early environmental-advisor/global-warming assets were heavily revised by the immediately following K-Mod commits; concrete historical GW defects from that sequence are already recorded under C007/C008 and are fixed in current SAS.
    C010: practical 3, SHA 2414225e64 (`Initial commit. K-Mod, 27/dec/10...`) (1 unique SHA).
      CERTIFIED after five dedicated OPEN audit passes over the approximately 8.5 MB / 201-file source-bearing snapshot. The layered review included compiler-valid assignment/precedence/shadowing hazards; 903 brace-aware assertion/bounds checks; arithmetic denominator and initialization checks; reset/read/write and C++/Python exposure symmetry; still-live K-Mod/karadoc blocks and current descendants; aggregate-getter/sibling-asymmetry checks; and all 33 context-filtered candidates produced by reverse-pointer scanning of later K-Mod bug/crash/OOS fixes. Two previously-unfixed surviving defects were confirmed: KI#211 (terrain attack was sign-reversed in the unknown-defender attacker-strength estimate; fixed/validated/committed as SAS 6168 / cf1272d7fe) and minor KI#212 (`CombatDetails` wrote the AI attacker-Barbarian modifier to the defender-Barbarian field; fix prepared, user validation pending). Numerous additional genuine practical-3 historical defects were proven but are already repaired in later K-Mod/AdvCiv, including the strategy-hash `&=` mutation, worker comment/code mismatches, two-city distance-maintenance bug, Advanced Start cumulative-tech-value multiplication, culture-decrease setter regression, city-attack double count in defender ejection and unsafe no-Leader-promotion guard. Final reverse-fix, serialization and CombatDetails sibling passes established no third surviving defect. The stale trade-culture `iLevel` Python parameter/help and permanently-zero `iDomainDefenseModifier` are compatibility/dead API fossils without an established wrong current value.
    C011: practical 2, SHA 3e82b5f465 (1 unique SHA).
      CERTIFIED as empty ancestry/SVN-import scaffolding rather than inferred from the title alone. The full anonymized K-Mod log contains no human commit message beyond the `git-svn-id` marker; the generated historical diff has no numstat, rename/mode summary, textual patch or omitted-file coverage after the archive's EOL-noise filtering. Practical 3 then introduces the entire audited source tree as new files, so none of that source exists at practical 2. No code/config/content defect can originate in this commit from the preserved ancestry.
    C012: practical 1, SHA 7dd0684d35 (root commit) (1 unique SHA).
      CERTIFIED as the empty root/SVN-import scaffold. The full log again contains no human message beyond `git-svn-id`; the generated root diff has no numstat, rename/mode summary, textual patch or omitted-file coverage. Its child practical 2 is likewise empty, while practical 3 is the first commit that adds the K-Mod source tree. No preserved project code/config/content exists here to audit for a surviving defect.

  Historical practical-number bookkeeping:
    Count actual archived SHAs rather than deriving progress from practical-number subtraction. K-Mod contains duplicate practical numbers 73 and 196, while practical 168 and 215 are absent. These irregularities leave the segment total at 1,249 SHAs but make some numeric ranges differ from their apparent size.

  Final K-Mod tail: COMPLETE
    K-Mod systematic sweep: 1,249 / 1,249 unique SHAs certified, practical 1249 -> 1.
    Combined AdvCivPreSAS + K-Mod systematic sweep: 4,343 / 4,343 complete.
    Practical 4 is C009, practical 3 is C010, practical 2 is C011 and root practical 1 is C012.
    No K-Mod SHA remains. The next planned archaeology stage is the separate newest-to-oldest AdvCiv-SAS history sweep; that later SAS sweep is not included in the 4,343 historical total above.
    Practical 3 is the very large initial source-bearing K-Mod commit (about 8.5 MB of diff across roughly 200 source files) and received its own layered audit rather than being treated like a normal small patch. Audit layers included whole-tree compiler-valid foot-gun searches, K-Mod/karadoc-marked regions, later fix commits used as reverse pointers, and current-SAS descendants of suspicious code.
    Practical 3 audit status: CERTIFIED after five dedicated layered passes. Two previously-unfixed surviving current-SAS defects were established from this commit: KI#211 is fixed/validated/committed; KI#212 has a fix prepared and is awaiting user compile/run/commit validation. Certification means the historical source snapshot itself has now been reviewed to the project evidence standard; KI#212 validation remains a separate current-tree fix step.
    The earlier `iIncome` divisor candidate was re-checked more strongly and closed as safe. Practical 3 computes `iIncome = 1 + GOLD commerce + RESEARCH commerce + max(0, goldPerTurn)`, so the divisor is at least 1 in the introducing code. The modern descendant uses `1 + AI_getAvailableIncome()`; the available-income cache is built from nonnegative available commerce with GPT clamped at zero, and the caller also asserts `iIncome > 0`.
    Whole-tree assignment/condition scans found a genuine historical strategy-hash mutation: `if (iLastStrategyHash &= AI_STRATEGY_DAGGER)` tests with compound assignment and therefore destroys unrelated bits in the saved strategy hash. AdvCiv practical 2179, SHA 3676ec243b, changes this to a non-mutating bit test; current SAS is safe.
    The same scan independently re-found the old crush-strategy test `m_iStrategyHash & !AI_STRATEGY_ALERT2`, which is always false for a nonzero flag and was later fixed by K-Mod practical 76; current SAS is safe.
    Two worker-AI comments in practical 3 describe intended changes that the live code had not actually received: a production-rank comparison remained `<` despite a comment saying it had been changed to `<=` (later fixed by K-Mod practical 220, SHA f5e1f06944), and a danger condition remained `if (bDanger ...)` despite a comment saying it had been changed to `if (!bDanger ...)` (later fixed by practical 103, SHA 5750806e12). Current SAS carries the later corrected behavior.
    Practical 3 also contains a wrong debug assertion bound in `CvGame::getUnitClassCreatedCount`: it checks against `GC.getNumUnitInfos()` rather than the unit-class count. This affects assertion validation rather than the returned array value; the storage/access implementation was later replaced by AdvCiv practical 3746 and current SAS no longer contains that bad assertion.
    A brace-aware audit of all 903 practical-3 assertion/bounds checks found no hidden family of enum-domain mistakes: the `getUnitClassCreatedCount` mismatch above was the only genuine parameter-domain/bound mismatch; earlier broader scanner hits were neighboring compact functions misattributed by the crude scan.
    Confirmed surviving current-SAS defect / KI#211 in `CvUnit::maxCombatStr`: in the special `pAttacker == this` mode used to estimate an attacker against an unknown defender, city, hills and feature attack modifiers are accumulated in `iTempModifier` and then sign-reversed with `iModifier -= iTempModifier`. The terrain-attack branch instead wrote its negative attack modifier directly to `iModifier`, so it uniquely bypassed that reversal. Current `CvSelectionGroupAI::AI_sumStrength` and several current `AI_currEffectiveStr(pPlot, unit)` callers deliberately reach this special mode, including current SAS low-power attacker ordering; `GroupPathFinder` uses `AI_sumStrength` for defended-plot attack-ratio/path cost and `AI_cityTargetStrengthByPath` uses it for reachable city attack strength, so the wrong estimate feeds live AI decisions rather than diagnostics only. Current SAS XML contains 30 unit types with nonzero terrain-attack modifiers, including multiple +20% cases. For a unit with only +20% terrain attack, the special estimate applies -20% instead: roughly 83.3% of base strength rather than the intended 120% before later effective-strength scaling. The exact asymmetric line is present in practical 3, is preserved by AdvCiv practical 1733 and survived current SAS; no correcting historical patch was found. SAS practical 6168, SHA cf1272d7fe, fixes the terrain branch by accumulating it in `iTempModifier`, like the other attack-modifier branches; user compile/run validation succeeded and CI passed.
    Assignment-looking `if (iSpreadExternalValue = 0)` remains a false positive because it exists only in a commented original-BtS reference block; K-Mod's live replacement already uses `== 0`. The other assignment-in-condition form found in the broad scan, `(pWaterArea = ...) != NULL`, is deliberate pointer assignment/testing.
    A further practical-3 culture-layer audit found that `CvCity::setCultureTimes100` had been changed from accepting any changed value to only accepting `iNewValue > iOldValue`, so legitimate culture decreases were silently ignored. K-Mod practical 726, SHA f52e4c54de, explicitly restores the `!=` test and adds handling/comments for negative plot-culture propagation; current SAS carries the corrected behavior.
    The trade-culture Python API was also traced end-to-end. Current `CyCity::getTradeCultureRateTimes100(int iLevel)` still accepts and ignores a destination-culture-level argument, its interface help still describes that argument as meaningful, and current `CvMainInterface.py` still supplies the selected city's culture level. However, practical 3 itself had already deliberately disabled destination-level capping, and current C++ intentionally computes trade culture from the source city only. The stale argument/help contract is therefore a compatibility/API cleanup candidate, but no wrong current gameplay or displayed numeric result is established; do not classify it as a KI on present evidence.
    A focused denominator/arithmetic pass used later K-Mod fixes as reverse pointers. Practical 104, SHA 6d084e7d69, guards a start-of-game `AI_commerceWeight` divisor when the all-team score total can be zero; practical 136, SHA eacd687a2e, guards `AI_knownTechValModifier` when no civilizations have been met. Both are genuine historical divide-by-zero repairs and current SAS contains the corrected logic. Additional scope-aware initialization/division scans in the practical-3 K-Mod-marked code produced only already-fixed or invariant-protected cases in this pass; no further surviving defect was established.
    A further still-live-AI pass found a genuine historical double count in `CvSelectionGroupAI::AI_ejectBestDefender`: practical 3 divided by `100 + cityAttackModifier() + getExtraCityAttackPercent()`, even though `cityAttackModifier()` already includes `getExtraCityAttackPercent()`. AdvCiv practical 2330, SHA 89f68d9b77, explicitly notes the double count and replaces the formula; current SAS keeps the corrected descendant. A systematic scan for sibling aggregate-getter-plus-underlying-extra expressions (city/hills/terrain/feature attack/defense) found no equivalent live duplication in current SAS.
    The same AI-value layer found an unsafe practical-3 Leader-promotion guard: `isHasPromotion(eLeader)` was evaluated before `eLeader != NO_PROMOTION`, so a ruleset with no Leader promotion could query `isHasPromotion(NO_PROMOTION)`. K-Mod practical 199, SHA a865535e85, rewrites this into a direct loop over actual Leader promotions; current SAS uses the rewritten form. This is historical-only.
    The practical-3 global-warming/Python exposure layer was checked separately for wrong getter/member wiring. The GW index/chance/tally, land-defence/sustainability and player-pollution bindings map to the intended C++ functions, and current descendants remain consistent; no second serialization/API wiring defect was found there.
    A reverse-history scan then used later K-Mod commits explicitly labelled fix/bug/crash/OOS/etc. as pointers back into practical 3. Raw exact-line matching is intentionally treated only as a candidate generator because generic statements can be removed by a rewrite and later reappear safely. The first pass produced 852 noisy textual coincidences; requiring a nontrivial removed line to occur exactly once in the same file in both practical 3 and current SAS reduced this to 33 contextual candidates. All 33 were then reviewed in their actual functions. The apparent survivors resolved as corrected surrounding guards/rewrites, commented historical reference code or safe caller invariants: e.g. the Pitboss `AddLaunch` call is behind `IsGraphicsInitialized()`, collateral arithmetic is inside K-Mod's deterministic vector rewrite, K-Mod 623's old cargo-defender formula survives only as commented reference, K-Mod 870's `getCenterUnit()` survives only inside the replacement `getDebugCenterUnit()` helper, and the transport `splitGroup` path can return NULL only for non-positive split size while its caller has already established positive cargo space. No reintroduced defect was confirmed from the completed reverse-fix queue.
    Practical 3 is certified. Practical 2 and practical 1 were then separately verified as empty ancestry/SVN-import scaffolding from their full logs, empty generated diff summaries/coverage and the fact that practical 3 introduces the full source tree as new files. The K-Mod sweep is complete.

K-Mod systematic sweep completion
---------------------------------
All 1,249 archived K-Mod SHAs have now been systematically certified, from practical 1249 through the root practical 1. The sweep found two previously-unfixed surviving defects in the giant practical-3 source snapshot (KI#211 and minor KI#212) in addition to re-proving many genuine historical bugs already corrected by later K-Mod/AdvCiv. KI#211 is fixed/validated/committed; KI#212's fix remains pending user compile/run/commit validation at the time of this checkpoint. That pending current-tree validation does not change completion of the historical K-Mod review.

KMod notable checked candidates / historical notes
---------------------------------------------------
These checks are preserved so later review does not rediscover the same candidates from scratch. Some were first investigated out of order while orienting in K-Mod history and are now also inside the certified contiguous range through practical 3.

KMod practical 1158 - AI_retreatToCity rewrite ancestry - ALREADY COVERED
  The retreat rewrite intersects the unsafe-current-city retreat behavior eventually fixed/documented under KI#204. Treat as known lineage, not a new systematic finding.

KMod practical 1099 - OOS checksum player-index shift - KI#326 / MINOR DIAGNOSTIC CLEANUP IMPLEMENTED
  SAS can iterate 48 civilization-player slots while K-Mod's OOS attitude-cache checksum shifted signed attitude values by the player index. Negative signed operands were questionable already, and indices >= 32 exceeded the width of a 32-bit int; this was only a deliberately lossy diagnostic checksum, not simulation/game state. KI#326 documents it explicitly while retaining the diagnostic-only, no-gameplay-defect classification outside the confirmed-defect counters. Replaced the shifts with position-sensitive unsigned polynomial mixing so all rival slots contribute safely to the diagnostic value.

KMod practical 861 - AI_vassalTrade exclusion rewrite - HISTORICAL BUG, CURRENTLY SAFE
  A rewrite changed the old exclusion of self OR prospective master into an && form that did not preserve the original logic in normal cases. This was a genuine historical bug in that revision, but later AdvCiv refactored the path into relation-filtered enemy iterators and current SAS no longer contains the bad condition.

KMod practical 643 - Great-Person specialist ternary precedence - HISTORICAL BUG, CURRENTLY SAFE
  The expression around getGreatPeopleRate() used a ternary without the necessary parentheses and therefore parsed differently from the apparent intent. KMod practical 876 later corrected the exact expression; current SAS is safe.

KMod practical 3 - initial source snapshot audit - CERTIFIED / KI#211 FIXED / KI#212 FIX PREPARED
  This giant source-bearing commit received five dedicated layered audit passes and is now certified. The current-descendant `iIncome` divisor candidate has been re-proved safe so far because the introducing calculation and modern available-income path keep the denominator positive. Whole-tree condition scans found the genuine `iLastStrategyHash &= AI_STRATEGY_DAGGER` mutation bug, later fixed by AdvCiv practical 2179, and independently re-found the already-fixed crush-strategy `& !AI_STRATEGY_ALERT2` bug. Two worker-AI comment/code mismatches were also genuine historical defects later fixed by K-Mod practical 220 (`<` -> `<=`) and practical 103 (`bDanger` -> `!bDanger`). A brace-aware audit of 903 assertion/bounds checks confirmed that the unit-class-created-count debug assertion was the only real enum-domain/bound mismatch; it was later removed with the storage/access refactor.
  A later reverse-pointer check found another genuine practical-3 maintenance bug: `CvCity::calculateMaintenanceDistance` updated its longest-distance term only for non-government-center cities. With exactly two cities, the non-capital city therefore saw only itself in that term and could get zero distance maintenance. K-Mod practical 1005, SHA 105c7a8c6e, explicitly fixes this by updating the longest-distance term for every city before the government-center test; current SAS retains the correction.
  Practical 3's Advanced Start tech-value loop also contains a historical variable-target mistake: after computing a local improvement-yield `iTempValue`, the early-era Advanced Start bonus multiplies cumulative `iValue` instead of `iTempValue`. This can repeatedly scale value accumulated from unrelated earlier terms. Later K-Mod tech-value rewrites remove the old branch (it is gone by practical 84, SHA ab84668b30); current SAS no longer contains it.
  The practical-3 global-warming state additions `m_iGlobalWarmingIndex` and `m_iGwEventTally` were separately checked for reset/read/write symmetry. Both are initialized and serialized in matching order, so no save/OOS defect was found in that state plumbing.
  A later culture-layer pass found a genuine practical-3 regression in `CvCity::setCultureTimes100`: the setter accepted only increases (`iNewValue > iOldValue`), so culture decreases were ignored. K-Mod practical 726, SHA f52e4c54de, restores the normal `!=` changed-value condition and handles negative plot-culture propagation; current SAS is safe.
  The surviving Python trade-culture wrapper/help contract was checked separately. `CyCity::getTradeCultureRateTimes100(int iLevel)` still accepts an ignored destination-level parameter and current `CvMainInterface.py` still passes one, but practical 3 had already intentionally removed destination-level capping and current C++ returns the source-city trade-culture rate used by gameplay. Treat this as stale compatibility/help surface, not a current numerical behavior bug on present evidence.
  Reverse-pointer arithmetic checks also re-verified two early K-Mod divide-by-zero repairs: practical 104 guards a zero all-team score denominator in `AI_commerceWeight`, and practical 136 guards `AI_knownTechValModifier` when no civilizations are met. Current SAS retains the corrected forms. Broader scope-aware initialization/denominator scans in this pass produced no additional surviving defect.
  A subsequent still-live-AI pass found that practical 3's `AI_ejectBestDefender` divided by `100 + cityAttackModifier() + getExtraCityAttackPercent()` even though the aggregate getter already contains the extra promotion component. AdvCiv practical 2330, SHA 89f68d9b77, explicitly calls out and replaces that double count; current SAS is safe, and a broader scan found no live sibling aggregate-plus-extra duplication. Practical 3 also evaluated `isHasPromotion(eLeader)` before checking `eLeader != NO_PROMOTION`; K-Mod practical 199, SHA a865535e85, rewrites promotion handling around actual Leader promotions, so the no-Leader-promotion hazard is historical-only.
  The GW/Python binding layer was checked for wrong getter/member wiring without finding another live defect. The later reverse-history pass reduced 852 raw fix-line coincidences to 33 unique same-file candidates and completed contextual review of every one; none represented a reintroduced current defect. A final independent sibling/asymmetry pass also checked surviving CombatDetails exposure, persistent-state serialization and NO_* guard ordering. `iDomainDefenseModifier` is a longstanding zero/dead compatibility field with no corresponding live domain-defense calculation rather than a missing current assignment; the apparent surviving unsafe Leader-promotion guard is inside commented BtS reference code while the live K-Mod loop is corrected. Core practical-3 read/write member sets are symmetric for CvGame, CvCity, CvTeam, CvArea, CvSelectionGroupAI and CvPlayerAI; the apparent CvPlayer/CvUnit/CvPlot exceptions are node data, a derived unit-info pointer and a nonserialized danger cache rather than state loss. No third surviving defect was established. Practical 3 is therefore certified; continue at practical 2.
  Confirmed minor surviving current-SAS defect / KI#212: when the attacker is Barbarian and the defender is AI, `CvUnit::maxCombatStr` stores the correct AI-Barbarian strength modifier in `CombatDetails::iAIBarbarianCombatModifierTB` instead of the corresponding attacker-Barbarian `...AB` field. The same copy-paste is already present in practical 3; current `...AB` is otherwise zeroed, exported to Python and read by `CvUtil.py` but never assigned. AdvCiv's `CombatDetails` comment identifies TB as defender/"This is Barbarian" and AB as "Attacker is Barbarian". Current SAS combat logging masks the error because both fields use the same text key/sign handling, and the actual strength math is already correct, so this is explicitly classified as a minor semantic/API defect rather than gameplay AI weakness. The prepared repair changes only the reporting write from `...TB` to `...AB`; user compile/run/commit validation is pending.
  Confirmed surviving current-SAS defect / KI#211: `CvUnit::maxCombatStr`'s unknown-defender attacker mode reverses city/hills/feature attack modifiers through `iTempModifier`, but the terrain-attack branch added the negative modifier directly to `iModifier` and therefore bypassed the reversal. Live `AI_currEffectiveStr(..., sameUnit, ...)`/`AI_sumStrength` callers reach this mode; `GroupPathFinder` uses that sum in defended-plot path costing and city-target strength logic uses it for reachable attack power. Current SAS has 30 terrain-attack-bearing unit types. The asymmetric line is present in practical 3, preserved by AdvCiv practical 1733 and survived current SAS; no later correction was found. SAS practical 6168, SHA cf1272d7fe, fixes the terrain branch by accumulating it in `iTempModifier` like its city/hills/feature siblings, preserving known-defender behavior while allowing unknown-defender mode to reverse it. User compile/run validation succeeded and CI passed. Practical 3 is now certified; the systematic sweep continues at practical 2.

KMod practical 3 - corporation external-spread assignment-looking condition - FALSE POSITIVE
  The alarming `if (iSpreadExternalValue = 0)` is preserved only inside a commented original-BtS reference block. K-Mod's live Unofficial Patch replacement already uses `== 0`; practical 3 therefore documents/fixes the inherited BtS issue rather than introducing a live K-Mod bug.

KMod practical 9 - ROUND_DIVIDE sign test - HISTORICAL BUG, CURRENTLY SAFE
  Practical 9 replaced the multiplication-based sign test with `a / b > 0` to avoid overflow, but integer truncation makes that test false for same-sign proper fractions such as 2/3. AdvCiv practical 1968, SHA b602f31c7d (`Bugfix in K-Mod's ROUND_DIVIDE`), replaces it with the corrected XOR-sign test; current SAS is safe.

KMod practical 7 - global-warming terrain selector initialization - HISTORICAL BUG, CURRENTLY SAFE
  The terrain selector could remain uninitialized after exhausting its random-search attempts. Practical 9 fixes the path; current SAS is safe.

KMod practical 13 - global-warming max-turn division - HISTORICAL BUG, CURRENTLY SAFE
  Chance scaling divided by getMaxTurns(), allowing a zero-divisor path with Time Victory disabled. Practical 20 fixes the case; current SAS is safe.

KMod practical 23 - global-warming responsibility squaring - HISTORICAL BUG, CURRENTLY SAFE
  A potentially negative responsibility factor was squared, which could turn a net-negative contribution positive. Practical 35 later replaces/clamps the transformation; current SAS is safe.

KMod practical 4 - Religion Advisor subset drawing - HISTORICAL BUG, CURRENTLY SAFE
  The practical-4 BUG Religion Advisor customization could populate `self.RELIGIONS` with a selected subset but still drew religion buttons by iterating the full `range(gc.getNumReligionInfos())`. K-Mod practical 777 explicitly fixes the subset options by iterating `self.RELIGIONS`; current SAS no longer contains that old screen implementation.


SAS-C063 - practical 5589 through 5585 TechChooser / Info / shared runtime-layout audit - COMPLETE
  Reviewed 5 runtime-source SHAs: practical 5589 / c5b7209bf5, 5588 / a00938c20d, 5587 / 208a8b6a91, 5586 / 8f60e16c51 and 5585 / 56bc90027a.
  Practical 5589 is the already-documented KI#114 Tech Advisor first-draw turns-left repair. Its first-draw label path is brought into parity with the existing refresh path: current-research turns are calculated with the same current-tech boolean, anarchy continues to suppress zero/invalid remaining-turn text, and the label is anchored to the same scroll-panel coordinate space as refresh rendering. No new research-state or widget-target regression was established.
  Practical 5588 deliberately replaces the older 0/1/2/3 horizontal-depth preset with the current low-resolution threshold + alternate scoreboard-margin model. Current descendants retain that new model and no stale reader of the removed define survives. GP preference icon anchors are recomputed in the same TechChooser-local coordinate space as the footer/Exit anchors; no child-vs-screen coordinate mismatch was found.
  Practical 5587 is dominated by CRLF/EOL churn. After normalizing line endings, the substantive runtime changes are limited to larger-font History LOG-button sizing plus Demographics/Wonders width/geometry adjustments; data getters, widget targets and table sort types are unchanged.
  Practical 5586 retunes Score-tab widths. The new separate Attitude width is counted once while the generic flag-width multiplier drops from nine to eight, so the total-width budget still accounts for every column exactly once; the existing shrink cascade remains presentation-only.
  Practical 5585 extracts shared advisor runtime-bounds/anchor/link-width helpers and moves Info/Foreign screen-dependent geometry out of init-time setup. Both screens compute runtime bounds before language/tab-width initialization and before tab draw code reads the relocated fields. The shared formulas are algebraic extractions of the prior per-screen math, and no surviving uninitialized/stale-geometry path was established.
  No new surviving defect was established in this range. Certified cursor advances through practical 5585 / 56bc90027a. Next source commit: practical 5584 / 59b10e4620.


SAS-C064 - practical 5584 Foreign Advisor Espionage layout audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5584 / 59b10e4620.
  Practical 5584 resizes and visually restructures the already-integrated Foreign Advisor Espionage tab. The apparently risky removal of the old TotalPane is paired in the same commit with disabling the duplicate drawEspionageSlider() refresh call; current descendants retain both decisions and use the globally visible commerce sliders instead. No live update path targets the removed panel/slider widgets.
  The remaining executable changes alter panel styles, margins/bleed and runtime row anchors while preserving the same player/team, city, mission and widget-action data paths. No surviving defect was established.
  Certified cursor advances through practical 5584 / 59b10e4620. Next source commit: practical 5583 / e138ad4f82.


SAS-C065 - practical 5583 Espionage -> Foreign Advisor integration audit / KI#234 candidate - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5583 / e138ad4f82.
  Practical 5583 removes the standalone CvEspionageAdvisor object/screen and integrates its logic directly into CvForeignAdvisor as an Espionage tab. Screen/input routing is coherent: CvScreensInterface redirects Espionage opens/input to the Foreign object, Foreign update handles Espionage_Advisor_DIRTY_BIT while the Espionage tab is active, and the migrated helpers use a separate ESP_ state/widget namespace.
  A tempting fixed-widget collision suspect was disproved: Foreign getScreen() names each tab as ForeignAdvisor + iScreen and a tab change hides the old screen before switching iScreen, so legacy fixed Espionage names such as LeftPane/RightPane/CitiesTitle remain isolated on the Espionage tab screen rather than leaking onto other Foreign tabs.
  Confirmed surviving SAS-specific candidate / KI#234: the deleted standalone interfaceScreen reset iTargetPlayer, iActiveCityID and iSelectedMission to -1 on every open. The integrated persistent Foreign object instead initializes ESP_iTargetPlayer/ESP_iActiveCityID/ESP_iSelectedMission only once and drawEspionage resets city/mission only when the retained target is already -1. drawEspionageContents rebuilds the currently valid alive/met target list but never validates a non--1 retained target against it. Thus a previously selected rival can die/become invalid between opens and remain selected despite being absent from ESP_aiKnownPlayers; a retained city/mission can likewise outlive the target's valid cities, creating stale/invalid mission-display state. Current 6177 retains the same missing target-membership validation.
  Fixed and runtime-validated later by validating the retained target against the rebuilt alive/met target set, clearing target/city/mission only when invalid, and then using the existing first-valid-target selection. See F031 and KI#234.
  Certified cursor advances through practical 5583 / e138ad4f82. Next source commit: practical 5582 / ef8766cb5f.


SAS-C066 - practical 5582 through 5581 Foreign layout constants / Bonus-Relations geometry audit - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 5582 / ef8766cb5f and 5581 / bbe944048e.
  Practical 5582 adjusts the Foreign Relations web radius and changes the Bonus icon grid from preferred-size centering to filling the runtime advisor bounds. Its -5 side margin is intentional panel overdraw and is reused only in positional/width geometry; it is never consumed as a row/column count or index. Resource/trade eligibility, icon-grid columns, widgets and scrolling logic are unchanged.
  Practical 5581 moves Foreign advisor edge/anchor constants into SASUtils while deliberately retuning the Foreign margins. The formulas are direct replacements of the previous runtime-resolution math; no gameplay/data value is centralized. Later shared-helper adoption by Info/Foreign was independently audited in practical 5585 without establishing a stale/uninitialized-bound path.
  No surviving defect was established. Certified cursor advances through practical 5581 / bbe944048e. Next source commit: practical 5580 / 67f48e63e9.


SAS-C067 - practical 5580 through 5579 Foreign runtime-layout / Info-tab audit - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 5580 / 67f48e63e9 and 5579 / 8a2494a806.
  Practical 5580 is the first Foreign runtime-resolution layout extraction later centralized by practicals 5581/5585. Screen-dependent bounds/footer/legend coordinates move out of __init__; tab widths are explicitly recomputed on every screen open after runtime X_EXIT is known, so removing the old resolution-change invalidation of the language cache does not leave geometry stale.
  Practical 5579 deliberately retires the optional original Info layout in favor of the table-style improved layout and adds favorite-religion icons. NO_RELIGION is guarded, and every FavoriteCivicDetector early-return branch adds the religion icon before returning, so no path silently omits it. The new field is fixed leader-definition data for already-listed leaders rather than dynamic hidden game state.
  No surviving defect was established. Certified cursor advances through practical 5579 / 8a2494a806. Next source commit: practical 5578 / bbbd9fea2c.


SAS-C068 - practical 5578 Foreign/Exotic Advisor flattening migration audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5578 / bbbd9fea2c.
  Practical 5578 removes the old interdependent CvExoticForeignAdvisor subclass/file, folds its seven-tab implementation into the canonical CvForeignAdvisor class, and rewires CvScreensInterface to instantiate CvForeignAdvisor directly. The large Python patch was audited as a migration rather than treated as ordinary line churn.
  The required inherited/base surface is preserved after flattening: the old Exotic interfaceScreen/drawContents/handleInput/update paths remain the canonical paths; the old base Relations implementation is retained as drawRelationsBase and the former Exotic wrapper still recenters before delegating to it; base screen/widget/line counters, selection state, getScreen/killScreen and generated-widget helpers are present in the flattened class; and the resource/tech/info/glance/city dictionaries/options remain the old Exotic seven-tab set. NUM_FOREIGN_SCREENS changes from the old base module's 4 to the flattened class's 7, which makes generated IDs match the actual tab count and does not create cross-tab collisions because each tab already uses its own ForeignAdvisor<iScreen> screen instance.
  External-reference audit found no surviving executable dependency on CvExoticForeignAdvisor. CvScreensInterface removes the only instantiated/imported Exotic object in this commit; current executable source has no Exotic reference, and later SAS diffs contain no subsequent executable removal/fix of a stale CvExoticForeignAdvisor import/call (later occurrences are documentation/comments or alternate-history merge diff context).
  The small CvDLLWidgetData.cpp and CvTeam.cpp hunks in this otherwise huge merge are comments/reference-name updates only; no DLL behavior changes. Military Advisor changes are line-ending/comment noise rather than executable semantics.
  No surviving defect was established. Certified cursor advances through practical 5578 / bbbd9fea2c. Next source commit: practical 5577 / ff496b5701.

SAS-C069 - practical 5577 through 5570 Main Interface anchor / Sevopedia sortable-chart audit - COMPLETE
  Reviewed 8 runtime-source SHAs: practical 5577 / ff496b5701, 5576 / 26abe649cd, 5575 / 22bd4581e6, 5574 / 9a31009440, 5573 / e9dfde4dea, 5572 / fac9cc9a6d, 5571 / 682579f808 and 5570 / ef902ca930.
  Practicals 5577-5575 form one Main Interface top-layout lineage. Practical 5576 first gives map/city commerce blocks separate offsets and recomputes commerce rectangles when city-screen state changes; practical 5577 replaces the fragile city-screen giant negative offset with an explicit left/right anchor selector plus local X/Y offsets. The old top-left-flag define rename is complete by 5576 and no executable reader of the retired TREASURY_SHIFT name survives. The extra build-row manual adjustment is resolution-scaled with sign preserved. No cross-mode stale commerce geometry or inverted define semantics was established.
  Practicals 5574-5571 adjust/split Sevopedia in-category charts. The Tech and Trait pair maps remain canonical; only the two members of each displayed pair are alphabetically ordered before being placed in separate sortable columns, so pair membership/counts are unchanged and symmetric duplicates are not created. The new fixed icon-column caps were checked against current XML rather than assumed safe: starting-tech combinations peak at 5 civs versus a 7-icon cap; a single starting tech peaks at 15 civs versus 20; untradeable techs peak at 10 per era versus 20; trait pairs peak at 2 leaders versus 6. Thus the helper's fixed-column truncation behavior does not currently discard any chart identities.
  Practical 5570 is presentation/naming cleanup: Trait chart style changes only, the Python cache/variable rename for the optional top-left flag still deliberately reads the then-existing XML define name, and practical 5576 later renames that XML define and both readers together. No surviving defect was established.
  Certified cursor advances through practical 5570 / ef902ca930. Next source commit: practical 5569 / 08d32a43ca.


SAS-C070 - practical 5569 through 5568 city-billboard DLL scaling/trimming experiment - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 5569 / 08d32a43ca and 5568 / b4bf8dfadd.
  Practical 5568 adds define-driven CITY_BILLBOARDS fScale mutation in CvArtFileMgr, a graphics-initialization city-entity reload hook and the temporary CvArtInfo scale setter. Practical 5569 layers timer-aware city-name/production-name character trimming on top of that experiment for larger BODY fonts. The trim affects only the two billboard strings when they include a turns-left suffix; ordinary no-timer names remain unchanged.
  This lineage was checked forward rather than judged from its temporary edge behavior. Practical 5623 / b8f2aab775 explicitly removes the complete DLL experiment together: all four SAS_CITY_BILLBOARD_FSCALE_FONT_* defines, all four SAS_CITY_BILLBOARD_MAX_CHARS_FONT_* defines, CvArtFileMgr::applyCityBillboardScaleFromDefines, the graphics-init reload hook, CvArtInfo::setScale, assignTrimmedBillboard and both of its callers. Practical 5624 then replaces only the scaling objective with the Python CIV4DetailManager.xml updater, whose later 6117 regex repair was already certified in the newer half of this sweep.
  Therefore no 5568/5569 scaling or truncation path survives in current SAS and no current defect is established from its temporary implementation details. Certified cursor advances through practical 5568 / b4bf8dfadd. Next runtime-source SHA: practical 5567 / fc94bc27ff.


SAS-C071 - practical 5567 through 5562 Sevopedia category-width / Leader-AIP / font-scaling audit - COMPLETE
  Reviewed 6 runtime-source SHAs: practical 5567 / fc94bc27ff, 5566 / b331c463c7, 5565 / fdeb58c165, 5564 / 10fa767ad4, 5563 / a5fcdfa937 and 5562 / 6d123adae2.
  Practical 5567 makes the Sevopedia category column narrower at the native label size and restores the wider 166-pixel column only when `SAS_UI_FONT_LABEL` is 4. The apparently strict `> 3` test is consistent with Civ4's native label size being 3; it therefore distinguishes the genuinely upscaled label size rather than omitting a supported size-3 upscale. Later descendants retain the same basic category-width model and no corrective runtime change indicates a lost category or routing path.
  Practical 5566 introduces font-specific Leader item-list widths and redistributes reclaimed horizontal space across the three AI Personality panels. The arithmetic preserves the intended total budget: the font-2 width is the baseline, only positive reclaimed width expands the AIP, and the gain is divided across all three panels before the label/value/scale subcolumns are split. `SevoPediaMain` and `SevoPediaLeader` choose the same 1/2/3/4 width defines with the same <=0 fallback; the later KI#230 defect is introduced separately by practical 5632's wide-screen no-reduction cutoff and does not exist in this original 5566 selector. The Civic/Religion `Leaders` -> `Favorites` rename changes panel naming/text only; all dimensions and favorite-leader data helpers are carried over consistently.
  Practical 5565 removes hardcoded AIP font tags from cached category data and applies the configured label/bold-label tags at render time. The generated predump is regenerated in the same commit, and later practical 5633 intentionally moves AIP category construction out of the predump entirely; no stale cached-font contract survives. Practical 5564 only redistributes complete AIP category tuples among right/middle/left columns. The later 5633-5635 resolution-aware category refactor, already certified from the newer side, supersedes this fixed ordering without exposing an omitted category.
  Practical 5563 scales Leader attitude/action labels, history and Traits text through `SASTextScale`; widget IDs/data and leaderhead actions are unchanged. Its use of `normalizeLabelText` for Leader Civilopedia/Traits text is deliberate because those strings can already contain embedded `<font=...>` tags. Practical 5562 applies the same scaling layer to Tech/Unit prerequisite labels, Tech chart cells/headers, help and quote text. `SASFontUtils` and `SASTextScale` already predate these callers in branch history, so there is no import-before-helper transition; current descendants retain the scaled callers. No changed prerequisite ID, widget target, table sort key or gameplay value was found.
  No surviving defect was established in this family. Certified cursor advances through practical 5562 / 6d123adae2. Next runtime-source SHA: practical 5561 / edfcb90a95.


SAS-C072 - practical 5561 through 5553 Corporation Movie / Corporation page / Sevopedia text-scaling / Media Player audit - COMPLETE
  Reviewed 9 runtime-source SHAs: practical 5561 / edfcb90a95, 5560 / fdfab488eb, 5559 / 09993883db, 5558 / 0b7eea7948, 5557 / dfcec2157a, 5556 / 75b842cb18, 5555 / 25868e322d, 5554 / c5d6e5ef56 and 5553 / 550acbb427.
  Practical 5561 adds Corporation movies as movie type 6 across the Sevopedia movie registry, info/source lookup, availability check, grouping and pedia redirect path. Movie keys continue to pack the type in the high 16 bits and a nonnegative info ID in the low 16 bits, so the new type does not collide with existing Victory/Wonder/Project/Religion/Era entries. Current descendants retain the Corporation branch consistently; no orphaned type, wrong info-family lookup or playback redirect was established. Practical 5560's runtime-source hunk is comment/formatting cleanup only.
  Practical 5559 substantially rebuilds the Corporation page and exposes `CvCorporationInfo::getBonusProduced` to Python through a thin integer-return wrapper. The wrapper reads the already-existing `m_eBonusProduced` field that is initialized to `NO_BONUS` and loaded from XML, and its Python declaration/binding agree. The new `Competes With` panel determines competition by overlap between prerequisite-bonus lists; this matches current `CvGame::isCompetingCorporation`, which uses the same prerequisite-bonus overlap test for distinct corporations. Consumed/produced bonus panels therefore derive from the underlying structured info rather than special-text parsing. No simulation state is changed. Practical 5558 then only routes Corporation special/history text through `SASTextScale`.
  Practicals 5557-5555 scale Religion, Civic and Project page text. The notable Religion simplification removes a redundant loop that could call `addMultilineText` repeatedly with the same complete special-text string and widget name; one call preserves the displayed content and avoids duplicate work. The remaining changes preserve info IDs, movie targets, help getters and panel data.
  Practical 5554 repairs Media Player queue windowing when a group transition would otherwise consume the last visible row with only a spacer/header. It checks that transition rows plus at least one actual item still fit; if they do not, the item is still rendered without an orphan transition row. Queue identity/order/playback state are untouched.
  Practical 5553 centralizes many page-local font wrappers into `SASTextScale` and upscales/polishes the Media Player. Every removed `_bodyText`/`_labelText`/`_paneText` caller was compared with its direct replacement. Most are exact wrapper substitutions. Three non-identical font-level substitutions are deliberate presentation choices retained by current SAS: Feature and Terrain type labels use label size while their yield/stat values remain title size, and the Unit combat-class description uses title size. No widget/data target changes accompany them.
  One genuine historical UI regression exists in practical 5553 but is already repaired in the certified newer lineage: the currently playing Media Player queue row changed from a bold label tag to ordinary `SASTextScale.labelText`, leaving only the `>` prefix as selection emphasis. Practical 5622 / 527e44b2a3 explicitly `fix sevopedia media player text not being bold` and restores the bold active-row tag; current SAS retains that repair. This is therefore historical lineage, not a surviving new KI. No other surviving defect was established in this family.
  Certified cursor advances through practical 5553 / 550acbb427. Next runtime-source SHA: practical 5552 / 915ede9ca0.


SAS-C073 - practical 5552 through 5544 Sevopedia scaling / KI#113 merge / Main Interface merge audit - COMPLETE; KI#235 FOUND (FIXED LATER)
  Reviewed 7 runtime-source SHAs: practical 5552 / 915ede9ca0, 5551 / 763c8e8503, 5550 / e756d55bc4, 5549 / 2f57a2d8a7, 5548 / b90338d72d, 5547 / b825de154f and 5544 / f1e860eb99. Practical-number gaps are non-runtime-source commits in the frozen denominator.
  Practical 5552 is a deliberate font-correctness pass: it restores the shared Tiny/Body/Label defaults to Civ4-native 1/2/3 sizes and promotes Sevopedia content that had been rendered as Body to Label or Title where the page role requires it. The subsequent practical 5553 refactor, certified in C072, mostly preserves these roles and its few presentation-level deviations were checked separately. No ID, widget target, gameplay value or sortable cell type changes in 5552.
  Practical 5551 correctly repairs the Build-list icon regression introduced by the KI#113 hybrid widget work. `SevoPediaMain.placeItems` may rewrite `data1` from a Build ID to the Build's prerequisite Tech ID for `WIDGET_HELP_IMPROVEMENT`; removal builds legitimately have `NO_TECH` / -1 there. Header/spacer identity therefore must come from the untouched list tuple `item[1]`, not mutated `data1`. Current filtering/navigation code uses the same `item[1] == -1` invariant, and the repair survives.
  Practical 5550 only shortens numTxt labels/centering and factors Hills Master tier formatting into `get_numTxt_promotion_tier_compact`. All eight tier-Boolean combinations remain distinguishable: none, each singleton, contiguous 1-2/2-3/1-3 ranges, and the gapped 1+3 form. Practical 5549 centralizes the optional Legend link and adds the numTxt/category concepts; the helper preserves the old Leader legend's NewConcept widget/page routing and safely returns when disabled or unresolved. No surviving routing defect was established.
  Practical 5548's initial Building upscaling is part of the font lineage explicitly corrected by practical 5552; its too-small role choices do not survive. Practical 5547 merges the KI#113 Build-hover branch into the UI-scaling branch. The hybrid contract is coherent: `WIDGET_HELP_IMPROVEMENT` uses data1 as Tech and data2 as Build for DLL hover generation, TechChooser leaves true Improvement builds to the native Improvement click and reroutes non-Improvement builds to Builds, while Sevopedia Build entries deliberately reroute valid Build data2 values to the Builds page. The left-list missing-icon side effect created by this data1 repurposing is exactly what newer practical 5551 fixes, so that transition flaw is historical-only.
  Practical 5544's optional top-left civilization-flag work is the early merge result of the later 5575-5577 anchor/offset lineage already certified from the newer side; current descendants contain the later explicit left/right-anchor and mode-refresh repairs. Its unrelated CvGameTextMgr collateral-help hunk, however, contains a surviving SAS regression.
  KI#235 candidate: side-branch practical 5506 / c9be31986e (`hide collateral damage unit bullet for non-combat units`) adds `pUnit->canFight()` to the actual-unit `CvGameTextMgr::setUnitHelp` collateral gate; practical 5544 merges that change, and the frozen source audited here retained it. `CvUnit::canFight()` is strictly `baseCombatStr() > 0`, whereas air combat strength lives in `airBaseCombatStr()`. Current XML gives Bomber (`iAirCombat=16`) and Stealth Bomber (`iAirCombat=20`) `iCombat=0` and `iCollateralDamage=100`; both therefore fail `canFight()` despite having functional collateral. The air-strike path calls `collateralCombat`, which explicitly uses `airBaseCombatStr()` for `DOMAIN_AIR`, proving this is real combat behavior rather than dead XML. Base AdvCiv 1.14's instance-help path still displays collateral whenever `pUnit->collateralDamage() > 0`, and the SAS basic/Sevopedia help branch added in the same 5506 commit correctly treats `u.getCombat() > 0 || u.getAirCombat() > 0` as combat-capable. Result: an actual Bomber/Stealth Bomber unit hover could omit its real collateral-damage/limit/max-units line while its type/Sevopedia help showed it.
  Surgical direction for KI#235: keep the civilian suppression but make the instance gate match the basic-unit semantics, e.g. `(pUnit->canFight() || pUnit->airBaseCombatStr() > 0)` (equivalently base-combat-or-air-combat), rather than broad `canCombat()` because `canCombat()` also admits nukes while the basic-unit gate intentionally requires land/base or air combat strength. No gameplay calculation needed changing. Implemented and runtime-validated later in commit f2ecb1334; see F032 and KI#235.
  No second surviving defect was established in this range. Certified cursor advances through practical 5544 / f1e860eb99. Next runtime-source SHA: practical 5541 / c9424a447d.


SAS-C074 - practical 5541 through 5536 Sevopedia Trait/Specialist/Promotion and shared text-scaling helper audit - COMPLETE
  Reviewed 6 runtime-source SHAs: practical 5541 / c9424a447d, 5540 / 59bc5515c8, 5539 / e089863a53, 5538 / 33771bb11b, 5537 / 6ed16c4251 and 5536 / 0c57c4b987.
  Practical 5541 routes Trait chart headers/cells, special effects and background text through the shared font scaler without changing trait membership, counts, widget targets or ranking values. Its only grouping-file hunk is a comment wording change. Practical 5539 similarly scales Specialist page labels/help; the subsequent practical 5540 merely centralizes the same embedded-font stripping into `SASTextScale.normalizeBodyText`, preserving the strip-then-wrap behavior used by Specialist, Promotion and History.
  Practical 5538 includes one genuine Base AdvCiv Sevopedia correctness repair rather than only font work: `placeLeadsTo` previously checked `getPrereqPromotion`, `getPrereqOrPromotion1` and `getPrereqOrPromotion2` but omitted K-Mod's exposed `getPrereqOrPromotion3`, even though the same Promotion page already displays that third OR prerequisite on the selected promotion's Requires side. Adding the fourth comparison makes reverse dependency display symmetric with the supported info model. Current SAS XML does not appear to populate a third OR prerequisite, so the repair is presently future/mod-data correctness rather than a visible current-chain change. The new None fallback is shown only when no dependent promotion is found.
  The rest of practical 5538 is presentation/helper cleanup: Promotion special-help text is stripped of embedded font tags then rewrapped at the configured Body size, unit-combat rows preserve their IDs/buttons, and duplicate inline None rendering is replaced by the shared no-content helper. No promotion eligibility/gameplay rule is changed.
  Practicals 5537 and 5536 establish/refactor `SASTextScale`: `applyFontTag` preserves None, empty strings and already-font-tagged text; the Info/Victory adapters are equivalent to their prior local wrappers apart from harmlessly normalizing string-like input through `unicode()`. Helper imports are introduced before later Sevopedia callers use them, and the 5540 normalization helpers are an exact centralization of regex logic already used by those pages. No import-order, widget, sorting or state regression was established.
  No new surviving defect was established in this family. Certified cursor advances through practical 5536 / 0c57c4b987. Next runtime-source SHA: practical 5535 / 9c2dfd6af3.


SAS-C075 - practical 5535 through 5528 merge / GP-bar / Unit-chart / Unit-page audit - COMPLETE; KI#236 FOUND (FIXED LATER)
  Reviewed 4 runtime-source SHAs: practical 5535 / 9c2dfd6af3, 5532 / 1dfa2e678f, 5529 / d2db8edf22 and 5528 / 4bfd65ec42. Practical-number gaps are non-runtime-source commits in the frozen denominator.
  Practical 5535's only executable hunk is a Sevopedia category-width comment merge; no runtime expression changes. Practical 5532 changes grouped Civics ordering deliberately: membership still comes from each civic option, but the BUG alphabetical sort inside each option is disabled so `CIV4CivicInfos.xml` progression order is preserved. This is an explicit presentation/order choice rather than lost data.
  Practical 5532 also makes the Great Person map bar clickable. The text overlay receives the exact currently displayed GP city's player-local city ID. The OneLine/TwoLine stacked bars themselves are created through `addStackedBar` with the default `data1=-1`, so their clicks cannot accidentally interpret a default zero as city 0; they deliberately fall back to `GPUtil.getDisplayCity()`. `CyPlayer.getCity(iID)` is therefore used only with a real player-local city ID, and `selectCity(..., true)` opens that city screen. Current descendants preserve this routing.
  Practical 5529's Unit chart keeps column count/index mappings coherent: non-air groups use 8 columns and exactly seven numeric-width terms; air groups use 10 columns and nine numeric-width terms. The 7-pixel redistribution primarily widens Collateral, while every row getter remains in its original column and `setTableInt` sorting semantics are retained. Header padding is clamped at zero for larger label fonts. No data column is dropped or shifted.
  Practical 5528 is mostly Unit-page font scaling plus a new `Requires`-panel None fallback, but auditing that function exposed a real inherited omission. `SevoPediaUnit.placeRequires()` handles tech, bonus, religion, building and (in SAS) reconstructed project prerequisites, yet never reads the already-exported `CvUnitInfo.getPrereqCorporation()`. Base AdvCiv 1.14 has the same omission. The DLL's `CvPlot::canTrain` explicitly rejects training when the city lacks the unit's required active corporation, and BUG `UnitUtil.canCityBuildUnit` independently enforces the same field, so this is a genuine gameplay prerequisite rather than descriptive/dead XML.
  KI#236 candidate: all seven current `UNIT_EXECUTIVE_1` through `UNIT_EXECUTIVE_7` entries have a non-NONE `PrereqCorporation` matching their corporation (and also a tech prerequisite). In the frozen source audited here, their Sevopedia Unit Requires panel therefore showed the tech but omitted the corporation that the city must actually have active to train the Executive. No other unit-help display path in that source surfaced this `getPrereqCorporation()` field; the only other Python occurrence was the BUG buildability helper. Practical 5528 did not introduce the omission, so this is classified inherited/pre-SAS, discovered while auditing its `placeRequires` changes. The newly added None fallback did not falsely show None for current Executives because each also had a displayed tech prerequisite.
  Surgical direction for KI#236: in `SevoPediaUnit.placeRequires`, add `getPrereqCorporation()` alongside the existing religion/building checks, attach the corporation button with `WIDGET_PEDIA_JUMP_TO_CORPORATION`, and set `isButtonFound = True`. No DLL/gameplay change was needed. Implemented and runtime-validated later in commit 26b2ac788; see F033 and KI#236.
  No additional survivor was established in this range. Certified cursor advances through practical 5528 / 4bfd65ec42. Next runtime-source SHA: practical 5527 / 5f07664057.


SAS-C076 - practical 5527 through 5519 Sevopedia Build/Bonus/Improvement/Terrain/Feature/chart/media/concept scaling audit - COMPLETE
  Reviewed 9 runtime-source SHAs: practical 5527 / 5f07664057, 5526 / 3a5be76ef4, 5525 / ef24535664, 5524 / 2a98882044, 5523 / fc4739f9ed, 5522 / b6e9e354b0, 5521 / 8c8521b144, 5520 / 16ff40ba1e and 5519 / 0acd3abeac.
  Practicals 5527-5524 add page-local font wrappers to Builds, Bonuses, Improvements, Terrains and Features and route existing labels/help/history through them. Info IDs, buttons, prerequisite/effect selection and widget targets are unchanged. Practical 5525 only applies the configured label tag to numTxt text and shifts its X position left by 3 pixels per label-font step above 2; it does not change the numTxt value or associated widget.
  Practical 5523 replaces hardcoded chart font tags with the shared `chart_font2` wrapper and modestly increases chart width/fill defines for larger text. Numeric cells continue to use `setTableInt`, so sortable numeric semantics are preserved. Practical 5522 applies title/body tags to Movie/Music labels and text without changing playback IDs/controls; practical 5521 only wraps Hints.
  Practical 5520 introduces the configurable Tiny/Body/Label/Title/Hover font defines and explicitly strips embedded engine `<font=...>` tags from Concept/NewConcept history before applying the configured Body tag. Later practical 5540 centralizes this same strip-then-wrap behavior in `SASTextScale.normalizeBodyText`, already certified in C074; therefore the early regex implementation is lineage rather than a surviving duplicate/format bug.
  Practical 5519 applies the new font roles across Civilization, Index and Sevopedia Main. Its fixed 166-pixel category width is superseded by the later font-dependent 5567 implementation certified in C071. Index data1/data2/widget routing remains unchanged while only displayed text is wrapped, including Concept/NewConcept routing. No sorting key or item identity is replaced by formatted text.
  No new surviving defect was established in this family. Certified cursor advances through practical 5519 / 0acd3abeac. Next runtime-source SHA: practical 5518 / c283deeb1e.


SAS-C077 - practical 5518 through 5510 hover / scoreboard / advisor font-scaling audit - COMPLETE
  Reviewed 9 runtime-source SHAs: practical 5518 / c283deeb1e, 5517 / 238d35129e, 5516 / 831aed2806, 5515 / b024a013df, 5514 / 8926e1e7a4, 5513 / 0137bbee35, 5512 / 36b53f94a8, 5511 / f017567353 and 5510 / 1f14da39a6.
  Practical 5518 adds optional hover scaling on both DLL-generated and Python left-side help. The DLL wrapper executes after the widget-specific `parseHelp` switch; the code following it only updates scoreboard-expanded state and does not append more help text, so no suffix is left outside the closing font tag. In this historical version both Python and C++ accept 1-4 and treat 0/invalid as no wrapping; later certified font-define work deliberately changes the validation contract. Existing embedded font/color/image markup is preserved inside the outer tag.
  Practical 5517 repairs Domestic Advisor cell scaling by using the Body define directly rather than deriving a sometimes-larger size from table width; the XML Tiny/Body/Label values are simultaneously returned to native 1/2/3 defaults after the transient all-4 experiment. Practical 5516 scales Scoreboard text and measures dynamic column width using the same scaled string that is rendered, preserving row values/widgets. Practical 5515 only applies the configured font to map-view help strings that do not already contain a font tag.
  Practicals 5514 and 5513 apply equivalent Body/Title wrappers to Espionage and Foreign/Exotic Advisor labels, list rows and icon-grid text. City/player/deal/tech IDs, visibility tests and widget actions remain unchanged; strings already carrying a font tag are left alone by the local scaling helpers. These standalone/integrated advisor paths later pass through the Foreign/Policy migration lineages already certified from the newer side.
  Practicals 5512 and 5511 are broad Info/Victory screen wrapper conversions. The underlying getters, table row construction, widgets and sort-capable cell APIs are retained; numeric-sort columns remain `setTableInt`. Victory's Settings/Options/Rivals panel captions are intentionally moved from the panel's fixed caption into separately scalable labels, and the turns-left string refactor is textually equivalent. Practical 5510 similarly replaces Religion Advisor hardcoded font tags without changing religion/city/unit/building data selection.
  No new surviving defect was established in this family. Certified cursor advances through practical 5510 / 1f14da39a6. Next runtime-source SHA: practical 5509 / d613a8b096.


SAS-C078 - practical 5509 through 5503 initial shared UI-font implementation audit - COMPLETE; KI#237 FOUND (FIX PREPARED LATER)
  Reviewed 7 runtime-source SHAs: practical 5509 / d613a8b096, 5508 / 4c3506bc39, 5507 / 1ef354763e, 5506 / 4dc561f908, 5505 / 51539c04da, 5504 / 008f512278 and 5503 / 3f762655a2.
  Practical 5503 introduces `SASFontUtils` and replaces hardcoded 1/2/3/4 UI font tags across Domestic/Customizable Domestic, Espionage, Foreign, Main Interface, TechChooser and Economics/Environment screens. The dynamic tag objects themselves do not read XML defines at import/construction time: `_getFontSizeOrDefault` is called lazily only when a tag is rendered/concatenated. Thus early Python module import cannot permanently freeze unavailable GlobalDefines. Practical 5504 adds a cache only after a value passes the 1..4 validity check, so an unavailable/invalid early lookup is not cached. Later certified practical 5744 intentionally changes this permissive fallback contract to fail-fast configuration validation.
  At their native/default sizes the practical-5503 screen replacements are mechanically equivalent to the removed hardcoded tags; widget IDs/data, gameplay getters and table cell APIs remain unchanged. The initial XML deliberately sets Tiny/Body/Label/Title all to 4 as an aggressive upscaling experiment; practical 5517 later restores the native 1/2/3 hierarchy while keeping Title 4, and the numerous later font/layout lineages already certified from the newer side repair the resulting presentation rough edges rather than leaving a current correctness survivor.
  Practical 5503 does contain two unclosed Domestic Advisor Research/Espionage header font tags; practical 5507 explicitly adds the missing `SAS_FONT_TAG_CLOSE` to both. Its wider-table cell-size logic then derives from the configured Label size and clamps at 4. Practical 5508 is a direct Civics-screen hardcoded-tag conversion, including multiline civic help; civic selection, revolution/upkeep calculations and widget targets are unchanged. Practical 5509 similarly wraps the existing Military Advisor title/GG label/unit-list text without changing unit-list membership or selection IDs.
  Practicals 5504-5506 expand Main Interface scaling and make valid font-define lookups restart-stable. Production queue/selected-unit rows preserve the same left/right buffers, buttons and `WIDGET_HELP_SELECTED` data. `GPUtil.getGreatPeopleText` practical 5505 deliberately removes its local hardcoded `<font=2>` suffix because the caller now supplies the outer configured font; that font migration itself is not the newly found defect.
  KI#237 candidate (inherited/pre-SAS, discovered while auditing practical 5505): `GPUtil.getGreatPeopleText` Maximum-types truncation is one candidate late. In each loop iteration it computes `szNewTypes = szTypes + next GP icon/percentage` but builds/measures `szNewText` from the old `szTypes`; only after that old string passes the width test does it assign `szTypes = szNewTypes`. Therefore an item that is itself the first one to push the rendered list over `iGPBarWidth - 10` is accepted and returned. The bug is already present in the earliest available K-Mod practical 4 BUG import (`szNewText ... % szTypes`) and Base AdvCiv 1.14 retains it. The frozen source audited here retained the same control flow. The BUG Main Interface option defaults GP Bar Types to `Maximum`, so this path is ordinary/default UI behavior; mixed-GP-point cities can supply multiple entries, and the top-bar call also includes the variable city name. Preferred surgical repair: measure the candidate (`szNewTypes`) before accepting it, preserving the existing sorted order and stop-on-first-nonfit policy. Any font-scale-aware measurement improvement can be considered separately, but is not required to prove/fix the off-by-one acceptance bug.
  Fixed later in the current implementation batch by applying that surgical repair; see F034 and KI#237.
  No additional surviving defect was established in 5509 -> 5503. Certified cursor advances through practical 5503 / 3f762655a2. Next runtime-source SHA: practical 5502 / 4a0682c481.


SAS-C079 - practical 5502 Sevopedia Leader attitude/action preview audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5502 / 4a0682c481. Practical 5501 / 886d380631 changes docs/generated history only and is skipped by the runtime-source denominator.
  Practical 5502 imports clickable Leader attitude previews (Furious/Annoyed/Cautious/Pleased/Friendly) and four animation actions (idle/no-animation, greeting, agree, disagree) into the Sevopedia Leader page. The action constants are valid engine `LeaderheadAction` values; `NO_LEADERANIM` is deliberately -1 and the enum comment states that passing it to leaderhead action playback produces the idle animation. The five attitude values are likewise the standard contiguous attitude enum members.
  Button routing is coherent. The page-specific `handleInput` handles WIDGET_PYTHON events when the event carries the Leader page's Python-file id; `SevoPediaMain.handleInput` provides the separate fallback path only when that page-specific forwarding did not occur, so a single click is not executed twice. Attitude input validates `iData2` against the five-element preview order before changing local preview state; action buttons are created only from the fixed four-value tuple. The original numeric WIDGET_PYTHON ids 6805/6806 have no conflicting Python runtime use and later certified code centralizes them as `SAS_MAGIC_PEDIA_PYTHON_LEADER_ATTITUDE/ACTION`.
  `applyLeaderAttitude` only changes `self.iSelectedAttitude`, recreates the 3D leaderhead widget and redraws the attitude controls. It does not touch `CvPlayer` diplomacy/attitude data. Keeping the selected preview attitude while browsing another leader is therefore persistent cosmetic preview state rather than leaked gameplay state. `applyLeaderAction` only asks the screen to play an animation on the current leader widget.
  The new attitude/action panel uses fixed widget IDs rather than `getNextWidgetName`; the same commit compensates by explicitly deleting those fixed widgets from `SevoPediaMain.deleteAllWidgets`, and `placeAttitudes` also deletes/recreates them before redraw. Current descendants preserve that cleanup and extend it to the later era-art buttons. The renamed Leader legend concept/link keeps the same pedia-description widget semantics while broadening the legend's content.
  No surviving defect was established in practical 5502. Certified cursor advances through practical 5502 / 4a0682c481. Next runtime-source SHA: practical 5500 / b421b9e33f.


SAS-C080 - practical 5500 release-boundary runtime audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5500 / b421b9e33f. Practical 5499 / 4bd8aa49b6 changes audio/XML/assets only and is skipped by the runtime-source denominator.
  Practical 5500 is a broad release commit, so its seven runtime-Python files were audited as separate semantic families rather than trusted from the release title. `SevoPediaIndex.py` only adds a KI#111 comment, and `SevoPediaUnitChart.py` switches to the already-shared standard chart style without altering rows, data or sort semantics.
  The Main Interface production-text indentation change is a historical correctness repair. Before 5500 the `if not bProdDetailAdded` fallback sat outside the non-process production branch even though `bProdDetailAdded`, `iProdCur` and `iProdNeed` are initialized only in that branch. 5500 moves the fallback inward beside those variables; current SAS preserves that placement. No process-production undefined-state regression survives.
  The Info Score-tab changes are coherent with the immediately older practical-5493 PowT introduction. Score, delta, absolute power, power-per-city, research-percent and numeric-attitude cells are inserted through `setTableInt` so Civ4's type-aware table sort remains numeric; unknown/hidden values retain text blanks rather than fake numeric defaults. Passing font/color-tagged display strings to `setTableInt` follows the long-established Domestic Advisor pattern and remains the current SAS implementation. PowT/C is intentionally recomputed from absolute visible power divided by revealed-city count instead of the earlier ratio/city hybrid; self absolute power is always known while self PowR remains blank. No visibility leak or arithmetic divide path was found.
  The new Civilization-by-ArtStyle grouping preserves all current data. All 38 current civilization entries have a nonempty valid ArtStyleType, and grouping changes only presentation order/header insertion. The Leader-by-Civilization grouping likewise covers all 55 real non-Barbarian runtime leaders exactly once in current XML: every such leader is associated with exactly one civilization, with no missing or duplicate association. The only existing three-element `data1 == -1` Sevopedia header tuple is the newly introduced `(civName, -1, civButton)` shape, so the generic optional-third-element header-button handling does not reinterpret older header metadata. Header rows use `WIDGET_GENERAL` and remain excluded from selectable-arrow navigation.
  The new AI-personality master toggle is internally consistent: when disabled, Leader-page AI panel/legend drawing and the expensive Leader AIP cache precompute are both skipped; the remaining Leader page does not dereference the empty cache. The define is restart-stable like the surrounding Sevopedia configuration.
  Civilization Select/Order audio classification changes from unconditional 3D to `szScript.startswith("AS3D_")`. Every current civilization Select/Order script is AS3D (72 / 72 XML entries), so current behavior is unchanged while a future AS2D script can take the correct 2D path. The Media Player consumes the stored boolean specifically to choose the 2D-vs-3D sound API.
  The small non-source XML hunks were also sanity-checked where they interact with runtime code: the Mehmed audio filename rename matches the renamed asset; Civilization/Leader XML changes are authorship headers; the new Sevopedia defines have valid enabled defaults; and the Paper/Professional Army flavor edits are intentional AI-preference tuning rather than structural prerequisite/data-schema changes.
  No new surviving defect was established in practical 5500. Certified cursor advances through practical 5500 / b421b9e33f. Next runtime-source SHA: practical 5498 / a3cf39a3a4 (practical 5499 is non-runtime-source and skipped).


SAS-C081 - practical 5498 through 5496 Sevopedia-music / BUG-startup causal lineage - COMPLETE
  Reviewed 3 runtime-source SHAs: practical 5498 / a3cf39a3a4, 5497 / b97bbac6e1 and 5496 / 6f94679507. Practicals 5495 and 5494 have no runtime C++/Python change and are skipped by the primary denominator.
  This sequence is best understood as one diagnosis/revert/root-cause lineage rather than three independent fixes. Practical 5496 reacts to intermittent startup traces by adding a `BugPath.initModFolder` `__file__` fallback and by changing TechChooser's direct `WidgetTypes.WIDGET_TECH_CHOOSER_ERA` access to `getattr(..., WIDGET_GENERAL)`. Practical 5497 deliberately reverts both symptom workarounds after identifying the newer Sevopedia music XML resolver's early `BugPath` calls as the causal startup-order disturbance; practical 5498 then repairs Sevopedia's own mod-XML resolution without touching BUG path state early.
  The TechChooser revert is safe in the normal SAS startup contract. `WIDGET_TECH_CHOOSER_ERA` is not a compiled `CyEnumsInterface` enum; BUG declares it in `Assets/Config/BUG Advisors.xml`, and `WidgetUtil.createWidget` dynamically assigns the new value onto `WidgetTypes` while BUG config loads. Thus the observed missing attribute is consistent with BUG initialization having failed before widget registration. Once BUG config loads normally, the direct historical/current access is valid.
  The premature-BugPath failure mechanism is concrete in current BUG code: `findAssetFile()` calls `initSearchPaths()`, which builds the mod/BtS asset search list and then permanently sets `_searchPathsInitDone = True`. If Sevopedia invokes those helpers before `initModFolder()` can resolve the mod directory, the search list can be initialized without the mod Assets path and stay frozen for later `Config/init.xml` lookup. Practical 5497 removes the unnecessary `BugPath.getModDir/getAppDir/findAssetFile` calls from `_SAS_findAssetXmlPath`, eliminating that early side effect.
  Practical 5498 replaces the earlier `__file__`-relative Sevopedia candidate with a BUG-independent mod candidate derived from `os.getcwd()`, `CvModName.modName` (`AdvCiv-SAS`) and `Mods/<mod>/Assets/...`, while retaining the relative `Assets/...` fallback. The helper validates candidates with `os.path.isfile`, prefers the mod Audio2DScripts copy containing `AS2D_OPENING_MENU_01`, and returns the first existing candidate otherwise. Current SAS preserves this resolver.
  The audio/XML removal in practical 5497 is outside the runtime-source denominator but was checked for causal consistency: it changes music-shuffle content/timing while the actual surviving fix remains the resolver/startup-order correction, matching the later KI#110 documentation. No current startup or TechChooser survivor was established from the 5496 workaround reverts.
  No new surviving defect was established in practicals 5498 -> 5496. Certified cursor advances through practical 5496 / 6f94679507. Next runtime-source SHA: practical 5493 / 08396a2b15 (5495/5494 skipped as non-runtime-source).


SAS-C082 - practical 5493 through 5492 Info Score / Leader-layout lineage - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 5493 / 08396a2b15 and 5492 / fa306068bd. Practicals 5491 through 5487 contain no runtime C++/Python and are skipped by the primary denominator.
  Practical 5493 is the first implementation of several Score-tab signals that practical 5500 later hardens. It adds absolute rival power (`PowT`) only when demographics are visible/debug, so it does not leak hidden rival power. The initial self row remains blank because `iTheirPower` is only assigned for rivals; practical 5500 explicitly repairs that by assigning the already-known active-player power for self. Likewise, 5493's initial `Pow/C` divides the shown relative power ratio by city count, mixing ratio and absolute units; practical 5500 replaces this with visible absolute `PowT / Cit` and renames the header `PoT/C`. Several newly numeric-looking cells are initially inserted as text and therefore sort lexicographically; 5500 converts the genuine numeric fields to `setTableInt`. These are real intermediate imperfections but not current survivors.
  Practical 5493's V/M rewrite is itself a correctness/readability improvement. The older code used `if team.isAVassal() ... elif teamHasVassals`, so a team that is both subordinate to a master and master of sub-vassals could display only the first relationship. The new independent tests append `|ID` master prefixes and `,ID` vassal prefixes, allowing both roles to coexist in one cell; player membership still comes only from the already-visible-player-by-team map.
  The Leader-page coordinate tweak moves Favorites immediately after the Music panel instead of deriving it from the History right edge. It does not alter leader/civic/religion data or widgets, and later certified Leader/AIP layout work supersedes/rebalances these coordinates without a compensating semantic fix.
  Practical 5492 computes `visiblePlayers` once before Score-table construction and reuses that exact list for rows/team maps. The optional max-render threshold therefore counts the same alive/team-score/player-score-filtered entries that the table actually shows. When reached, the table expands symmetrically vertically and to screen margins, reserves a scrollbar-width budget, and moves the optional Legend link; otherwise legacy bounds remain. The new `SAS_SHOW_LEGEND_LINK` gate affects only the Score/Leader legend links. Later certified resolution/font layout lineages retain and refine the same runtime-layout model.
  No new surviving defect was established in practicals 5493 -> 5492. Certified cursor advances through practical 5492 / fa306068bd. Next runtime-source SHA: practical 5486 / 0870e0adc8; practicals 5491-5487 are skipped as non-runtime-source.



SAS-C083 - practical 5486 SAS map-script batch / Parallel Lines starting-slot audit - COMPLETE; KI#238 FOUND / FIXED AND VALIDATED LATER
  Reviewed 1 runtime-source SHA: practical 5486 / 0870e0adc8. Practicals 5491 through 5487 were already skipped as non-runtime-source by C082.
  `SAS_Parallel_Lines.py` is new in this commit. Its five custom-map-option definitions are internally index-consistent: Players Per Line has 2 values/default 0, Height Per Player 9/default 3, Inter-Line Bands 2/default 0, Line Thickness 5/default 2, and Revolving World 4/default 0. The selected line thickness is at least 3 plots, so the center-column custom start is not removed by the script's edge-only waviness trims. Plot-type arrays use the generated map dimensions consistently, and the line/gap hard cap cannot collapse below a one-plot water gap.
  Confirmed KI#238: `_line_count` and `_build_layout.line_slots` size the custom starting-slot topology from the world's XML default-player count, but `findStartingPlot` indexes it with the actual alive-player list. The loop has no out-of-capacity guard; after exhausting all slots it retains the final line/final position, so every player beyond the world-default capacity reuses the last custom starting plot. Current SAS practical 6182 retains this exact control flow. The DLL accepts a non-null Python start immediately and `setStartingPlot` does not deduplicate it, so no later engine pass repairs the duplicate. World default players are only a recommendation/tuning value (`CvGame::getRecommendedPlayers`), while actual alive players come from independent taken/computer setup slots; configurations above the world default are therefore legitimate.
  Preferred surgical repair is to mirror the sibling-map contract: if `iIndex` is outside the total `line_slots` capacity, call `CyPythonMgr().allowDefaultImpl()` and return so the DLL's ordinary taken-start-aware search handles the excess player. This preserves custom topology for supported slots without pretending the map has a bespoke extra slot.
  The rest of practical 5486 closes without another surviving runtime-source defect. Boreal's `isAdvancedMap` return 1 is an explicit setup-list visibility decision. Large Facing Islands' new Revolving World option is complete across option count/name/value/description/default/random handling and `getWrapX`. Longworld and Spiky Avenues' new option arrays/defaults are bounded and their grid/layout consumers use the same option helpers. The old `from SAS_WorldSizes import *` helper also exposed map-script callback names, but Longworld, Spiky Avenues and Parallel Lines each define their own `getDescription` and `getGridSize`, overriding those helper callbacks locally; the broader callback-leak bug affected scripts lacking their own override and was repaired later at practical 5897. Longworld/world-size enum-key problems and related SAS map-profile cleanup are later historical repair lineage at 5854/5855, already certified from the newer side, not current survivors from this checkpoint.
  The mixed non-source additions were sanity-checked where they directly interact with these scripts: the Parallel Lines WorldPicker entry has one Random size plus all 11 current world-size entries, and adding the map name to the land-heavy override is configuration/tuning rather than executable Python semantics and is not used to certify this runtime-source SHA.
  Certified cursor advances through practical 5486 / 0870e0adc8. Next runtime-source SHA: practical 5485 / 34d4d7d392.


SAS-C084 - practical 5485 opening-menu shuffle-bag / Sevopedia audio-path audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5485 / 34d4d7d392.
  `CvMainMenuInfo::getSoundtrack` introduces a process-local shuffle bag only for the configured trigger soundtrack. Disabled mode and non-trigger menu profiles return the XML soundtrack unchanged. Empty/`NONE` pool entries are skipped, the trigger script is inserted as a nonempty fallback if the configured pool is empty, the remaining bag is refilled only after exhaustion, and `ASyncRand` selects from a guaranteed nonempty vector before erasing exactly the chosen entry. Current source retains this implementation; the current configured pool is smaller than at introduction but remains valid. No out-of-range/empty-bag or synced-game RNG mutation was established.
  The new Sevopedia audio XML resolver in `_sevopedia_main_groupings.py` is historically important: practical 5485 is where the early `BugPath.getModDir/getAppDir/findAssetFile` calls first appear. Those calls could initialize BUG's asset search path before mod-directory discovery was ready, producing the later KI#110 startup/TechChooser symptoms. This is not a current survivor: practical 5497 removes the premature `BugPath` interaction and practical 5498 replaces it with the BUG-independent mod-path resolver; that causal repair lineage was already certified in C081.
  Opening-track grouping itself is coherent: classifying an entry as Opening when either `SoundID` starts `SONG_OPENING` or `ScriptID` starts `AS2D_OPENING_MENU` lets custom opening scripts remain in the Opening group even when their sound IDs vary. Parse counters/debug prints are disabled by default and do not alter grouping state. Current descendants retain the intended classification while using the repaired resolver.
  No new surviving defect was established in practical 5485. Certified cursor advances through practical 5485 / 34d4d7d392. Next runtime-source SHA: practical 5484 / 923840f152.

SAS-C085 - practical 5484 dominated/equivalent strategic-resource trade audit - COMPLETE; KI#239 + KI#240 FOUND / FIXED AND VALIDATED LATER
  Reviewed 1 runtime-source SHA: practical 5484 / 923840f152.
  The commit adds an XML-toggleable early `AI_bonusTrade` exclusion for selected Copper/Iron/Horse/Camel/Elephants combinations and era ranges. The configured bonus and era lookups resolve to live current entries; `iChange >= 0` intentionally covers both proposed buys and cancellation checks, and the recipient/buyer is correctly represented by `kPlayer = GET_PLAYER(eToPlayer)`. The individual Classical Horse/Camel/metal substitute rules and later Horse/Camel obsolescence rules are explicit design heuristics; no enum-order, invalid-info-type or arithmetic defect was established in those branches merely from their balance approximations.
  Confirmed KI#239: the hard exclusion is not scoped to an AI recipient. An AI seller therefore applies the AI-buy optimization to a human buyer before reaching AdvCiv's existing human-recipient carve-out. Resource `DENIAL_JOKING` is explicitly filtered from human trade inventories by `CvPlayer::buildTradeTable`, and `AI_checkCancel` also reaches the gate with `iChange == 0`; this can hide first-copy strategic resources from a human or cancel an existing AI-to-human export solely because an AI purchasing heuristic says the resource is dominated. The commit/docs consistently describe AI buying, so this is unintended scope leakage rather than a documented player restriction. Preferred surgical repair: require `!bPlayerHuman` for the hard dominated-buy gate.
  Confirmed KI#240: the unconditional Medieval+ Copper rule is not semantically valid even for AI buyers. Base AdvCiv 1.14 and current SAS both let the generic Privateer use Copper OR Iron; current SAS places it at Medieval `TECH_PROFESSIONAL_ARMY`, so a Medieval AI without Iron can still gain a live strategic unit from Copper. Corporation 4 and Corporation 5 also consume Copper, while `AI_corporationBonusVal` and the pre-existing duplicate-resource filter explicitly preserve corporation-useful copies. The 5484 early return bypasses both ordinary bonus valuation and that corporation safeguard. Preferred repair: remove era-alone Copper domination and let Copper reach ordinary value logic when Iron is absent, and skip the hard dominated gate when recipient corporation value is positive.
  Both fixes were implemented and validated later in commit 9fb1f179a; see F036/F037 and KI#239/KI#240.
  No third surviving defect was established in practical 5484 after checking the buyer/seller direction, new-trade and cancellation call sites, trade-table filtering, configured strategic/era identifiers, current unit prerequisites and corporation valuation path. The HRE Houfnice is an additional Copper-or-Iron use, but its special-case omission had already been documented elsewhere as a deliberate simplification; it is not counted separately from the generic Privateer/corporation proof above.
  Certified cursor advances through practical 5484 / 923840f152. Next runtime-source SHA: practical 5483 / e92a36c49d.


SAS-C086 - practical 5483 through 5482 Info Score-tab / Statistics default-order audit - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 5483 / e92a36c49d and 5482 / 8827c3e2d9.
  Practical 5483 introduces the Info Screen Score tab plus optional Sevopedia/NewConcept legend links. The Score-tab implementation was compared against the BUG/Main Interface scoreboard paths it explicitly mirrors rather than treated as a presentation-only table. Player visibility inherits the scoreboard team/player filters; score-delta turn selection matches Main Interface; ordinary city counts use revealed cities; rival power and research remain behind their existing demographics/research visibility gates; attitude/willingness/worst-enemy and Golden-Age/anarchy behavior follow the same player-direction/self-exclusion conventions as the scoreboard. The new Land% field uses exact `getTotalLand`, but this does not establish a new protected-information leak: AdvCiv's Victory Screen already identifies the strongest met rival and displays that rival's exact land percentage for land-victory conditions, while Top Civs' Size ranking also uses exact total land.
  Several genuine first-version Score-tab defects in 5483 are historical lineage already repaired by newer practicals 5493/5500: numeric-looking fields initially use text sorting; the active row has blank absolute power; Pow/C initially divides a displayed power ratio by city count rather than using comparable absolute power; and V/M initially uses mutually exclusive vassal-vs-master rendering. Those later repairs were independently certified from the newer side, so none is a current survivor at this cursor. The NewConcept helper returns a validated index or -1 and both Score/Leader legend callers skip the link when missing; the added GameSpeedInfo patch is comment/whitespace-only after excluding XML comments/blank lines, with no value change.
  Practical 5482 changes only the default Statistics-tab row ordering: units/buildings sort by descending historical Built count and improvements by descending Current count, with localized description and info ID as stable tie-breakers. Row filtering, displayed statistics, table cell types and click widgets are unchanged, and users can still sort by table headers afterward. No surviving defect was established.
  Certified cursor advances through practical 5482 / 8827c3e2d9. Next runtime-source SHA: practical 5481 / 42823ae07d.



SAS-C087 - practical 5481 through 5478 diplomacy trade-row / vassal-offer / Treaties audit - COMPLETE; KI#241 FOUND / FIXED AND VALIDATED LATER
  Reviewed 4 runtime-source SHAs: practical 5481 / 42823ae07d, 5480 / 51122e118b, 5479 / 0656c16e6d and 5478 / 2078e7e8e9.
  Practical 5481 adds target-leader portrait buttons to Peace/War/Embargo trade rows. It preserves the existing `TradeableItems` and target-team IDs; the target team name is unchanged, and the new icon lookup first checks `getLeaderID()!=NO_PLAYER` and that the leader is alive before reading its leaderhead button. No diplomacy state, denial or target direction is changed.
  Practical 5480 repairs KI#108's inconsistent pure Vassal/Surrender acceptance. In the supported direction, `AI_considerOffer` is the AI evaluating a human proposal while `kWeGive` is the AI side: accepting a one-item AI Vassal/Surrender offer with an empty human counteroffer matches the pre-existing counter-proposal result, and the Python helper suppresses generic MADE_DEMAND memory only when that exact accepted AI-side offer shape is present. The C++ condition is syntactically symmetric and would also early-accept the reverse human-self-vassal shape, but current AdvCiv explicitly configures `BBAI_HUMAN_AS_VASSAL_OPTION=0` as unsupported and `CvPlayer::canPossiblyTradeItem` rejects a human offering Vassal/Surrender to an AI under that configuration. This is therefore retained as an unsupported-configuration caveat rather than counted as a current supported-game defect.
  Practical 5479 iconizes Diplomacy Screen trade rows without changing item identity/value semantics. Gold/GPT retain the same numeric strings with a gold char prefix; World Map gains the existing map symbol; Vassal/Surrender/Open Borders/Defensive Pact/Permanent Alliance use art keys that resolve in current `CIV4ArtDefines_Interface.xml`. Offer quantities, current-deal lookup, denial checks and `m_bOffering` state remain unchanged.
  Confirmed KI#241 in practical 5478: Treaties rows now omit all `TRADE_RESOURCES` items from their visible text while preserving one clickable `WIDGET_DEAL_KILL` row for the complete `CvDeal`. Mixed annual deals are ordinary reachable states: `CvDeal` saves both resource and GPT items, and `CvPlayerAI::AI_foldDeals` deliberately creates combined resource(s)+net-GPT deals during normal AI end-of-round processing. `doDealKill` cancels by deal ID only, so a visible GPT/treaty row can cancel an undisclosed resource term. Resource-only deals being omitted entirely is intentional reduced scope and is not itself the defect; the mismatch is partial display plus whole-deal action.
  Fixed and validated later in the current Foreign Advisor batch; see F038 and KI#241.
  No second surviving defect was established in this diplomacy tranche. Certified cursor advances through practical 5478 / 2078e7e8e9. Next runtime-source SHA: practical 5477 / b33174f828.


SAS-C088 - practical 5477 through 5470 obsolete-scrap / teardown guard / Sevopedia Space debounce / random-name cleanup audit - COMPLETE
  Reviewed 4 runtime-source SHAs: practical 5477 / b33174f828, 5475 / abaf980a63, 5472 / 9fee472086 and 5470 / 086670f330. Practicals 5476, 5474, 5473 and 5471 contain no runtime C++/Python change in the frozen denominator.
  Practical 5477 is historically important because its World-Wonder hunk is the already-confirmed KI#205 SAS game-speed regression: it removes game-speed scaling from the cheap-Wonder cap while `iCost` actually comes from game-speed-scaled `getProductionNeeded`. Practical 6127 already restores the required scaling and was independently validated earlier in this archaeology, so this is recorded as known repaired lineage rather than a new survivor.
  The same 5477 commit adds hardcoded obsolete-unit scrap XML-cost caps by domain/era. The raw `CvUnitInfo::getProductionCost()` comparison is internally consistent with raw XML-cost thresholds, sea cargo currently aboard is protected, and land combat/sea domains are separated deliberately. These exact hardcoded gates do not survive current architecture: later practicals 5945/5979 replace/remove them in favor of the newer obsolete-only delay/protection rules, and current `canScrap` no longer uses the 5477 era/XML-cost tables. Historical rough edges such as protecting only currently loaded (`getCargo()>0`) rather than empty cargo-capable transports therefore do not constitute current survivors.
  Practical 5475 adds `isFinalInitialized()` teardown guards to `CvGame::updateSelectionList`, `updateTestEndTurn`, `canHandleAction` and `handleAction`. Each guarded path otherwise reaches active-player/UI state that may be invalid after returning to the main menu; the chosen pre-final-init behavior is neutral (`return` or `false`). Current SAS retains these guards and no normal initialized-game action path is changed.
  Practical 5472 adds Space to the existing Sevopedia type-to-filter key debounce so duplicate `NOTIFY_CHARACTER` delivery cannot turn a single separator into two spaces. It uses the same per-key alternating duplicate suppression already applied to letters/digits/backspace and changes no search matching, navigation or list data. Practical 5470 changes only one malformed French random-name string (`Berang�r` -> `Beranger`) in `RandomNameUtils`; no random-name generation/index semantics change.
  No new surviving defect was established in this block. Certified cursor advances through practical 5470 / 086670f330. Next runtime-source SHA: practical 5469 / 1767667d15.


SAS-C089 - practical 5469 formatting-character cleanup audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5469 / 1767667d15. Practicals 5468, 5467, 5466 and 5465 contain no runtime C++/Python change in the frozen denominator.
  Practical 5469 is a broad apparent C++/Python diff titled `remove error-prone formatting characters`, so it was mechanically audited rather than accepted from the title. All 148 paired changed runtime lines across `Assets/Python` and `CvGameCoreDLL` were compared before/after. After normalizing smart double quotes to ordinary ASCII double quotes, every pair is identical except one `CvGlobals.cpp` diagnostic example comment that additionally removes a mojibaked em-dash sequence. The only two changed C++ lines lacking a same-line `//` marker are continuation lines inside an existing `/* ... */` Worker-AI comment block.
  Therefore no executable token, Python/C++ string literal, TXT/art/type key, path, identifier, comparison, numeric value or control-flow expression changes in this runtime-source commit. XML/text/documentation changes are outside the runtime-source denominator and do not alter this certification. No surviving defect was established.
  Certified cursor advances through practical 5469 / 1767667d15. Next runtime-source SHA: practical 5464 / fe25618f49.


SAS-C090 - practical 5464 optional doomed-city evacuation audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5464 / fe25618f49.
  Practical 5464 makes SAS's always-evacuate-doomed-cities behavior optional behind a default-enabled GlobalDefine. With the define enabled, `AI_evacuateCity` retains the immediately preceding SAS behavior: after the existing `AI_isEvacuating()` gate, eligible combat UnitAI groups call `AI_retreatToCity()` unconditionally. With the define disabled, the restored branch matches AdvCiv 1.14's original probabilistic evacuation logic: the same combat/no-defensive-bonus gate, hit-point/fortification/plot/city-defense adjustments, staying-behind count, `SyncRandSuccess` decision and retreat call are reinstated.
  The define gates the complete alternative behavior inside `AI_evacuateCity`; it does not leave a separate SAS evacuation mission/state mutation partially active when disabled. The existing caller still limits this routine to the same city/owner and combat UnitAI set, while `AI_isEvacuating` itself is merely the precondition used by both algorithms. The function-local static define cache is consistent with restart-tunable SAS GlobalDefines. Current descendants rename/log this choice but do not contain a corrective semantic rewrite attributable to 5464.
  No surviving defect was established. Certified cursor advances through practical 5464 / fe25618f49. Next runtime-source SHA: practical 5463 / 96163add02.


SAS-C091 - practical 5463 Game Speed timeline/chart + shared chart LOG audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5463 / 96163add02. Practical 5462 contains no runtime C++/Python change in the frozen denominator. Developer-only `LLM_Helpers` Python introduced by 5463 remains excluded from the primary runtime denominator.
  The large Game Speed chart expansion was checked against Civ4's actual default-calendar implementation rather than only by visual examples. For all eight current GameSpeed XML entries, every game turn from 0 through the configured final turn was independently converted by the chart's cumulative-month algorithm and the DLL `getTurnMonthForGame`/`getTurnYearForGame` semantics; no year/month mismatch occurs. C++ truncation toward zero could in theory differ from Python integer division for fractional-year BC dates, but every current increment while the timeline remains BC is divisible by 12 months, so that trap is unreachable with current data. Fractional-year increments begin only after AD. The 5%-step Summary helper clamps to TotalTurns and uses a consistent ceil-to-turn rule; increment-boundary handling matches the DLL turn-increment loop.
  Calendar, Increments and Summary rows derive only from `getGameTurnInfo`/START_YEAR and do not mutate game state. `TotalTurns` is the exact sum of `iNumGameTurnsPerIncrement`; per-speed chunk rows are bounded by `max_increments`, leaving ordinary empty cells for shorter lists. The BC->AD arrow is presentation-only and is triggered once on the first segment whose end crosses nonnegative year; no current segment/summary endpoint relies on displaying mathematical year 0 as an in-game year label.
  The new shared chart LOG path was also audited across Era/GameSpeed/Handicap/WorldSize. `chart_add_csv_log_button` returns no widget when the existing log-button define is disabled. When enabled, page-level function-name handling and central `WIDGET_PYTHON`/data1 routing are fallback alternatives rather than duplicate actions: successful page handling returns immediately, while the data1 path catches generated-widget-name failures. CSV cleaning removes only font/color tags and invisible chart sort marks, doubles embedded quotes, quotes comma-containing cells and emits Latin-1 replacement output without changing the underlying cached table. The import-time define read is a restart/static UI setting, not mutable game state.
  `CHART_TABLE_STYLE` changes the four chart table skins from EMPTY to STANDARD without changing row/cell APIs or sorting. Removed helper compatibility aliases have no post-5463 runtime caller. No surviving defect was established.
  Certified cursor advances through practical 5463 / 96163add02. Next runtime-source SHA: practical 5461 / 94a336ef48.


SAS-C092 - practical 5461 Leader AIP predump-emitter audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5461 / 94a336ef48. The same mixed commit contains balance XML changes; those do not consume the source-focused runtime denominator.
  The handwritten runtime change in `SevoPediaLeaderAIPValues.py` changes only predump serialization/diagnostics: `print` calls become a local stdout `emit`, the leader-index:type mapping is added as a generated comment, and the formerly one-line `%r` leader dictionary is emitted deterministically with sorted integer leader indexes and sorted homogeneous string cache keys. The resulting Python module remains literal dictionary/category data; no AIP getter, normalization, category membership, scale calculation or UI consumer is changed by this source hunk.
  The omitted/generated `SevoPediaLeaderCachePredumped.py` patch was not assumed safe merely because the historical renderer summarizes it. Current runtime loading still uses that generated module, so the supplied practical-6182 tree was checked with the repository's independent XML+UWAI predump validator (`aip_predump_values.py`): 7,810 effective AIP values compared, 0 mismatches and 0 missing/unparsed entries. The leaders whose victory-weight XML is adjusted in 5461 are represented by the current predump; no stale-cache survivor remains. Later predump tooling has been refactored, but retains the same data-only serialization principle.
  No surviving defect was established. Certified cursor advances through practical 5461 / 94a336ef48. Next runtime-source SHA: practical 5460 / d185fc9208.


SAS-C093 - practical 5460 SAS_Large_Facing_Islands birth audit - COMPLETE; KI#242 FOUND / FIXED AND VALIDATED LATER
  Reviewed 1 runtime-source SHA: practical 5460 / d185fc9208.
  Practical 5460 introduces `SAS_Large_Facing_Islands.py`, a fixed-island-footprint map whose description and start-placement design explicitly promise that each player starts on a large island. The map creates one custom starting slot per generated island and safely calls the DLL default implementation when the actual alive-player index exceeds that slot list; unlike KI#238, it does not fall through onto and duplicate the final custom slot.
  KI#242 candidate: the birth profiles nevertheless under-provision islands for two ordinary world sizes relative to the game's own default player counts. Arena uses `(1, 1)` = 1 island while `WORLDSIZE_ARENA` has 2 default players; Small uses `(1, 4)` = 4 islands while `WORLDSIZE_SMALL` has 5 default players. On an ordinary default-player game, the overflow player therefore cannot satisfy the map's one-player-per-large-island contract. `allowDefaultImpl()` cannot create an absent island: the DLL's normal start search may choose an otherwise legal plot on an already-used landmass, but it does not reserve an entire island for each existing start.
  The mismatch is present from 5460 rather than introduced by later world-size work. Later edits to `_world_profile` at practicals 5855, 5864 and 5971 only replace the dictionary's raw integer world-size keys with successively safer constants/enums; the `(1, 1)` Arena and `(1, 4)` Small values remain unchanged through the supplied current tree. Later `CIV4WorldInfo.xml` edits likewise do not change Arena's 2 or Small's 5 default-player values.
  The remaining map birth logic was checked separately. Generated plot-array dimensions and island rectangles are in bounds for the supported profiles; custom start centers are unique and correspond one-to-one with generated islands; same-island fallback indexes the matching island; and the original two custom map options have coherent counts/defaults/value bounds. The mandatory WrapX birth behavior makes its wrap-edge horizontal coast connector legitimate.
  The historical `from SAS_WorldSizes import *` import does not add a second callback leak at 5460, but the original C093 wording was too broad and is corrected here: `SAS_WorldSizes.py` had already become a playable helper map at practical 5415 and therefore exported `getDescription()` and `getGridSize()`. `SAS_Large_Facing_Islands.py` defines both callbacks itself after the import, so those imported names are overwritten locally; no other conflicting map callback was exported by the helper at this point. The broader import-star callback-leak problem affected scripts that lacked their own replacement callback and was later repaired by moving reusable helpers to `SAS_WorldSizeUtils`; current Large Facing Islands imports the safe helper module.
  Fixed and validated later in commit 49732aa36: Arena now uses `(1, 2)` and Small `(1, 5)`, providing one island per default player without a broader start-placement rewrite. Screenshots 0066 and 0067 confirm the two- and five-island layouts; the refreshed `PythonErr.log` remained empty.
  No additional surviving source defect was established in practical 5460. Certified cursor advances through practical 5460 / d185fc9208. Next runtime-source SHA: practical 5459 / 3aaba12341.


SAS-C094 - practical 5459 Sevopedia panel-width / feature-production / worker-speed-help audit - COMPLETE
  Reviewed 1 runtime-source SHA: practical 5459 / 3aaba12341.
  The broad panel-dimension refactor was checked as data capacity rather than assumed cosmetic. The new one-button helper reproduces the old 84 px mini-panel exactly, the shared standard height reproduces 110 px, and the four-button multilist helper reproduces the existing 282 px leader-panel width. Project Requires is over-provisioned at two buttons because `CvProjectInfo` exposes only one `TechPrereq`. Tech Leads To is intentionally sized for four direct buttons; the current tech graph peaks at exactly four direct successors and contains no duplicate AND/OR reference that would render a fifth copy. Improvement TerrainMakesValids peaks at exactly five entries (Fort), matching the new five-button panel; FeatureMakesValids peaks at two. No current data-capacity truncation was established.
  `SevoPediaBuild` and `SevoPediaFeature` add a Feature Production panel. `get_feature_production_modifier_techs()` enumerates the actual global tech field used by the player feature-production multiplier; current XML has only Mathematics at +50%, so the two-button panels have spare capacity. Build pages show the modifier only when that Build has positive removal production for at least one feature. Feature pages require a standalone non-improvement removal Build with positive production; that restriction does not make Forest/Jungle falsely empty in this lineage because dedicated `BUILD_REMOVE_JUNGLE` and `BUILD_REMOVE_FOREST` already existed before 5459, and Jungle's dedicated removal production had been raised to +20 by practical 4876 while Forest retained positive chop production. Current data continues to provide those dedicated positive-production removals. Fallout has zero removal production and correctly does not advertise a production modifier. Later 5817 changes these panels to multilists with percentage numTxt but does not repair their underlying eligibility semantics.
  The Feature Special refactor also fixes rather than introduces a historical rough edge: it filters to positive standalone removal builds before selecting the representative production value, instead of the prior loop's possibility of breaking on the first removal Build regardless of whether that particular entry produced hammers. Current Forest/Jungle removal Builds use consistent production values per feature, so selecting the first positive dedicated removal does not misstate a second value.
  The new Civilopedia-only worker build-speed bullet was traced to gameplay state. Tech, civic and building `iWorkerSpeedModifier` values are exactly the three XML classes accumulated into `CvPlayer::m_iWorkerSpeedModifier` (`CvTeam::processTech`, civic processing and `CvPlayer::processBuilding`), and both `CvUnit::workRate`/player work-rate calculation apply that player-wide modifier. The display is gated on positive base WorkRate plus at least one Unit build, so it does not attach the generic modifier list to non-builder combat units. Existing literal-link syntax is already used elsewhere by the same game-text manager. No omitted fourth modifier source or gameplay mutation was found.
  The Improvement top-right geometry rewrite was also compared with the pre-5459 adaptive layout. At normal wide Sevopedia widths it exchanges a variable terrain-valid width for an exact five-button width and gives the remaining width to the animation; in constrained widths both old and new algorithms hit the same 120 px side-panel floor behavior rather than 5459 newly creating the pre-existing narrow-screen limitation. No new overlap/negative-width range unique to 5459 was established.
  Other changed Sevopedia pages only replace equivalent 84/110/282 constants or make small presentation-coordinate adjustments while preserving widget/data tuples. `PrivateMaps/Water.py` changes description wording only. No surviving runtime-source defect was established. Certified cursor advances through practical 5459 / 3aaba12341. Next runtime-source SHA: practical 5458 / 936bb50818.


SAS-C095 - practical 5458 Water.py birth audit - COMPLETE; KI#243 AND KI#244 CANDIDATES FOUND
  Reviewed 1 runtime-source SHA: practical 5458 / 936bb50818.
  Practical 5458 introduces the adapted `PrivateMaps/Water.py`. Its ordinary generator structure was audited separately from the two findings: the generated plot array uses exactly width*height entries; water percentage is clamped to 20..40; peak percentage is clamped to 0..100 and uses the same low-fractal-value peak threshold direction as `CvMapGeneratorUtil.FractalWorld`; `findStartingArea` intentionally keeps starts on the biggest land area; `addLakes`/`normalizeAddLakes` deliberately suppress the engine's extra lake pass because the script already generates many lakes; and the script has no custom-map-option index surface. The historical `from SAS_WorldSizes import *` imports that helper map's `getDescription`/`getGridSize`, but Water defines both callbacks itself after the import, so they are overwritten locally and no additional helper callback collision is present.
  A separate birth-version grid-size defect is historical-only: the direct dictionary mixed Arena key `0` with old hardcoded `WorldSizeTypes` values after Arena shifted runtime world-size indices. Practical 5855 explicitly repairs Water's grid keys and documents empirical Large/Huge size validation; current Water uses the corrected XML-aligned world-size keys. This repaired lineage is not counted as a new survivor.
  KI#243 candidate: Water's `addRivers()` mutates the shared global defines `PLOTS_PER_RIVER_EDGE`, `RIVER_SOURCE_MIN_RIVER_RANGE` and `RIVER_SOURCE_MIN_SEAWATER_RANGE`, including non-idempotently replacing `PLOTS_PER_RIVER_EDGE` with half of its *current* value, and then calls `allowDefaultImpl()` so the DLL river generator continues. That is incompatible with SAS practical 5083's earlier optimization in `CvMapGenerator::addRivers`, which made those same three define reads function-local `static const`. The Python callback runs before the static declarations. Therefore the first default river generation in a DLL/process fixes the C++ values for subsequent generations: if Water runs first, Water's denser river values become cached and can contaminate later non-Water map generations; if a normal map initializes the statics first, a later Water callback mutates `CvGlobals` but the DLL keeps using the already-cached normal values, so Water's intended extra-river override no longer reaches the default generator. Repeated Water generation also keeps halving the shared global define even though the C++ static remains fixed after its first initialization. `CvGame::regenerateMap()` regenerates within the same running DLL and does not reset function statics or these global defines, so this is a source-reachable lifetime/order dependency rather than a hypothetical restart-only concern. Base AdvCiv 1.14 still reads the three values per call, confirming the process-static part is a SAS optimization interaction; Water's unscoped global mutation itself is also persistent shared state. Current SAS retains both Water's mutations and the static C++ reads.
  Surgical direction for KI#243: make the Water river override per-generation and scoped rather than persistent shared state, and do not process-cache map-script-mutable river defines. The three C++ river values need to be read for the current generation after the Python callback rather than frozen in function statics, while any temporary Water define changes should be restored after that generation (or replaced by an explicit map-specific parameter/callback mechanism). Merely removing `static` without scoping/restoring Water's mutations would still leak its altered globals into later maps; merely restoring the globals without removing the statics would still make Water's effect depend on which map initialized the statics first.
  KI#244 candidate: Water declares `getWrapX() == True` and `getWrapY() == False`, but `WaterFractalWorld.isSurroundedByLand()` computes every cardinal neighbor with modulo on both coordinates: `((y+dy) % height) * width + (x+dx) % width`. Civ4's actual `CvMap::plot`/`coordRange` only wraps an axis when that axis's wrap flag is true, so north/south map edges are not adjacent. Water therefore invents a Y-edge connection during its explicit "Convert 1-tile lakes into flatland" pass. For example, a genuinely one-tile water area on the north edge with land to east/west/south can be left unconverted solely because an unrelated south-edge tile at the same X is water. `CvArea::isLake()` is based on the real connected water area's tile count, so the opposite-edge tile is not part of that lake on this non-WrapY map. The modulo-Y helper survives unchanged in current Water.py; practical 5887 only removes blank lines around it.
  Surgical direction for KI#244: preserve X modulo wrapping, but do not modulo-wrap Y when WrapY is false. For north/south out-of-bounds neighbors, treat the missing coordinate as no connected water (or use a bounds/wrap-aware helper with the map's actual wrap flags) so the test reflects the map topology it is trying to classify.
  During this checkpoint, the C093 helper-export explanation was corrected: `SAS_WorldSizes.py` already exported playable-map `getDescription/getGridSize` callbacks at 5460; Large Facing Islands remained safe because it overwrote both locally. This correction changes rationale only, not C093's KI#242 conclusion or certified cursor.
  No additional surviving source defect was established in practical 5458. Certified cursor advances through practical 5458 / 936bb50818. Next runtime-source SHA: practical 5457 / 3fffd5ea98.


SAS-C096 - practical 5457 -> 5456 map-classification / SAS_Spiky_Avenues birth audit - COMPLETE; KI#245 FOUND / FIXED AND VALIDATED LATER
  Reviewed 2 runtime-source SHAs: practical 5457 / 3fffd5ea98 and 5456 / 15aa086e93.
  Practical 5457 is a small follow-up. `Mixed_Continents.py` changes only `isAdvancedMap()` from 0 to 1, deliberately removing a multi-option map from Simple Game; `SAS_Longworld.py` removes an unused `CvMapGeneratorUtil` module import while retaining its direct `TerrainGenerator`/`FeatureGenerator` imports; and `SAS_Spiky_Avenues.py` removes an unused local `CyGlobalContext`. The GlobalDefine map-name classification change is XML-only. No runtime behavior defect was established in these Python hunks.
  Practical 5456 introduces `SAS_Spiky_Avenues.py`. Its generator was audited as executable map logic rather than inferred from the visual design. Plot generation creates one land `house` for each custom start slot, two houses per spike; slot coordinates are unique; the slot assignment cache and `sas_used_start_plots` prevent custom-slot reuse; invalid/missing custom slots call `allowDefaultImpl()` rather than falling through to the last slot; the safety search is bounded to radius 1..2 around the intended house and refuses an already-used plot; plot-array indexing stays within the map dimensions produced by the same profile formula; and X-only wrap plus Y-edge margins are internally coherent. The later `SAS_WorldSizes`/world-size-constant refactors do not alter the profile values discussed below.
  KI#245 candidate: the map's own layout contract says `house = ... where one player starts`, `1 spike = 2 houses = 2 player starts`, the profile `scales with world size to match default player counts`, and the public description says `One player starts per house`. However the birth profile uses Tiny `(1 street, 1 spike)` = only 2 houses for `WORLDSIZE_TINY`'s 3 default players, and Small `(1, 2)` = only 4 houses for `WORLDSIZE_SMALL`'s 5 default players. Those world-size defaults were already in force at practical 5456 and remain 3/5 in current `CIV4WorldInfo.xml`. No later Spiky profile edit repairs the mismatch: practical 5855 only replaces raw world-size keys with SAS constants, 5864 renames those constants, 5897 moves the helper import, and 5971 replaces the constants with `WorldSizeTypes`; all preserve Tiny `(1,1)` and Small `(1,2)`. Current SAS retains the same under-capacity profiles.
  The overflow guard is therefore safe against KI#238-style duplicate-last-slot behavior but cannot preserve the map's stated layout on an ordinary default-player Tiny/Small game. Once `iIndex >= len(slots)`, Civ4's default start finder may place the excess player on some legal generated land, but no missing third/fifth house exists to assign: by simple capacity, at least one default player cannot start in a distinct designed house. This is independent of start-distance tuning and of whether fallback happens to choose an avenue, bridge or an already represented house area. Large intentionally has one spare house (12 for 11 players), showing exact equality is not required; the correctness requirement is only that designed one-player house capacity not fall below the normal default player count.
  Fixed and validated later in commit 49732aa36: Tiny now uses 2 spikes / 4 houses and Small 3 spikes / 6 houses, the next even capacities supported by the symmetric spike primitive. Screenshots 0068 and 0069 confirm both layouts; the refreshed `PythonErr.log` remained empty.
  No additional surviving source defect was established in practical 5457 -> 5456. Certified cursor advances through practical 5456 / 15aa086e93. Next runtime-source SHA: practical 5455 / fa79a78fde.


SAS-C097 - practical 5455 SAS_Longworld birth audit - COMPLETE; KI#246 CANDIDATE FOUND
  Reviewed 1 runtime-source SHA: practical 5455 / fa79a78fde.
  Practical 5455 introduces `SAS_Longworld.py`. The generator was checked as a full map/start-placement implementation. `generatePlotTypes()` allocates exactly width*height plot entries; `getStripBounds()` leaves polar water margins and produces a nonempty central band for supported heights; every generated X column keeps at least three land rows; and the top/bottom edge movement is limited to one row from the preceding column, so adjacent columns retain overlapping land and the intended single horizontal landmass stays connected. Hills are added only after land creation and do not make starts impassable. The nominal start formula uses the actual sorted alive-player list rather than world-default capacity and, by itself, produces unique `(X,Y)` pairs even at 48 players on the smallest supported layout because repeated X values are staggered through strip rows.
  The birth-version `getGridSize()` does contain the older Arena/`WorldSizeTypes` key-offset problem: with Arena inserted into XML while the old Python enum values remained shifted, several dictionary keys/fallback comparisons address the wrong runtime world size. Practical 5854/5855 explicitly repairs that lineage (including the observed Huge wrong-size/KeyError behavior) and current Longworld uses XML-aligned `WorldSizeTypes` keys. That repaired grid-size defect is historical-only and is not counted again here. Later option/import/world-size refactors also leave the finding below's start fallback semantics unchanged.
  KI#246 candidate: `findStartingPlot()` has no per-player assignment cache or used-start set. A nominal start that lands on a wavy strip edge trimmed to water enters a fallback that scans `strip_rows` at the same X first and immediately returns the first valid land plot, without checking whether another player has already been assigned that plot. High-player-count small maps make repeated X coordinates ordinary under the script's own spacing formula. In the current default Arena profile the actual map width is 36 plots, giving `iSpan = 32`; with 48 alive civ players, players 0 and 1 both receive X=2 while their nominal Y rows are 6 and 7. The default strip height is four. The generator's normal top-trim RNG can remove row 6 at X=2 while leaving row 7 land (the first top-trim branch occurs with 35% probability, and a one-row trim still satisfies the minimum-band-height rule). Player 0 then sees `(2,6)` as water and its fallback checks `(2,6)`, then returns `(2,7)`; player 1's nominal start is already `(2,7)`. The same class of collision exists in the birth version (which also ships the 48-civ DLL) despite its then-different grid-size profile.
  The engine does not rescue this duplicate. As established independently during KI#238, a non-null Python `findStartingPlot` result is returned directly by `CvPlayer::findStartingPlot`, and `setStartingPlot` does not deduplicate Python-supplied plots. This is therefore a source-reachable duplicate starting-site path, not merely a spacing/aesthetics concern. Current SAS retains the same fallback body; practical 5816 only reformats around it, and no later Longworld commit adds used-start tracking.
  Surgical direction for KI#246: make Longworld start assignment collision-aware and stable across repeated callback calls, preferably mirroring the already-defensive Spiky pattern with a per-generation `player -> plot` assignment cache plus a used-plot set reset in `beforeGeneration`. When the nominal edge tile is invalid, choose an unused valid strip tile (same/nearby X first) rather than blindly returning the first valid tile in that column. Do not merely change the horizontal spacing formula: repeated X is acceptable because different strip rows are intentional; the defect is reuse during fallback.
  No additional surviving source defect was established in practical 5455. Certified cursor advances through practical 5455 / fa79a78fde. Next runtime-source SHA: practical 5454 / 093b59bae4.


SAS-C098 - practical 5454 BTG_Grid import/adaptation audit - COMPLETE; KI#247 -> KI#253 CANDIDATES FOUND
  Reviewed 1 runtime-source SHA: practical 5454 / 093b59bae4.
  Practical 5454 imports/adapts `BTG_Grid.py` and makes several much smaller map-related changes. The non-Grid runtime hunks close cleanly: `BTG_Cross.py` consistently removes the BTG-only Desert->Marsh choice by reducing option 11 from three values to two, making Normal the default and leaving the remaining Desert-removal branch as Grassland; `Peirce.py` changes only the displayed author string; and `PerfectMongoose.py` / `Terra.py` deliberately switch `isAdvancedMap()` so they leave Simple Game while remaining available in Custom Game. The accompanying map-name classification / WorldPicker changes are XML/art rather than runtime-source-denominator changes and are consistent with the intended map-pool update.
  `BTG_Grid.py` was audited as a full executable imported generator: all ten custom-option declarations/defaults/random flags were mapped to their runtime option reads; `beforeGeneration`, static template selection/clamping, `GridMultilayeredFractal.generatePlotsByRegion`, `generatePlotTypes`, both custom-start branches, default-fallback behavior, normalization/mirroring, world-size/grid callbacks and the current C++ Python-callback contracts were traced. Birth practical 5454 was compared with current SAS rather than assuming later lint/world-size refactors repaired old logic. Seven independent current survivors were established; several other suspicious paths were explicitly rejected or left unnumbered below rather than inflating the count.
  KI#247 candidate (SAS-specific imported Grid adaptation defect): the custom-start capacity guard counts `len(regions_in_use)`, but generation always appends the chosen source region once and then appends the complete set of active region IDs again. The source region is therefore present twice, so list length overstates *distinct* hub capacity by exactly one. This defeats the guard on one-over-template configurations. With 11 actual players, 1-Line Flat and default `Empty Land = Yes`, the template clamps from 11 requested regions to 10 real regions, `regions_in_use` nevertheless has length 11, the guard sees `11 < 11` as false, `iTotLoop = iNumRegions = 10`, and Python assigns only ten shuffled players before returning success. Current `CvGame::assignStartingPlots` returns immediately when the Python callback succeeds, so player 11 is not filled by the C++ default path. With `Empty Land = No`, the same 11-player/10-region case loops over all eleven players; the final `reg = 10` lookup fails and the generic fallback in the script reuses `region_data.keys()[0]`, allowing the extra player to reuse region 0 (and in the mirrored-position branch, the same copied offset/start). Equivalent one-over-template reuse exists for 13 players / 2-line / Empty Land No and 16 players / 3-line / Empty Land No. The guard, duplicate list construction and fallback survive current SAS.
  Surgical direction for KI#247: capacity checks must use distinct usable region IDs / actual assignable slots, not raw `regions_in_use` length; never return successful Python assignment unless every actual non-spectator civ player received a unique valid start. If requested players exceed custom region capacity, call `allowDefaultImpl()` before assigning any custom starts (or provide enough actual templates) rather than under-assigning or reusing region 0.
  KI#248 candidate (SAS-specific imported Grid data defect): the 9-region `1 Line Flat` template defines region 5 as `[0.755, 0.666, 0.0, 1.0]`, so west longitude is greater than east longitude. All neighboring entries form the expected monotonic sequence (`...0.444->0.555`, then this entry, then `0.666->0.777`), and a scan of every Grid template rectangle found this is the only west/east or south/north inversion. At runtime the region therefore has negative width. In non-mirrored generation that invalid dimension reaches `generatePlotsInRegion`/`CyFractal.fracInit`; in mirrored modes the target-copy loops for the negative-width region perform no regional copy while the region is still retained for starting logic, so a player can receive an invalid/out-of-hub copied offset or the custom start path can encounter a region with no land areas. The exact malformed rectangle is present in the 5454 birth file and survives current SAS.
  Surgical direction for KI#248: repair the region-5 west boundary to the contiguous intended boundary (the sequence strongly indicates `0.555`, yielding `[0.555, 0.666, ...]`) and validate the 9-player 1-line layout under all four Mirrored Hubs choices. Also keep a lightweight template invariant check (`west < east`, `south < north`, in [0,1]) in development tooling so this class cannot silently recur.
  KI#249 candidate (broader inherited third-party/Firaxis-style map-script defect): three Grid branches use `if pPlot.isFreshWater:` instead of calling `pPlot.isFreshWater()`. `CyPlot.isFreshWater` is exported to Python as the method `bool ()`; in Python 2 the bound-method object itself is truthy. Grid's coastal regional scoring therefore always takes the freshwater branch for every adjacent water tile, and both custom-start branches add the intended +1000 freshwater preference to every land candidate. That erases freshwater preference, distorts regional yield scoring and can promote plots whose underlying found value is zero. This is not unique to Grid: current `BTG_Cross.py` has the same three missing calls, and current `Archipelago.py`, `Islands.py` and `Equal_Islands_V2_beta.py` each contain the same predicate form. The archived practical-5437 BTG_Cross birth patch already contains it, so the defect is classified as inherited third-party/Firaxis-style map-script logic rather than seven separate SAS regressions; upcoming source-commit audits that introduce/copy those files should treat it as this same KI lineage, not re-count it.
  Surgical direction for KI#249: audit all runtime map scripts for method objects used as Boolean predicates and change every live `pPlot.isFreshWater` condition to `pPlot.isFreshWater()`. Re-check analogous `isX`/`canX` method predicates rather than fixing only the five currently known files.
  KI#250 candidate (SAS-specific Grid option-index drift): the visible custom option 7 is `Start Distance` with values Normal/16 tiles, Far/20 tiles and Far Mixed/20 horizontal + 16 vertical. `getGridSize()` is the only code that implements those three physical spacing tables, but its first branch tests `getCustomMapOption(5) == 0`; option 5 is `Empty Land`, not Start Distance. With default Empty Land = Yes, *every* Start Distance selection is forced through the Normal/16-size table, so Far and Far Mixed do nothing. With Empty Land = No, Start Distance Far selects the 20-size table, while both Normal and Far Mixed fall into the mixed table, so Normal becomes incorrectly enlarged/mixed. Option 7 has no other runtime use that could restore its advertised behavior. The wrong option-5 test is already present in practical 5454 and survives current SAS; sibling `BTG_Cross.getGridSize()` provides a control by reading its Start Distance option directly before selecting its size table.
  Surgical direction for KI#250: select all three Grid size branches solely from custom option 7 (`0` normal, `1` far, `2` far-mixed); keep Empty Land option 5 limited to whether unused template regions are generated/filled and to the corresponding player-region bookkeeping.
  KI#251 candidate (SAS-specific Grid table-completeness defect): `assignStartingPlots()` directly indexes `minimums[iNumRegions]`, but the dictionary contains only 2..12 plus 15,16,18. Exact control-flow simulation against the script's own line/empty-land configurations shows reachable supported missing keys, notably one-player 1-line and 3-line layouts (plus one-player 2-line with default Empty Land), and 14 players / default 3 Lines Tall / default Empty Land, which reaches `iNumRegions == 14` after the inflated capacity guard passes. The resulting `KeyError` happens before Grid assigns custom starts; `callMapFunction` therefore reports the Python callback unsuccessful and C++ safely falls back to ordinary starting plots, so this is not a turn-0 NULL-start catastrophe. It is nevertheless a real Python error and silently discards the map's intended regional start placement for those supported setups. Sibling `BTG_Cross.py` already uses the robust pattern `if iNumRegions in minimums ... else [0.05, 0.08]`; Grid does not. The direct lookup survives current SAS.
  Surgical direction for KI#251: make the distance lookup total for every reachable custom region count, preferably with explicit tuned values where meaningful and a conservative fallback for unusual counts. Do not rely on an exception to trigger C++ placement.
  KI#252 candidate (SAS-specific high-player/mirroring state defect): with 19..48 actual players and `Empty Land = No`, `beforeGeneration()` deliberately sets `iPlayers` to the actual count and returns early because Grid's custom regional generator supports at most 18. `generatePlotTypes()` then safely uses generic `FractalWorld`, and `assignStartingPlots()` safely calls the default C++ implementation. However, if the user also selects `Mirrored Hubs = Yes - Mirrored` (option 1 == 3), `normalizeAddExtras()` later unconditionally calls `mirrorizeMap()`. That function immediately reads global `region_duplicated` and `other_regions`, but those globals are initialized only inside `GridMultilayeredFractal.generatePlotsByRegion`, which this >18 fallback path never ran. On a fresh module generation the names are undefined and can raise; in a persistent/reused map-script interpreter they can instead retain stale geometry from an earlier custom generation. The high-player generic fallback therefore is not self-contained under the fully mirrored option. Current SAS retains the unconditional normalization call and the generator-only initialization.
  Surgical direction for KI#252: reset per-generation Grid globals in `beforeGeneration` and track whether custom Grid regional generation actually completed; gate `mirrorizeMap()` on that explicit state. Generic-fractal/default-start fallback must never execute regional mirroring against uninitialized or stale coordinates.
  KI#253 candidate (SAS-specific Grid design/capacity defect): Grid's public contract says `Each player has its own hub` and the SAS adaptation description explicitly says `SAS48/high player counts supported`, but the static templates top out at 10 one-line hubs, 12 two-line hubs and only 15 hubs for the default 3-line layout. Current Huge has 16 default players and SAS24/32/40/48 have 24/32/40/48. Under the default `Empty Land = Yes` path, a default Huge or any SAS-size high-player game therefore generates at most the 15-hub 3-line template and then the script's safety guard hands starting placement to C++; generic placement can keep the game playable, but it cannot create the absent 16th through 48th hubs, so the map cannot satisfy its defining one-player-per-hub layout. This under-capacity exists from practical 5454: the birth script already advertises high-player support while shipping the same template ceilings. Later SAS world-size/grid refactors expand physical dimensions but do not add high-count hub templates.
  Surgical direction for KI#253: either generate region templates dynamically to at least the actual/default supported player count (preferred if the advertised SAS48 Grid identity is retained), or explicitly cap/disable the custom per-player-hub mode above its real region capacity and adjust the description. Merely calling default starting-plot placement does not satisfy a missing-hub geometry contract, just as with KI#242/KI#245.
  Two additional Grid code smells were deliberately *not* promoted. Both custom-start branches reset `iPass = 0` inside the retry loop, whereas sibling BTG_Cross initializes it outside; current AdvCiv's Python-compatibility `startingPlotWithinRange` always returns false and Grid's own `min_dist` does not change across retries, so the retry is logically unable to relax as written. However no independent ordinary valid-geometry configuration was source-proven to require a second pass without another already-confirmed Grid defect, so it remains an unnumbered robustness flaw rather than a manufactured KI. Likewise the optional Precious Metal normalizer can index `plotsboundaries[0]` without proving that the stochastic diagonal boundary list is nonempty (same pattern exists in BTG_Cross); no deterministic supported configuration forcing an empty list was established here, so it remains an audit note rather than a confirmed defect.
  Several broader candidates were positively rejected. The initial theory that all >18-player Grid games leave players unassigned is false: default high-count paths hit the explicit capacity/default-start fallback, and `Empty Land = No` uses generic FractalWorld plus C++ starts. The strange `region_duplicated` comparison effectively selects the widest source region, but within each live Grid template row heights are equal and no current wrong output was established from that predicate alone. The birth-version Arena/world-size dictionary issue is historical lineage later repaired by practical 5854/5855 and is not counted again.
  No additional surviving source defect was established in practical 5454 outside KI#247 -> KI#253. Certified cursor advances through practical 5454 / 093b59bae4. Next runtime-source SHA: practical 5453 / 866bab2de4.


SAS-C099 - practical 5453 Peirce import/adaptation audit - COMPLETE; INHERITED THIRD-PARTY KI#254 + KI#255 FIXED/VALIDATED
  Reviewed 1 runtime-source SHA: practical 5453 / 866bab2de4.
  Practical 5453 imports/adapts `Peirce.py` as a full executable quincuncial map generator and makes two much smaller runtime changes: `BTG_Cross.py` corrects the invalid `BONUS_GEMSTONESTONES` lookup to the real `BONUS_GEMSTONES`, and `Equal_Islands_V2_beta.py` only narrows its descriptive text by removing the stronger "equalized starts/resources" wording. Those smaller hunks are clean. Peirce was audited across its two custom options, world-size square-grid callback, wrap modes, plot/terrain/feature post-processing, latitude/frame/crease helpers, arbitrary-polygon region generation and custom starting-area filter. Path history after 5453 contains only the 5454 author-string correction, formatting/blank-line/print/BOM cleanup and the later SAS world-size-helper import rename; none changes either finding below, so both survive current SAS.
  KI#254 candidate (inherited third-party Peirce math defect): all four distance-squared tests in `isPeirceCrease()` use expressions of the form `(iX - centerX)^2+(iY - centerY)^2`. In Python `^` is bitwise XOR, not exponentiation, and operator precedence makes `dx^2+dy^2` parse as `(dx ^ (2 + dy)) ^ 2`, not even as the sum of two XOR terms. The function therefore does not compare Euclidean distance squared with `iRadius2`, despite calculating `iRadius2 = iRadius*iRadius` immediately above and documenting the crease as the area outside the relevant circular disc. The result materially changes map topology: on the current Standard Pangaea square (side 14), the toroidal XOR predicate marks 0 crease plots where the corresponding `dx*dx + dy*dy` predicate marks 56; no-wrap marks 96 instead of 124. On larger current layouts the defect can erase the crease entirely as well (for example SAS48 Pangaea side 35 marks 0 rather than 236 toroidal and 0 rather than 506 no-wrap). `generatePlotTypes`, `generateTerrainTypes` and `afterGeneration` all rely on this helper to force/clean the crease ocean, so this is a live geometry error rather than dead arithmetic. The four `^2` expressions are already present both in the independently downloaded original `Peirce.py` and in the 5453 SAS birth file, and remain byte-for-byte in current Peirce.
  Surgical direction for KI#254: express the squared distance explicitly as `dx * dx + dy * dy` (or `dx ** 2 + dy ** 2`) in all four quadrant tests, preferably via one tiny local/helper calculation so precedence cannot recur. Re-test all five map types under both toroidal and no-wrap layouts because crease classification is consumed by plot, terrain and post-generation passes.
  KI#255 candidate (inherited third-party Peirce E-W anchor copy/paste defect): in the E-W-continent branch, each of the four northern quadrant cases creates a landscape `generatePlotsParallelContinent` arm from `iRefX1/iRefY1`, then computes a *different* `iRefX2/iRefY2` for the portrait-oriented companion arm. The following portrait call nevertheless passes `iRefX1/iRefY1` again. The same four mistakes are duplicated in the second wrap-mode implementation, for eight live sites total (current lines around 897/907/917/927 and 1433/1443/1453/1463). The computed second anchors are overwritten later without being read anywhere else, making the copy/paste error explicit. `iPrim` omits only one of the four northern quadrant cases, so every E-W generation executes three of these wrongly anchored portrait pieces. On a comfortably large current no-wrap E-W square (side 37), for example, the top-right first anchor is `(16,19)` while the code explicitly computes the portrait anchor `(20,13)`; the live call instead generates that portrait polygon around the first anchor, shifting/overlapping the intended orthogonal piece by several plots. The public/local Peirce design describes E-W as a northern continent wrapping around the central pole, which is consistent with the distinct computed anchors rather than discarding them. The eight bad calls exist both in the independently downloaded original `Peirce.py` and in practical 5453; no later Peirce commit changes them.
  Surgical direction for KI#255: in each of the eight E-W companion-arm calls, pass the freshly computed `iRefX2, iRefY2` instead of `iRefX1, iRefY1`; then visually/regression-test E-W under both wrap modes and multiple `iPrim/iSec` rolls. Keep the first landscape calls on `iRefX1/iRefY1`; only the portrait companion calls are wrong.
  Current repair and validation: KI#254 now uses explicit delta multiplication in all four crease-distance tests. KI#255 now passes the computed second anchor to all eight portrait companion-arm calls while leaving the first landscape calls unchanged. The independently downloaded original at `C:\Users\PC\Downloads\Peirce_3\Peirce.py` confirmed that both defects predate SAS integration. User runtime smoke tests successfully generated E-W maps with both toroidal and no-wrap layouts; no visible issue was found at a glance.
  Two additional Peirce concerns were deliberately not promoted. The new SAS world-size scaling allows small Arena/Duel squares where legacy continent formulas yield negative or reversed nominal lengths/widths, but exhaustive call tracing across all five map types, both wrap modes and all primary/secondary quadrant rolls still produced multiple nonzero polygons intersecting the map without a deterministic exception; `MapAreaPolygon` derives a positive padded bounding box from arbitrary vertex order and clips whole-world writes, so awkward small-map geometry alone is not enough to call a correctness defect. `getCustomMapOptionDefault()` returning `-1` for both options is also unusual, but `NO_CUSTOM_MAPOPTION/-1` participates in Civ4's implicit random-custom-option convention in other stock-style scripts; without a source-proven setup failure it remains unnumbered rather than speculative. No out-of-bounds square-array, zero latitude denominator, invalid `TERRAIN_PEAK` lookup or separate custom-start failure was established.
  No additional surviving source defect was established in practical 5453 outside KI#254 and KI#255. Certified cursor advances through practical 5453 / 866bab2de4. Next runtime-source SHA: practical 5452 / 031a95cc7c.


SAS-C100 - practical 5452 Equal Islands import/map-cleanup audit - COMPLETE; KI#256 CANDIDATE FOUND
  Reviewed 1 runtime-source SHA: practical 5452 / 031a95cc7c.
  Practical 5452 imports/adapts `Equal_Islands_V2_beta.py`, adds it to the naval-heavy map classification/world picker, changes Earth2 and Lakes to Advanced-map-only visibility, performs quote normalization across runtime Python, and updates legacy map resource tags to current SAS names. The large Equal Islands script was audited across player/region templates (1..48), actual plot dimensions, island generation, custom starting-plot assignment, terrain/features/bonuses generation order, source-region selection, resource balancing, whole-region cloning, river cloning and cleanup outside used regions. The existing missing-parentheses `pPlot.isFreshWater` occurrence belongs to already-recorded broader inherited KI#249 and is not counted again.
  KI#256 candidate (SAS-specific imported Equal Islands cloning/fairness defect): the map's own purpose/description says all players get the exact same island, and `normalizeAddExtras()` copies the selected best source region's plot type, terrain, bonus, improvement, feature and rivers to every other player's corresponding region. Features are generated before bonuses (`CvMapGenerator::addGameElements`: `addFeatures()` then `addBonuses()`), so high-latitude target regions can legitimately contain `FEATURE_ICE` before the clone. The clone then calls `p.setBonusType(p0.getBonusType(-1))` but explicitly refuses to replace/remove Ice: `if p.getFeatureType() != ice: ... p.setFeatureType(...)`. For water-to-water coordinates, `setPlotType(PLOT_OCEAN, ...)` is a no-op when both source and target are already water, so pre-existing target Ice survives. `CvPlot::setBonusType()` simply assigns the requested bonus and updates counts/yields; it does not call `canHaveBonus()` or remove an invalid feature. Current Fish/Molluscs/Crab/Whale BonusInfos have Coast/Ocean terrain validity but no feature-valid entries, while `FEATURE_ICE` is Coast/Ocean-only, impassable and no-city. A normal ice-free source seafood coordinate can therefore be forcibly cloned onto a corresponding polar target water tile while that target's Ice is retained, yielding e.g. Fish under impassable Ice; current Work Boats have `bCanMoveImpassable=0`, so the copied resource can be inaccessible to that player. Even without a bonus, independent retained Ice barriers make the supposedly identical player islands/topology differ. The Ice-preservation block is present from the 5452 birth file and survives unchanged in current SAS; 5453 only removes the stronger phrase `equalized starts/resources` from the description but retains the explicit `exact same ... island` contract.
  Surgical direction for KI#256: make feature cloning authoritative for the used player regions instead of preserving each target region's independently generated Ice. The simplest equality-preserving repair is to copy the source feature exactly (including source Ice/no-Ice) after plot type/terrain have been cloned, with legality/order checks as needed. At minimum, never leave target Ice in place while forcibly copying an ice-incompatible source bonus; validate the final feature/bonus combination and preserve the map's equal-island premise. Re-test cold-climate/high-latitude configurations and water resources.
  Two initially strong Equal Islands candidates were positively rejected. The region-yield sorter does not overrun `sorting_regions`: after the real player regions are consumed, `region_numbers` is empty, so the inner loop executes zero times and the suspicious `sorting_regions[regionTestLoop]` expression is never evaluated for unused template regions. `region_numbers = regions_in_use` does alias and empty the global list, but later normalization reconstructs its work from `region_coords` and player starts and no current behavioral consequence was established. A second apparent zero/negative-width fractal failure came from confusing XML/world-size grid cells with actual map plot dimensions; recomputing every current world size against 1..48 players in real plot coordinates keeps generated region/island dimensions positive, so that candidate is discarded.
  Practical 5452 also contains two genuine but historical-only regressions. Its BTG_Cross resource cleanup mistypes the already-correct `BONUS_GEMSTONES` as nonexistent `BONUS_GEMSTONESTONES`; strict `getInfoTypeOrFail` makes that a real transient failure, but practical 5453 immediately restores `BONUS_GEMSTONES`. Fantasy Realm's six resource-list assignments are accidentally indented one level too deep in 5452; practical 5892 / 0e6854abcd (`repair legacy map script indentation`) explicitly repairs that parse/indentation break. Neither survives and neither receives a new KI. The remaining map-resource replacements were mechanically checked against current XML and use valid SAS tags (e.g. Maize, Gemstones, Grapes, Molluscs, Cattle, Elephants); quote-only Python changes are semantically neutral. Earth2/Lakes `isAdvancedMap` flips match the commit's explicit removal-from-Simple-Game intent.
  No additional surviving source defect was established in practical 5452 outside KI#256 (plus existing KI#249 lineage). Certified cursor advances through practical 5452 / 031a95cc7c. Next runtime-source SHA: practical 5451 / 1e8acff71e.


SAS-C101 - practical 5451 through 5450 Simple Game stale-option warning / map-helper callback-leak lineage audit - COMPLETE
  Reviewed 2 runtime-source SHAs: practical 5451 / 1e8acff71e and 5450 / 0dcc98613c.
  Practical 5450 deliberately reverts temporary BTG_Lagoon/Boreal guards that had returned harmless-looking fallback values for stale/foreign custom-map-option indices and records inherited Base AdvCiv KI#106 instead. This restores the ordinary dictionary KeyError rather than silently substituting an empty label, one option value, default 0 or false randomization for an option that the selected map does not actually own. The change does not create the stale Simple Game option index; the commit documentation independently reproduced that cache problem in minimally modified Base AdvCiv. Practical 5451 then adds one shared diagnostic wrapper: on the first missing option key it raises an explanatory KI#106 KeyError containing the requested and real option counts, while later stale accesses return from the helper and therefore reach the same original per-map dictionary KeyError. The shared boolean is intentionally process/module-global so the explanatory notice is emitted once rather than once per callback/map; valid indices never enter the warning helper. No malformed option-count argument, accidental valid-index rejection or surviving warning-state defect was established.
  A genuine historical SAS regression is present in practical 5451 but is already repaired and therefore is not counted as a new current KI. To make the shared warning helper available, 5451 newly adds `from SAS_WorldSizes import *` to fourteen stock/ported maps (including Archipelago, Big_and_Small, Continents, Custom_Continents, Fractal, Hemispheres, Islands, Medium_and_Small, Mixed_Continents, Pangaea, PerfectMongoose, Shuffle, Terra and not_too_Big_or_Small). At this point `PrivateMaps/SAS_WorldSizes.py` was itself a playable helper map and exported Civ4 map-callback names `getDescription()` and compact `getGridSize()`. Import-star therefore made those callbacks part of importing map scripts that did not override them, allowing unrelated scripts to inherit the helper's compact grid sizing. Practical 5897 / 731d4fb5e9 explicitly repairs this architecture by moving reusable helpers to `Assets/Python/SAS_WorldSizeUtils.py`, removing map callbacks from the helper module and leaving a separate `SAS_Simple_Flat_Grass.py` playable wrapper. That repair documents an empirical RandomScriptMap Huge change from the leaked 60x44 result back to 120x84 and states that Archipelago/other importers were likely affected. Current map imports use the callback-free helper, so the 5451 leak does not survive.
  The remaining 5451 runtime hunks are the same missing-key warning insertion in map option name/value/default/random/description dictionaries; a mechanical pass found no executable additions beyond those guards, the fourteen helper imports and the shared warning helper (Team_Battleground only also gains blank-line spacing). No additional surviving source defect was established in 5451/5450. Certified cursor advances through practical 5450 / 0dcc98613c. Next runtime-source SHA: practical 5449 / eaa9a887f9.


SAS-C102 - practical 5449 Planet Generator 0.68 import/adaptation audit - COMPLETE; KI#257-KI#261 FOUND
  Reviewed 1 runtime-source SHA: practical 5449 / eaa9a887f9. This commit imports and adapts the approximately 3,700-line `PrivateMaps/Planet_Generator_0_68.py`; the audit therefore treated the map as executable generator code rather than certifying the commit from its import title. Current descendants were traced through practicals 5816/5865/5867/5868/5897: those later Planet edits are formatting/Python-syntax/import-helper maintenance and do not semantically repair the findings below.
  Confirmed KI#257 (inherited Planet/C2C feature-spread alias): `addFeatures()` retains `plot = map.sPlot(x,y)` as the source seed, then obtains neighbors with another `map.sPlot(tx,ty)` and subsequently expects both Python objects to refer to different plots. Current `CyMap::sPlot` uses one process-static `CyPlot`, retargets it on every call, and is exposed with `reference_existing_object`; after the neighbor lookup both references therefore point to the neighbor. In the live Forest/Jungle spreading branch, `isRiverCrossing(tilePlot, plot)` effectively compares the neighbor with itself and returns no crossing, so intended river barriers are lost; source-terrain reads used when assimilating neighboring Desert/Tundra similarly read the retargeted neighbor terrain instead. Upstream C2C Planet contains the same retained-source `sPlot` pattern and the same static/reference-existing-object binding, so this is inherited rather than created by SAS. Preferred repair: use independent `map.plot(...)` objects (or otherwise snapshot all source state before any `sPlot` reuse) wherever source and neighbor must coexist. The inherited helper `isRiverCrossing()` must be repaired in the same change: Planet wraps X, but the helper derives direction from raw `x2-x1`; seam-adjacent x=0/x=W-1 plots therefore select the wrong E/W direction. That direction bug is real but currently masked in this caller by the stronger shared-`sPlot` alias, so it is recorded as the second required half of KI#257 rather than inflated into another KI.
  Confirmed KI#258 (SAS-specific Smart Selection unit regression): upstream Planet 0.68 computes `mapArea = worldInfo.getGridHeight() * worldInfo.getGridWidth() * 16`, converting WorldInfo terrain-cell area to plot-space before comparing against the following 40x24, 48x28, 52x32, 64x40, 84x52 and 128x80 plot-space buckets. SAS 5449 removes `* 16` while retaining those exact thresholds (and current source even documents them as plot-space). The default Smart Selection therefore systematically classifies a different size unit. Concrete current examples: Standard + separation <4 + 13 continents is classified Tiny and selects three rows although its plot-space bucket is Large and selects two; Huge + separation >=4 + 1-2 continents is misclassified Standard and loses the Huge rule's special center distribution. Preferred repair: restore the terrain-cell-to-plot-area conversion (or equivalently convert the thresholds consistently) and re-test the smart 2-row/3-row/center boundaries across all current SAS world sizes.
  Confirmed KI#259 (inherited Planet latitude callback / AdvCiv True Starts interaction): Planet derives `top = 30 - 2 * polarTemperature` and `bottom = -top`. Six supported climate-option combinations make `top < bottom` (examples include -14/+14, -38/+38, -62/+62 and -86/+86). `CvMap::reset()` clamps each result to +/-90 but does not reorder them and explicitly asserts `top >= bottom`. Ordinary `CvPlot::calculateLatitude()` damage is narrower than first suspected because it later uses absolute latitude, so the earlier broad climate-catastrophe theory was rejected. AdvCiv True Starts, however, explicitly treats the map as latitude-aware only when `getTopLatitude() > getBottomLatitude()`; on the reversed Planet combinations it therefore sets its map-has-latitudes state false and disables latitude-sensitive civilization start fitness, high-latitude water/climate handling and latitude restrictions in its start/resource evaluation. Upstream C2C Planet contains the same latitude formulas. Preferred repair: make the callbacks always return ordered latitude extremes while preserving the intended symmetric absolute bands, then validate both ordinary map latitude and True Starts behavior for every temperature/variation combination.
  Confirmed KI#260 (SAS-specific low grid-percent contract failure): 5449 replaces upstream Planet's explicit positive map-size override/world-size path with `SAS_PLANET_GENERATOR_GRID_PERCENT` and documents/clamps the define as valid in `[1,100]`. The selected WorldInfo terrain-cell dimensions are rounded by `int((dimension * percent + 50) / 100)`, so the lower documented range is not safe. Merely keeping both dimensions positive already requires Arena 5%, Duel 4%, Tiny 3%, Small 3%, Standard/Large/Huge/SAS24 2%, and SAS32+ 1%; e.g. Standard 1% yields `fW=fH=0`, after which the spacing-expansion path divides by `float(fW)`. Other low-but-positive combinations can create 4-plot-high maps and supported distribution seeds on a north/south edge, exposing inherited helpers such as `getTilesAround()` whose ascending deletion of invalid Y-neighbor indices is unsafe at the poles. These are consequences of the same too-small scaling contract rather than separate ordinary-default defects: an exhaustive current 40% check across every world size, 1-18 continents, separation 1-10 and all explicit distributions found no edge seed. Preferred repair: enforce a generator-safe minimum resulting grid (not merely nonzero arithmetic), validate dimensions after scaling/spacing expansion, and keep the default 40% behavior unchanged.
  Confirmed KI#261 (inherited Planet/C2C builder-pruning index defect): `TileBuilder._getPossibleGrowTiles()` scans `self.builders`, appends each exhausted builder's positional index to `rem_ind`, then ignores those saved indices. With `x = len(rem_ind)` it repeatedly executes `self.builders.pop(x)` while decrementing x, which removes positions `len(rem_ind)-1 ... 0`, not `rem_ind[...]`. Thus a mixed set such as productive builders at positions 0/1 and an exhausted builder at 2 records `[2]` but deterministically removes productive position 0 and leaves exhausted position 2. This helper is live in ordinary continent growth whenever the possible-tile cache is refreshed; the surrounding fallback searches can sometimes recover productive active tiles later, so the defect is not described as a guaranteed generation crash, but it corrupts the intended frontier-pruning state and biases/needlessly degrades continent growth. Upstream C2C Planet contains the identical loop. Preferred surgical repair: remove the recorded indices in descending order, e.g. pop `rem_ind[x]`, while keeping `builderList` bookkeeping consistent with any direct builder removal.
  Several additional Planet candidates were deliberately not promoted. `probabilityArray` initializes `_maxpower` to a `bArray`, but its malformed fallback is mathematically unreachable for the live positive-weight/getRand range; `ContinentPart.deepMaxTiles` has a mutable default but every live caller supplies its own used-set; the nonstandard fair-resource implementation contains obvious defects but is dead because current `addBonuses()` hardcodes `bonusGeneration = "standard"`; the internal `worldArray` wraps Y although the actual map does not, but normal continent/pole constraints yielded no independent north-south gameplay connection; and the apparently reversed `world._dimensions[0]` use is correct because `worldArray` deliberately stores reversed dimensions.
  Two real inherited robustness oddities are retained as audit notes rather than extra KIs. `getTilesAround()` removes invalid Y-neighbor entries with ascending `pop` indices, which can itself mis-remove or overrun at an actual pole, but normal 40% Planet geometry does not place generated land there; demonstrated reachability is through KI#260's unsupported-in-practice tiny results. `peakIntersect`/`hillIntersect` use `ax != ox and ay != oy`, which ignores more than the single `(ox,oy)` tile named by the comment, but upstream Planet has the same predicates and its v0.42 changelog explicitly says the hill-intersection change was intended to produce more hills close together; no harder supported-game failure was established, so this remains inherited generator behavior rather than a manufactured KI.
  The center-distribution grid-expansion branch can also return a Python float width because `required_width` is floating-point. Civ4's sequence-to-`vector<int>` bridge asserts that returned sequence items are `PyInt` before coercing with `PyInt_AsLong`; many explicit Around Center combinations at the current 40% scale exercise that branch. This violates the debug/type contract and release coercion truncates the sub-cell fraction, but no substantial release-game failure beyond that rounding was proven; it is kept as a repair/audit note rather than KI#262. Similarly, `getSelValue()` computes but fails to return its stored default for an out-of-range custom-option value, but valid current indices do not take that branch and stale/foreign option state belongs to the already-recorded Base AdvCiv KI#106 Simple Game lineage.
  The remaining live map callbacks close cleanly around these findings: rivers/lakes/bonuses/goodies and starting-plot assignment intentionally delegate to the default implementation; standard resource generation keeps the broken fair-resource branch unreachable; normalization delegates under that standard mode; `getTopLatitude()` is invoked before `getBottomLatitude()` by the DLL so the latter's lack of its own climate-init call is harmless; map-option persistence is name/value based and broad exception handling prevents a missing cfg from becoming a generation failure; WrapX=True/WrapY=False matches the intended topology. No sixth current Planet root met the evidence threshold.
  Certified cursor advances through practical 5449 / eaa9a887f9. Next runtime-source SHA: practical 5448 / 3dbee874c3.


SAS-C103 - practical 5448 through 5445 map curation / stock-map import audit - COMPLETE; KI#262-KI#266 FOUND
  Reviewed 4 runtime-source SHAs: practical 5448 / 3dbee874c3, 5447 / 30d39de13d, 5446 / c5cc188373 and 5445 / a1db61111f.
  Practical 5448 closes clean-current. Its Simple Game visibility / WorldPicker changes are deliberate map-pool presentation choices. The temporary Boreal/Lagoon stale-option guards are the older harmless-default masking lineage deliberately removed by practical 5450 so Base AdvCiv KI#106 surfaces instead of silently continuing with stale options; they are not a separate survivor. The shared almost-all-land grid profile is reduced to Arena 2x2 through Huge 15x11 terrain cells and those values survive current `SAS_WorldSizeUtils.py`; AdvCiv multiplies custom map-script grid cells by four into actual plots, so even Arena is 8x8 rather than a zero/single-row geometry. No consumer-specific sizing invariant failure was established. Practical 5447 and 5445 are likewise deliberate WorldPicker/Simple Game curation changes (Earth2/Big_and_Small/Hemispheres and Boreal respectively) with no gameplay-state defect.
  Practical 5446 cannot be treated as mere alphabetical-ordering XML: it copies seven complete stock/Firaxis-style scripts into `PrivateMaps` (`Archipelago`, `Balanced`, `Big_and_Small`, `Continents`, `Custom_Continents`, `Islands`, `Medium_and_Small`), making roughly 3,200 lines of executable Python part of the mod-local runtime surface. All seven birth copies were audited against their current descendants and, where needed, Base AdvCiv 1.14 / stock controls. `Balanced.py` and `Continents.py` close cleanly. The already-known missing-call `if pPlot.isFreshWater:` predicates in Archipelago/Islands belong to inherited KI#249 and are not counted again. Five independent current roots survive the broader audit.
  Confirmed KI#262 (broader inherited tiny-island coordinate copy/paste defect): current `Big_and_Small.py`, `Medium_and_Small.py`, `Custom_Continents.py`, `Mixed_Continents.py` and `not_too_Big_or_Small.py` each independently roll `tinyWestLon` and `tinySouthLat`, but then compute `tinySouthY = int(self.iH * tinyWestLon)` instead of using the latitude roll. `tinySouthLat` is therefore dead, and every one-to-four supposedly random tiny-island patch origins is forced onto an X/Y-correlated diagonal band rather than receiving independent longitude/latitude placement. This is live map topology, not an unused cosmetic value: the multilayered-fractal pipeline lays these tiny-island patches first and later water in overlapping region passes does not erase already-created land. The exact mistake is inherited from stock Firaxis Big/Medium/Custom Continents code; Base AdvCiv 1.14 also carries it in its `Mixed_Continents` / `not_too_Big_or_Small` descendants. Current `RandomScriptMap.py` provides a clean control by rolling the same latitude and correctly using `tinySouthY = int(self.iH * tinySouthLat)`. Preferred repair: change all five live copies to use the latitude variable and preserve their existing RNG calls/order so only the intended coordinate dependence changes.
  Confirmed KI#263 (inherited `Medium_and_Small` eastern-continent grain copy/paste defect): option 0 defaults to Random. In that path the script makes two explicit independent rolls, `iContinentsGrainWest = 1 + dice.get(4, ...)` and `iContinentsGrainEast = 1 + dice.get(4, ...)`. The western continent correctly consumes the western roll, but the eastern continent's `generatePlotsInRegion` call also passes `iContinentsGrainWest`; `iContinentsGrainEast` is never read afterward. Thus the advertised/randomized two halves are forced to share one continent grain, discarding the east roll; with independent 1..4 rolls they would differ 75% of the time. The stock BtS source has the same copy/paste line and current SAS retains it. Current `RandomScriptMap` again supplies a useful control: its Medium-and-Small implementation rolls west/east grains independently and passes `iContinentsGrainEast` to the eastern region. Preferred repair: pass `iContinentsGrainEast` in the eastern continent call without changing the surrounding shift/island logic.
  Confirmed KI#264 (SAS-specific 48-civ compatibility regression exposed by practical 5743, latent inherited table assumption): `Custom_Continents.beforeGeneration()` defaults custom option 0 to Random and reads the actual `countCivPlayersEverAlive()`. Its `byPlayerIndex` weighting table, inherited from stock, contains keys only 2..18 and is indexed directly. The original 18-civ engine kept ordinary high-player setups within that range; practical 5743 changed SAS `MAX_CIV_PLAYERS` from 18 to 48 without extending this table. A normal 19-48-player Custom Continents game using the default Random continent count therefore raises `KeyError` before `iNumConts` / `cont_data` are initialized. On a clean generation the subsequent Python `generatePlotTypes()` also fails because those globals were not established; AdvCiv's `CvPythonCaller::generatePlotTypes` reports no successful override and `CvMapGenerator::generatePlotTypes` explicitly fills every plot with `PLOT_LAND`. In a reused Python process stale globals are also possible, so silent reuse is no safer contract. This corrects the earlier C032 certification of 5743: the DLL-capacity change itself is valid, but it exposed an inherited fixed-18 map-script assumption that the earlier pass did not trace. Preferred repair: define deterministic Random-continent weighting for every supported current player count (or a documented generalized/clamped rule) and guard generation so an unsupported count can never fall into the all-land error fallback.
  Confirmed KI#265 (SAS-specific 48-slot sparse/high-ID compatibility regression exposed by practical 5743, broader inherited fixed-ID assumption): several current inherited maps correctly keep their custom topology capped at <=18 actual players/<=6 teams, but then incorrectly assume those alive entities must occupy IDs 0..17. Archipelago and Islands build `player_list` only with `for plrCheckLoop in range(18)` and then shuffle `countCivPlayersEverAlive()` entries. Tectonics has the same 0..17 player enumeration inside its <=18-player custom start path. A legitimate sparse Custom Game with e.g. two alive civs in slots 0 and 18 passes the topology-count gate but produces only one enumerated player; the next shuffle indexes an empty list. Archipelago/Islands then fail `assignStartingPlots` before custom assignment and AdvCiv falls back to the generic C++ start path; Tectonics' per-player `findStartingPlot` callback likewise fails before its all-player custom assignment and `CvPlayer::findStartingPlot` falls back to the generic search. Custom Continents One-Per-Team similarly builds `team_num` only for team IDs 0..17; a live team ID 18..47 can index beyond that list in `findStartingPlot`, losing the intended team-continent constraint for that player. Practical 5743 made IDs 18..47 legitimate by raising `MAX_CIV_PLAYERS` to 48 (and the corresponding civil-team range), so this is a SAS compatibility regression even though the literal loops are inherited. Preferred repair: enumerate the full current max player/team ID ranges or construct actual alive-ID lists, while retaining the scripts' intentional <=18-region / <=6-team topology limits rather than pretending their static templates support 48 custom regions.
  Confirmed KI#266 (inherited `Custom_Continents` six-continent template shift-axis typo): supported template `(6,3)` has paired outer/inner layers whose variance fields are structurally mirrored. Continent 0's outer and inner layers both use horizontal `+0.63`; continent 5's pair both use horizontal `-0.63`; continent 4's outer layer also uses horizontal `-0.63`, but its corresponding inner layer uniquely stores `[... horzVar=0, vertVar=-0.63 ...]`. With no binary shifts the inner layer starts nested inside its outer shell. Once shifts are enabled, the outer shell moves horizontally while the inner layer does not, and/or the inner layer is shifted through the south edge instead; three of the four `(xShiftRoll,yShiftRoll)` states therefore break the intended nested/mirrored pairing. The exact bad field placement is present in stock Warlords/Firaxis Custom Continents and survives current SAS (and even later third-party "improved" descendants), so it is inherited rather than SAS-created. The surrounding template symmetry makes the intended entry unusually low-ambiguity: the inner layer should use horizontal `-0.63`, vertical `0`, matching its outer layer and the other mirrored pair. Preferred repair: correct that one axis field and validate all four binary-shift states for explicit six-continent variant 3.
  Several tempting 5446 candidates were deliberately not promoted. Islands resets `iPass = 0` inside its retry loop, making the fallback machinery objectively incapable of relaxing its first-pass constraints (and current AdvCiv's compatibility `startingPlotWithinRange` no longer adds an independent relaxation), but no ordinary supported configuration was proven to require that retry; it remains a robustness note like the analogous Grid branch. Custom Continents One-Per-Team reconstructs vertical but not horizontal template shift in its start-region constraint; balanced template geometry still leaves a large overlapping valid region and no hard start failure was established. Two `(2,4)` Custom Continents layers can shift slightly beyond the eastern edge, but intended wrap behavior/fix direction is less clear than KI#266 and no independent hard failure was proven. These remain audit notes rather than manufactured KIs.
  A final current-map sparse-ID scan found no other `range(18)` player/team enumerator beyond Archipelago, Islands, Tectonics and Custom Continents. One separate `range(iPlayers)`/`getPlayer(assignLoop)` shape exists in `BTG_Cross.py`, whose own practical source commit is still ahead of this backward cursor; it is intentionally deferred to that file's proper audit rather than half-reviewed here.
  Local reconciliation against current source confirmed all five C103 roots: the five longitude-for-Y expressions and clean RandomScriptMap latitude control, Medium and Small's discarded east-grain roll and clean control, Custom Continents' 2..18-only weighting table, all four fixed-ID loops, and the asymmetric `(6,3)` inner-layer shift entry. Certified cursor advances through practical 5445 / a1db61111f. Next runtime-source SHA: practical 5444 / c02570f328.


SAS-C104 - practical 5444 through 5440 Deer-option / map-state / fail-fast / Military Advisor tranche - COMPLETE; no new defect
  Reviewed 5 runtime-source SHAs: practical 5444 / c02570f328, 5443 / b90811a0d3, 5442 / be75be8cf7, 5441 / accf8702eb and 5440 / 955b2298bd.
  Practical 5444 closes clean-current. Arboria and Boreal each gain a visible Deer Percentage option with exact 0/10/25/50/75/100 values while their pre-existing wrap choice moves consistently to custom-option index 1 and remains the one hidden trailing option. `dice.get(100) < iDeerPercent` is exact for Civ4's 0..99 map RNG: 0 never passes, 100 always passes and intermediate values have the advertised percentage. The current descendants retain this option plumbing; later 5448/5450/5451 stale-option changes belong to the already-recorded Base AdvCiv KI#106 Simple Game lineage rather than a 5444 regression.
  Practical 5443's Arboria optimization is behavior-preserving. The script caches the two terrain IDs and forest feature ID instead of resolving them repeatedly, and drops an unused latitude local. Its one superficially risky change extends the Arboria override `generateTerrainAtPlot` with cached terrain arguments, but Arboria also overrides the sole live `generateTerrain()` caller and passes those arguments itself; no external/base call site invokes the subclass override with the base two-argument shape. Current descendants later reorganize the same calculations without indicating a semantic repair.
  Practical 5442's broad `getInfoTypeOrFail` conversion was audited as executable code rather than accepted from the title. All 43 distinct live XML tags newly hardened by this commit resolve in current XML, each converted map imports the helper, and the apparent strict lookup of `NO_IMPROVEMENT` is only inside an already-commented debug line; live code continues to use `ImprovementTypes.NO_IMPROVEMENT`. The non-mechanical part is `BTG_Lagoon.py`: removing the permanently-false `isBTPon` gates makes its existing Center Resources routine execute when custom option 3 requests Extra Resources (the default), replaces unsupported BTG-only resources with live SAS resources, removes spectator/BTP-only branches and keeps the non-BTP team-start path. This was traced into the helper and DLL rather than assumed safe. An initial concern that the Whale call would leave Coast terrain on a land plot is false: `CvPlot::setTerrainType` automatically changes `PlotType` when the terrain water flag disagrees, so setting Coast converts the selected tile to water before Whale is assigned. The helper can also force ordinary land resources onto otherwise nonstandard plot combinations, but this is an explicit hand-sculpting routine; bonus-linked improvements can make themselves valid and no broken gameplay/state contract was established. Those candidates are rejected rather than promoted.
  Practical 5441 likewise closes clean-current after tracing the KI#105 state refactor through the DLL map-generation order. `SASUtils.py` becomes the shared fail-fast lookup home and all Sevopedia users that lose the local helper receive direct imports. Arboria/Boreal remove the temporary `_ensureFoodFractal`/dimension bookkeeping and initialize the persistent `food` fractal in `beforeGeneration()`. `CvMapGenerator::generateRandomMap()` unconditionally calls the map script's `beforeGeneration()` before plot/terrain generation, while `addGameElements()` invokes rivers, lakes, features and only then bonuses, so the fractal exists before every normal custom `addBonusType()` callback. The terrain/feature refactor recomputes thresholds and XML IDs locally from the same persistent fractals; no changed threshold, RNG call, grid dimension or bonus-placement predicate was found. Historical bad BTG Cross tags in the same surrounding lookup-hardening lineage were later repaired by 5452/5453 and are already recorded as repaired history, not current survivors.
  Practical 5440 is a clean Military Advisor geometry correction. At the historical hardcoded 1920x1080 layout, the previous minimap height cap inherited `H_TEXT = 797` px and could overlap the bottom Great General bar. The new cap is derived from that bar's actual Y position: `H_MAP_MAX = Y_GREAT_GENERAL_BAR - 10 - Y_MAP = 757` px, leaving the intended 10 px separation. If the aspect-ratio-derived minimap is clipped, its width is recomputed from the map aspect ratio and the Great General bar width is then synchronized to that final minimap width. No negative geometry or mismatched minimap bounds occurs in the historical supported layout; later resolution-aware rewrites supersede the hardcoded shell without evidencing a repair to this 5440 calculation.
  The tranche therefore contributes no new KI. Several plausible candidates were explicitly killed only after engine-level checking, especially the Lagoon Coast/Whale concern; preserving those rejections matters so a later pass does not rediscover and miscount them. Certified cursor advances through practical 5440 / 955b2298bd. Next runtime-source SHA: practical 5439 / c7704e4825.


SAS-C105 - practical 5439 Simple Game terminology / Shuffle import / map-pool curation - COMPLETE; no new defect
  Reviewed 1 runtime-source SHA: practical 5439 / c7704e4825.
  The C++ changes in `CvGameTextMgr.cpp` and `CvInfo_GameOption.cpp` only rename comments from Play Now to Simple Game; no branch, value or UI-state logic changes. The same terminology migration moves the `TXT_KEY_MAIN_MENU_PLAY_NOW` override into the SAS text file and changes its English value to `Simple Game`. Omitted non-English fields use Civ4's ordinary fallback behavior; no runtime-key mismatch was established. BTG Cross/Lagoon edits are comment-only, while Tectonics deliberately flips `isAdvancedMap()` from 0 to 1 so it no longer appears in Simple Game.
  `PrivateMaps/Shuffle.py` is a genuine 139-line executable import and was therefore audited as a map generator rather than dismissed as menu curation. Its generator body matches the stock Firaxis/Warlords Shuffle script: two custom options, both deliberately hidden; default cylindrical wrap and standard resources; consistent wrap accessors; standard BonusBalancer interception/normalization; the same 1..8 grain roll remapping; and ordinary FractalWorld/TerrainGenerator/FeatureGenerator delegation. SAS's intentional semantic difference is `isAdvancedMap() = 1`, keeping Shuffle in advanced/custom lists rather than Simple Game. Because the map is itself advanced-only, its hidden option dictionaries do not create a new Simple Game stale-option path; the later KI#106 guard lineage remains separate. Current descendants add shared world-size/warning infrastructure without a semantic repair to this 5439 generator.
  No new KI is promoted from 5439. Certified cursor advances through practical 5439 / c7704e4825. Next runtime-source SHA: practical 5438 / 551ef0b93a, the much larger BTG Lagoon birth/import, which remains unopened at this checkpoint.

SAS-C106 - practical 5438 BTG Lagoon birth/import - COMPLETE; four current defects confirmed
  Reviewed 1 runtime-source SHA: practical 5438 / 551ef0b93a. This imports the modified `BTG_Lagoon.py` from Beyond the Game 2.43 into SAS. The full executable map contract was audited against the current descendant, the original BTG PublicMaps archive supplied by the user, AdvCiv/Civ4 map-generator/DLL callback behavior and every later Lagoon history hunk through the current tree. Four independent current roots survive; several initially alarming resource/null/team candidates were explicitly rejected after engine-level tracing.
  KI#267 is an inherited BTG Lagoon compact-player-ID/custom-start mapping bug. `beforeGeneration()` creates `player_list = [0, 1, ..., countCivPlayersEverAlive()-1]`, shuffles those compact template IDs, and stores a list whose length equals the alive-player count. `findStartingPlot(playerID)` later indexes that list with the real player ID through `shuffledPlayers[playerID]`. Sparse but legitimate active slots therefore break even under the original 18-slot engine: e.g. actual alive player IDs 0 and 2 create a two-element assignment list, then player 2 indexes beyond it. AdvCiv's Python-callback wrapper treats the failed override as unsuccessful and `CvPlayer::findStartingPlot()` falls back to the generic C++ finder, so the game need not crash but Lagoon silently loses its handcrafted start-region contract for the affected player and can mix custom and generic starts. The original BTG 2.43 Lagoon contains the same compact-list/real-ID mismatch verbatim, and no later SAS Lagoon commit repairs it. Surgical direction: enumerate the actual ever-alive player IDs and store/look up template assignments by those IDs rather than by compact count position.
  KI#268 is an inherited BTG Lagoon Top-v-Bottom unequal-team unbounded-retry bug. Start Position defaults to Top v Bottom. `BTPTopBottomTwoTeams(False)` correctly returns unless there are exactly two civ teams, at least four players and an even total player count, but it never requires the two teams to have equal sizes. It randomly selects one team as `teamOne`, then for every `teamOne` player currently in the top half repeatedly reshuffles all players until it finds a different, opposite-team player whose start is in the bottom half. In a legitimate 4-player 3-vs-1 game, Lagoon's four-player start template has two top and two bottom starts; if the three-player team is selected as `teamOne`, at least one of its players must remain top while at most one opposing player exists. Once that lone opponent is top (or has already been swapped top by an earlier repair), no opposite-team bottom candidate exists and the `while` condition can never become false. The same impossibility generalizes to unequal even-sized two-team games when the larger team is selected. There is no attempt bound or eligibility precheck, so map normalization can hang indefinitely. Original BTG 2.43 contains the same helper. Surgical direction: require equal team sizes for this normalization or construct finite eligible top/bottom swap lists and fall back safely when a complete separation is impossible.
  KI#269 is a SAS-specific Lagoon world-size adaptation regression left behind when Arena was inserted before Duel. The inherited generator comment/formula assumes the original BTS ordering in which Large had raw world-size value 4: `iShiftBias = 4; iShift = iShiftBias - int(CyMap().getWorldSize())`. Arena already existed at SAS practical 5311 before Lagoon was imported at 5438, so current runtime indices are Arena=0, Duel=1, Tiny=2, Small=3, Standard=4, Large=5, Huge=6 and the later SAS sizes continue upward. Yet `iWidthSize`/`iSmallWidthSize`, which control Lagoon's solid/partial outer land bands and bridge geometry, still use the old raw index. Compared with the original BTG tuning, Tiny through Huge therefore use the next-larger legacy bucket (e.g. Large computes 8/6 rather than the original Large=4 tuning of 7/5 at default Size Factor), with SAS24+ drifting farther beyond the old range. Practical 5854/5855 repaired Lagoon's separate direct `getGridSize()` key/Arena problem but never touched this internal arithmetic; it survives current source. Surgical direction: derive the tuning from aligned `WorldSizeTypes`/SAS world-size helpers or an explicit per-size table rather than subtracting the raw enum index.
  KI#270 is an inherited BTG Lagoon wrapped-plot shift/desynchronized-coordinate bug. Lagoon uniquely calls `FractalWorld.shiftPlotTypes()` after hand-building its central disk, outer land bands and bridges when the user selects Cylindrical or Toroidal wrapping. `shiftPlotTypes()` intentionally chooses a best seam and can relocate the entire `plotTypes` array by a nonzero X/Y offset. Lagoon never applies that offset to its later coordinate-dependent logic: `findStartingPlot()` still searches fixed fractional template regions such as 10%/50%/90% X and 25%/75% Y; `addRivers()` still sculpts the six-tile central peak/desert patch around the unshifted map center; and the default Extra Resources option still calls `BTPresourceFromCenter()` around that same old center. Thus a supported nonzero wrapped shift moves the handcrafted Lagoon geography while leaving starts and center follow-up features/resources anchored to pre-shift coordinates. The original BTG 2.43 file has the same `shiftPlotTypes()` call and fixed-coordinate follow-up logic; sibling hand-designed BTG Cross/Grid do not rely on this shift. Surgical direction: either disable plot shifting for Lagoon's wrapped modes or persist/apply the chosen shift to every downstream handcrafted coordinate/template.
  One separate Lagoon birth regression is historical-only and therefore not assigned another current KI: the imported `getGridSize()` dictionary did not account for SAS's already-existing Arena/runtime-index shift. Practical 5854/5855 explicitly repaired the direct world-size lookup and later refactors moved it to aligned SAS helpers. KI#269 is distinct because the internal `iShiftBias` shape arithmetic was missed by that repair and remains live.
  Fixed/validated with GPT-5.6-Sol after ChatGPT-5.6-Sol's archaeology: Lagoon now maps actual ever-alive player IDs directly to shuffled template IDs for its portion of KI#267; KI#268 replaces the unbounded retry with equal-team validation and finite misplaced-player pairing; KI#269 replaces raw enum subtraction with an explicit aligned Arena-through-SAS48 width table; and KI#270 keeps Lagoon's handcrafted wrapped plot array fixed so all downstream coordinate logic remains aligned. Static parsing, the focused Ruff error gate, exact Duel-through-Huge width comparison and finite unequal/equal-team logic cases pass. A Huge Lagoon game generated and completed an 11-turn autoplay with an empty refreshed `PythonErr.log`; the sparse-ID, unequal-team and wrapped-alignment paths were validated from focused source/logic checks rather than separate targeted runtime reproductions. Later checkpoints broaden KI#267 to Team Battleground, Ring, Hub and Inland Sea; the Team Battleground portion is also fixed/validated, while Ring/Hub/Inland Sea remain deferred.
  Several plausible candidates were rejected rather than promoted. The apparent Barbarian-team leak in `BTPTopBottomTwoTeams` is false because `BARBARIAN_TEAM == MAX_CIV_TEAMS` and the Python loop iterates only `range(MAX_CIV_TEAMS)`, excluding that sentinel ID. `BTPForceEnrichFood()` lacks its sibling's explicit null-plot guard, but every live Lagoon call uses `bMainLandOnly=True`; AdvCiv's null CyPlot returns neutral values and area -1, so the biggest-land-area predicate rejects an out-of-bounds candidate before mutation. The 5442 Center Resources Whale concern remains rejected after deeper checking: setting Coast on a land plot converts its plot type to water, and AdvCiv has an explicit resource-bearing-lake exception allowing an adjacent city to train a Work Boat. Forced nonstandard resource/terrain combinations are part of BTG's explicit hand-sculpting routine and no independent broken gameplay contract was established. Finally, the inherited option label `Balanced - Including Marble` is stale under AdvCiv 1.14 because BonusBalancer explicitly no longer eliminates Marble; option 2 still differs by adding extra food balancing, so this is retained only as a wording/audit note rather than a generator defect.
  A final mechanical pass over custom-option indices, world-size reads, list/dictionary indexing, method-call predicates, terrain/features/rivers and all resource helpers found no fifth independent current root. Later Lagoon history was searched specifically for `shuffledPlayers`, `BTPTopBottomTwoTeams`, `iShiftBias`/`getWorldSize` and `shiftPlotTypes`; all four defect mechanisms survive the current descendant. Certified cursor advances through practical 5438 / 551ef0b93a. Next runtime-source SHA: practical 5437 / 3f17c40b84, the BTG Cross birth/import, intentionally left unopened at this checkpoint.



SAS-C107 - practical 5437 BTG Cross birth/import - COMPLETE; KI#267 broadened and KI#271-KI#274 FOUND
  Reviewed 1 runtime-source SHA: practical 5437 / 3f17c40b84. This imports and substantially adapts `BTG_Cross.py` from Beyond the Game 2.43: BTG-only options are removed, the option surface is reshaped, grid sizing is replaced, player limits are lifted to `gc.getMAX_CIV_PLAYERS()`, and region assignment is changed to reuse the fixed 4/5/7/9 Cross templates through modulo indexing for high-player/SAS48 support. The full birth file, current descendant, every later Cross history hunk, current AdvCiv DLL/map-generator contracts and the user's original BTG 2.43 PublicMaps archive were compared. That original archive was particularly useful for separating dormant/inherited BTG behavior from defects created when SAS exposed or scaled it.
  KI#267 is broadened from Lagoon to an inherited BTG Lagoon/Cross compact-player-ID/custom-start mapping root. Cross actually begins correctly by enumerating the real ever-alive IDs into `player_list` and `shuffledPlayers`, explicitly commenting that open slots must be handled. But its Numerical Order and the two team-positioning branches discard those real IDs and use `playerID = assignLoop`. A legitimate sparse setup with alive IDs 0 and 2 therefore assigns a start to compact/dead player slot 1 and can leave real player 2 without any start. This is more severe than Lagoon's per-player lookup failure because Cross overrides the global `assignStartingPlots()` callback: current `CvGame::assignStartingPlots()` returns immediately when that Python callback reports success, and `CvPlayer::setStartingPlot()` contains no alive-player or cross-player uniqueness guard. Thus C++ does not subsequently fill the omitted real player. Original BTG Cross contains the same compact-ID use in its selectable Numerical/Together branches, while the correctly built real-ID shuffle proves the intended open-slot-aware contract. Surgical direction for the shared KI#267 root: carry actual ever-alive player IDs through every assignment mode; never substitute compact loop positions for `PlayerTypes`, and do not return successful global Python assignment until every actual participating civ has a valid start.
  Cross's KI#267 portion is fixed/validated with GPT-5.6-Sol after ChatGPT-5.6-Sol's archaeology: preserve Cross's ordered actual-ID list before shuffling and use those IDs in both duplicated Numerical/team-positioning assignment blocks. Full-shuffle behavior remains unchanged. Static parsing and the focused Ruff error gate pass. A Huge Cross game generated and completed an 11-turn autoplay with all 16 players alive and an empty refreshed `PythonErr.log`; the sparse-slot branch itself was validated from focused source/logic checks rather than a dedicated sparse-slot runtime setup. The Team Battleground portion is also fixed/validated, while later Ring/Hub/Inland Sea portions remain deferred.
  KI#271 is a SAS-specific BTG Cross high-player modulo-region/positional-mirroring duplicate-start regression. Practical 5437 deliberately replaces original direct region indices with `assignLoop % iNumRegions` (or modulo `best_regions`) so player counts can exceed the fixed physical 4/5/7/9 hub templates. In `Mirrored Hubs = Shape and position` and `Mirrored`, however, Cross finds only the first player's relative `(dX, dY)` inside its hub and then assigns every later player the same relative point in that player's selected region. Once modulo assignment cycles back to a region already used, the same region plus the same `(dX,dY)` is the exact same plot. For example, with the default five-region template, six FFA players and positional mirroring, assignments 0 and 5 both select region 0; ordinary current region widths give the first start a nonzero 20%-inset `dX`, so the sixth player is deterministically placed on the first player's plot. `CvPlayer::setStartingPlot()` does not reject a plot already used by another player. Original BTG did not contain the modulo high-player reuse; this is part of the SAS48 adaptation. Even outside positional mirroring, modulo reuse contradicts the map's new SAS description that each player receives its own small hub with an extra central prize hub. Surgical direction: do not reuse a positional-mirrored hub as though it were a distinct start slot; either provide enough distinct regions, independently search an additional valid start within a reused region, or safely fall back before assigning when physical hub capacity is exceeded.
  KI#272 is a SAS-specific activation/exposure regression in the Cross Positioning option. Original BTG 2.43 already contains label index 3, `Outside positions, crossed`, and a dormant `else` branch, but `getNumCustomMapOptionValues()` reports only 3 values for Positioning, so only indices 0..2 are selectable. Practical 5437 changes that count to 4 and exposes Crossed in SAS without implementing distinct behavior. In the live two-team positioning branch, Together and Crossed both execute exactly `playerID = assignLoop` and `reg = assignLoop % iNumRegions`; `iTeam` is computed immediately beforehand but never used. The two now-selectable choices are therefore behaviorally identical. Surgical direction: either implement a genuinely crossed team/region mapping using actual player/team IDs, or restore the three-value option surface until such behavior exists.
  KI#273 is a SAS-specific Cross source-mirroring dimension regression caused by replacing BTG's template-aligned grid contract. Original BTG derives grid dimensions as exact multiples of 3 for the 4/5/9-region thirds templates and multiples of 4 for the seven-region quarter template, so every source/target hub rectangle has matching dimensions. Practical 5437 replaces that with new world/high-player sizing while retaining the literal fractional templates and copy loops. The script chooses one widest region as `region_duplicated`, then for every target iterates the target's full `iWidth/iHeight` and reads `sourceWest + x, sourceSouth + y`; `mirrorizeMap()` later repeats the same source-relative scheme for plot types, terrain, rivers, features, bonuses and improvements. When rounding produces e.g. a 10x9 source and a 9x10 target, the target's tenth row reads a row outside the selected source rectangle and imports unrelated neighboring map data rather than a mirror. This is already reachable in the 5437 birth sizing for selectable distance modes, and current tuning broadens it: exhaustive current Arena-through-SAS48 checks across all four Start Distance tables and 4/5/7/9 layouts found 45 affected `(distance, world-size, region-layout)` combinations spanning 15 distance/world-size pairs; the quarter-based seven-region layout remains aligned. Current Normal Arena, for example, is 28x28 plots and its default five-region template contains a 10x9 source/9x10 target mismatch. Surgical direction: preserve equal mirror-source/target dimensions by construction, or explicitly normalize/crop/transform coordinates within each rectangle instead of assuming identical rounded sizes; assert this invariant before copying.
  KI#274 is an inherited BTG Grid/Cross source-region double-generation/ranking/bookkeeping defect discovered while using the original BTG archive as Cross provenance. Both original BTG scripts first choose a `region_duplicated` source hub, append it to `regions_in_use`, and generate its outer/core fractals. With `Mirrored Hubs = No`, they then iterate the complete physical region set through `remaining_regionsTwo`, which still includes that same source, append it again and independently generate it a second time. `MultilayeredFractal.generatePlotsInRegion()` deliberately ignores ocean when applying a regional pass, so the second source pass cannot restore first-pass land to water; it layers an additional independent land/core fractal onto the source while every other hub receives only one such pass. Under BTG's equal-sized regions the selected source is normally region 0, not the central prize hub, so this is not the intentional central-hub advantage. Mirrored modes avoid the second independent fractal but still append the complete region set after the already-recorded source, leaving `regions_in_use` with N+1 entries for N physical hubs. `assignStartingPlots()` then aliases `region_numbers = regions_in_use`, calculates the source yield twice and ranks only `iNumRegions` entries; the source can therefore occupy two ranking slots while a real hub is omitted, and the global list itself is mutated down while sorting. The same inherited machinery survives current Grid and Cross. This is distinct from SAS-specific KI#247: KI#247 is the later hard Grid capacity/start-loss regression created when a SAS safety guard trusted the inflated raw list length; KI#274 records the older gameplay/map-generation and ranking defect that made that list non-unique in the first place. Surgical direction: generate each open-mode hub exactly once; in mirrored mode record the source only once and keep target IDs distinct; rank a copied list of unique physical region IDs rather than an aliased duplicate-bearing global.
  Several adjacent candidates were explicitly rejected or kept below KI threshold. Cross's inherited missing `isFreshWater()` calls are already part of broad KI#249 and are not recounted. `AI_updateFoundValues(True)` intentionally invalidates cached values; `CvPlot::getFoundValue(playerID)` lazily recomputes them, so the scorer does not remain at -1. Cross's retry counter is initialized outside the retry loop in current SAS, unlike the independently broken Grid version, and can reach its fallback. All 12 current option indices were mechanically checked and are in range. `mirrorizeMap()` is called only when its mirrored-generation globals are initialized. Resource-tail list indexing can be fragile if a malformed/no-start state has already occurred, but no deterministic independent supported configuration was proven beyond the recorded start roots. AdvCiv's compatibility `startingPlotWithinRange()` now always returns false, and Cross's own `min_dist` check uses raw non-wrap-aware axis differences; a theoretical seam-distance discrepancy remains on some toroidal 4/9-region layouts, but no independent hard start collision/launch failure was established, so it is retained only as a robustness note rather than KI#275.
  A final current/history grep for modulo assignments, Positioning option exposure, source-relative copy loops, `region_duplicated`, `regions_in_use`, list indexing, retry loops, resource forcing and map callbacks found no fifth independent current Cross root. The four new mechanisms survive the current descendant. Certified cursor advances through practical 5437 / 3f17c40b84. Next runtime-source SHA: practical 5436 / 393a82a296 (`RandomScriptMap.py` fix), intentionally left unopened at this checkpoint.


SAS-C108 - practical 5436 RandomScriptMap import/adaptation audit - COMPLETE; KI#275-KI#277 FOUND
  Reviewed 1 runtime-source SHA: practical 5436 / 393a82a296. Despite the short commit title (`Fix RandomScriptMap.py`), this creates a roughly 2,200-line runtime `PrivateMaps/RandomScriptMap.py`. The birth file was reconstructed from the commit mirror and compared line-by-line with the current descendant, Refar's archived Random Script Chooser 1.25, the BTG-distributed original map, stock BTS map-script controls, every later RandomScriptMap history hunk, current `CvMapGeneratorUtil.py`, world-size helpers/XML and the relevant AdvCiv Python/DLL callback contracts. The executable body is overwhelmingly inherited Refar code; SAS's birth changes mainly import shared world-size sizing and adapt the old six-size assumptions.
  KI#275 is an inherited Refar start-option defect in `isValidBoth()`. The function begins with `gl_isValidCalls += 1`, but `gl_isValidCalls` is never initialized anywhere and the function has no `global gl_isValidCalls` declaration. Python therefore treats the augmented-assignment target as local and attempts to read that uninitialized local immediately, raising `UnboundLocalError` before any map/coast validity test. The exact line is present in Refar 1.25, the BTG-distributed copy, practical 5436 and current SAS. It is directly reachable through RandomScriptMap's human `Player Start` choice `Coastal on Biggest Land`, whose `findStartingPlot()` path passes `isValidBoth` to `CvMapGeneratorUtil.findStartingPlot`. AdvCiv's map callback failure path can continue with generic C++ starting-plot placement, so the practical result is a Python exception plus silent loss of the user's requested custom coastal/Old-World placement rather than necessarily a failed game launch. The counter has no other live use. Surgical direction: remove the obsolete increment, or explicitly initialize/globalize the counter only if diagnostic counting is still desired.
  KI#276 is an inherited Refar embedded Medium-and-Small control-flow typo. The generator rolls `yShiftRoll1` from two values and `yShiftRoll2` from four, then when the latter is greater than 1 tries to coerce it to the opposite binary orientation of `yShiftRoll1`; however both supposed assignments are written as standalone comparisons: `yShiftRoll2 == 1` and `yShiftRoll2 == 0`. They have no effect. Downstream `if yShiftRoll2` tests therefore use the original truthy 2/3 value. When `yShiftRoll1 == 1` and `yShiftRoll2` originally rolls 2 or 3, the intended second orientation is false but the live one remains true, so both halves take the same vertical shift instead of the intended opposite relation. Independent rolls make this 1/2 * 2/4 = 25% of embedded Medium-and-Small generations. Refar 1.25, the BTG copy, the 5436 birth and current SAS all contain the typo; later SAS reformatting commits touched this exact block without changing the comparisons. A current AST scan found these are the file's only standalone comparison-expression statements. Surgical direction: restore the intended assignments (`=`) while preserving the same RNG sequence and orientation logic.
  KI#277 is an inherited Refar Terra quadrant-randomization bound defect. Firaxis' stock Terra uses fixed safe margins before its optional quadrant flips: the New World's eastern longitude 0.35 becomes 0.95 after a +0.60 shift, and Eurasia's southern latitude 0.45 becomes 0.05 after a -0.40 shift. Refar randomizes those base margins but keeps the full shifts: `newworldEastLon` can be 0.36..0.42 and `eurasiaSouthLat` 0.38..0.46. When the relevant flip rolls fire, the New World east edge can therefore become 0.96..1.02 and Eurasia's south edge -0.02..0.06. The horizontal overflow alone is ordinary and world-size-independent: the horizontal flip occurs half the time and 3 of the 7 east-margin rolls are 0.40/0.41/0.42, so 3/14 (about 21.4%) of Terra generations request an eastern region boundary at or beyond map width. Larger maps can also turn the negative fractional south values into negative integer Y coordinates.
  KI#277's consequence was traced through the actual region compositor rather than inferred from comments. `CvMapGeneratorUtil.MultilayeredFractal.generatePlotsInRegion()` explicitly requires regions to remain inside the map, then writes regional results into `wholeworldPlotTypes` using the flat index `wholeworldY * self.iW + wholeworldX`; it does not normalize or coordinate-wrap out-of-range region coordinates. An X coordinate at/above width therefore spills into a following linear row, while negative Y can use Python negative list indexes near the end of the map array. The result is corrupted/misplaced Terra quadrant geometry rather than intentional cylindrical/toroidal wrapping. A deterministic geometry harness preserving the current formulas ran 20,000 RNG seeds for every current Arena-through-SAS48 tier across all embedded RandomScriptMap generator families; Terra was the only family that issued out-of-bounds `generatePlotsInRegion()` rectangles. Fractal, Archipelago, Pangaea, Big+Small, Medium+Small and Hemispheres remained positive and in bounds. Surgical direction: retain Refar's randomized quadrant flavor but constrain the randomized base margins so the subsequent fixed shifts preserve the stock safe bounds, or explicitly transform/normalize region coordinates before compositor indexing.
  Two SAS-side 5436 adaptation problems seen in the birth file are historical lineage already repaired by newer commits and are not recounted. First, `from SAS_WorldSizes import *` leaked the helper's `getGridSize()` into this map-script namespace; the later 5897 helper split/callback-leak repair covers that family. Second, the initial Arena/world-size conversion corrected several obvious legacy tables but left raw `getWorldSize()` threshold assumptions inside embedded generators; practical 5899 later repaired those remaining raw runtime-index sites and the associated current world-size mapping. Those repaired SAS defects are recorded only as birth-history context here.
  Several adjacent candidates were deliberately rejected. `shiftRegionPlots()` computes an ineffective upper clamp for `stripRadius`, but every live RandomScriptMap caller supplies strip widths already within 3..15. Fractal's continent-grain random roll is collapsed to grain 2 but matches the routine's intended/default contour behavior and no broken option contract was established. Terra's small-subcontinent retry loop is probabilistic but has a nonzero valid solution interval on every supported current map size; the apparent `scsWestX` definite-assignment warning is false because the loop executes at least once. Pangaea's zeroed `scVertRange`, several unused local variables and RNG intermediates, and coastal-peak terrain sculpting have no demonstrated runtime failure. The custom `R_TerrainGenerator` remains compatible with current AdvCiv because base `TerrainGenerator.processCustomizations()` detects the overridden `initFractals()` and disables the Earthlike specialization before choosing the subclass's expected fractals. Dynamic RNG bounds and custom-option accesses were also stress-checked; no nonpositive `dice.get()` bound, fixed-18 player enumeration, KI#249-style missing `isFreshWater()` call or fourth independent current defect was established.
  Current-history tracing confirms KI#275-KI#277 all survive the current descendant. The Medium-and-Small typo is preserved even through later commits that reformat that exact block, and the Terra quadrant shifts survive later line-splitting cleanup unchanged. Certified cursor advances through practical 5436 / 393a82a296. Next runtime-source SHA: practical 5435 / 2270a70c79 (`Fix Rainforest.py; move it to almost_all_land`), intentionally left unopened at this checkpoint.


SAS-C109 - practical 5435 Rainforest import/adaptation audit - COMPLETE; KI#278 FOUND
  Reviewed 1 runtime-source SHA: practical 5435 / 2270a70c79. Practical 5435 creates the executable `PrivateMaps/Rainforest.py`, moves Rainforest from the broad land-heavy classification to the almost-all-land classification, and documents it as another SAS-compatible map. The map body was compared line-by-line with the original BTS/Sirian Rainforest script from the supplied BTG public-map archive, the current SAS descendant, every later Rainforest history hunk, current `SAS_WorldSizeUtils.py`, current world-size XML, the sibling Sirian regional bonus systems in Great Plains/Fantasy Realm, `CvMapGeneratorUtil`, `CvFractal`, bonus/improvement legality and the AdvCiv starting-area/path contracts. The birth adaptation is intentionally small: retain the stock Rainforest generator, route its grid through the shared compact almost-all-land profile, replace the inherited fixed-18 player enumeration with `gc.getMAX_CIV_PLAYERS()`, and replace the old direct forced-resource size-table indexing with `sas_lookup_world_size()`.
  KI#278 is a SAS-specific Rainforest forced-resource world-size adaptation defect. The custom Fur/Silver/Deer placement derives `count` from a fixed world-size component plus a randomized player component. Rainforest retains Sirian's original six-entry fixed component table: Duel/Tiny/Small = 1, Standard/Large = 2, Huge = 3, and now calls `sas_lookup_world_size(sizekey, sizevalues)`. `sas_lookup_world_size()` returns the largest key present when the requested key is absent. Because Rainforest's copied table omits SAS's already-existing Arena tier, an Arena game therefore resolves to the table's Huge entry and contributes `3` rather than the smaller legacy-tier value `1`; the fixed component is non-monotonic at the bottom (Arena 3 -> Duel 1). The table also omits SAS24/SAS32/SAS40/SAS48, so all four larger tiers remain capped at the legacy Huge component 3. This survives current Rainforest unchanged.
  The intended adaptation is unusually well evidenced internally rather than inferred from preferred balance. The same shared helper module provides `sas_default_sizevalues()` with the complete progression Arena/Duel/Tiny/Small = 1, Standard/Large = 2, Huge = 3, SAS24/SAS32/SAS40/SAS48 = 4/5/6/7. Current Great Plains and Fantasy Realm use that complete helper in the corresponding Sirian regional forced-bonus count code, while Rainforest alone manually duplicates only the legacy subset. At minimum Arena must map to 1 instead of falling upward to Huge; using the shared complete size-value table would also keep the larger SAS tiers consistent with the sibling adapted scripts. The randomized player component remains separate, so this finding concerns only the deterministic world-size component and does not depend on a particular RNG roll. Surgical direction: replace the retained local legacy table with `sas_default_sizevalues()` (or explicitly add every intended SAS tier), then regression-check forced Fur/Silver/Deer counts across Arena through SAS48.
  Several adjacent candidates were deliberately rejected after deeper tracing. Rainforest's custom pocket-opening start routine still works under AdvCiv despite `CvMap::updateMinOriginalStartDist()` using `stepDistance()` instead of the old path-distance call: AdvCiv enables `PASSABLE_AREAS`, whose DFS splits land areas at impassable mountain/ice barriers, so a peak-separated pocket remains in a different area and keeps the required `-1` signal. This was checked against Base AdvCiv 1.14 and current Highlands rather than inferred from one function in isolation. The raw Euclidean nearest-start/bulldozer geometry is not promoted because Rainforest's only wrap custom option is hidden and defaults to Flat; non-flat stale-option reachability belongs to the already-known Simple Game option-state lineage rather than an ordinary supported configuration here.
  The much smaller compact grids do not invalidate Rainforest's fractals: `CvFractal::fracInitInternal()` clamps smoothness/grain to its internal exponent range, so the grain-7 food fractal and feature grains remain defined even on the current 8x8-plot Arena result. The almost-all-land profile stays positive through SAS48 and no independent start-capacity failure was established. The direct custom Jungle food and forced Fur/Silver/Deer placements intentionally bypass ordinary natural-spawn legality; Civ4's improvement contract lets a matching bonus make its resource improvement valid, and no inaccessible-resource state was established. The one-element `banana`/`pig`/`rice` declarations are strings rather than tuples, but membership is tested only after `type_string` has already been restricted to the three exact Jungle-food bonus names, so no false match was found. `dice.get(int(players/1.6))` reaches a zero bound for a one-player game, but the DLL RNG safely returns zero for a zero multiplier; it merely permits the documented negative/sparse player modifier and does not throw. No undefined global, nonpositive fractal dimension, stale bonus tag, custom-option index drift, `sPlot` alias misuse or second independent current defect was established.
  Later Rainforest history is formatting/strict-lookup/tag-renaming/helper-relocation work and does not repair KI#278. The 5452 Clam->Molluscs rename is a correct content-tag adaptation; 5442's strict lookups resolve against current XML; 5897 moves the shared helpers out of `PrivateMaps` without changing this local size table. Certified cursor advances through practical 5435 / 2270a70c79. Next runtime-source SHA: practical 5434 / 29d10c2a8f (`Fix Global_Highlands.py`), intentionally left unopened at this checkpoint.


SAS-C110 - practical 5434 Global Highlands import/adaptation audit - COMPLETE; KI#279 FOUND
  Reviewed 1 runtime-source SHA: practical 5434 / 29d10c2a8f. Practical 5434 creates the executable `PrivateMaps/Global_Highlands.py` by importing Firaxis/Sirian's stock Global Highlands script and making a deliberately narrow SAS adaptation: import the shared world-size helper, route the legacy six-tier grid table through `sas_lookup_world_size()`, and document the map as SAS-compatible. The birth file was compared line-by-line against the supplied original BTS `Global_Highlands.py`, the current SAS descendant, all later Global Highlands history hunks, current `SAS_WorldSizeUtils.py`, current world-size XML/enums, and the sibling Highlands script whose stock Mountain Pattern table is identical.
  KI#279 is a SAS-specific Global Highlands Mountain Pattern world-size adaptation defect. The stock script chooses the hill/peak fractal grain from a six-entry Duel..Huge table: Duel/Tiny use `[3,4,5]`, while Small/Standard/Large/Huge use `[4,5,6]`; the list is reversed before indexing the Scattered/Ridgelines/Clustered choice. Practical 5434 changes the direct dictionary lookup to `sas_lookup_world_size(map.getWorldSize(), worldsizes)` but leaves that legacy six-entry table unchanged even though Arena and SAS24/32/40/48 already exist in the runtime world-size set. At 5434 the hardcoded `WorldSizeTypes` enum was still the old Duel=0..Huge=5 sequence while runtime XML had Arena=0 before Duel, so the adapted lookup was already mis-bucketed: Arena happened to hit the old Duel entry, Duel hit old Tiny, but runtime Tiny=2 hit old Small and therefore used `[4,5,6]` instead of Tiny's original `[3,4,5]`; sizes beyond the old enum range fell back to the legacy Huge entry. Practical 5971 correctly aligns `WorldSizeTypes` itself with XML, which fixes that old offset, but because this particular Global Highlands table still omits the new tiers the current manifestation becomes Arena falling through to the largest present key (Huge=`[4,5,6]`) while SAS24/SAS32/SAS40/SAS48 likewise remain capped at `[4,5,6]`.
  The intended current mapping is strongly evidenced by the direct sibling rather than inferred from fractal aesthetics. Stock `Highlands.py` contains the same legacy Mountain Pattern table, and current SAS Highlands already expands it explicitly to Arena=`[3,4,5]`, Duel/Tiny=`[3,4,5]`, Small through Huge=`[4,5,6]`, and SAS24/SAS32/SAS40/SAS48=`[5,6,7]`. Global Highlands retained only the old subset. Consequently current Arena gets every Mountain Pattern grain one step finer than the corresponding adapted Highlands/legacy small-tier tuning, while every SAS24+ tier gets a grain one step coarser than the established SAS extension. Surgical direction: give Global Highlands the same complete Arena-through-SAS48 Mountain Pattern table as Highlands (or centralize that shared table), preserving Global Highlands' intentionally different peak/hill density percentages.
  The separate Global Highlands grid-size lineage is historical-only here. The 5434 birth retained the stock oversized six-tier grid and inherited the then-current runtime/enum indexing difficulties, but practical 5853 and later world-size work explicitly repair/calibrate that path under KI#138; current `getGridSize()` has an Arena anchor, calibrated SAS tiers, and the empirically reduced one-size-smaller base profile. That repaired grid issue is not counted again as KI#279. The duplicate `NiTextOut` call is cosmetic; `GHFractalWorld`'s unused intermediate hill/peak thresholds are dead computation rather than output state; landmass shifting followed by an independent global mountain fractal does not preserve a coordinate-dependent structure that needs shifting; stock wrap, BonusBalancer, terrain, feature and coastal-peak-flattening callbacks remain coherent; and the script has no custom player-ID/start-placement machinery. No second independent current defect was established.
  Current-history tracing confirms no later Global Highlands commit repairs the Mountain Pattern table. 5451 only adds the KI#106 stale-option warning, 5853/5855/5864/5966/5971 repair/refactor the separate grid/world-size infrastructure, 5871 only expands a one-line conditional, and 5897 moves the helper import; the grain table remains the same six legacy entries through current source. Certified cursor advances through practical 5434 / 29d10c2a8f. Next runtime-source SHA: practical 5433 / 202d3dbbaf (`fix Earth2.py: keep same size at Huge and beyond...`), intentionally left unopened at this checkpoint.


SAS-C111 - practical 5433 Earth2 import / staged-start-distribution audit - COMPLETE; KI#280-KI#282 FOUND
  Reviewed 1 runtime-source SHA: practical 5433 / 202d3dbbaf. The mixed commit also contains a Sevopedia indentation-only source hunk and XML/docs, but the substantive runtime change is the 1,320-line `PrivateMaps/Earth2.py` import/adaptation. The birth file was compared against the supplied original BTS Earth2 script, the current SAS descendant, every later Earth2 history hunk, current `CvMapGeneratorUtil.findStartingPlot()`, current world-size helpers/XML/enums and the generated Earth2 region geometry. The explicit 5433 design is unusually clear from the commit message, Python comment, defines and still-current Main Changes Guide: keep classic biggest-landmass starts at low player counts, from 16 players also populate Africa, from 32 populate the full world, and for Huge-and-beyond avoid further map enlargement because Earth2 is already large enough.
  KI#280 is a SAS-specific 5433 middle-stage start-distribution predicate bug. For fewer than 16 players, the new validator correctly preserves the stock behavior by requiring `pPlot.getArea() == iBiggestAreaID`. It then computes an explicit Africa rectangle (`iAfricaWestX/iAfricaEastX/iAfricaSouthY/iAfricaNorthY` and `bInAfrica`) consistent with the stated "then unlock Africa" stage. However the only condition that references `bInAfrica` is `if iNumPlayers < iAfricaMinPlayers and bInAfrica`, which is unreachable because the function has already returned for every `< iAfricaMinPlayers` call. For 16-31 players the live code instead rejects only `bInAustralia or bInAmericas` and accepts every other candidate plot. This therefore unlocks all other non-Americas/non-Australia land areas, not merely the original biggest landmass plus Africa: Earth2 explicitly generates separate British-Isles/Emerald and Japan regions inside that admitted coordinate space. `CvMapGeneratorUtil.findStartingPlot()` applies the supplied validator directly and has no hidden biggest-area restriction; any admitted plot with the highest positive found value can be selected. Surgical direction: in the middle stage admit `(pPlot.getArea() == iBiggestAreaID) or bInAfrica`, and reserve unrestricted `True` for the full-world threshold, preserving the define-driven 16/32 staging.
  KI#281 is a later SAS-specific Earth2 map-size policy regression introduced at practical 5854. The 5433 birth intentionally calls ordinary `sas_lookup_world_size()` on a table ending at Huge `(40,24)`, so absent larger tiers fall back to Huge exactly as the commit states: solve higher-player crowding by widening eligible start landmasses rather than enlarging this already-large map. Practical 5854 replaces that with `sas_lookup_world_size_with_calibrated_sas()` and explicitly says SAS24-48 should scale up from Huge. That generic calibration survives current source even though the 5433 `findStartingPlot()` comment still says better distribution is preferred to forcing larger grids and the current Main Changes Guide still promises "for Huge and beyond world sizes, keep Earth2 at its existing Huge-scale sizing". With current XML default players (Huge 16, then 24/32/40/48), the helper expands the `(40,24)` grid-unit Huge anchor to approximately SAS24 `(48,29)`, SAS32 `(57,34)`, SAS40 `(63,38)`, SAS48 `(70,42)`; map-script grid units are multiplied by four by the engine, so SAS48 is about 280x168 = 47,040 plots versus Huge 160x96 = 15,360 plots, over three times the plot count. This is not merely stale documentation: it directly negates the rationale that created the staged start system. Surgical direction: keep the corrected Arena-through-Huge keys but return them through ordinary `sas_lookup_world_size()` (or otherwise explicitly cap `>= Huge` at Huge) so SAS24-48 reuse `(40,24)` while the 16/32 player thresholds distribute starts.
  KI#282 is a SAS-specific Earth2 Arena regional-grain world-size adaptation bug. `EarthMultilayeredFractal.generatePlotsByRegion()` retains the stock six-entry Duel..Huge grain matrix and changes only the direct lookup to `sas_lookup_world_size(sizekey, sizevalues)`. At 5433, runtime XML already had Arena inserted before Duel while the hardcoded enum was still the old six-value sequence, so this incomplete adaptation initially produced shifted buckets (notably runtime Tiny selected the old Small grain tuple). Practical 5971 correctly aligns `WorldSizeTypes` with XML, but the Earth2 grain table itself remains untouched. In current source Arena is therefore the missing key and `sas_lookup_world_size()` falls back to the largest present key, giving the smallest map Huge's `(ScatterGrain, BalanceGrain, GatherGrain) = (5,2,1)` instead of the Duel/Tiny small-map tuple `(3,2,1)`; Duel and Tiny immediately drop back to `(3,2,1)`. This bottom-tier non-monotonicity is the same incomplete-world-size-table failure mode seen in adjacent imported scripts. Because Earth2's intended policy is to cap geometry at Huge, the existing Huge fallback for SAS24+ grain is not independently classified as wrong here. Surgical direction: add Arena explicitly as `(3,2,1)` (and optionally explicit SAS aliases to Huge for clarity) while preserving the legacy Duel..Huge progression.
  The remainder of the imported Earth2 runtime body was audited without establishing a fourth defect. The current 30 fixed geographic region rectangles are all positive and in bounds across the supported geometry; even Arena's smallest generated region remains positive, and `CvFractal::fracInitInternal()` handles positive one-row/one-column regional dimensions without a separate zero-dimension path. The western/eastern hemisphere coordinate filters cover the generated Americas/Australia footprints as intended for the full-world gate; the slight broadness of those rectangles is companion geometry to KI#280 rather than an independent failure. The stock repeated `generatePlotsInRegion()` calls are deliberate layered landmass shaping, unlike the unintended BTG source-region duplication in KI#274. The terrain generator uses XML terrain-grain change rather than stale raw world-size arithmetic; current strict terrain tags exist; no custom fixed-player-ID enumeration, `sPlot` aliasing, unbounded retry, invalid RNG bound or region-array overflow was found. The inherited `NiTextOut("Generating Terrain (Python Terra) ...")` label is cosmetic only.
  Later-history tracing cleanly separates the three roots. KI#280's staged-start code is never semantically edited after 5433. KI#281 begins specifically in 5854's grid-calibration hunk and is carried through later helper-key/import refactors (5855/5864/5897/5966/5971) without restoring the fixed-Huge policy. KI#282's grain matrix is likewise never semantically edited; 5971 fixes the global enum alignment but leaves this legacy subset missing Arena. Certified cursor advances through practical 5433 / 202d3dbbaf. Next runtime-source SHA: practical 5432 / 2addf7daeb (`Fix base AdvCiv bug of most bonuses ... first Civ4 launch`), intentionally left unopened at this checkpoint.


SAS-C112 - practical 5432 Arboria/Boreal KI#105 first-launch bonus-fractal guard audit - COMPLETE; no new defect
  Reviewed 1 runtime-source SHA: practical 5432 / 2addf7daeb. Its executable changes are in two runtime files, `PrivateMaps/Arboria.py` and `PrivateMaps/Boreal.py`. The accompanying Known Issues documentation describes an intermittent first-Civ4-launch case in which custom forest/boreal bonuses such as Deer could be absent until returning to the main menu and generating again. The commit adds the same defensive state pattern to both scripts: module-level `food = None` plus stored `food_iW/food_iH`, `_initFoodFractal()` called from `beforeGeneration()`, and `_ensureFoodFractal()` immediately before the custom `food.getHeightFromPercent(...)` bonus-placement thresholds. The ensure helper recreates the fractal only when it is missing or its remembered dimensions differ from the active map.
  This closes clean-current rather than establishing another KI. The normal-generation semantics were traced through the DLL rather than inferred from the empirical issue description. `CvMapGenerator::generateRandomMap()` invokes the map script's `beforeGeneration()` unconditionally before plot/terrain generation, and bonus callbacks occur later through `addGameElements()` after rivers/lakes/features. Thus on every normal fresh/regenerated map the `food` fractal has already been freshly initialized with the active map dimensions and map RNG before the first custom `addBonusType()` call; `_ensureFoodFractal()` is a no-op on that live path. The parent scripts already initialized `food` in `beforeGeneration()`, so the 5432 change does not alter ordinary placement thresholds, RNG consumption or bonus predicates when the documented callback order holds.
  A historical diagnosis nuance is preserved without promoting it to a defect. `_ensureFoodFractal()` can detect absent state and dimension mismatch, but cannot prove freshness when a hypothetical stale fractal came from a previous map with the same dimensions. Therefore the 5432 comment that the guard removes dependence on callback/state timing is broader than what the guard alone can establish. That limitation is immaterial under the actual engine callback contract because `beforeGeneration()` refreshes the fractal regardless of dimensions. No supported callback path was found that reaches Arboria/Boreal custom bonus placement without that initialization.
  Practical 5441 later deliberately removes the temporary module-level `None`/dimension cache and `_ensureFoodFractal()` while preserving direct `food` initialization in `beforeGeneration()`, alongside a broader KI#105 state-locality refactor. C104 already audited that descendant change against the DLL order and certified it clean-current. No intervening 5433-5440 commit semantically touches these two map scripts, and current Arboria/Boreal retain the simple 5441 form. The empirical KI#105 history therefore remains useful as repaired/debugging lineage, but 5432 contributes no surviving runtime-source defect and does not change the existing defect counters. Certified cursor advances through practical 5432 / 2addf7daeb. Next runtime-source SHA: practical 5431 / 30dd3faa8c (`refactor multilists`), intentionally left unopened at this checkpoint.


SAS-C113 - practical 5431 Sevopedia / Info Screen multilist literal-centralization audit - COMPLETE; no new defect
  Reviewed 1 runtime-source SHA: practical 5431 / 30dd3faa8c. The commit touches eleven executable Python files: nine Sevopedia pages (`Bonus`, `Building`, `Civic`, `Feature`, `Promotion`, `Religion`, `Terrain`, `Trait`, `Unit`), shared `_sevopedia_helpers.py`, and `CvInfoScreen.py`. Despite the broad file count, the substantive pattern is narrow: replace repeated multilist literals with named constants and remove repetitive local `buttonCalculate = 1` / `columnIndex = 0` assignments. `_sevopedia_helpers.py` defines `SEVOPEDIA_MULTILIST_NUM_LISTS_AUTO_CALCULATE = 1` and `SEVOPEDIA_MULTILIST_COLUMN_INDEX_AUTO = 0`; every affected Sevopedia page already imports that helper with `from _sevopedia_helpers import *`. `CvInfoScreen` intentionally mirrors the same values as `self.iTopCitiesWonderNumListsAutoCalculate = 1` and `self.iTopCitiesWonderColumnIndexAuto = 0` rather than introducing a Sevopedia dependency.
  A normalized old/new executable comparison was performed across all eleven files. After substituting the new named constants back to literal `1`/`0` and ignoring comments/indentation, every `addMultiListControlGFC` and `appendMultiListButton` call retains the same arguments, order and widget IDs. Building/Unit/Terrain/Feature panels that place numeric modifier text beneath buttons preserve their `iButtonIndex` progression, `maxButtonsPerRow` calculations and text coordinates; no button, prerequisite, unit, promotion, leader or wonder row is removed. Current source still defines the shared constants as exactly `1` and `0`, and later Sevopedia work reuses those constants rather than repairing a semantic mistake from 5431.
  The only visually non-mechanical hunk is `SevoPediaTerrain.placeRelevantUnits()` for Peak. The old suite below `if can_walk_on_peak:` is over-indented relative to neighboring code but consistently so and therefore syntactically forms the same Python suite; 5431 normalizes that indentation while retaining the same append call, terrain attack/defense reads, modifier text placement and `iButtonIndex += 1`. This is readability cleanup, not a branch or display-content change. No missing helper import, name collision, circular dependency or multilist-column behavior change was established. Certified cursor advances through practical 5431 / 30dd3faa8c. Next runtime-source SHA: practical 5430 / 3bf18c08ec (`add SAS sentinel/guard defines to make sure they are loaded`), intentionally left unopened at this checkpoint.


SAS-C114 - practical 5430 SAS define launch-guard lifecycle audit + practical 5429 non-runtime main-menu art step - COMPLETE; no new defect
  Reviewed 1 runtime-source SHA: practical 5430 / 3bf18c08ec. Practical 5429 / c4fad2d421 contains no runtime C++/Python change (main-menu NIF/DDS, `CIV4ArtDefines_Interface.xml` path/comment and documentation only), so it advances the practical certified cursor without consuming the frozen 1,317 runtime-source denominator.
  Practical 5430 introduces `SASDefineGuard.py`, integer/string sentinel defines in `GlobalDefines_advciv_sas.xml`, and five verification entry points: BUG/Firaxis `preGameStart`, `CvEventInterface` `OnLoad` and `GameStart`, Civilopedia creation, and Sevopedia show. The guard itself caches only a `CyGlobalContext()` wrapper plus constant names/expected values; sentinel values are read afresh on every `verify_or_raise()` call. `CyGlobalContext` holds a reference to the singleton `CvGlobals::getInstance()`, so constructing the wrapper while Python modules are first imported does not snapshot the pre-XML define table. This matters because `CvEventInterface.initAfterReload()` explicitly documents that the module is initially imported while the global context is not yet ready; no define access occurs during that phase.
  The DLL define-load path was traced directly. `CvXMLLoadUtility::SetGlobalDefines()` loads the normal/developer/AdvCiv define files and then explicitly calls `ReadGlobalDefines("xml\\GlobalDefines_advciv_sas.xml", cache)` before the later BBAI/AI define files and before `GC.cacheGlobals()`. The sentinel therefore resides in the live `FVariableSystem` by the time ordinary game/UI Python entry points can use it. `CvGlobals::getDefineINT` returns its default 0 for a missing integer, while `getDefineSTRING` initializes its return pointer to NULL and returns that missing value; the Python-side comparisons fail safely and the guard's `%s` diagnostic can represent the missing string rather than dereferencing it. In debug builds the DLL's missing-define assertions may surface first, which is complementary fail-fast behavior rather than a release-only semantic difference.
  The earliest decisive runtime hook is safe with respect to load order. BUG extends Firaxis' `CvAppInterface.preGameStart` in `EXTEND_INSTEAD` mode, and the wrapper calls `SASDefineGuard.verify_or_raise()` before invoking the original function. BUG's own lifecycle documentation states that `CyGlobalContext` is not ready during the early `Init` event, but BUG initialization is deliberately deferred until `OnLoad` or `PreGameStart`; `OnLoad` precedes `PreGameStart`, which in turn precedes `GameStart`. Firaxis' original `preGameStart()` is already at the UI/game-start phase: it initializes dynamic font icons, preloads the tech chooser and shows the main interface. Thus a valid launch has completed define loading before the sentinel is checked, while a mismatched/missing sentinel aborts the wrapper before the normal main interface is initialized. No supported valid-start path was found where this creates a false fatal.
  A wording/bridge nuance was investigated and rejected as a separate defect. `CvDllPythonEvents::postEvent()` records the boolean result of `callFunction("CvEventInterface", "onEvent", ...)`, but void reporters such as `reportGameStart()` do not propagate that result farther; therefore an exception raised specifically at the `GameStart` event may be logged/swallowed by the native event bridge rather than terminating the engine process. The same event hook is nevertheless redundant defense: `PreGameStart` already runs the same verification before Firaxis' interface initialization, and save-load sequencing also reaches `PreGameStart` after `OnLoad`. The guard's documented practical outcome (bad configuration cannot continue as a normally initialized/playable SAS session) is therefore established without requiring each redundant event reporter itself to terminate the executable.
  The Civilopedia/Sevopedia hooks are likewise late and side-effect-free. `CvScreensInterface.createCivilopedia()` verifies before constructing either Pedia implementation, and `SevoPediaMain.pediaShow()` verifies again before TraitUtil/category state initialization. Repeated verification only performs two live variable-system reads and comparisons; it mutates no define or Pedia/game state. PitBoss does not create a bypass: BUG can skip some component registration for a PitBoss host, but the globally installed `preGameStart` wrapper itself still exists and runs its guard before Firaxis' original host-aware routine. Current source retains the same sentinel values and runtime verification design; later history adds build/CI checks around the sentinel but no runtime repair of a 5430 lifecycle flaw was found.
  Practical 5429 was inspected because it sits between runtime-source commits. It renames the static main-menu DDS, swaps the background NIF to the Middle-earth-mod-derived static-wallpaper asset, updates the corresponding Interface XML path/comment, deletes no-longer-needed auxiliary DDS files and edits documentation/screenshots. No executable C++/Python is changed, and the XML path points to the renamed asset present in the same commit. It is therefore outside the primary runtime-source denominator and establishes no source defect. Certified practical cursor advances through 5429 / c4fad2d421. Next runtime-source SHA: practical 5428 / 0b9ab6d22f (`Fix Donut.py`), intentionally left unopened at this checkpoint.

SAS-C115 - practical 5428 Donut import/adaptation and wrapped-center contract audit - COMPLETE; KI#283 FOUND
  Reviewed 1 runtime-source SHA: practical 5428 / 0b9ab6d22f. The commit adds the complete 330-line Firaxis/Sirian `PrivateMaps/Donut.py` runtime map script to SAS, with SAS world-size helper integration and an explicit Arena-through-Huge square grid table. Current Donut retains the same core generator and follow-up coordinate logic; later changes make strict terrain lookups, Simple Game visibility/stale-option handling, helper relocation, enum-key alignment, and deliberate Large/Huge size reductions, but do not alter the defect below. Stock BtS Donut from the Firaxis/Civ4CE source contains the same core pattern, establishing inherited rather than SAS-specific provenance.
  KI#283 (inherited Firaxis Donut wrapped-plot seam shift/fixed-center terrain desynchronization): Donut hand-builds its circular plot geometry around `iCenterX = width/2` / `iCenterY = height/2`, then calls the generic `FractalWorld.shiftPlotTypes()` whenever the selected map wrap enables X and/or Y wrapping. The generic seam scorer counts only `PLOT_LAND`; randomly generated `PLOT_HILLS` and `PLOT_PEAK` cells contribute exactly like ocean. Although Donut already leaves an ocean border around the circle, the random hill/peak pattern can therefore make the adjacent wrapped seam (`width-1` and/or `height-1`) score below seam 0. `shiftPlotTypesBy()` then rotates the completed plot array by one tile, while `DonutTerrainGenerator` independently recomputes the center, radius and hole from the original unshifted map coordinates and never receives the chosen seam offset. The requested Center Region plot disk and its terrain disk consequently no longer coincide on those wrapped generations. The user-selectable Desert center makes the consequence especially explicit: some ordinary ring cells inside the old fixed hole are forced to Desert while the corresponding shifted center-disk cells on the opposite edge of the hole receive normal Grass/Plains terrain. Peaks/Hills center choices similarly shift the special plot disk away from the fixed terrain disk; Ocean center shifts the structural hole itself away from the map center. Flat wrap is unaffected because no shift is requested.
  Reachability was verified against the actual current algorithms rather than a generic random surrogate. A direct replay of current `CvRandom` (the 32-bit LCG in `CvRandom.cpp`), `CvFractal::fracInitInternal/getHeight/getHeightFromPercent`, Donut's current hill/peak thresholds, and `FractalWorld.findBestSplitX/Y` produces legal wrapped Donut generations whose best split is `width-1`; for example a current Tiny Donut is 32 x 32 plots and a legal fractal-entry RNG state of 0 selects X split 31. The RNG state transition uses odd multiplier 1103515245 and is therefore bijective modulo 2^32, so such entry states are reachable from ordinary game/map seeds after the fixed preceding RNG calls. Replays across current Tiny-through-SAS48 dimensions repeatedly produce either seam 0 or the adjacent `dimension-1` seam; the issue is not dependent on an invalid grid size. On Tiny, shifting the strict Desert-center hole by one X tile makes the original and shifted hole masks differ on 22 plots; the boundary mismatch grows with larger radii. Toroidal mode can independently apply the same one-tile Y displacement.
  Preferred surgical repair is to disable generic plot shifting for Donut (e.g. generate its plot types with `shift_plot_types=False`), because this handcrafted circular map already guarantees an ocean margin at the map boundaries and has coordinate-dependent follow-up generation centered on fixed map coordinates. An alternative would require carrying the exact X/Y seam offsets through every center-dependent follow-up calculation, which is needlessly broader. This root is conceptually similar to inherited BTG Lagoon KI#270 but is not the same code/provenance and was independently proven in Firaxis Donut.
  The remainder of the map was audited without finding a second current root. The smallest current Arena override becomes 20 x 20 actual plots under AdvCiv's custom-map 4-plots-per-cell contract, giving positive radius 6 and hole radius 3; all current SAS calibrated tiers remain safely positive. `waterArea()` on a land plot returns the largest adjacent water area wrapper, and the script's `isNone()` / `isLake()` checks correctly enforce a non-lake coastal start rather than dereferencing null state. Custom-option indices and wrap/resource defaults are internally consistent; balanced bonuses use the stock `BonusBalancer` contract; climate/sea-level controls are intentionally disabled; the no-Ice and optional-Jungle feature overrides are deliberate. No fixed-18 player enumeration, stale raw world-size indexing, unbounded retry, missing predicate call, malformed regional rectangle or additional current Donut defect was established. Historical world-size/helper-key cleanups remain previously audited lineage rather than new 5428 findings. Certified cursor advances through practical 5428 / 0b9ab6d22f. Next runtime-source SHA: practical 5427 / 1f51abbd83 (`Fix Boreal.py; move it to almost_all_land maps`), intentionally left unopened at this checkpoint.


SAS-C116 - practical 5427 Boreal import/adaptation + shared CvMapGeneratorUtil Y-seam audit - COMPLETE; KI#284 FOUND
  Reviewed 1 runtime-source SHA: practical 5427 / 1f51abbd83. The commit imports the full Firaxis/Sirian `PrivateMaps/Boreal.py` generator and moves Boreal into the SAS almost-all-land map classification. The birth adaptation already improves one important stock limitation: its custom starting-plot allocator enumerates `range(gc.getMAX_CIV_PLAYERS())` and shuffles the actual alive player IDs rather than retaining Firaxis' literal `range(18)`, so Boreal does not join the KI#265/KI#267 high-ID/sparse-slot families. Its custom Arena-through-Huge compact grid table and later calibrated SAS-size descendants remain positive/capacious through current SAS. The full plot/terrain/feature/start/bonus callbacks and all later Boreal history were audited before closure.
  KI#284 (inherited Firaxis CvMapGeneratorUtil Y-seam strip-size guard dimension typo, whole-world + regional variants): current `FractalWorld.findBestSplitY(stripRadius)` computes `stripSize = 2 * stripRadius`, allocates/scans a Y score array of length `self.iNumPlotsY`, wraps every weighted target with `% self.iNumPlotsY`, but decides whether the strip is too large using `if stripSize > self.iNumPlotsX: return 0`. The symmetric X routine correctly tests `self.iNumPlotsX`. The same copy/paste mistake is independently duplicated in `MultilayeredFractal.findBestRegionSplitY(iRegionWidth, iRegionHeight, stripRadius)`: all scoring/wrapping is over `iRegionHeight`, yet its early guard tests `iRegionWidth`. Base AdvCiv 1.14 and public Firaxis/Civ4 map-utility source retain the same two wrong-width guards, establishing inherited provenance rather than a SAS change.
  The whole-world path has a direct ordinary current-SAS reproduction in Boreal. `FractalWorld.stripRadius` is 15, hence the seam kernel has 30 entries. Current Tiny Boreal is 8 x 5 custom terrain cells = 32 x 20 actual plots and Small is 10 x 6 = 40 x 24; Toroidal wrap is a supported custom option. Correct Y logic must therefore return shift 0 for both because 30 exceeds the map height, but the wrong X guard sees widths 32/40 and runs the scoring kernel instead. A direct replay of the current `CvRandom` 32-bit LCG, `CvFractal::fracInitInternal/getHeight/getHeightFromPercent`, Boreal's continent/hills/peak thresholds and current seam-weight calculation found nonzero Y splits for 91 of the first 100 legal Tiny fractal-entry RNG states and 95 of the first 100 Small states (e.g. entry state 0 chooses Tiny split 19 and Small split 23). Since each LCG transition is bijective modulo 2^32, these are reachable ordinary map-generation states, not synthetic impossible seeds. The 30-entry kernel wraps around a 20/24-row score array and double-counts rows instead of taking the explicit too-small-dimension fallback.
  This shared helper defect is broader than Boreal, so it is deliberately one KI rather than one number per affected map. Confirmed current whole-world dimension/wrap combinations with the same `width >= 30 > height` behavior include Global Highlands Arena/Duel and the shared compact profile's Standard tier in Rainforest, Fantasy Realm and Lakes, in addition to Boreal Tiny/Small. `HintedWorld` inherits the same FractalWorld shift method when callers explicitly request shifting. The regional copy can analogously affect any `MultilayeredFractal` region whose width admits the strip while its height does not; that routine shifts the local regional array, so the wrong dimension is part of the same root even where global map dimensions are large. The neighboring `shiftRegionPlots` upper-clamp oddity (`min(15, iStrip)` followed by `max(3, iStrip)`) was also checked: no current map call supplies `iStrip > 15`, so no independent current defect was promoted from it.
  Preferred surgical repair is the two symmetric one-token dimension corrections: `findBestSplitY` should compare `stripSize > self.iNumPlotsY`, and `findBestRegionSplitY` should compare `stripSize > iRegionHeight`. This preserves the existing explicit small-dimension fallback and leaves all legal X behavior unchanged.
  The remainder of Boreal produced no second current root. Its player shuffle uses the real alive IDs and scales to the 48-civ DLL; the custom start finder/pocket opener is inherited Highlands logic and current calibrated map sizes provide ample candidate area. The opener chooses its nearest prior start with raw non-wrapped Euclidean deltas even when Cylinder/Toroid is selected, so it can carve a longer-than-necessary corridor instead of the shortest wrapped route, but it still crosses/removes an impassable barrier and no independent failed-start, hang or null-start configuration was established; retained as a robustness note only. The feature generator retains only one live `CyMap.sPlot()` wrapper at a time, so it does not reproduce KI#257's simultaneous-static-wrapper alias bug. River terrain upgrades and custom Forest placement are internally coherent.
  The custom bonus subsystem was checked against current XML and its historical renaming. Boreal intentionally hand-places Deer/Gemstones/Wheat/Sheep through regional fractal bands and can bypass ordinary random-spawn terrain restrictions; bonus-linked improvements remain valid and this is inherited regional-map behavior rather than an inaccessible-resource defect. Practical 5427 did import stock resource strings `BONUS_CORN`, `BONUS_CLAM` and `BONUS_GEMS` even though SAS had already renamed those types to `BONUS_MAIZE`, `BONUS_MOLLUSCS` and `BONUS_GEMSTONES`, temporarily defeating the intended custom elimination/placement branches for those renamed resources. Practical 5452 / 031a95cc7c explicitly updates all three Boreal strings, so that is recorded as repaired historical adaptation lineage, not a new current KI. The 5432/5441 food-fractal lifecycle sequence and 5444 Deer Percentage option were already independently certified; no duplicate issue was created here. Certified cursor advances through practical 5427 / 1f51abbd83. Next runtime-source SHA: practical 5426 / c4c7d14c2b (`Fix Arboria.py`), intentionally left unopened at this checkpoint.

SAS-C117 - practical 5426 Arboria import/adaptation audit - COMPLETE; no new defect
  Reviewed 1 runtime-source SHA: practical 5426 / c4c7d14c2b. The commit imports Firaxis/Sirian `PrivateMaps/Arboria.py` into SAS and adapts its square world-size callback to the then-current shared SAS sizing helper. The birth file was compared against the public BTS Arboria source and the current SAS descendant, and the complete topology/terrain/features/options/normalization/custom-bonus surface plus every later Arboria-touching history entry was reviewed before closure. No new current defect was established.
  Arboria's fixed `HintedWorld(16,8)` is a hint lattice rather than a 16 x 8 plot-size assumption. Current `HintedWorld` computes the legal hint-grid shape for the selected wrapping mode before `fracInitHints`: Cylindrical uses 16 x 9 hints, Toroidal 16 x 8, and Flat 17 x 9 after the non-wrapped border rows/columns are added. Those shapes satisfy the hint-fractal grain search, and the hints are interpolated onto the actual map dimensions. The smallest current Arena override is 4 x 4 custom terrain cells = 16 x 16 actual plots, still positive and valid; Duel-through-Huge and the calibrated SAS24/SAS32/SAS40/SAS48 tiers simply provide more interpolation resolution. The continent seed is deliberately constrained away from the forced polar-ocean hint rows, and the requested 50% continent target remains below the available non-polar hint capacity in all wrap modes. No HintedWorld assertion, zero-dimension region, out-of-range RNG bound or malformed plot-array path was found.
  The apparent double-shift concern was also checked rather than inferred from KI#270/KI#283. `HintedWorld.__doInitFractal()` may shift the hint lattice to a low-land seam before constructing its continent fractal, and Arboria then requests the ordinary `FractalWorld.generatePlotTypes(..., shift_plot_types=True)` map-array seam shift. Unlike Lagoon or Donut, Arboria has no later fixed-coordinate center/region contract that must remain aligned with the pre-shift geometry: terrain, Forest/Jungle and Silver/Deer placement are generated from independent full-map fractals/plot properties, and no custom start rectangle is retained. Thus a legal seam shift does not desynchronize downstream logic. Shared KI#284's wrong Y strip-size guard is already recorded separately and is inert for Arboria's square current grids because width equals height.
  Current option/resource behavior was audited through the later 5444 Deer Percentage insertion. Arboria now exposes Deer Percentage values 0/10/25/50/75/100 and keeps wrap as the trailing hidden option; every `getCustomMapOption()` use matches the two-option table. Eligible Deer plots first satisfy the inherited custom fractal bands plus Forest + flatland constraints and are then retained with the requested exact 0..100 map-RNG percentage; Silver placement is independent. The inherited `silver = ("BONUS_SILVER")` / `deer = ("BONUS_DEER")` expressions are strings rather than one-element tuples, but the outer `type_string in forest` check is exact tuple membership over only Silver and Deer before either substring membership is reached, so no other bonus can be misclassified. Hand placement puts Deer on flat Forest and Silver on hills; their associated improvements remain valid, and no inaccessible-resource state was proven. The normalization overrides that preserve Arboria's Forest-paradise character are stock intentional behavior.
  Later lineage was checked explicitly. Practical 5432 temporarily added a defensive `_ensureFoodFractal()` dimension cache for KI#105, and 5441 safely removed that redundant state after relying on the proven `beforeGeneration()` callback ordering; neither is reopened here. 5443 refactors cached terrain/feature handles without changing output, 5444 adds the audited Deer-density option, 5451 adds stale-option diagnostics, 5452 is formatting-only for Arboria's bonus strings, and later helper/world-size key migrations preserve the same current square sizes/calibration. No fixed-18 player enumeration, compact-player-ID custom-start bug, raw world-size-index arithmetic, simultaneous `CyMap.sPlot()` aliasing, unbounded retry or surviving malformed lookup was found in Arboria. Certified cursor advances through practical 5426 / c4c7d14c2b. Next runtime-source SHA: practical 5425 / 8993525573 (`Fix Wheel.py; add it to landheavy maps`), intentionally left unopened at this checkpoint.



SAS-C118 - practical 5425 Wheel import/adaptation + inherited starting-distance/forest-variety audit - COMPLETE; KI#285-KI#287 FOUND
  Reviewed 1 runtime-source SHA: practical 5425 / 8993525573. The commit imports/adapts Firaxis/Sirian `PrivateMaps/Wheel.py` for SAS world sizes and the 48-civilization DLL, while adding Wheel to the land-heavy map classification. The 1,398-line birth file was reconstructed from the frozen commit mirror and compared locally against the user's archived original Vanilla/Warlords Wheel scripts, current Wheel, current/original sibling Hub and Ring, current `CvMapGeneratorUtil.py`, current start-placement C++ and the exact pre-SAS AdvCiv refactor that moved Python map callbacks into `CvPythonCaller`. Every later Wheel history hunk through the current tree was searched before closure. Three independent current roots survive: one older AdvCiv API-semantics regression exposed by Wheel, one SAS-specific >18-player Wheel adaptation omission, and one low-severity inherited Firaxis forest-art boundary defect shared by Hub/Ring/Wheel.
  KI#285 is an inherited/pre-SAS AdvCiv negative `minStartingDistanceModifier()` contract regression introduced at AdvCivPreSAS practical 1648 / 553b1a0f8d. Before that refactor, Firaxis/BtS `CvPlayer::startingPlotRange()` called the map script and applied `iRange *= max(0, lResult + 100); iRange /= 100`, so negative map values were deliberately meaningful: `-95` means 5% of the ordinary spacing range, `-35` means 65%, etc., while values below -100 clamp at zero. Practical 1648 moved the callback into `CvPythonCaller::minStartingDistanceMod()` but changed the semantics to `return std::max(0, toInt(lResult));`; `CvGame::updateStartingPlotRange()` then still performs `iRange *= 100 + minStartingDistanceMod(); iRange /= 100`. Every negative Python value is therefore clamped to zero before the +100 step and becomes an unintended neutral 100% multiplier. Current SAS retains this exact C++ path. A local current-PrivateMaps sweep finds 30 scripts with at least one negative return, including Arboria, Big_and_Small, Boreal, BTG_Lagoon, Custom_Continents, Donut, Earth2, Fantasy_Realm, Global_Highlands, Great_Plains, Hemispheres, Highlands, Hub, Inland_Sea, Lakes, Mirror, Mixed_Continents, not_too_Big_or_Small, Oasis, Rainforest, RandomScriptMap, Ring, four SAS custom maps, Team_Battleground, Terra, Water and Wheel. Wheel/Hub/Ring's `-95`/`-50` pair is therefore currently indistinguishable to the engine even though the scripts explicitly use it to switch between tightly packed handcrafted-region starts and fallback placement.
  KI#285 has direct current start-selection consequences rather than being dead map metadata. `CvGame::getStartingPlotRange()` feeds `CitySiteEvaluator`/starting-found-value distance scoring through `startingPlotDistanceFactor`, and map-script `CvMapGeneratorUtil.findStartingPlot()` obtains/evaluates those found values while selecting starts. The original reduced-distance contract is especially important for dense regional/team maps that deliberately return values as low as -95. Preferred surgical repair is to preserve the 1648 caller split while restoring Firaxis semantics at the callback boundary, e.g. `return std::max(-100, toInt(lResult));`; the existing caller's `100 + value` then exactly reproduces the original lower clamp while retaining all positive modifiers. This finding adds a fifth targeted cross-lineage trace to the tracker because the SAS Wheel audit led directly to the pre-SAS 1648 source regression.
  KI#286 is a SAS-specific Wheel >18-player default-start-fallback omission created by the 5425 48-civ adaptation. Stock Wheel simply sets `bSuccessFlag=False` above 18 players and falls back. SAS intentionally keeps Wheel plot generation usable by capping its handcrafted geometry to 18 regions while retaining the actual player count separately: for `iActualPlayers > 18`, `beforeGeneration()` sets `bSuccessFlag=True`, `bUseDefaultStartPlacement=True` and `iPlayers=18`. `assignStartingPlots()` then builds the 18 region centers and safely enumerates the real ever-alive IDs through `MAX_CIV_PLAYERS`, assigning at most 18 of those IDs to template slots via `playerTemplateByID`, before allowing the C++ default placement pass. `minStartingDistanceModifier()` correctly recognizes `bUseDefaultStartPlacement` and returns the fallback value. However, Wheel's live `findStartingPlot()` tests only `if bSuccessFlag == False` and omits the new fallback flag. C++ default assignment still invokes the map script's per-player `findStartingPlot()` first, so the omission materially overrides the intended fallback: in a 19-player game 18 randomly selected real players remain constrained to Wheel template-center windows while the remaining player is merely constrained to the biggest landmass; at 48 players the split is 18 constrained versus 30 loose. This is neither the advertised/default all-player fallback nor a one-hub-per-player Wheel layout.
  KI#286's intended repair has an unusually strong local sibling control. Current Hub and Ring use the same SAS `bUseDefaultStartPlacement` design and both guard their `findStartingPlot()` with `if bSuccessFlag == False or bUseDefaultStartPlacement == True: return CvMapGeneratorUtil.findStartingPlot(playerID)`. Their birth adaptations contain that guard as well; Wheel alone omitted it. The Wheel comment itself says `Set up for maximum of 18 players! If more, use default implementation.` Preferred surgical repair is therefore to mirror Hub/Ring's two-condition fallback check. KI#285 currently masks Wheel's -95 versus -50 spacing distinction, but it does not mask KI#286's live per-player validity-window constraints; the two defects are independent.
  KI#287 is a low-severity inherited Firaxis Hub/Ring/Wheel forest-variety transition-boundary arithmetic defect. The shared feature generator intentionally assigns forest art variety 2 inside the center rectangle, variety 0 outside `pineWest/East/South/North`, and variety 1 in the intermediate band. Current forest art XML maps those varieties in order to `TreeLeafy`, `TreeEvergreen` and `TreeSnowy`, giving a clear Snowy center -> Evergreen transition -> Leafy outer-region intent. West/south boundaries are computed outward from the center with `pineWestX = int(cenWestX * 0.7)` and `pineSouthY = int(cenSouthY * 0.7)`. East/north instead use `grid - int((centerEdge + grid - 1) * 0.35) - 1`, mixing the center coordinate with the full far-edge coordinate rather than scaling the distance from that edge. On symmetric center geometry these formulas place `pineEastX` and `pineNorthY` inside the Snowy center itself, so the east/north Evergreen transition bands are empty while west/south retain them.
  KI#287 is deterministic across the actual Wheel templates, not a single rounding corner. On current Standard Wheel (64 x 40 plots), all 9 Buffered center templates (2-10 regions) and all 17 Normal center templates (2-18 regions) produce nonempty west/south Evergreen bands and zero-width east/north Evergreen bands. A representative Normal 8-player center is X=23..38, Y=14..23; current boundaries become west/east=16/28 and south/north=9/18, yielding west Evergreen X=17..22 and south Y=10..13 but no possible east band after center X=38 or north band after center Y=23. The same formulas are present in the archived original Vanilla/Warlords Hub, Ring and Wheel and survive all three current SAS descendants, establishing Firaxis provenance. Preferred repair is to compute east/north symmetrically from their distance to the far edge, e.g. `pineEastX = (W-1) - int(((W-1)-cenEastX) * 0.7)` and the analogous Y expression, while preserving west/south behavior. This is classified explicitly as a visual map-generation defect rather than a yield/gameplay-balance defect.
  Existing KI#284 also has a substantial Wheel consumer surface, but no duplicate issue is created. Wheel's custom regional generator repeatedly calls `shiftRegionPlots` with strip radius 3 (and 5 for the center). Across current Arena-through-SAS48 dimensions and the 2-18 region templates, many region/core/buffer/center rectangles are wide enough to pass the erroneous width-based Y-strip guard while shorter than the Y strip itself, exactly the shared `findBestRegionSplitY` condition already recorded under KI#284. The C118 audit therefore broadens KI#284's affected-consumer evidence to Wheel while retaining a single shared utility root.
  The remainder of Wheel was exhaustively rejected as additional current defects. Every Normal/Buffered region, buffer, 25%-inset core and center rectangle remains positive and inside the map across Arena through SAS48 and all supported 2-18 region counts; even the tight Arena Buffered cases have valid dimensions. Every derived template-center starting window remains nonempty after Wheel's two-plot edge clamp. The user-facing `2 areas/player - 10 players max` option is independently gated at generation time for <=10 players, so 11+ gracefully uses the normal one-area geometry rather than reading nonexistent buffer data. All six custom-option indices/defaults match their consumers; random bounds and line/spoke divisions are guarded/positive; no unbounded retry, simultaneous `CyMap.sPlot()` alias, raw stale world-size subtraction, malformed strict lookup or out-of-range regional compositor path survives. SAS also fixed stock Wheel's compact-player-ID assumption at birth by enumerating real alive IDs and using `playerTemplateByID`, so it does not reproduce KI#267. Later Wheel commits are visibility/stale-option diagnostics, strict lookup conversion, helper/world-size key migration, formatting/refactoring and calibration plumbing; none repairs KI#285-KI#287. Certified cursor advances through practical 5425 / 8993525573. Next runtime-source SHA: practical 5424 / 620880a0f5 (`Fix Tilted_Axis.py; move it to navalheavy maps`), intentionally left unopened at this checkpoint.

SAS-C119 - practical 5424 Tilted Axis import/adaptation + tilted-latitude contract audit - COMPLETE; NO NEW DEFECT
  Reviewed 1 runtime-source SHA: practical 5424 / 620880a0f5. The commit imports/adapts Firaxis/Sirian `PrivateMaps/Tilted_Axis.py`, adds SAS square world-size handling and moves the script from the land-heavy classification to naval-heavy. The 225-line birth file was reconstructed from the frozen local commit mirror and compared locally against the user's archived original Vanilla and Warlords Tilted Axis scripts plus current SAS source. The audit covered all custom-option/default consumers, square grid sizes and fractal exponents, permanent Y wrapping, plot shifting, terrain/feature latitude, bonus latitude legality, balanced-resource hooks and later helper/world-size descendants. No current defect was established.
  The SAS world-size adaptation is internally coherent. Arena uses the low-resolution `(6,6)` fractal exponent family already used by Duel/Tiny/Small, Standard through Huge use `(7,7)`, and SAS24/SAS32/SAS40/SAS48 intentionally reuse the Huge 25x25 square grid and Huge exponent profile through `sas_lookup_world_size()`. This is an explicit identity-preservation policy rather than accidental missing-key flattening of the kind recorded for Rainforest or Global Highlands. All actual plot dimensions remain square/even and positive, so the tilted latitude formulas' integer half-width divisions are exact.
  A Donut/Lagoon-style wrapped-plot/fixed-coordinate desynchronization was explicitly rejected. Tilted Axis generates its continent fractal with `FRAC_WRAP_Y` and may rotate the plot array along Y, but both custom climate generators intentionally derive latitude from X (`abs(width/2 - x)`). A Y seam shift therefore cannot move terrain/feature climate away from the intended east-west polar axis. Shared KI#284 is likewise inert for this script because every supported Tilted Axis map is square: the erroneous Y-strip guard and the correct height guard receive equal dimensions.
  Bonus latitude also follows the tilted axis correctly even though the Python script does not implement `isBonusIgnoreLatitude()`. Current AdvCiv `CvPlot::calculateLatitude()` explicitly detects the Tilted Axis topology (`!isWrapX() && isWrapY()`) and switches the latitude coordinate from Y to X before applying the map's top/bottom latitude range. `CvPlot::canHaveBonus()` uses that stored latitude for min/max bonus-latitude legality. Resources therefore obey the same east-west climate orientation as terrain/features rather than silently retaining north-south placement. This closes the last plausible inherited contract concern.
  The naval-heavy classification change is consistent with the generator rather than an option-dependent misclassification: the five Landmass Size choices alter continent grain/rifts (Massive Continents through Tiny Islands), while all use the same inherited `FractalWorld.generatePlotTypes()` water baseline. Massive Continents means larger connected land pieces, not a high land-percentage map. Later descendants add strict/stale-option diagnostics and enum-key/helper migration without changing these runtime contracts. Certified cursor advances through practical 5424 / 620880a0f5. Next runtime-source SHA: practical 5423 / 9d01f7598c (`Fix Team_Battleground.py; add it to almost_all_land maps`), intentionally left unopened at this checkpoint.


SAS-C120 - practical 5423 Team Battleground import/adaptation + circular-start/world-size audit - COMPLETE; KI#288-KI#291 FOUND; KI#267 BROADENED
  Reviewed 1 runtime-source SHA: practical 5423 / 9d01f7598c. The commit imports/adapts the roughly 830-line BTS/Ruff `PrivateMaps/Team_Battleground.py`, adds it to SAS's almost-all-land map family, replaces the stock six-tier grid logic with Arena/SAS-aware helpers and expands team enumeration to the live DLL team range. The birth file was reconstructed from the frozen local commit-diff mirror and compared locally against the user's archived original BTS Team Battleground, Vanilla/Warlords/community variants, every later Team Battleground descendant, current `SAS_WorldSizeUtils.py`, `CvMapGeneratorUtil.py`, and the current AdvCiv start-assignment/SPI callback path. No web source was needed; the supplied local map archive contains the relevant upstream controls.
  Existing KI#267 is broadened, without increasing the defect count, from the BTG Lagoon/Cross compact-ID family to an inherited map-script compact-player-ID/custom-start mapping root that also affects BTS/Ruff Team Battleground Round and Donut. `assignStartingPlots()` sets `player_num = countCivPlayersAlive()` and then constructs/shuffles literal player IDs `0..player_num-1` instead of enumerating the actual alive player IDs. With alive slots 0 and 2, for example, the direct circular assignment operates on slots 0 and 1, assigning a start to dead slot 1 while real player 2 is omitted. Current AdvCiv's StartingPositionIteration recognizes the partial script-set state; when the omitted real player's Round/Donut `findStartingPlot()` obtains its NULL start, converts (-1,-1) to an invalid plot index and returns no Python site, SPI reverts the provisional starts and C++ falls back to generic assignment. The game can therefore launch, but the intended Team Battleground circular/team ordering is discarded. This is the same conceptual root as KI#267: compact alive count is not a valid substitute for real sparse player IDs. Preferred repair is to build the shuffle from actual alive IDs across `MAX_CIV_PLAYERS` while preserving RNG ordering as closely as practical.
  Reconciliation note: the Lagoon/Cross and later Team Battleground repairs fix and validate those three KI#267 surfaces, while C121/C122 broaden the same root to Ring, Hub and Inland Sea. KI#267 therefore remains deferred overall rather than counted as fully fixed.
  KI#288 is a SAS-specific Team Battleground scalar world-size-calibration helper misuse introduced at practical 5423. `sas_lookup_world_size_with_calibrated_sas()` is a grid-size helper: when a SAS24/SAS32/SAS40/SAS48 key is absent, it takes the largest base-tier value and unpacks it as `(iAnchorWidth, iAnchorHeight)` before calibrating a two-dimensional grid. Team Battleground correctly passes tuple-valued square grids to that helper in `getGridSize()`, but the 5423 adaptation also passes scalar integer tables at three live sites: Four Corners bridge `shift`, Donut plot-generation `hole_radii`, and Donut terrain-generation `hole_radii`. Arena through Huge have explicit scalar keys and therefore appear fine; SAS24 through SAS48 miss the keys and reach the tuple-unpack path with Huge scalars 10 or 8, raising a Python TypeError. The same misuse exists in the reconstructed 5423 birth file (against the helper's then-current three-argument contract) and survives current source after the helper's API cleanup.
  KI#288 is directly reachable through supported custom-game combinations. SAS24+ Four Corners reaches the scalar bridge lookup during `generatePlotTypes()`; SAS24+ Donut reaches the hole-radius lookup during plot generation and, if generation is allowed to continue/fall back, the corresponding terrain lookup again. AdvCiv's failed Python plot-generation override can fall back to generic/all-land plot generation rather than necessarily aborting the process, but the requested Team Battleground geometry is lost and Python errors are produced. A current repository sweep found no other map passing scalar tables to `sas_lookup_world_size_with_calibrated_sas`; Team Battleground's three sites are the concrete misuse. Preferred repair is not to make the grid helper silently accept arbitrary scalars: use an explicit scalar world-size progression/calibrator or explicit SAS24-SAS48 scalar values appropriate to bridge/hole radius, so the intended physical scale can be reviewed independently of two-dimensional grid calibration.
  KI#289 is an inherited BTS/Ruff Team Battleground proximity-option collapse. The UI exposes three distinct `Team Setting` values: Start Together, Start Separated and Start Anywhere. For Round/Donut, however, only Start Together changes behavior: it re-sorts the already randomized player list by team. Start Separated and Start Anywhere both leave the same randomized list untouched, use the same circular coordinates, and `normalizeStartingPlotLocations()` suppresses default C++ rearrangement for both, making those two choices behaviorally identical. The same collapse also occurs for every Team Battleground shape when more than four teams are alive: `findStartingPlot()` returns true before consulting the proximity setting, and normalization is suppressed for both Separated and Anywhere. The exact Round/Donut code and option labels are present in the archived BTS source and survive current SAS. Preferred repair is to give Start Separated an explicit ordering/placement policy distinct from random Anywhere (for a circle, e.g. deliberately interleave teammates rather than merely declining the Together sort), and decide/document the corresponding >4-team policy rather than exposing two identical selections.
  KI#290 is an inherited BTS/Ruff Round/Donut angular-spacing bug caused by Civ4 Python 2 integer division. The source comment explicitly says starts are `uniformly spread around the circle`, but computes both `base_theta = 360 / player_num * dice.get(1000, ...) / 1000` and `theta = base_theta + playerLoop * 360 / player_num` with integer operands. `360 / player_num` therefore truncates whenever player count does not divide 360, and the base-angle division is truncated too. The first N-1 angular gaps use `floor(360/N)` degrees while the closing wrap gap absorbs the discarded remainder. This is already visible inside the original <=18-player contract (e.g. 11 players use ten 32-degree steps plus a 40-degree closing gap); SAS48 makes it severe (48 players use forty-seven 7-degree steps and one 31-degree gap instead of 7.5 degrees uniformly). The exact arithmetic is inherited from the archived BTS/Ruff script and survives current SAS. Preferred repair is to compute a floating step once, e.g. `fStep = 360.0 / player_num`, and use floating division for the randomized phase as well. This fixes fairness/symmetry but is intentionally not treated as the repair for KI#291 below.
  KI#291 is a separate SAS-specific 48-player compatibility regression in Round/Donut direct circular starts. The original BTS DLL was capped at 18 civ players, and exhaustive evaluation of all 1,000 possible base-angle rolls shows all 18 starting coordinates remain unique even on the same smallest 16x16 Duel circle. SAS expands the executable player capacity to 48 but leaves Team Battleground's radius-only direct `setStartingPlot()` algorithm without a capacity/uniqueness check or fallback. On current 16x16 Arena/Duel, seed-dependent duplicate coordinates begin at 25 players and all 1,000 base-angle rolls are duplicate by 32 players; at 48 players the current integer-angle algorithm produces only 30-34 unique coordinates. Current 20x20 Tiny is guaranteed duplicate at 48 (40-42 unique), and 24x24 Small duplicates in 429 of the 1,000 possible base-angle rolls at 48. Standard and larger have enough circumference for the current 48-player maximum. Recomputing the circles with a mathematically uniform floating angle confirms KI#291 is not merely KI#290's truncation symptom: 16x16 still has only 32-36 unique rounded coordinates for 48 players even with perfect 7.5-degree spacing.
  KI#291 survives the current C++ start pipeline. `CvPlayer::setStartingPlot()` does not reject a plot already used by another player. For ordinary high-count team games, AdvCiv's StartingPositionIteration deliberately bails when `players^2 > 36 * teams`; e.g. a 32-player two-team Arena/Duel game satisfies that large-team bailout, so SPI does not replace the duplicated direct starts. Team Battleground then suppresses normal team-start rearrangement for the normal 2-4-team proximity modes. Thus duplicate starting plots can reach turn 0 rather than being merely a transient Python assignment. Preferred repair is a circle-capacity/uniqueness guard: either choose distinct nearby circumference/ring plots, enlarge/adapt the radius/grid for the requested player count, or hand start placement back to the generic engine when unique handcrafted starts cannot be guaranteed. A floating angle from KI#290 should be applied too, but is insufficient by itself on Arena/Duel/Tiny.
  Existing KI#285 also affects Team Battleground's inherited `minStartingDistanceModifier() = -65`, but no duplicate finding is created here; current AdvCiv clamps that negative value to neutral until KI#285 is repaired. The Team audit also evaluated a possible StartingPositionIteration side-effect: SPI's discarded probe calls mutate `assignedPlayers` once before it bails on a Python-provided site, but that counter is used only modulo 2/4 to rotate the randomized separated-side/corner cycle. The extra increment changes phase but preserves the separation cycle, so no independent defect was established. Round/Donut's awkward actual-plot-coordinate writes into the smaller `HintedWorld` lattice were also traced: flat out-of-lattice writes safely return false, unset hints are randomized, and the terrain pass converts residual interior water to Plains/land; no independent topology/start failure was demonstrated. All custom-option indices, team-ID compaction for non-Round shapes, random bounds, Four Corners/Left-v-Right base rectangles and current square-grid dimensions were checked; no fifth new Team Battleground root survived.
  Later Team Battleground history consists of strict-lookup/stale-option diagnostics, helper import/API migration and formatting/refactoring; none repairs KI#267/KI#288-KI#291. Certified cursor advances through practical 5423 / 9d01f7598c. Next runtime-source SHA: practical 5422 / f80f6258fc (`Ring.py` import/adaptation), intentionally left unopened at this checkpoint.


SAS-C121 - practical 5422 Ring import/adaptation + shared buffered-template audit - COMPLETE; KI#292 FOUND; KI#267 BROADENED
  Reviewed 1 runtime-source SHA: practical 5422 / f80f6258fc. This checkpoint was reconciled from ChatGPT-5.6-Sol's final C120-backed ceiling transcript after its downloadable C121 packaging was interrupted, then checked against the current local Ring/Hub/Wheel source and Git identities with GPT-5.6-Sol. The audit covered Ring's SAS world-size helpers, ordinary and buffered player-region templates, cores and center regions, ring/line generation, starting windows, >18-player fallback, compact player-ID handling and shared Firaxis regional-map data.
  Existing KI#267 is broadened again, without increasing the defect count, to Ring. Ring creates a compact shuffled list of length `countCivPlayersEverAlive()` and later indexes it with the actual `playerID`. Current defensive bounds checks prevent an exception for a sparse/high-ID player, but merely reject Ring's custom validator and hand that player to generic fallback while compact low-ID players can retain handcrafted ring regions. This is the same root already established in Lagoon, Cross and Team Battleground: a compact alive-player position is not an actual `PlayerTypes` ID. The Lagoon/Cross and Team Battleground portions are fixed/validated, but Ring and the later Hub/Inland Sea surfaces remain deferred, so KI#267 remains deferred overall.
  KI#292 is an inherited Firaxis Hub/Ring/Wheel nine-player buffered-region boundary typo. All three current scripts share player-region entry `[0.5, 0.677, 0.8, 1.0]`, while its symmetric bottom counterpart ends at `0.667` and the adjacent neutral/buffer region begins at `0.667`. The upper player region therefore overlaps its intended adjacent buffer from X=0.667 through X=0.677 rather than meeting it at one boundary. On current grids the deterministic overlap is one X column by 11 plots on Large, 13 on Huge and 16/19/20/23 on SAS24/SAS32/SAS40/SAS48. Buffers generate first and player regions layer afterward, so the strip is overwritten by player-region generation instead of retaining the selected neutral/buffer treatment. The shared one-character surgical repair is `0.677` -> `0.667` in Hub, Ring and Wheel.
  KI#292 retrospectively corrects C118's Wheel conclusion that no fourth independent Wheel root survived. The typo became apparent only when Ring's symmetric player and buffer rectangles were compared exhaustively; recording that later discovery explicitly is preferable to leaving the earlier conclusion silently stale. Local current-source grep confirms the same `0.677` entry at `Hub.py`, `Ring.py` and `Wheel.py`, with the corresponding `0.667` symmetric/buffer boundaries in all three.
  Ring does not reproduce Wheel's SAS-specific KI#286 because its >18-player default-start fallback is checked consistently in assignment, `minStartingDistanceModifier()` and `findStartingPlot()`. It also avoids Team Battleground KI#288: Ring uses the calibrated helper only with `(width, height)` grid tuples, while scalar start-variance tables use the ordinary world-size lookup. Exhaustive Arena-through-SAS48 checks found all ordinary 2-18-player and buffered 2-10-player regions, cores, center regions and clamped starting windows positive and in bounds, including 18-player Arena and 10-player buffered Arena. Existing shared KI#284, KI#285 and KI#287 remain separate; no second new Ring-specific root was established.
  Certified cursor advances through practical 5422 / f80f6258fc as runtime-source SHA 605 / 1,317. Confirmed totals become 90 = 37 inherited + 53 SAS-specific. Next runtime-source SHA: practical 5421 / f1180bfc25 (`Fix Oasis.py`), intentionally left unopened at this checkpoint.

SAS-C122 - practical 5421 through 5406 map/import + worker-AI audit - COMPLETE; KI#293-KI#298 FOUND; KI#267 BROADENED
  Reviewed 16 runtime-source SHAs: practical 5421 / f1180bfc25, 5420 / 2424190053, 5419 / 9e72a66f59, 5418 / 32fe9da7ca, 5417 / 3f862dc357, 5416 / 61937cf0e2, 5415 / a32ad525be, 5414 / 7e70020bbb, 5413 / 74aed0721c, 5412 / 54778f0948, 5411 / a595618dd9, 5410 / d87fe00984, 5409 / 196f83d69c, 5408 / f6f4347655, 5407 / 8d9e5a8e38 and 5406 / f9604bf681. The audit used the local generated commit-diff mirror, current source, archived Firaxis/Warlords map scripts and current C++ map/start contracts; no web lookup was required.
  KI#293 is a SAS-specific Oasis compact-grid river-lane bounds regression. The inherited Warlords Oasis river generator uses fixed every-fourth-column Nile-style lanes plus helper probes at `x+1`, and its original world-size dimensions leave that required headroom. Practical 5421 introduces substantially smaller SAS grid sizes without adapting those hardcoded river bounds; practical 5448 later reduces the shared compact profile further. On several supported compact tiers the old final lane can reach the rightmost column even though the helper still probes the next column, and current Arena/Duel can additionally form a negative vertical random range in the retained lane setup. Release behavior can truncate/misplace intended river work while debug builds can assert on the invalid coordinate/range assumptions. Preferred repair is to derive/cap the river lane endpoints and random ranges from the actual plot width/height instead of retaining dimensions tuned to the Warlords grid table. The separate high-player Oasis starting-band crowding eventually hands individual players back to safe generic placement and was retained as a density limitation rather than a second defect.
  KI#294 is an inherited Firaxis/Warlords Mirror two-team special-case bug. The script first determines that the game contains exactly two equal teams, but its special assignment path then treats literal TeamTypes 0 and 1 as those two teams instead of using the actual alive team IDs. A legitimate setup whose two teams are, for example, IDs 2 and 3 therefore passes the equality test on the empty 0/1 slots and subsequently classifies every real player onto the same side. Depending on the randomized mirror branch, the routine can either iterate an empty counterpart list, assign zero starts and return Python success, or index the empty counterpart and raise. AdvCiv's `CvGame::assignStartingPlots()` returns immediately when the Python assignment callback succeeds, so the zero-assignment branch receives no later DLL fill-in pass. Mirror's ordinary three/four-team and generic paths already compact actual team IDs correctly; surgical repair is to construct/use the two actual alive TeamTypes in the special path rather than hardcoded 0/1.
  Practical 5419 Hub closes without a new defect but broadens existing KI#267. Hub, like Ring, builds a compact shuffled player-position list and later indexes it with a real `PlayerTypes` ID; sparse/high-ID players can therefore lose the handcrafted Hub region validator while compact low-ID players retain it. Hub's >18-player fallback is otherwise wired consistently, and exhaustive Arena-through-SAS48 checks kept ordinary/buffered regions, cores, center regions and starting windows positive and in bounds. Existing shared KI#284, KI#285, KI#287 and KI#292 are not recounted. Practical 5418 Maze also closes clean-current: compact-size cell arithmetic stays positive and generated writes remain bounded.
  Practical 5417 Lakes closes clean-current. A plausible fixed-pole/plot-shift candidate was rejected after tracing `shiftPlotTypes()`: the utility shifts Y only when the map wraps Y, so Flat/Cylindrical Lakes keep the deliberately forced polar water rows at the true north/south edges; Toroidal Lakes intentionally has no fixed polar seam. No Lakes-specific survivor was established in this commit.
  KI#295 is an inherited Firaxis/Warlords Inland Sea small-world start-region bug. The <=18-player custom layout forms each rectangular start window around a fractional template center and then clamps its bounds to a two-plot safety margin. In the inherited 17-player template, extreme 0.95/0.05 centers are already too close to the edge on original Warlords Duel 24x16 and Tiny 32x20 actual-plot grids: the safety clamp can produce `south > north`, making the player's predicate rectangle mathematically empty. For Duel four 17-player template slots are affected and for Tiny two are affected; SAS Arena adds still more affected slots but is not the provenance root. Repeated custom scans cannot find a plot in an empty rectangle, so those players abandon the intended Inland Sea ring region for generic/default placement. Surgical direction is to clamp/recenter the template center inside the safety envelope before constructing min/max bounds, guaranteeing every configured rectangle remains nonempty. Inland Sea also broadens KI#267 because its compact shuffled-player array is indexed by real player ID. A lone inherited `0.677` template asymmetry was quantified separately and causes only a small boundary-row difference on some larger sizes; no invalid region or independent gameplay failure was established, so it remains an audit note rather than another KI.
  Practicals 5415, 5414 and 5413 close clean-current. The map-heaviness helper migration preserves the intended built-in classifications; its exact-name matcher can rely on the normal bare `getMapScriptName()` contract even though an old comment suggested path/`.py` tolerance. Ice Age's SAS grid/fractal-grain adaptation remains positive on the smallest tiers; AdvCiv's `CvFractal` clamps the effective smoothing exponent when requested grain exceeds the available internal exponent. Practical 5413 centralizes the compact almost-all-land profile; its later 5448 size reduction is where KI#298 below becomes reachable for Highlands, but no separate zero-dimension or generic profile arithmetic failure is assigned here.
  KI#296 is a SAS-specific worker-AI stale partial-Farm-progress bug introduced at practical 5412. The new future-food adjustment counts every BFC plot with `getBuildProgress(eBuildFarm) > 0` as a Farm already being built by another worker and immediately credits the full future Farm food. Engine semantics do not make positive build progress an active commitment: interrupted build progress is intentionally retained for `DELAY_UNTIL_BUILD_DECAY = 8` turns before it even begins decaying. A worker can therefore start a Farm, be chased/cancelled/reassigned, and leave residual progress that suppresses other needed Farm choices for many turns based on food no worker is currently delivering. Later practical 5906 correctly fixes a separate 5412 error by replacing raw XML Farm food with plot/player-adjusted improvement yield, but it retains the positive-progress commitment test; later irrigation-chain logic also reuses that test to conclude another Farm is already fixing an irrigation source, broadening the same root. Preferred repair is a shared live-build-commitment test: require an actual same-owner worker/group on that plot with an active `MISSION_BUILD` for the Farm rather than mere residual build progress.
  Practical 5411's low-food-BFC heuristic has a real historical weakness but no current survivor: its birth calculation judges natural terrain food without the compensating food from visible bonuses and their improvements. Practical 5976 explicitly replaces that with the shared potential-food evaluator so bonus-specific improvement food offsets poor terrain. Removing the old `isFoodProduction()` exemptions remains coherent because Settler/Worker production pauses growth without removing the city's structural long-term food needs.
  Practicals 5410 through 5407 close clean-current. Great Plains' SAS grid/grain/maxshift tables were checked against current smallest-grid Mississippi/Ozark bounds, including the `x+1` river helper and the fixed-height Mississippi coordinate list; no overrun or current tag defect survived. Practical 5409's first shared world-size utility contains only SAS-prefixed constants/helpers at this point, so the later import-star callback leak has not yet been introduced; its old 52-player constant matches the then-used player-slot model rather than indexing a 48-civ-only array. Fantasy Realm's imported SAS size adaptation and aDebugMap's fixed tiny debug grid do not establish a current runtime defect; Fantasy's later malformed indentation belongs to the already-repaired 5452->5892 historical lineage.
  KI#297 is a SAS-specific Highlands climate-orientation regression introduced directly at practical 5406. Stock Warlords `beforeInit()` declares `global shiftMultiplier` before randomly assigning 0.0 or 1.0, producing the intended 50/50 north-cold versus south-cold orientation. Practical 5406 adds a safe module default but removes the `global` declaration from `beforeInit()`. In Python the randomized assignment is therefore function-local and discarded; every later latitude reader continues seeing module `shiftMultiplier = 0.0`, so current Highlands always uses the north-cold orientation even though it still consumes the RNG roll. No later Highlands commit touches this scope bug. Surgical repair is simply to restore `global shiftMultiplier` inside `beforeInit()`; read-only latitude callbacks need no declaration.
  KI#298 is a separate SAS-specific current Highlands start-capacity regression whose provenance is practical 5448, discovered retrospectively while closing the 5406 descendant. C103 had previously certified practical 5448's shared almost-all-land Arena reduction from 4x3 to 2x2 map-script cells (16x12 to 8x8 actual plots) without establishing a consumer-specific sizing invariant failure; the Highlands deep audit now supplies one and explicitly corrects that earlier conclusion. Current default Highlands makes water from a 5-percent lake fractal and Peaks from the 77th-percentile terrain threshold before custom start assignment. A source-exact replay of current `CvRandom` plus `CvFractal` over 1,000 legal RNG seeds on the 8x8 Arena grid produced only 23-47 non-water/non-Peak plots; every tested state has fewer than the 48 legal city sites needed by the supported 48-civ DLL. AdvCiv's found-value distance logic strongly penalizes already assigned starts rather than freely reusing them, so after legal sites are exhausted `CvMapGeneratorUtil.findStartingPlot()` returns `-1`; `CyMap.plotByIndex(-1)` wraps NULL, Highlands eventually calls `setStartingPlot(NULL, true)`, and the Python assignment callback still returns success, preventing a DLL rescue pass. Preferred repair is to enforce a Highlands/world-size/player-count capacity floor (larger grid or safe generic fallback before direct assignment) rather than allowing a supported 48-player Arena setup on an 8x8 mountainous/lake grid. This finding does not imply that every map sharing the compact profile is broken; only Highlands is promoted here because its current generator and assignment path were source-replayed and proven.
  Certified cursor advances through practical 5406 / f9604bf681 as runtime-source SHA 621 / 1,317. Confirmed totals become 96 = 39 inherited + 57 SAS-specific. Next runtime-source SHA: practical 5405 / d9e8051723 (war/stack withdrawal logic), intentionally left unopened at this checkpoint.


SAS-C123 - practical 5405 through 5375 war/UI/media/Timeline/chart-linking audit - COMPLETE; KI#299-KI#300 FOUND
  Reviewed 27 runtime-source SHAs: practical 5405 / d9e8051723, 5404 / feaaae6ce3, 5402 / b665b8f7c0, 5400 / 24cfc0803d, 5398 / 0d3a9071b1, 5397 / bdf38de46f, 5396 / 0ccd2d3265, 5395 / 118ca10d88, 5394 / 4679093282, 5393 / 2d0b157265, 5392 / 8662ed5667, 5391 / d2309b1018, 5390 / 7580bd828b, 5389 / e5b99b5c4b, 5388 / 177af35b51, 5387 / 159bb04ccc, 5386 / 75ff2b87ed, 5384 / 978e8252b3, 5383 / d2500dc401, 5382 / beac43db40, 5381 / 5102770120, 5380 / 02bdeb35bb, 5379 / 2a4e1ecc12, 5378 / 7503091323, 5377 / c13f5a3c61, 5376 / 8c59cab631 and 5375 / e653b9b2bb. Practical 5403, 5401, 5399 and 5385 have no primary runtime-source hunk and do not consume the denominator. The audit remained entirely local against the generated commit-diff mirror, current source/XML and current DLL contracts.
  Practicals 5405, 5404, 5402 and 5400 close clean-current. The ordinary attack-city minimum-nine stack threshold is an intentional XML-tunable readiness floor rather than a hidden control-flow failure; barbarian expeditions deliberately bypass it and later composition gates still run. The Main Interface production-bar bounds guard is valid historical hardening later strengthened by KI#162 descendants. Map-heaviness fallback classification executes only after starting plots exist, and persisting its two classification booleans avoids needless recomputation on load. The NO_PLAYER guards neutralize presentation/translator/graphics work only in the autoplay/no-active-human state that previously crashed.
  KI#299 is a SAS-specific Sevopedia media-state leak introduced at practical 5390. Opening a Sevopedia movie saves the user's `GRAPHICOPTION_NO_MOVIES` value and temporarily forces the option off; normal `closeMoviePlayer()` restores the saved value. The Movie-to-Music toggle, however, directly stops the media, closes the generic overlay and jumps to Music without calling the movie-specific close/restore path. A user who normally has No Movies enabled can therefore play a Pedia movie, toggle to Music, exit from Music and find the graphics preference silently left disabled for the rest of the session. Current descendants retain the exact bypass, and the only restoration site remains the movie-specific close path. Preferred surgical repair is to restore/clear the movie-scoped saved option before any transition out of Movie mode, ideally through one shared helper used by both ordinary close and Movie-to-Music.
  The surrounding 5398-through-5386 media lineage otherwise closes clean-current. Queue/group header rows are presentation-only and not selectable media actions, so inserted headers do not shift playable track routing. A surviving duplicate `setCurrentLabel()` writes the same label twice and is redundant rather than semantically wrong. The generic `playGeneralSound()` fallback lacks a stoppable returned handle, but no ordinary supported media-script path was established where valid direct 2D/3D playback fails while that fallback alone succeeds; it remains an audit note rather than another defect. Practical 5380's script/ID label enrichment preserves the packed playback key and stored track tuple. Practicals 5379-through-5375 add Civilization/Leader/Era Music parsing and redirects while preserving the underlying packed IDs; no current wrong-entry or selection-index mismatch was established.
  Practical 5384 is a broad Main Interface revamp but closes clean-current. It did contain a real historical production-detail unbound-local regression: the initial fallback sat outside the non-process branch even though the locals it read were created only inside that branch. Practical 5500 moves the fallback inward, a repair already documented from the newer side at C080 and preserved currently. Current building-filter partitions, pre-sized building-table row emission, process-production handling and research/production widget routing remain coherent; KI#101's obsolete-building Great-Person-rate display matches the DLL total.
  Practical 5383 closes as the already-known KI#100 repair lineage. Its null selection-list/group/plot guards preserve independent control actions before requiring a selected unit, make mission/command/path queries fail safely instead of dereferencing missing state, and skip only a cosmetic RiseFall center-unit refresh when the group has no plot. No new current side effect was established.
  KI#300 is a SAS-specific Info Screen History/Timeline spoiler introduced at practical 5382. Religion founding stores a replay major-event message containing the real holy-city name even for observers whose live notification correctly says the religion was founded in an unknown/distant city. Timeline deliberately allows that globally known religion event on a hidden plot and tries to sanitize it by replacing the *current* city object's current name with `Unknown City`. If the holy city is razed before the observer ever reveals the plot, `pPlot.getPlotCity()` is NULL; the sanitizer therefore has no name to replace and renders the old replay text verbatim, revealing a city name the observer never legitimately learned. The ordinary raze message does not rescue the knowledge contract: the live raze notification is sent only to teams that reveal the city, and Timeline suppresses an ordinary raze replay on a hidden plot. Later 5859 fixes a separate birth-cache freshness weakness by keying Timeline cache on game turn, but no descendant changes this historical-name masking assumption. Preferred repair is to sanitize/reconstruct an allowed hidden religion-founded line independently of current city existence/name rather than searching the historical replay text for `pPlot.getPlotCity().getName()`.
  Practical 5381's shared in-category chart helpers close clean-current. Ranking-bar arithmetic and Leader/Civ/Tech widget routing are mechanically equivalent to the per-page implementations. The new 16-pixel minimum icon-column width can be wider than the old Improvement-specific 10-pixel floor in unusually dense birth layouts, but later 5571/5573 deliberately replace those dynamic capacities with fixed layout-budgeted icon columns; current SAS therefore retains no 5381 overflow root.
  A separate current Leader-hover defect was source-proven while checking practical 5376 but is intentionally held unnumbered and uncounted until its actual birth commit is reached. Several Sevopedia Leader jump buttons pass literal `data2 = 1`; clicking ignores that field, but `CvDLLWidgetData::parseLeaderHelp()` casts it to `CivilizationTypes` and `CvGameTextMgr::parseLeaderTraits()` uses that civilization to substitute trait-affected units/buildings. CivilizationTypes 1 is current Arabia. Current XML therefore gives concrete wrong hovers for leaders whose actual civilization replaces a fast-production class, e.g. Shaka can show ordinary Barracks instead of the Zulu Ikhanda, Pacal ordinary Worker instead of Maya Quick Worker, and Charlemagne/Peter ordinary Courthouse instead of their UBs. Local history traces the first SAS introduction of this hardcoded convention to practical 4732, which is still outside the certified cursor; practical 5376 merely adds another occurrence in Music. Preserve this as a proven prospective root and assign/count it only when 4732 itself is audited, so the certified-through accounting is not contaminated by an unopened older SHA.
  Certified cursor advances through practical 5375 / e653b9b2bb as runtime-source SHA 648 / 1,317. Confirmed totals become 98 = 39 inherited + 59 SAS-specific. Next runtime-source SHA: practical 5374 / 06c01a7e58 (broad Era/schema/Sevopedia/UI change), intentionally left unopened at this checkpoint.

SAS-C124 - practical 5374 through 5350 Era/chart/media/grouping/Build UI refactor audit - COMPLETE; clean-current
  Reviewed 24 runtime-source SHAs: practical 5374 / 06c01a7e58, 5373 / 42b5a7f496, 5372 / 6facedf5dd, 5371 / 3d62f3ca63, 5370 / b6956fe7a1, 5369 / f280d07818, 5368 / 2daca4bca3, 5367 / 3ff6d3ceab, 5366 / 6c796d9df8, 5365 / 0bb4e939b3, 5364 / 53469ab261, 5363 / 0dcd8437fd, 5362 / b1773e07c6, 5361 / f03be46689, 5360 / b5edaf3616, 5359 / c9f76b0ea6, 5357 / fedd1d40b5, 5356 / 0e20b8f04d, 5355 / 42c83a5ce8, 5354 / 6886e4c7e4, 5353 / d12cdf8d7e, 5352 / 37caed0b06, 5351 / 59643efc94 and 5350 / 22723a30cc. Practical 5358 has no primary runtime-source hunk and does not consume the denominator. This tranche remained entirely local against the generated commit-diff mirror plus current source/XML; no internet lookup was needed.
  Practicals 5374 and 5373 close clean-current. Era `<Button>` storage uses the already-supported CvInfoBase/CvXMLInfo Button field and the new XML entries reproduce the prior hardcoded era art paths, including the deliberate Ancient/Classical shared image and Future/Modern shared image of that historical state. The Starting/Untradeable Tech chart cache cannot be skipped by a first direct Tech hyperlink: non-main `pediaJump()` shows/builds the destination category before the item screen, and the Tech list generation performs the one-time statistics precompute. Current chart values and combination counts remain derived from the actual Civ/Tech infos rather than a stale parallel table.
  Practical 5372's Movie/Music mouse-keyboard handling closes clean-current. Escape/Return/Numpad Enter and overlay exit events can redundantly reach close logic on some UI routes, but both media close callbacks are state-guarded and a second event becomes a no-op; group/queue presentation rows are not selectable media actions. Practicals 5371 and 5370 have no executable runtime token change after stripping their comment/whitespace/text cleanup from the runtime hunks; 5370's actual user-facing fix is XML localization.
  Practicals 5369 through 5362 are a clean-current Sevopedia ownership/grouping refactor lineage. Moving `gc`/`localText` to helper-module scope caches live wrappers rather than early XML-derived values; all moved grouping/cache call signatures reconcile with current callers; and 5364 correctly stops re-sorting base lists already sorted by SevoPediaMain. There is one real but fully historical regression inside the lineage: 5365 changed the moved Leader Music calls from the correct exposed `getDiploPeaceIntroMusicScriptIds`/`getDiploPeaceMusicScriptIds`/war equivalents to wrong old getter names, temporarily breaking those entries; 5367 explicitly restores all four correct getters. The simultaneous `> 0` -> `!= -1` ID test is an improvement because audio script ID zero is not semantically invalid. No current KI is counted from this repaired two-commit detour.
  Practicals 5361 and 5360 close clean-current. The Tech Chooser's mode-2 right-side reservation repair restores the intended intermediate width instead of making mode 2 behave like max-width mode 3. The optional city-selected plot-list early return happens only after the common hide/update pass, so it does not strand stale unit buttons/arrows. Military/Pedia define caches are initialized from normal screen constructors after XML initialization. The Main Interface religion/corporation rework deliberately left-anchors the organization row while retaining existing wrap/capacity placement; its numerous ID/art/text lookups merely move into `initState()` and do not change the selected values.
  Practical 5359, birth of Sevopedia Music, closes clean-current after tracing its later descendants. The added CvEraInfo soundtrack-name array is initialized, sized with the existing soundtrack array, bounds-asserted on access and deleted with the EraInfo lifetime. Packed Music keys and their side arrays preserve the selected Tech/Era/Leader/script identities. Several real rough edges are historical-only and already repaired later: birth Leader music rejected possible script ID zero with `> 0`; raw relative Audio2D/3D XML opens were later hardened; and later 5894/5907 repair 3D preview namespace/volume behavior. The previously audited no-handle `playGeneralSound()` fallback remains only an unproven safety-net asymmetry, not another source-proven survivor.
  Practicals 5357 and 5356, birth of Sevopedia Movies and optional Era-chart buttons, close clean-current. At Movie birth `GRAPHICOPTION_NO_MOVIES` is saved and restored by the only supported open/close lifecycle; internal Replay/Next/Movie-to-Music transitions do not exist yet, so KI#299 correctly remains a later 5390 regression rather than being backdated here. The historical Era-chart header play shortcut deliberately targets the Classical era entry and is later replaced; per-era buttons preserve their actual packed era IDs. No current wrong-target path survives.
  Practicals 5355 and 5354 close clean-current. Improvement Leader weights come directly from each live LeaderHeadInfo's `getImprovementWeightModifier`, with actual leader-to-civilization mapping used for clickable leader cells; the session cache is safe because those XML infos do not mutate during play. Water(Food)/Water(Other) grouping tests real bonus-specific food yield and current XML matches the intended Fishing/Whaling versus Offshore split. The historical era-order dictionary cannot ambiguously overwrite one current improvement with a second creating Build: current CIV4BuildInfos has at most one Build per improvement. Growth-chain ordering is bounded by a visited set and current chains are acyclic.
  Practicals 5353 and 5352 also close clean-current. The Build-page FeatureStruct redesign moves feature-specific prerequisite/time/production/remove information into per-feature panels and keeps Build prerequisites, created Improvement/Route, capable Units, build time/cost/kill and Civilopedia information represented. `BuildInfo.getHelp()` stops being displayed explicitly here, but current Help strings are generic order descriptions (for example instructing a worker to construct the already-shown improvement or clear the already-shown feature), while the redesigned page reconstructs those gameplay facts directly; no unique current rule loss was established. Current Builds contain at most three FeatureStruct entries, so the equal-width feature panels remain within the intended layout. The Leader count/% helper in 5352 reuses the same playable/excluded leader mapping as the surrounding Trait/Leader pages and integer-percent presentation is intentional.
  Practical 5351's apparent C++ gameplay touches are comment/text cleanup only. Its World Size `Ratio to Largest` calculation uses the last WorldInfo as denominator; current WorldInfo order is monotonically increasing from Arena through SAS48 (14x10 through 99x70), so SAS48 is indeed the current largest and the displayed ratios are correct. Practical 5350 then centralizes chart constants/beautification/base-5 invisible sort keys/chunking/Tech-list formatting. Comparison against the four removed per-chart implementations shows equivalent live inputs: Handicap Tech lists are TECH_* values and retain beautification/abbreviation; Era inputs remain valid; chart sort-key group/row ranges stay below their encoded capacities in current tables. No current data, widget-routing or sorting regression was established.
  Bookkeeping correction: one conversational progress update during this tranche informally said 667/1,317 through practical 5354. That was an off-by-one mental tally. The frozen inventory is SHA-based, and there are exactly 20 runtime-source SHAs from C123's next 5374 through 5354 inclusive, so the correct semantic count there was 668. C124 is generated from the exact C123 bytes and advances by exactly 24 runtime-source SHAs to 672; no earlier frozen checkpoint counter is changed.
  Certified cursor advances through practical 5350 / 22723a30cc as runtime-source SHA 672 / 1,317. Confirmed totals remain 98 = 39 inherited + 59 SAS-specific; this tranche adds no new current defect. Next runtime-source SHA: practical 5349 / 67cdf2fade (Trait-chart clickable leaders plus mixed Tech Chooser/C++ cleanup), intentionally left unopened at this checkpoint.

SAS-C125 - practical 5349 through 5331 Trait/Build/Terrain/Tech/Specialist Sevopedia audit - COMPLETE; KI#301-KI#303 confirmed
  Reviewed 19 runtime-source SHAs: practical 5349 / 67cdf2fade, 5348 / 46c5df4319, 5347 / 78dbfab4b2, 5346 / efa7dbfb6c, 5345 / 44f109a5ab, 5344 / 7fa2d16180, 5343 / 4a015ac5be, 5342 / 2b99464462, 5341 / a44b7c6997, 5340 / 0f715f76fc, 5339 / e84d79c0cf, 5338 / 3eaae094a9, 5337 / fb9054847f, 5336 / d0c8fa479d, 5335 / 4a72a7877e, 5334 / 00689f2daf, 5333 / 419e28e4b0, 5332 / 8340dcb4d9 and 5331 / d0360f3c47. This tranche was audited locally against the tracked commit-diff mirror plus current source/XML; no internet lookup was needed.
  Practicals 5349 through 5345 close clean-current. Trait statistics deliberately iterate every LeaderHeadInfo, but current data contains no hidden trait-bearing pseudo-leader that would contaminate counts: LEADER_DEFAULTS has no traits, and LEADER_BARBARIAN also contributes none. The clickable leader-to-civilization map therefore changes navigation only, not the statistics population. The removed CONCEPT_TRAIT_* wrappers are lossless for current content: all ten old Civilopedia keys are exactly the corresponding CvTraitInfo Civilopedia keys, current trait descriptions are unique, and literal-link routing therefore remains unambiguous. A separate Leader-hover data2/civilization defect remains source-proven but intentionally unnumbered here because local history places its birth at practical 4732; later copies such as 5376 do not justify backdating/counting it before that SHA is audited.
  Practicals 5344 through 5341 also close clean-current. Current SAS has exactly two RouteInfos, Road and Railroad, and exactly one Build producing each, so replacing the old Road/Railroad concept links with Build links is deterministic. The first Build-index attempt was deliberately reverted at 5342 and immediately reintroduced at 5343 with the stable selectable-table row-to-Build mapping that survives today. Practical 5341 is overwhelmingly text consolidation: a mechanical tag comparison found 591 removed XML text-tag occurrences versus 590 added, with the sole removed-only key already inside an XML comment and its only Python reference commented out. No live localization key is lost.
  Practical 5340 introduces KI#301, a SAS-specific Build-to-standard-Civilopedia namespace regression. `pediaJumpToBuild()` correctly enters the custom Builds category while Sevopedia is enabled, but its supported non-Sevopedia fallback calls `pediaJump(PEDIA_IMPROVEMENT, argsList[0], ...)` with the raw BuildTypes ID. BuildTypes and ImprovementTypes are independent numeric namespaces: current examples include Build 0 = Road while Improvement 0 = Land Worked, and Build 18 = Remove Jungle while Improvement 18 = Cottage. Practical 5508 later repairs ordinary improvement-producing Tech-Chooser Builds by restoring the engine's native improvement widget, but deliberately keeps `pediaJumpToBuild()` for non-improvement Builds. Consequently current Road, Railroad, Remove Jungle, Remove Forest and Scrub Fallout icons still open unrelated Improvement pages when the user disables Sevopedia through the supported BUG option. Preferred surgical repair: map each non-improvement Build to a meaningful standard-Pedia destination when one exists (Route/Feature-equivalent concept/page), or safely decline the unsupported standard-Pedia jump rather than reinterpreting a Build ID as an Improvement ID.
  Practical 5339 is repaired historical lineage rather than a current root. Its first Hills Improvements formula omitted several engine-validity routes; practical 5938 / KI#153 later expands the current Hills calculation to include the missing real-terrain, hill, bonus and related validity paths. Practical 5338 nevertheless contains independent KI#302 in Terrain `placeBuildUnits()`: that sibling panel reconstructs valid improvement Builds only through `ImprovementInfo.getTerrainMakesValid(terrain)` instead of using the broader validity routes represented by the Improvements panel. Current Hills therefore shows hill-valid improvements such as Mine/Windmill while the Units (Any Build) panel can disappear despite Worker variants being able to build them; current Coast/Ocean likewise shows `bWater` improvements while omitting Workboats that can perform Fishing Boats, Whaling Boats and Offshore Platform Builds. The Feature-side analogous code is incomplete in the abstract for combined improvement-plus-feature-removal actions, but current XML gives every removable feature a dedicated removal Build and the same current Worker variants can perform those dedicated actions, so no present visible Feature-unit omission was established. KI#302 is one shared incomplete-validity root, not separate hill/water defects. Preferred repair: derive Terrain build/unit relevance from the same complete improvement-validity helper/predicate used by the Improvements panel rather than maintaining a second partial formula.
  Practicals 5337 through 5332 close clean-current. Unit Obsolete-With and Tech Obsoletes use the real obsolete-tech field and the click-routing fixes preserve their intended destinations. Tech First-to-Discover is data-complete in current XML: each live first-discovery technology has at most one reward family. The expanded Enables logic intentionally consults a Build's feature-specific tech only when that Build lacks its own global prerequisite; e.g. Agriculture enables Farm itself while later feature-techs merely enable clearing Forest/Jungle during that already-available Build, whereas standalone Remove Forest/Jungle/Fallout Builds need their FeatureStruct prerequisite to identify their unlock. Distinct removal art and 5335's C++ right-click routing correctly prefer a created Improvement and fall back to a removed Feature only for pure-removal Builds. The inherited vertical obsolete-text formatting change is presentation-only.
  Practical 5331 introduces KI#303, a SAS-specific omission in the new Specialist `Extra Yields` panel. The page enumerates Civic and Tech `SpecialistExtraCommerce` plus Building per-specialist `SpecialistYieldChange`, but omits Building `SpecialistExtraCommerces`. This field is gameplay-active and directly matches the panel's scope: `CvPlayer::processBuilding` calls `changeSpecialistExtraCommerce(c, kBuilding.getSpecialistExtraCommerce(c) * iChange)`, `CvPlayer::specialistCommerce()` adds that player-wide value to every specialist's base commerce, and city specialist totals consume that routine. Current XML contains four live examples: Great Library (+1 research per specialist), Temple of Kukulcan (+1 culture), Natya Shastra (+1 culture) and Rock 'n Roll (+2 culture). Current `SevoPediaSpecialist.placeYields()` still scans only Building `getSpecialistYieldChange(self.iSpecialist, yield)`, so those building effects are absent from every affected Specialist page. The Building `SpecialistExtraCommerce` enum map is not currently exposed through Boost.Python, unlike Civic/Tech extra commerce and Building specialist-yield change, so the surgical fix requires a tiny CvBuildingInfo Python getter exposure (or equivalent helper) and Building-commerce rows in `placeYields()`. The Extra Slots side otherwise closes current-safe: Building specialist slots/free specialists and unlimited-specialist Civics are represented, and practical 5692 later adds the VoteSource-specific free-specialist source that was absent at 5331 and remains present today. Outside SevoPediaSpecialist, a mechanical runtime-Python comparison of 5331 found only authorship/header comments plus one whitespace-only arithmetic-expression change; the commit's broad XML/doc churn is non-runtime for this source phase.
  Certified cursor advances through practical 5331 / d0360f3c47 as runtime-source SHA 691 / 1,317. Confirmed totals become 101 = 39 inherited + 62 SAS-specific. Next runtime-source SHA: practical 5330 / 56f93e51f4 (`scrap tolerance beyond unit support costs`), intentionally left unopened at this checkpoint.

SAS-C126 - practical 5330 through 5312 scrapping/fallback/history/found-value/Era-chart audit - COMPLETE; KI#304 confirmed
  Reviewed 15 runtime-source SHAs: practical 5330 / 56f93e51f4, 5329 / 8fb9a5bb93, 5326 / 70a2966dbf, 5325 / 0fcb14ed50, 5324 / 6fc45fa0b9, 5322 / 044d26e918, 5321 / 6bd3873c83, 5320 / 2046519feb, 5319 / 2f0b12f754, 5318 / 21f67530d5, 5317 / a134450486, 5316 / 78d91cee6e, 5314 / e8659a5e4c, 5313 / c4ce1e7796 and 5312 / 0d426eaa1b. Practical-number gaps in this range are non-runtime-source/XML/docs-only commits in the frozen denominator. The tranche was audited locally against current source/XML plus the tracked historical diffs; no internet lookup was needed.
  Practicals 5330 and 5329 close clean-current. The new era-scaled extra-unit scrapping tolerance converts the AI's existing recommended unit-cost-per-military ratio into an approximate extra allowance; free support and marginal unit costs mean it is not an exact physical-unit count, but the surrounding K-Mod/SAS contract is itself an approximate spending guideline rather than a hard cap, and no source contradiction requires literal N-unit equivalence. The ObsoleteTech toggles are applied coherently to AI scrapping eligibility/value. Practical 5329 centralizes two fallback-unit selectors while preserving their caller-specific policy through parameters (offense/defense scope, current-unit skip, siege caps and fallback classes). Its one semantic cleanup is beneficial: the old AI_chooseUnit path could let an enough-Trebuchets cap veto all siege, whereas the shared helper limits that cap to Trebuchet-like candidates while retaining the independent total-siege cap.
  Practicals 5326 through 5324 are repaired historical lineage, not a current defect. Practical 5324 temporarily moved Unit ObsoleteTech reading into the wrong XML reader position relative to the TechTypes child traversal; practical 5326 explicitly restores the safe sibling-field read and removes the older sprawling very-cheap-unit production caps as ObsoleteTech becomes the cleaner mechanism. Practical 5325's runtime-source edits are comment/text cleanup. Practicals 5322 through 5316 then close clean-current: 5322 fixes Sevopedia Back by comparing against the latest history entry instead of the oldest; 5321's deterministic hammer-cost-heavy disband ordering is intentional policy and remains arithmetically safe for current data; 5320's World Size W/H ratio guards invalid dimensions and uses explicit floating division; 5319 centralizes the deliberately inverted BUG-option semantics so affected Technology/Religion Advisor branches remain consistent; 5318's remembered last Pedia jump complements rather than conflicts with the later back-stack repair; 5317's ObsoleteTech birth is initialized/serialized/read/exposed/enforced coherently; 5316 is runtime-source comment/message cleanup only.
  Practical 5314 introduces KI#304, a SAS-specific partial signed-16-bit-to-int migration in the found-value pipeline. The commit correctly widens CitySiteEvaluator/AIFoundValue returns, CvPlot's per-player found-value cache, CvArea best-found storage, CvPlayerAI locals/return type and StartingPositionIteration maps/vectors to int, and current source retains those widened types. It also changes CvPythonCaller::AI_foundValue's declared return type to int, but its body still preserves the old `FAssert(lResult <= MAX_SHORT)` and `return safeIntCast<short>(lResult)`. `safeIntCast<short>` is assertion-checked but non-clamping: debug builds assert above signed-short range and release builds still perform the narrowing conversion. Therefore an enabled official `USE_GET_CITY_FOUND_VALUE_CALLBACK` returning, for example, 40000 can feed a wrapped/narrowed value back into the otherwise-int pipeline instead of 40000. The callback is disabled by default, making normal stock SAS unaffected, but PythonCallbackDefines/BUG still support and register this configuration and no source contract marks it unsupported. Whole-current-source inspection found no second surviving found-value `short` container, so this is a narrow missed conversion rather than a broader incomplete migration. Preferred surgical repair: remove the obsolete MAX_SHORT assertion/short cast and return the Python numeric result through an int conversion consistent with the surrounding CvPythonCaller helpers.
  Practicals 5313 and 5312 close clean-current. 5313 is Leader-page presentation/text spacing. Practical 5312 adds the Era Chart and exposes the additional CvEraInfo fields needed by it; every chart getter maps to a real EraInfo member, current era/tech indices are bounds-checked, tech membership uses each TechInfo's real era, and later Era-chart descendants are layout/media/shared-helper changes rather than repairs to a wrong row formula. Its simultaneous Civilopedia trait formatting passes a dedicated bCivilopediaText flag so free-promotion unit-combat applicability is folded onto one line while non-Civilopedia help retains the old bullet form; the underlying trait/promotion relation is unchanged. No additional current defect was established.
  Certified cursor advances through practical 5312 / 0d426eaa1b as runtime-source SHA 706 / 1,317. Confirmed totals become 102 = 39 inherited + 63 SAS-specific. Next runtime-source SHA: practical 5311 / 77dc1e64a7 (birth of Arena/SAS24-SAS48 world sizes and related 48-civilization/map-size adaptation), intentionally left unopened at this checkpoint.

SAS-C127 - practical 5311 through 5291 world-size/GameSpeed/Handicap/index/category audit - COMPLETE; KI#305 confirmed
  Reviewed 20 runtime-source SHAs: practical 5311 / 77dc1e64a7, 5310 / ba5517013b, 5309 / c521adea07, 5308 / 8ecf997835, 5307 / c81414cc08, 5306 / 9fd0575c0c, 5305 / c70b01c8a2, 5304 / b9c21a28c8, 5303 / 6901fd1796, 5302 / fd1e7dda81, 5301 / 1f7c8fab22, 5300 / bebac73dcf, 5299 / 506070bea1, 5298 / aca1924815, 5297 / 6fc9cd7a6f, 5296 / 855d664a31, 5294 / e89473e189, 5293 / a7c677fe62, 5292 / de6ad59ae1 and 5291 / 55a2425f1e. Practical 5295 and 5290 are non-runtime-source commits in the frozen denominator. The tranche was audited entirely from the local commit-diff mirror/current source/XML; no internet lookup was needed.
  Practical 5311 is the historical birth of Arena/SAS24-SAS48 plus the first 48-civilization/world-size adaptation. Its then-deliberate decision to add five XML WorldInfos without extending the fixed WorldSizeTypes alignment caused real broad map-script incompatibility, explaining much of the later map repair run. That architecture is historical-only today: later script-specific repairs and especially practical 5971 align the world-size enum/XML/helper model. It is not re-counted as another umbrella KI because the surviving map defects have already been attributed to their precise roots.
  KI#305 is a SAS-specific World Size Chart unit/label defect. Practical 5307 creates the chart and labels `GridWidth * GridHeight` as `Grid Tiles`; practical 5311 then derives `Tiles Per Default Player` from the same product. `CvWorldInfo` grid dimensions are map/terrain-cell units, not the final number of playable `CvPlot`s: ordinary `CvMap::reset()` doubles both axes (4x plots), while custom map-script grid overrides can use a different scaling path. For current Standard, XML 39x28 / 8 default players is reported as roughly 136 `Tiles Per Default Player`, whereas the ordinary map becomes 78x56 = 4,368 playable plots, about 546 plots/player. The same root therefore affects both the 5307 `Grid Tiles` label and the 5311 per-player density row; one KI is used rather than manufacturing a second number. Preferred repair: either label the raw values explicitly as XML/map-script grid cells or convert through the actual map-dimension contract before calling them tiles/plots.
  Practicals 5310 through 5308 close clean-current. 5310 replaces fragile chart-icon sort prefixes with stable invisible ordering keys without changing displayed values. 5309 immediately repairs 5308's short-lived calendar display issue that suppressed December (`m12`) as though it were equivalent to the next January; current code hides only January `m1`. 5308's special-page cleanup helper is mechanically equivalent to the repeated search/item-list cleanup it replaces.
  Practicals 5306 through 5300 close clean-current around several repaired historical Game Speed Chart issues. Practical 5306 is mechanically documentation/comment/message cleanup: runtime Python/C++ executable tokens are unchanged apart from user-facing diagnostic string prose, and apparent XML data removals (e.g. the Internet project Button line) are quotations inside comments. Practical 5305 repairs the chart's high-year segment calculation. Practical 5304 is another large comment/docs cleanup with no executable runtime/XML data change established. Practical 5303 correctly fixes inherited Base AdvCiv `ExtraFreeOutsideUnits`: the field is a unit count added to free outside-unit supply, not a percentage, so the old missing-tag default of 100 produced an accidental +100 allowance; current default 0 plus explicit XML values are coherent. Practical 5302 is a data-preserving chart/icon/spec refactor. Practical 5301's Slow/Very Slow timeline rebalance is intentional data; an independent current XML sum confirms every speed Nitro through Very Slow has the advertised 165/250/330/500/750/1000/1500/2000 turns and reaches exactly year 2105 with zero leftover months. Practical 5300's Game Speed Chart C++/Boost.Python getter exposures map one-for-one to actual CvGameSpeedInfo fields; current chart coverage contains every scalar GameSpeedInfo XML field.
  Practicals 5299 through 5291 also close clean-current. 5299 refactors AI-Personality contact/memory aggregation while retaining the two-pass adjusted-value -> per-field min/max -> normalize-to-100 -> aggregate pipeline and improves one malformed diagnostic raise. 5298 centralizes Sevopedia category metadata; current registry has 35 unique category enums with no duplicate registrations. 5297's Index-as-category conversion genuinely repairs the inherited corporation-row bug by using the Corporation button instead of Religion's button, while its search/input state is later hardened without changing that routing. 5296 adds Nitro/Turbo/Slow/Very Slow: current source has exactly eight GameSpeedInfos and every current CultureLevelInfo contains exactly those same eight SpeedThreshold entries with no duplicates, while current source contains no fixed four-speed Python indexing assumption found in the audited paths. 5294/5293/5292/5291 are the Handicap Chart birth/binding/icon evolution. Mechanical current-data validation finds all 72 scalar HandicapInfo XML fields represented exactly once, every referenced getter exposed through Boost.Python, all live GoodyTypes represented by the grouped rows, and current FreeTech/AIFreeTech arrays empty as displayed. No current chart-data omission or misbinding was established.
  Certified cursor advances through practical 5291 / 55a2425f1e as runtime-source SHA 726 / 1,317. Confirmed totals become 103 = 39 inherited + 64 SAS-specific. Next runtime-source SHA: practical 5289 / cc0c5d1e29 (Victory Screen button-height repair), intentionally left unopened at this checkpoint.



SAS-C128 - practical 5289 through 5277 Victory/performance/AI-building-cache/Main-Interface/upgrade audit - COMPLETE; KI#306-KI#307 confirmed
  Reviewed 12 runtime-source SHAs: practical 5289 / cc0c5d1e29, 5288 / 347e62f704, 5287 / 121d7739ae, 5286 / f83dcc927d, 5285 / d8a03aaf84, 5284 / 8d9804971f, 5282 / 1f9b99fd95, 5281 / 3e072ae409, 5280 / b5809dd518, 5279 / a728524ed6, 5278 / 79f376ba1c and 5277 / dfb0c84894. Practical 5283 is non-runtime-source in the frozen denominator. The tranche was audited entirely from the local commit-diff mirror/current source; no internet lookup was needed.
  Practicals 5289 through 5284 close clean-current. 5289 is a Victory Screen table-button-height presentation repair. 5288, 5287, 5285 and 5284 normalize to comment/documentation/prose cleanup in runtime source. Practical 5286's low-power stack-attack ordering runs only after the group has already committed to an attack and retains the ordinary attackability/movement/can-enter filters before ranking bombard/collateral and expendable low-effective-strength/low-XP units. Later practical 6097 repairs the separate inherited sacrifice-fallback override recorded as KI#200; that later root is not re-counted here.
  Practical 5282 closes clean-current. Its CvCityAI substitutions cache the same owner/team objects as the replaced repeated calls (`kOwner.getTeam()` is the city's team); old suspicious expressions such as `GET_TEAM(getOwner())` that predate this commit are left untouched rather than introduced by the optimization and therefore remain for their older birth archaeology. Foreign Advisor cached text is explicitly keyed by `CyGame().getCurrentLanguage()` and rebuilt when the advisor is opened after a language change, while Sevopedia's newly reset selectable-index maps directly fix stale arrow-navigation state.
  KI#306 is a SAS-specific stale per-turn player-wide maintenance-ranking cache introduced by practical 5281 in `CvCityAI::AI_buildingValue`. The optimization caches the best/second-best city maintenance (`getMaintenanceTimes100`) and count above the government-center threshold under only `(player, game turn, number of cities)`. Civ4 processes a player's cities sequentially; inside each `CvCity::doTurn`, `doGrowth()` occurs before production choice, and `changePopulation`/`setPopulation` immediately calls `updateMaintenance()`. Consequently an earlier non-capital city can populate the Forbidden Palace/government-center maintenance cache, a later city can grow and change its maintenance without changing game turn or city count, and that later city's `AI_chooseProduction`/`AI_buildingValue` reuses stale empire rankings. Current source retains the same cache key and no later descendant adds invalidation. This can alter whether a city passes the high-maintenance government-center gate for that turn. Preferred repair is to invalidate from a maintenance-state revision/change, scope the cached ranking to an immutable evaluation snapshot, or simply recompute this small cross-city ranking rather than assuming all city maintenance is turn-immutable.
  Practical 5280 closes clean-current as the intended Willem -> Willem van Oranje text rename plus broad comment/prose cleanup; apparent removed XML art/data lines inspected in the diff are quotations inside comments rather than live tag removals.
  KI#307 is a SAS-specific Main Interface localization-cache invalidation defect introduced by practical 5279. Before this optimization, labels such as Strength, Air Strength, Movement, Range, Level, Experience, Maintenance and the BUG dead-civ tag were translated with `localText.getText(...)` when rendered. 5279 moves these translated strings into `CvMainInterface.initState()`. That function's own contract says it runs when the main interface is created after starting/loading a game or when Python modules are reloaded; it has no `getCurrentLanguage()` key or `LanguageChanged` handler. The in-game language option calls `CyGame().setCurrentLanguage(...)`, and current Main Interface still consumes the cached strings afterward. Other current advisors explicitly track `iLanguageLoaded` and rebuild localized caches, confirming live language changes are an expected UI lifecycle. Therefore changing language during a running game can leave these cached Main Interface labels in the previous language until the interface/Python state is recreated. Preferred repair: cache the current language alongside these strings and refresh them when it changes, or keep these few localized lookups dynamic.
  Practical 5278 is clean-current: its caches are language-independent symbol IDs/yield glyphs and the movement denominator, and the replacements preserve formatting/value semantics. Practical 5277's AI upgrade ROI gates also close clean-current as explicit tuning policy: affordability remains mandatory; the gates are AI-only and applied only when raw strength increases; upgrades without positive strength gain remain allowed for non-strength benefits; experienced units receive configured extra allowance; and later descendants tune the same ROI architecture rather than repair a broken call contract. The same commit temporarily changes the source `MAX_CIV_PLAYERS` setting from 48 back to 18 as build/configuration lineage; practical 5743 later restores the current 48-player source setting, so that toggle is historical rather than a current survivor.
  Certified cursor advances through practical 5277 / dfb0c84894 as runtime-source SHA 738 / 1,317. Confirmed totals become 105 = 39 inherited + 66 SAS-specific. Next runtime-source SHA: practical 5276 / f1309cd115 (BUG Help external-file-path repair), intentionally left unopened at this checkpoint.


SAS-C129 - practical 5276 through 5250 Victory/Info-Screen/search-navigation/predump transition audit - COMPLETE; no new defect
  Reviewed 27 runtime-source SHAs: practical 5276 / f1309cd115, 5275 / 9946feb2c0, 5274 / a71a79bf44, 5273 / f5214503d3, 5272 / d510e08afa, 5271 / 2ac9b5b71d, 5270 / 2e1df7d43e, 5269 / 8d11a29078, 5268 / 20b6addfb7, 5267 / edfcef1f0d, 5266 / 46c09243a5, 5265 / befbb28aa2, 5264 / 0be5966414, 5263 / 7b044ab447, 5262 / 884ddc0acc, 5261 / 92df58c1ad, 5260 / 172c871b75, 5259 / cd1292fe3f, 5258 / 4295623ff1, 5257 / 09e0c5100e, 5256 / 1b9d2c8d9e, 5255 / a9da325fd7, 5254 / e81ea035ab, 5253 / ed2b7c6586, 5252 / 10fd402eff, 5251 / e30d02cf8e and 5250 / ee22ac967d. The tranche was audited from the local historical-diff mirror/current source without internet lookup.
  Practicals 5276 through 5269 close clean-current. The Victory Screen's icon/text beautification and caching preserve the pre-existing known-player/team gates. Practical 5273 introduces an explicit `iLanguageLoaded`/`initText()` refresh path, so the localized strings cached by 5274 do not reproduce KI#307: opening the Victory Screen after `CyGame().setCurrentLanguage(...)` refreshes its labels. `getPlayerOnTeam()` still chooses the first player slot on a team and can therefore use an arbitrary/dead member as a representative leader icon in team games, but no hidden-information or gameplay consequence was established; this is retained only as a low-severity presentation audit note. Practical 5276's then-fixed BUG Help file-path behavior is later generalized by the current dynamic-path lineage; no present supported-install failure remains from this commit family.
  Practicals 5268 through 5260 also close clean-current as Info Screen presentation/widget evolution. The Top Cities/Wonders/Statistics changes preserve their original information-visibility gates: finished world-wonder identity was already public on this screen while owner/city names remain gated by revelation/knowledge, in-progress wonder entries require the active team or investigate-city espionage/debug access, and the zoom-city widget is attached only where that city visibility is legitimate. Practical 5261 uses the correct Civilopedia jump widget/data IDs, while 5260 moves the wonder icon to its own column and the zoom action to the city column without changing the underlying permission checks. The old Statistics tab affected by several of these commits is removed later at practical 5716, so its layout-only rough edges do not survive current SAS.
  Practical 5259's large Info Screen upscale/refactor closes as repaired historical lineage rather than a current defect. It initially hardcodes a 1920x1080 reference/bounds model and caches several screen-dependent positions, but practicals 5585-5590 move those calculations onto shared runtime advisor bounds and current `CvInfoScreen.py` obtains `X/Y/W/H` dynamically through `getAdvisorRuntimeBounds(...)`. Current Info Screen localization is likewise `iLanguageLoaded`/`initText()` aware. The Demographics/Top Cities/Wonders/Statistics data and widget hunks inspected in 5259 did not establish a surviving visibility, ranking or click-routing error.
  Practicals 5258, 5257 and 5256 close clean-current. 5258 is intentional Sheep/Pig terrain-balance XML; 5257 normalizes to documentation/comment/predump-reference prose rather than executable behavior; and 5256 correctly debounces the duplicated Backspace search event, an early member of the later Sevopedia keyboard-input hardening lineage.
  Practical 5255's AIP predumped-leader-cache optimization has a genuine maintenance weakness in its historical form but is repaired current lineage, not a new survivor. It adds the optional/default predumped cache path used to avoid expensive Leader-page recomputation. Later practical 5973 explicitly builds the AIP predump checker/reconstructor, detects stale predumped display tuples (including percent metadata drift), adds automated rewrite support and leads into CI freshness enforcement. No independent present cache-semantic defect was established in 5255; unrelated broad `except` style changes are not promoted as correctness roots.
  Practicals 5254 through 5252 close clean-current around Sevopedia Up/Down navigation. 5252 scans the current filtered non-header list, 5253 repairs focus behavior and 5254 precomputes selectable indices/item-to-position mappings. A theoretical risk from the current dictionary being keyed by item ID was investigated: duplicate selectable entity IDs could overwrite a position, but current grouping construction partitions rather than duplicates the entities examined (e.g. multi-improvement bonuses receive a combined group rather than repeated list rows), and no reachable duplicate-ID list was established. It therefore remains an unproven generalization concern rather than a KI.
  Practical 5251 simply renames the Military Advisor filter from `All Units` to `No UnitCombat`, matching the predicate it already implements. Practical 5250 changes the Play Now/new-game default for `GAMEOPTION_NO_EVENTS`; this is an explicit gameplay/configuration choice rather than a hidden source contradiction. Both close clean-current.
  Certified cursor advances through practical 5250 / ee22ac967d as runtime-source SHA 765 / 1,317. Confirmed totals remain 105 = 39 inherited + 66 SAS-specific. Next runtime-source SHA: practical 5249 / f4adfa8911, intentionally left unopened at this checkpoint; practical 5247 below it is the Sevopedia search-bar birth and should receive an independent input/filter/navigation audit.


SAS-C130 - practical 5249 through 5212 Sevopedia search/dynamic bonus-build/UI/grouping birth audit - COMPLETE; no new defect
  Reviewed 31 runtime-source SHAs: practical 5249 / f4adfa8911, 5248 / 7b0f94bf00, 5247 / 6c67df16f9, 5240 / b5ecf7a37a, 5239 / 04708e8fd1, 5238 / 3f6d0c0999, 5237 / 3811b18648, 5236 / 58c7a2158c, 5235 / 9d6e66d2a6, 5234 / e83cf66c8e, 5233 / 19b9ab29f0, 5232 / 2b692c7358, 5231 / 363224c004, 5230 / e50ca14599, 5229 / f184fae49f, 5228 / 1894f822cd, 5227 / 1fd378ad97, 5226 / 4a67cd2480, 5225 / f8f9ef6d2c, 5224 / fa70ede96e, 5222 / 703410efc1, 5221 / 26f0b4ffb4, 5220 / ad4d8f8cb6, 5219 / bba7b44dc0, 5218 / 2a2a43ee04, 5217 / 3f50f580c4, 5216 / fc1bcdb31c, 5215 / 4114793634, 5214 / 5e2c60c8fb, 5213 / d7d4ee5ed0 and 5212 / 957e7e202c. Non-runtime practicals between these commits (including merge/XML/docs-only steps) do not consume the frozen denominator. The tranche was audited from the local mirror/current source with descendant-history checks and no internet lookup.
  Practicals 5249 through 5247 close clean-current around the Sevopedia search-bar birth. The initial 5247 implementation filters names case-insensitively and correctly resets state on category/special-page transitions, but it had several short-lived input/list rough edges that are repaired lineage rather than current survivors. In particular, pediaJump originally selected a filtered table row using the original list index; 5248 immediately adds an original-list-index -> displayed-row map and preserves only headers/spacers belonging to groups with matches. Practical 5256 later adds Backspace to duplicate-character-event debounce and 5472 adds Space after empirical duplicate events were observed. Current search architecture additionally centralizes active-list refresh/navigation. Practical 5249's `except:` -> `except Exception:` around safe widget deletion does not alter ordinary screen-delete failure handling. No present search-state/filter/routing defect was established here.
  Practical 5240's dynamic `CvUnitAI::getBonusSpecificLandBuilds` replacement closes clean-current and is later superseded. At its historical data state the scan replaces a fragile manually maintained define list by iterating BuildInfo -> ImprovementInfo, excluding water improvements and mapping `isImprovementBonusTrade` bonuses; the then-current SAS data has one intended land resource improvement per bonus, with Oil's additional Offshore Platform correctly excluded by domain. Practical 5976 removes this CvUnitAI helper entirely in favor of current `CvPlot::SAS_getBonusSpecificBuild`, which handles both domains, excludes fort-like generic connectors and has explicit same-domain tie scoring. The same commit's 18 -> 48 `MAX_CIV_PLAYERS` change belongs to the already-traced build/config lineage later temporarily reversed and restored; it is not another current defect.
  Practicals 5239 through 5232 close clean-current. 5239 adds production-queue button images while preserving the existing `WIDGET_HELP_SELECTED` row/data contract. 5238 is Project-page presentation. 5237 adds icon buttons to Military/Sevopedia lists without changing entity IDs. 5236's `refreshUnitSelection` refactor preserves row ordering, unit/group selection data, active-team visibility checks and minimap flash semantics while caching repeated colors/player/team objects. 5235's CvUnitAI hunk is comment-only; its Building-page work makes the Great Person icon a proper pedia redirect. Practical 5233 genuinely repairs inherited Base AdvCiv Project-button help/redirect use of the wrong loop project ID, and 5232 repairs the Building requirement display that could render Ironworks as Coal+Coal rather than Coal+Iron; both fixes survive current source and therefore contribute repair lineage, not new current KIs.
  Practicals 5234 and 5231 through 5212 form the first broad Sevopedia grouping architecture and close clean-current after tracing the evolving predicates into the current centralized grouping helper. Several first-version weaknesses are explicitly repaired inside the lineage: 5214's Building-era grouping initially does not fully represent SpecialBuilding/religion-derived availability, while 5217 adds the missing effective-tech gates; 5224's first Bonus-by-improvement grouping is refined by 5225 so a bonus with multiple valid trade improvements is emitted once under a combined header instead of being duplicated or reduced to an arbitrary primary improvement; and Terrain/Feature grouping later recognizes that SAS graphical Hill/Peak TerrainInfos report `bWater`, explicitly excluding those high graphical pseudo-terrain IDs from the `true water` feature classification. Current helpers bucket each input entity once and preserve the already-sorted base-list order rather than re-sorting redundantly.
  The other grouping families are similarly coherent in current data. Techs partition directly by TechInfo era; Civics by their single CivicOptionType; Projects/Religions/Corporations by their effective prerequisite/founding-building era; Specialists by type; Improvements by mutually exclusive land/water/growth/bonus-capable/special-map rules. Unit and Building availability helpers use the latest era among all AND tech requirements and include prerequisite Building/SpecialBuilding/religion gates. Practical 5216 deliberately gives Executives direct prerequisite techs matching their corporation-building technology gates, so actual production availability and era grouping agree rather than requiring a display-only approximation. Practical 5212 also fixes a concept routing detail by retaining the original item ID in `data2` after `data1` is repurposed to a generic concept page type. No duplicate/missing current entity or wrong supported-era bucket survives from this birth sequence.
  Certified cursor advances through practical 5212 / 957e7e202c as runtime-source SHA 796 / 1,317. Confirmed totals remain 105 = 39 inherited + 66 SAS-specific. Next runtime-source SHA: practical 5211 / 90da888635 (`code cleanup and revert some ruff changes to be safe...`), intentionally left unopened because it touches 41 runtime Python files and should first be normalized mechanically before semantic inspection.

SAS-C131 - practical 5211 through 5180 cleanup/performance/AI-building/tech-tree/advisor/City-Screen audit - COMPLETE; KI#308 confirmed
  Reviewed 30 runtime-source SHAs: practical 5211 / 90da888635, 5210 / 39914c9b3d, 5209 / a2ec040e6b, 5207 / e13f6d6b2c, 5206 / 3d8108edcc, 5205 / ebaa95ce16, 5204 / c28e40dd39, 5203 / 8a13288983, 5202 / 66401dd016, 5201 / 939296900e, 5200 / 74a24578a7, 5199 / cc3e2951cd, 5198 / 42edcfeb82, 5197 / c277ad1928, 5196 / 9c01aa6e3f, 5195 / c87b2e7bc1, 5194 / 83462bd90c, 5193 / 905b71dc7e, 5192 / b9d4edfdac, 5191 / 1aeb7b55b9, 5190 / 67fa5064a8, 5189 / 80c5610110, 5187 / d97c6dd19f, 5186 / 46cb16fb08, 5185 / e7deb96dcf, 5184 / 96e8d47e66, 5183 / 8a47389a17, 5182 / 9cd573cbec, 5181 / fd098e6b1e and 5180 / 9950a57f15. Non-runtime practicals in this interval do not consume the frozen denominator. The tranche was audited locally against current C++/Python/XML plus descendant diffs; no internet lookup was needed.
  Practical 5211 closes clean-current after mechanical normalization of its 41-file Ruff-revert cleanup. The runtime changes restore imports previously removed as apparently unused, remove noqa annotations and restore one unused Corporation-list local; no game-control flow, widget IDs, formulas or XML runtime values change. Practical 5210's Refrigeration -> Environmentalism migration is internally complete in current live XML/source, while its production-chooser pinned-row fix clamps against populated rows and lets explicit tab/scroll controls update the pin. Practical 5209 is City-Screen geometry only.
  Practicals 5207, 5206 and 5205 close as exact or near-exact performance refactors. Replacing the new cached kGame reference back with GC.getGame() mechanically restores the old executable expressions, and 5205 only moves an existing live owner reference outward for reuse. No game state is snapshotted, so there is no stale-cache analogue to KI#306.
  Practicals 5204 through 5202 close clean-current/repaired-history around CvCityAI::AI_buildingValue. 5204 is a genuine repair excluding Great-Person-built/zero-or-negative-XML-cost buildings from hammer-efficiency gates that could suppress shrines. 5203's cheap-World-Wonder cap is correctly game-speed-scaled at birth; the much later unscaled regression is already KI#205 lineage. Its late border-obstacle Wonder gate applies to the current Great Wall and is explicit tunable AI policy. 5202 externalizes Wonder thresholds with matching defaults and adds deliberately approximate race/hammer gates whose comments define their heuristic scope; no correctness contradiction survives.
  Practicals 5201 through 5192 form intentional tech-tree/data evolution and close clean-current. The present descendant was validated globally: current SAS has 92 unique technologies, no missing prerequisite TechTypes and no prerequisite cycles, and all live XML tech references resolve. Music -> Music Theory -> Romanticism and Refrigeration removal are complete migrations rather than dangling identifiers. Practical 5196 genuinely repairs combat-modifier help that had become suppressed outside Sevopedia, while 5195's configurable terrain/feature attack/defense modifiers wrap the real CvUnitInfo getters so gameplay and help share the configured values.
  Practicals 5191 through 5180 form the first Tech-Chooser-width / enlarged-advisor / City-Screen-extra-row / Specialist-Breakdown family. Practical 5191 is the known KI#85 repair that restores the turn-0 bulbing-indicator initialization guard after 5187's experiment; its four Tech-Chooser width modes are later refined, including the already-repaired mode-2 width issue. 5189's early advisor sizing still depended on fixed reference dimensions, but later runtime-bounds work supersedes that limitation. 5184's optional extra production rows are later complemented by 5210's pinned-row auto-scroll repair. 5183 immediately repairs the 5182 Specialist Breakdown widgets not being hidden when leaving the City Screen.
  KI#308 (SAS-specific) is confirmed at practical 5182 and survives current source: the City Screen Specialist Breakdown computes raw building+specialist GPP and then claims to recover the 'real' modifier through ((finalRate * 100) / rawRate) - 100. CvCity::getGreatPeopleRate() has already applied integer truncation to (base * getTotalGreatPeopleRateModifier()) / 100, so that transformation is not invertible. The comment's premise that Python lacks the full modifier was already false at birth: CyCity.getTotalGreatPeopleRateModifier() was exposed in K-Mod and remains exposed. Current ordinary specialists generate 3 raw GPP; with the current Parthenon's +50% global modifier, C++ yields 4 GPP while the panel displays +33%, and with the Roman Forum's +25% local modifier C++ still yields 3 so the panel displays no modifier at all. Surgical repair: use pHeadSelectedCity.getTotalGreatPeopleRateModifier() - 100 directly (subject to the existing desired display policy), retaining getGreatPeopleRate() only for the final rate/turn estimate.
  Practical 5180's Building-list +GPP icon has an older real omission but it is already KI#101 repair lineage rather than a new root: its birth placement inside the active-building branch hid still-counted obsolete-Wonder GPP, and practical 5384 explicitly moves the display outside the active/obsolete split with See KI#101. Practical 5181's gray-header removal is layout-only. No second surviving 5180-5182 source defect was established.
  Certified cursor advances through practical 5180 / 9950a57f15 as runtime-source SHA 826 / 1,317. Confirmed totals become 106 = 39 inherited + 67 SAS-specific. Next runtime-source SHA: practical 5179 / daa528a557, intentionally left unopened at this checkpoint so the older Foreign/Domestic/Military-advisor and gameplay tranche can start from a clean boundary.

SAS-C132 - practical 5179 through 5160 advisor/vassal-tooltip/performance/tech-trade/religion-value audit - COMPLETE; KI#309 confirmed
  Reviewed 19 runtime-source SHAs: practical 5179 / daa528a557, 5178 / 39fcce8ec0, 5177 / e7d5d4d75f, 5175 / 916d3a319f, 5174 / b6c8dbfae9, 5173 / 1022ec5365, 5172 / 8cf1108949, 5171 / cc066d71c5, 5170 / a519366eb7, 5169 / 437a041a62, 5168 / 3605499185, 5167 / 80be9845ce, 5166 / 602f4f308e, 5165 / ceb7306d3c, 5164 / 735354daab, 5163 / 27adaba3c1, 5162 / 75085838bc, 5161 / 632163dc4b and 5160 / 2a453a1f3f. Practical 5176 and the XML/docs/export-only gaps below do not consume the frozen runtime-source denominator. The tranche was audited locally against current descendants and engine contracts.
  Practicals 5179 through 5177 close clean-current around the Foreign Advisor vassal-willingness indicator. 5179 merely consolidates three duplicated C++ tooltip checks into parseVassalWillingnessHelp; TradeData has a proper default constructor, the helper bounds player IDs, refuses every non-active-player row and uses the same canTradeItem/getTradeDenial tests the active player can obtain through diplomacy. The 5177 Python star/vassal icons use the same supported trade-denial contract on the human row and preserve the existing met-player Glance population. No new hidden-information boundary or wrong player direction survives.
  Practicals 5175 through 5162 are predominantly advisor layout/refactor/performance evolution and close clean-current. Domestic Advisor production icons preserve the actual Unit/Building/Project IDs and use WIDGET_GENERAL for Processes, which have no native Pedia page in this lineage. Foreign/Military Advisor enlargements are later superseded by runtime-bounds work; the early fixed/reference geometry is historical presentation limitation rather than a current failure. Practicals 5170-5172 hoist repeated live player/team/game objects rather than snapshotting mutable scalar state. Favorite Religion display is static LeaderHeadInfo data analogous to the already-public leader personality information; it does not bypass the existing Favorite-Civic-knowledge gate. The bonuses/tech/glance/info panel changes preserve their entity IDs and visibility sets.
  Practical 5161's widely-known-tech trade discount closes as intentional market-valuation policy rather than a correctness regression. AI_dealVal values what the current player receives from eFromPlayer, and the tech value is reduced once the technology is known by at least two teams (seller plus at least one alternative source), with a configurable percentage and turn-scaled minimum floor. Applying the same lower market value when buying and selling a widely available technology is coherent with the stated opportunistic-market policy; later related tech-trade commits tune master-vassal/internal-source and military-tech valuation rather than repairing this case.
  KI#309 (SAS-specific) is confirmed at practical 5160 and survives current source. AI_religionValue replaces the old equal-player diplomacy term with a power-weighted loop over PlayerIter<MAJOR_CIV,OTHER_KNOWN_TO>. Vassal players are skipped, but every remaining player is assigned iBlocPower = 100 + GET_TEAM(itOther->getTeam()).getPower(true). CvTeam::getPower(true) already sums every player on that team plus all vassal players, so a rival team with multiple non-vassal players has the same full team/bloc power added once for each team member. E.g. a two-player rival team with 2,000 combined power contributes roughly 2*(100+2000), while an equally powerful one-player rival team contributes only 100+2000. This contradicts the feature's stated goal of weighting religions by military power and can materially bias team games. The block is unchanged semantically in current source. Repair direction: either use player-level power in the player loop or de-duplicate team/bloc power and explicitly define mixed-religion multi-player-team semantics.
  The rest of practical 5160's runtime-source edits are comment/formatting cleanup around unrelated City/Worker/Sevopedia paths. Its religion power-weighting itself is otherwise deliberate policy (including skipping subordinate vassal players and treating the master/block as a strategic unit); no second independent current root was established in this commit.
  Certified cursor advances through practical 5160 / 2a453a1f3f as runtime-source SHA 845 / 1,317. Confirmed totals become 107 = 39 inherited + 68 SAS-specific. Next runtime-source SHA: practical 5157 / ae78db3d20; practicals 5159 and 5158 are export/XML/docs-only in the frozen source denominator. The next tranche returns to upgrade costs and emergency-production/AI gameplay code and should be audited independently.

SAS-C133 - practical 5157 through 5134 upgrade-cost/emergency-production/define-refactor/master-vassal-tech audit - COMPLETE; KI#310, KI#311 and KI#312 confirmed
  Reviewed 18 runtime-source SHAs: practical 5157 / ae78db3d20, 5155 / 684dcd64d6, 5154 / 48b58caf18, 5153 / 9b72b79660, 5151 / c4e736db8a, 5148 / 93acedd84a, 5147 / 07c971a50a, 5146 / c9cd39fa00, 5145 / 2d05e69b06, 5144 / 7364d7bbf0, 5143 / 42a2184744, 5142 / 96934bd1bd, 5140 / 829c236b48, 5139 / df56f22fc5, 5138 / 88657f2f72, 5137 / f983e6f982, 5135 / 137bb408c9 and 5134 / 673bfc82b6. Practicals 5159, 5158, 5156, 5152, 5150, 5149, 5141 and 5136 are XML/docs/export-only in the frozen runtime-source denominator. The tranche was audited locally from the commit-diff mirror and current source; no web retrieval was required.
  Practical 5157 closes clean-current. Setting every handicap iAIUnitUpgradePercent to 100 and applying SAS_UPGRADE_PRICE_UNIT_UPGRADE_COST_PERCENT in CvUnit::upgradePrice uses the same price path actually paid by humans and AI. The GlobalDefine is static configuration, the cached lookup is performed only after XML initialization through normal gameplay, and the subsequent per-unit upgrade discount remains multiplicative apart from ordinary integer rounding. No second hidden AI half-price path or human bypass was found.
  KI#310 (SAS-specific) is confirmed across practicals 5153-5155 and survives current source. Practical 5153 introduces bInnerRingMostlyWaterNonPeak and excludes such cities from the ordinary forced-unit no-production fallback. Practical 5155 partially mitigates that by trying three configured water-building classes (current Harbor, Port and Lighthouse), but if all three SASTryEmergencyBuilding calls fail it logs WATER_BUILDING_NONE and leaves the fallback block without choosing any production. This is reachable both before the relevant Seafaring/Navigation/Sailing techs and after the city already owns the three buildings; doProduction has already run before the safety net and there is no second normal-production retry afterward. Practical 5919 adds an earlier Port preference, 6085 adds logging and 6086 correctly removes the separate historical bHeadProcess false-positive trigger, but none supplies a terminal rescue. Repair direction: after the water-building attempts fail, fall through to a safe unit/other generic fallback rather than suppressing the ordinary safety net completely.
  The Process-as-no-production behavior visible at 5153-5155 is therefore historical-only lineage, not another finding: later practical 6086 removes bHeadProcess from bNeedFallback because an AI Process can be an intentional production choice. SASTryEmergencyBuilding itself resolves the civilization-specific building class, checks ownership/canConstruct and safely clears/pushes the queue only on success; no independent helper defect was established.
  Practical 5151 is comment-only and correctly clarifies that one-argument AI_contactRoll uses its default multiplier. Practical 5148 is a mechanical SAS define namespace migration: all 63 removed XML define names have paired replacements and all 63 live C++ string lookups are renamed correspondingly, including the intentionally shortened specialist-commerce define; no value or behavior change survives normalization. Practicals 5147 and 5146 hoist repeated live CvPlayerAI references and isHuman booleans inside single calls. Player identity/human status cannot change during those calls, and no branch/formula change remains after substitution, so both close clean-current.
  KI#311 (inherited/pre-SAS) is confirmed while auditing practical 5145. In CvPlayerAI::AI_bonusTradeVal, eFromPlayer is a PlayerTypes and the function already resolves kFromPlayer.getTeam(), yet inherited live code uses GET_TEAM(eFromPlayer) when testing whether the seller team has met a third party and when applying the voluntary-vassal discount. The same root survives twice in AI_processPeacetimeValue when testing whether eFromPlayer is capitulated. GET_TEAM accepts TeamTypes; TEAMID(x) is explicitly defined as GET_PLAYER(x).getTeam(), and neighboring code uses TEAMID correctly. Thus permanent-team/custom-team games where player ID differs from team ID query an unrelated team, mispricing resource trades and misapplying capitulated-vassal peacetime/rival-trade attitude bookkeeping. Repair direction: replace the PlayerTypes-as-TeamTypes calls with TEAMID(eFromPlayer) / kFromPlayer.getTeam(). This predates the SAS commit; 5145 only exposed it during the touched-line audit and is not blamed as its origin.
  Practicals 5145 through 5134 otherwise form a coherent master-vassal technology cooperation policy lineage. 5145 suppresses the SAS key-strategic substitute multiplier only for trades within the correctly constructed master/vassal/sibling locus. 5143's military-tech research/trade weighting and 5140's contact-power modifier are explicit tunable heuristics using team power and military flavor; no hard legality/control-flow contradiction was established. 5142 consolidates multiple SAS research modifiers into one additive percentage applied once; defaults stay positive and the comments explicitly define this anti-compounding policy. 5135/5138 increase contact frequency for master-vassal and sibling-vassal tech trading; the earlier omitted rContactProbMult parameter is repaired by 5135 and current code retains that repair.
  KI#312 (SAS-specific) is confirmed as one shared master/vassal technology-availability root, born at practical 5134 and broadened at 5139/5144. Practical 5134 devalues self-research to the configured master/vassal percentages whenever another locus team merely knows eTech. It never checks GAMEOPTION_NO_TECH_TRADING or TechInfo::isTrade; current XML contains 32 bTrade=0 technologies, so supported games can heavily suppress research of knowledge that cannot be transferred at all. Practical 5139 explicitly claims to redirect an outsider offer only when a viable internal source exists, but it tests locus isHasTech plus AI_techTrade()==NO_DENIAL; actual CvPlayer::canTradeItem additionally rejects kOurTeam.isNoTradeTech(eTech), so under No Tech Brokering an internally acquired no-broker copy can wrongly suppress a valid outsider source. Practical 5144 repeats the same assumption more softly by discounting outsider AI_techTradeVal whenever any locus team merely knows the technology. Later descendants touch these blocks only for refactoring/comments/define organization; current code retains every missing legality guard. Repair direction: centralize an actual internal-tech-supply predicate that respects global no-tech-trading, per-tech bTrade, no-brokering/no-trade state, recipient research legality and (where the caller requires an immediately viable deal) actual canTradeItem/denial status.
  No additional survivor was established in the master-vassal tech family. The policy can intentionally prefer/devalue internal vs external sources and deliberately approximate future willingness; those tuning choices are not promoted unless they contradict a supported trade-legality contract. KI#312 captures the concrete cases where internal transfer is legally impossible rather than merely strategically uncertain.
  Certified cursor advances through practical 5134 / 673bfc82b6 as runtime-source SHA 863 / 1,317. Confirmed totals become 110 = 40 inherited + 70 SAS-specific. Next runtime-source SHA: practical 5133 / f5d96ce5c1, returning to human auto-specialist/city AI logic.

SAS-C134 - practical 5133 through 5124 human specialist/BFC-Artist convenience audit - COMPLETE; KI#313 confirmed
  Reviewed 10 runtime-source SHAs: practical 5133 / f5d96ce5c1, 5132 / 92b7a7299a, 5131 / 7e5e439e96, 5130 / 710c78523c, 5129 / 380d155ab0, 5128 / 9d5a1a113d, 5127 / 790ae906b4, 5126 / d56716574e, 5125 / 8d5621c320 and 5124 / e7fa40612f. Practicals 5123 and 5122 do not touch primary runtime C++/Python in the frozen denominator; 5122 changes only a developer/test expected-output Python file. The tranche was audited locally from the commit-diff mirror and current source with no web retrieval.
  Practical 5133 closes clean-current. Its XML changes are deliberate technology-cost pacing reductions. The only runtime C++ change replaces GC.getInfo((SpecialistTypes)new_job.second).getType() with GC.getInfo(eSpec).getType() after eSpec has just been assigned from that same new_job.second value, so the diagnostic behavior is exactly equivalent.
  Practical 5132's six human no-auto-specialist convenience toggles are internally coherent. They act only inside AI_jobChangeValue's automatic candidate valuation for human-owned cities, return the existing forbidden-job sentinel for the configured specialist classes, and leave direct/manual specialist forcing legal. Defaults are disabled for Scientist/Engineer/Merchant/Priest/Spy/Artist, while the already-separate no-auto-Citizen convenience remains enabled. The same commit changes the BFC-Artist cleanup margin from 4 to 0; this broadens the cleanup window down to immediately after the normal BFC threshold but does not create an independent root from KI#313.
  Practical 5131's no-auto-Citizen human convenience also closes clean-current. It extends the existing forbidden-Citizen valuation only to the automatic human city-AI assignment path when configured, while preserving manual assignment and exempting human cities from several AI-only specialist heuristics. Practical 5130 is a real historical repair: it restores the missing default-specialist/Citizen forbidden-job gate inside AI_jobChangeValue. Current source retains the repair, so no survivor is counted from that temporary omission.
  Practical 5129 is additional hardening around the BFC-Artist feature. It checks isSpecialistValid before adding or removing the forced Artist and adds diagnostics/forbidden-job constants; those guards survive current source and do not track who owns a force increment. They therefore prevent invalid specialist-count operations but do not repair KI#313's provenance problem.
  KI#313 (SAS-specific) is confirmed at practical 5128 and survives current source. The new human post-BFC cleanup explicitly says it should remove the automatic BFC Artist 'in a way that does not conflict with the human player's choices' and that later manually forced Artists should not be prevented. However no per-city flag, counter or other provenance state records whether SAS itself added the forced Artist. Once the city is past the BFC culture level, during the configured cleanup window the code merely reads getForceSpecialistCount(eArtist) and, if it is positive, calls changeForceSpecialistCount(eArtist,-1). With the current/default cleanup margin of 0 from 5132, ordinary normal-speed culture just above the BFC threshold through twice that threshold is sufficient. A human who manually forces one or several Artists during that window can therefore have one forced Artist removed each city turn until the raw count reaches zero. Current source contains the same decrement and no auto-owned-Artist marker anywhere. Repair direction: persist/derive ownership of the exact force increment added by the BFC helper and only undo that increment; raw forced-specialist count cannot safely stand in for provenance.
  Practical 5127 externalizes existing AI_buildingValue switches and the maximum-Wonder-turn constant behind SAS defines with defaults preserving the active behavior. The changed gates wrap already-existing valuation blocks rather than altering formulas at their default values; no independent current defect was established. Practicals 5126 and 5125 are primarily guide/file renames and comment/link cleanup. Their runtime-source edits are indentation/comment/text-reference churn around existing City/Worker/AI code and do not change gameplay control flow after normalization.
  Practical 5124 is the birth of the human BFC-Artist convenience. It correctly makes the existing force-one-Artist helper available to humans through a default-enabled convenience define. In the same commit, an existing AI_assignWorkingPlots validity/clamp safeguard is moved behind an experimental define defaulting to 0 because it interfered with forcing the Artist; this is documented as an experiment and later/current source has broader specialist normalization plus 5129's doTurn validity checks. No second current survivor was established from that temporary experiment. KI#313 belongs specifically to the later 5128 attempt to automatically undo a forced Artist without ownership state.
  Certified cursor advances through practical 5124 / e7fa40612f as runtime-source SHA 873 / 1,317. Confirmed totals become 111 = 40 inherited + 71 SAS-specific. Next runtime-source SHA: practical 5121 / 5f27d95b1d; practical 5123 has no primary runtime source and 5122 touches only developer/test expected-output Python.

SAS-C135 - practical 5121 through 5103 civic/event and AI_civicValue audit - COMPLETE; KI#314-KI#317 confirmed
  Reviewed 8 runtime-source SHAs: practical 5121 / 5f27d95b1d, 5115 / 8d6fb60205, 5114 / 07ed4d273d, 5110 / 59d8c92c41, 5108 / 221d218f2d, 5106 / 2d718e9858, 5104 / a85a49b1af and 5103 / e3d2a79855. Practicals 5120-5116, 5113-5111, 5109, 5107 and 5105 do not touch primary runtime C++/Python in the frozen denominator. The family was audited from the local commit-diff mirror/current source; no web retrieval was needed. The only source dependency not present locally is the unchanged base-BTS localization body for the legacy Earth Day event, which is optional for exact prose comparison and not required to establish the executable/event-ID mismatch below.
  KI#314 (SAS-specific, low severity content/semantics) is confirmed at practical 5121. The civic rework removes Environmentalism and mechanically retargets CvRandomEventInterface canApplyEarthDay2/applyEarthDay2 plus EVENTTRIGGER_EARTHDAY's Civic requirement to CIVIC_TRADE_BLOC. Current source still identifies the trigger, outcomes, help/world-news keys and callbacks explicitly as Earth Day, and the trigger remains a global Industrialism/Radio-era Earth Day. Current Trade Bloc Civilopedia/source design, meanwhile, defines an intergovernmental/regional free-trade or customs/economic-integration bloc rather than an environmental civic. No descendant rethemes, renames or removes Earth Day; later touches are formatting/safe-lookup refactors. Thus an environmental Earth Day event can still require and spread an unrelated Trade Bloc civic. Repair direction: retheme/rename the event and its prose/effects to fit Trade Bloc, retarget it to a thematically appropriate mechanic, or disable/replace the legacy event rather than mechanically substituting a removed civic ID.
  Practical 5115's expanded Ancient/Classical/Medieval/Renaissance very-cheap-unit caps are historical production-policy lineage rather than current architecture. The commit deliberately separates a Renaissance-plus land tier and keeps the general AI_chooseUnit/fallback gates tunable. Later fallback/scrapping/production refactors supersede these old caps, and current source no longer retains this exact SAS_NO_EXCESS_VERY_CHEAP_MILITARY_UNITS architecture. No current survivor is attributed to 5115. Practical 5114's iCivicPercentAnger Civilopedia display is correct: it supplies the civic's numeric percent value to the existing localized list-help template and appends the linked civic description; the later Theocracy data uses the same field correctly.
  Practical 5110 closes as tuning rather than a correctness root. It reuses the same specialist-commerce helper weights introduced for unlimited-specialist evaluation and computes the BFC-need state once; the changed calculations remain explicit configurable AI_civicValue weighting, not a legality/state invariant. Practical 5108 adds civic-switch hysteresis and changes the unlimited-specialist anarchy scaling from adoption-only to any civic change. The hysteresis is a tunable anti-oscillation policy. Its own comments observe a bias toward dropping unlimited-specialist civics after the 5106 rewrite, but the commit only dampens switching and does not repair KI#315's underlying sign inversion.
  KI#315 (SAS-specific) is confirmed at practical 5106. AI_civicValue is an absolute civic score used by AI_bestCivic to compare candidates. The new generalized unlimited-specialist block sets iDelta=iS, where iS is +1 for a civic not currently active and -1 for the currently active civic, computes a positive iTotal from the specialists the civic enables, then executes iValue += iDelta * iTotal. Consequently the same unlimited-specialist benefit raises a candidate civic's score before adoption but lowers that civic's score after adoption, creating a systematic pick-then-drop/undervalue-active-civic bias. K-Mod's previous unlimited-specialist component and neighboring intrinsic positive civic benefits add their value positively; iS is appropriate only to genuinely marginal state-change calculations, not to negate the civic's own benefit. Current source retains iValue += iDelta * iTotal, while 5108 merely adds hysteresis around the resulting oscillation. Repair direction: score the civic's intrinsic unlimited-specialist benefit positively in its absolute value and use adoption/drop state only for genuinely state-dependent marginal terms/anarchy costs.
  KI#316 (SAS-specific) is a separate practical-5106 root in the same block. The new Great People Points term states that it values specialist GPP at the player's average GP multiplier but calculates iGPP * max(0, AI_averageGreatPeopleMultiplier() - 100) / 25. AI_averageGreatPeopleMultiplier is the full percentage based on city getTotalGreatPeopleRateModifier and has a normal 100% baseline. Therefore every ordinary specialist's base GPP contributes exactly zero at a normal 100% GP modifier; only bonus multiplier above the baseline is valued. Current ordinary specialists have positive base GPP, so this is reachable without exotic data. The defect is independent from KI#315 because fixing the sign would still leave GPP absent. Repair direction: apply the intended scaling to the full GP-rate multiplier (with deliberate normalization/weighting), rather than subtracting away the baseline before valuing GPP.
  Practical 5104's Emancipation-to-Wage-Labor migration is internally complete for the touched runtime event predicates and civic valuation reference. Interstate and High Warlord callbacks plus their XML civic gates are mechanically redirected because the old civic ID is removed/reworked; unlike Earth Day, the local source does not establish a comparably hard thematic or executable contradiction, so these substitutions are retained as audit notes rather than manufactured KIs. The simultaneous Serfdom/Wage Labor XML redesign is deliberate balance/content work.
  KI#317 (SAS-specific) is confirmed at practical 5103. The replacement iCivicPercentAnger valuation introduces separate SAS scale defines for unhappy and happy cities, but inside the city loop it computes current slack, sets bCityHappy=(iSlack>=0), and immediately continues when bCityHappy is true. The later ternary that selects the happy-city scale is therefore unreachable. More importantly, for an already-active pressure civic the AI is evaluating the consequences of dropping it: a currently happy city with small positive slack can become unhappy after the hypothetical civic-anger increase, yet the code discards that city before computing/applying iDeltaUnhappy. Current source retains this exact ordering, and current Theocracy has a nonzero iCivicPercentAnger value, so the path is live. Repair direction: compute the hypothetical anger/unhappiness and resulting post-change slack first, then value the actual welfare change (and apply happy/unhappy scaling to a reachable post-change state) rather than filtering solely on the city's pre-change happiness.
  No additional current survivor was established in this family. The non-runtime civic/tech XML changes are deliberate balancing/pacing work, and the older cheap-unit production architecture is superseded. Certified cursor advances through practical 5103 / e3d2a79855 as runtime-source SHA 881 / 1,317. Confirmed totals become 115 = 40 inherited + 75 SAS-specific. Next runtime-source SHA: practical 5101 / b61ee3f03f; practical 5102 has no primary runtime C++/Python change.

SAS-C136 - practical 5101 through 5090 assert/AI-update hardening and tech-tree repair audit - COMPLETE; KI#318-KI#319 confirmed
  Reviewed 8 runtime-source SHAs: practical 5101 / b61ee3f03f, 5096 / 07180b5b11, 5095 / 4500c8c327, 5094 / 7f434b7fe0, 5093 / 1798ac76f4, 5092 / 453353c3f8, 5091 / f0b9e8757b and 5090 / 2d3c2dd4a5. Practicals 5100, 5099, 5098 and 5097 contain no primary runtime C++/Python change in the frozen denominator. The family was audited against the local commit-diff mirror and current descendants; no web retrieval was required.
  Practical 5101's sole runtime-source hunk is a comment spelling correction in CvUnitAI. Its substantial technology XML prerequisite edits are deliberate beeline/pacing work; the current descendant technology graph was already globally validated during the newer 5201-5192 audit as 92 unique technologies with no missing prerequisite IDs or prerequisite cycles. No current source defect is attributed to 5101.
  Practical 5096 fixes the SAS-introduced define typo INITIAL_MILITARY_UNITS_POPULATION_PERCENT -> INITIAL_FREE_MILITARY_UNITS_POPULATION_PERCENT and replaces terse debug assertions in CvGlobals define getters with more informative missing-name/XML diagnostics. The default/runtime define lookup semantics are unchanged when the value exists, and the corrected startup field now resolves to the intended define. Practical 5095 moves the expensive stuck-loop assert message construction behind the debug/failed-condition gate; release behavior and the attempt limit remain unchanged. Practical 5094 adds caller-side NO_UNIT guards around AI_bestSpreadUnit reuse and removes false FErrorMsg assumptions that AI_chooseUnit must succeed after a spread candidate was found. The guarded branches fall through to the pre-existing production search when no concrete/trainable spread unit can be chosen; no valid-production path was removed.
  KI#319 (SAS-specific) is confirmed at practical 5093. The new CvSelectionGroupAI::AI_update no-progress tripwire records only the current head unit's x/y coordinates, movesLeft and the group's mission-queue length. After the head's CvUnitAI::AI_update returns, if those scalar values appear unchanged it pushes MISSION_SKIP on the group and breaks. This is not a complete progress predicate. CvUnitAI::AI_setUnitAIType, used by ordinary role-conversion paths, changes the unit's AI role and then calls joinGroup(NULL), detaching the acting head from its old group. Current examples include collateral -> ATTACK_CITY in AI_collateralMove, excess PILLAGE -> ATTACK/ATTACK_CITY in AI_pillageMove and excess EXPLORE -> ATTACK in AI_exploreMove. Before 5093, if other units remained in the old group, the enclosing selection-group loop naturally iterated again and let the new head act. With the tripwire, the new head is not compared by identity; stacked units normally share the same plot and can have the same movement allowance, so the old group can satisfy the unchanged x/y/moves/queue test even though its head and group membership changed. The tripwire then pushes MISSION_SKIP onto the remaining stack, consuming that group's turn. Current source retains the same scalar-only snapshot and runs the tripwire before later group-state handling. Repair direction: treat head identity/group membership and relevant semantic state changes (at minimum head unit ID/UnitAI type, preferably an explicit progress result) as progress, or narrowly target the originally observed truly-stuck explorer state rather than interpreting unchanged geometry as unchanged AI state.
  KI#318 (SAS-specific) is confirmed at practical 5092. The inherited CvRandom::getInt contract explicitly allows iNum=0 and returns 0 for both iNum=0 and iNum=1. The commit correctly hardens negative ranges in CvRandom, but three CvCityAI opportunistic-Wonder callers simultaneously wrap their entire old calculation in `if (iWonderConstructRand > 0)`. Before the change, a zero value still supplied a zero random contribution while preserving the fixed base terms: the affected paths used +7, +8 or a base iWonderRand of 8. After 5092, zero skips those opportunities entirely. Current leader XML has ordinary zero-valued leaders Montezuma and Sitting Bull (in addition to default/Barbarian), so this is reachable current AI behavior, and current source retains all three guards. A fourth guard in AI_bestBuildingThreshold is harmless at zero because the old random term itself was zero. Repair direction: distinguish invalid negative ranges from valid zero; preserve the inherited zero-random-contribution semantics while guarding/asserting only negative inputs.
  Practical 5091 makes CvBuildInfo::isFeatureRemove(NO_FEATURE) return false before indexing/asserting the Feature array; that is the correct semantic result for 'no feature' and survives as defensive hardening. Practical 5090 clamps forced specialist counts to available slots and refuses to force an invalid specialist in AI_assignWorkingPlots. This is a valid safety repair; later BFC-Artist convenience commits explain why Artist was the observed trigger and add their own lifecycle guards. The old tracker called the observed assert a Base-AdvCiv issue, but current lineage shows the important present behavior is simply the safe validity/clamp guard; no additional survivor is counted here.
  Certified cursor advances through practical 5090 / 2d3c2dd4a5 as runtime-source SHA 889 / 1,317. Confirmed totals become 117 = 40 inherited + 77 SAS-specific. Next runtime-source SHA: practical 5089 / 6adb8d40d5.

SAS-C137 - practical 5089 through 5080 building-value/performance, Great General and settler-safety lineage audit - COMPLETE; no new defect
  Reviewed 9 runtime-source SHAs: practical 5089 / 6adb8d40d5, 5088 / 8d1073b371, 5087 / 7da5726acf, 5086 / 594f132e6d, 5085 / 511f2f8f73, 5084 / c5e91acf2a, 5083 / 118d145c30, 5081 / d975fc2a12 and 5080 / b2ed0d14d9. Practical 5082 has no primary runtime C++/Python change. No new current defect was established.
  Practicals 5089 and 5088 are successive AI_buildingValue policy/crash-hardening revisions. 5088 reduces the overly large force-first building-value sentinel from 999999 to 100000 after a reproducible crash and externalizes maintenance gates; 5089 then moves the sentinel behind the non-Barbarian/non-minor fast-path scope, makes water/naval building tests coastal-aware and relaxes the National-Wonder danger treatment so military-pump effects such as Heroic Epic can remain valuable under moderate pressure. The surrounding top-hammer, maintenance, coastal-scaling and government-center rules are explicit heuristic policy; current descendants retain/refine them without a source-proven overflow, invalid-index or legality failure. No current KI is attributed to this tuning family.
  Practicals 5087, 5086, 5085 and 5084 are primarily performance/refactor work. Their new locals cache live CvGame/CvPlayer/CvPlot references, per-call city ranks/rates or XML/GlobalDefine identifiers whose values are mod-static after XML load. The strategic-resource names moved behind SAS defines without changing the resolved BonusTypes, and the AI_buildingValue rank/maintenance calculations remain live per evaluation. No stale player/city/game snapshot analogous to KI#306 was established.
  Practical 5083 is important provenance for already-counted KI#243 rather than a new defect. Among a broad set of otherwise-safe function-local static GlobalDefine lookups, CvMapGenerator::addRivers changes RIVER_SOURCE_MIN_RIVER_RANGE, RIVER_SOURCE_MIN_SEAWATER_RANGE and PLOTS_PER_RIVER_EDGE to function-static values. Those are exactly the three globals that later imported Water.py mutates immediately before invoking the default river generator. C095/KI#243 already proved the resulting process-lifetime/order dependency and non-idempotent Water mutation; this commit is therefore recorded as the older C++ half of that same root. A scan of current runtime Python define mutation did not reveal another 5083-cached gameplay key with the same map-generation interaction; camera/minimap/FOV keys mutated by UI Python are different defines. The remaining 5083 statics are immutable mod configuration or are guarded until XML is available.
  Practical 5081 deliberately shifts Great General policy strongly toward Military Instructors, especially in top-hammer/Heroic-Epic cities, and raises the join-count ceiling. Current descendants retain the specialist valuation concept while later Great-General work adds era-sensitive ordering/logging and other safeguards. The values are aggressive/tunable but the civ-specific Heroic-Epic building-class resolution, specialist experience calculation and AI_join legality are coherent; no current correctness root was established.
  Practical 5080 introduces the early/low-city `bSafe` override that allowed Settlers to consider city sites without a defending group. That empirical workaround is real historical SAS behavior and later became part of the KI#179 settler-safety lineage: current AI_found explicitly removes the old early-city override, first tries to attach a local city escort, and then requires a defended/invisible group or an existing guard-city mission before normal founding selection. Thus the risky 5080 behavior is repaired in current source and is not re-counted here. The same commit's CvCity doTurn changes primarily cache live game/speed values and refactor emergency-building selection through civ-specific BuildingClasses.
  Certified cursor advances through practical 5080 / b2ed0d14d9 as runtime-source SHA 898 / 1,317. Confirmed totals remain 117 = 40 inherited + 77 SAS-specific. Next runtime-source SHA: practical 5079 / 1787dea4ff.

SAS-C138 - practical 5079 through 5067 chooseUnit/Worker/promotion/emergency-building and bonus-build lineage audit - COMPLETE; no new defect
  Reviewed 13 runtime-source SHAs: practical 5079 / 1787dea4ff, 5078 / 1801066967, 5077 / c91f2e0463, 5076 / ce8cc2aa4d, 5075 / 60fb4cffd1, 5074 / 37fdd430a5, 5073 / 88d099b120, 5072 / 6b42984519, 5071 / 9795f45348, 5070 / 1a28467981, 5069 / 0efd42054b, 5068 / fc4b91a31f and 5067 / 8a12b125dd. No new current defect was established.
  Practical 5079 adds the master `SAS_CHOOSE_UNIT_OPTIMIZE` switch around the then-large SAS AI_chooseUnit policy and renames the matching no-production fallback switch. With the default enabled, behavior is unchanged; the XML comments explicitly warn that independently toggling production/scrapping systems can produce odd policy combinations. The switch itself does not bypass Civ4 trainability/UnitAI legality. Practical 5078 replaces hardcoded era ordinals with real EraTypes lookups and moves several constants behind SAS defines; this is a correctness/maintainability improvement. The current era table retains chronological enum order and every referenced era key resolves. Practical 5077's runtime-source hunk is comment/text support around broad XML balance changes; no source survivor established.
  Practicals 5076 and 5075 are the early cross-city Worker-mobility/parking refinement lineage. 5076 makes `countImprovedTiles` overlap-tolerant, restores an `AI_improveCity` attempt when the Worker remains in its current city and prevents a self-city fallback from reporting success unless it can actually move/build. 5075 wakes safe owned-territory retreat/HOLD Workers and adds a last-chance reassignment before MISSION_SKIP. Current Worker AI retains these ideas but has since been substantially reworked: threat handling is narrower inside borders, cross-city switching/actionability has stronger logging/guards, and the same helpers have later demand/irrigation/build-policy changes. No current no-op success, invalid pointer or hard parking root specifically attributable to these commits was established. The broad 'wake HOLD' behavior is retained as an empirical parking recovery and no supported intentional AI-Worker HOLD state that it demonstrably corrupts was proven.
  Practical 5074 expands the SAS promotion-value policy with configurable hard-block/always-first sentinels and role/war-strength-sensitive priorities. Candidate promotion legality remains controlled by the normal promotion-selection path; the new code only changes valuation among legal candidates. The city pointer used for local danger is null-guarded, enemy-power checks use current war/chosen-war team logic, and later/current promotion descendants retain the architecture with additional tuning rather than repairing a source invariant. The strong anti-niche/city-raider/Combat preferences are balance policy, not counted as defects.
  Practical 5073 externalizes civ-specific-unit score inflation, moves/reorganizes naval UnitAI production gates and cleans several doTurn values. Unique units are detected by comparing the civilization's UnitClass replacement against the class default, so ordinary default units are not accidentally inflated. The configurable multiplication/division is denominator-guarded. Later production/scrapping architecture substantially supersedes the old one-off gates; no current wrong-namespace or trainability failure was established.
  Practical 5072 introduces the Walls/Castle emergency-defense override. It resolves the owner's civ-specific BuildingTypes from configured BuildingClasses, checks absence plus `canConstruct`, and uses the existing queue mechanics for a deliberate hard override under danger/war/enemy-power policy. Later descendants factor the push through the shared emergency-building helper and add water-heavy exceptions, but do not reveal an invalid-building or queue-corruption root in the birth logic. Practical 5068 similarly introduces the low-food coastal Harbor-class emergency choice; current code still requires a real ocean coast, low food, non-food production, no immediate city danger, a civ-specific class mapping and successful construction. The separate current water-heavy *empty fallback* defect remains KI#310 at the later 5153/5155 safety-net architecture, not this proactive Harbor block.
  Practical 5071 is runtime comment/documentation cleanup only after normalization. Practicals 5070 and 5069 optimize and externalize the old manual bonus-specific land Build registry. That infrastructure is historical: practical 5240 later replaces the fragile manual registry with an XML-driven scan, and still-newer Worker code centralizes bonus-specific Build selection in the plot/build-policy helpers. No current registry-index/configuration defect is carried forward from 5069/5070.
  Practical 5067 is the birth of the force-one-Artist-until-BFC behavior. The AI-only birth code and specialist-value suppression intentionally create early border culture. Later practical 5090 prevents forcing an invalid specialist, 5129 adds lifecycle validity hardening, and 5124+ extends the convenience to humans. The surviving human forced-Artist ownership/provenance defect is already counted separately as KI#313 at practical 5128; no second current 5067 root was established.
  Certified cursor advances through practical 5067 / 8a12b125dd as runtime-source SHA 911 / 1,317. Confirmed totals remain 117 = 40 inherited + 77 SAS-specific. Next runtime-source SHA: practical 5066 / a18c4ade75.


SAS-C139 - practical 5066 through 4990 performance/Sevopedia/Worker/production/found-value/war-risk lineage audit - COMPLETE; no new defect
  Reviewed 72 runtime-source SHAs: 5066 / a18c4ade75, 5065 / a550f8772c, 5064 / e1dcfe8f41, 5063 / 158f4efe45, 5062 / 51964a6c72, 5061 / 3c8f353c1a, 5060 / 6df4f199b0, 5059 / 6539df9873, 5058 / efcf539c1d, 5057 / 74cac25613, 5055 / b8c861af13, 5054 / 08c73df0c7, 5053 / be91870024, 5052 / 1959817ddc, 5051 / a276719b71, 5050 / 31fc38bd86, 5049 / 6efb7c12cd, 5048 / 2cbb1069bf, 5047 / 6784bb924d, 5046 / 8d8af2a7bb, 5045 / a8c169c9cb, 5044 / 324bf1e23d, 5043 / 52cf753676, 5042 / fcbb00dfcb, 5041 / 47ac86538b, 5040 / a308df2945, 5039 / 37be7be033, 5038 / 152dccffcc, 5037 / a4255a54e6, 5036 / e06283d3d1, 5035 / 64a6ba2b7c, 5034 / c3d736ff5c, 5033 / 8a4001086b, 5032 / c7a632d3fe, 5031 / 09f8a1b317, 5028 / daae0079dc, 5027 / ce25fe35fd, 5026 / aa5b941381, 5025 / 8138f8af00, 5024 / 93f2d43f9f, 5023 / f497a3cd0e, 5022 / 488018f391, 5021 / 1e0478201c, 5020 / 226e3f11a5, 5019 / 1733842bdd, 5018 / 4e081236e1, 5017 / bb2a9f7671, 5016 / 69fa0194c6, 5015 / 65b761985a, 5014 / 735b01651f, 5013 / dbbe97d9fb, 5012 / 694c168a31, 5011 / dbc3fa1f2f, 5010 / 55650b9656, 5008 / 3f0ce10633, 5007 / d858011091, 5006 / 59a43d2102, 5005 / a527a357d7, 5004 / 8b4761f112, 5003 / a114d63566, 5002 / 9a9f73b4ac, 5001 / 2d354f3ba6, 5000 / f52b846443, 4999 / 70aceae778, 4998 / daf6576b0a, 4997 / fce68b70e9, 4995 / bf56a70088, 4994 / 6c71dafc04, 4993 / 47669361f6, 4992 / c5b10be19a, 4991 / af794dd9a6, 4990 / cbd6eb7f95. Practicals 5056, 5030, 5029, 5009 and 4996 are non-runtime-source in the frozen denominator. The tranche was audited from the local tracked commit-diff mirror/current SAS source and XML; no internet lookup was needed.
  Practical 5066 is mostly safe function-local caching of mod-static configuration and IDs, but it contains a genuine malformed initializer in CvGameTextMgr.cpp: `static const SpecialistTypes eDefaultSpecialist = (SpecialistTypes)eDefaultSpecialist;` rather than reading `GC.getDEFAULT_SPECIALIST()`. The defect survives current source; however static zero-initialization yields SpecialistTypes enum 0, and current XML enum 0 is SPECIALIST_CITIZEN, which is also the current DEFAULT_SPECIALIST. Thus present data masks the typo exactly. It is retained as a latent/code-quality archaeology note rather than promoted to a current KI; a future default-specialist reorder/change would expose it.
  Practicals 5065 through 5063 are Sevopedia Leader performance/refactor work and close clean-current: repeated lookups are cached without changing list/filter/rank semantics, and no stale mutable game-state snapshot was established. Practicals 5062/5061 repair Terrain/Feature unit-panel capability display. Their suspicious Ice/Peak overlap is not reachable in current XML: no current unit has an Ice FeaturePassableTech; Attack Submarine bypasses Ice through bCanMoveImpassable, and current all-terrain Peak-capable units also carry the engine-level impassable movement ability. No current contradictory panel classification was established.
  Practical 5058's one-worker-per-tile/cross-city rewrite contains a real historical successful-no-op self-city fallback: a Worker could select its current city as the next city and report success without actually moving or improving. Practical 5076 later explicitly repairs that exact path by restoring local AI_improveCity work and requiring actionable cross-city behavior. Current Worker logic retains the strict one-worker-per-tile policy as deliberate optimization; no surviving 5058 root is counted.
  Practicals 5055 through 5043 close clean-current around intentional data/balance work and performance caching. The cached GlobalDefine/configuration values inspected are not mutated by current runtime Python. Practical 5048 genuinely repairs an earlier class/unit double-counting error that at the time affected siege; current code retains the corrected counting. Practical 5047 fixes the Renaissance strategic-bonus metal/iron mismatch, and practical 5046 restores missing FreePromotionUnitCombat trait data while rebalancing traits. These are repairs/data changes rather than current archaeology survivors.
  Practicals 5042 through 5034 likewise close clean-current. Practical 5038 explicitly repairs practical 5036's missing no-trebuchet-like gate in the new no-production siege fallback. Practical 5035's performance/crash-related changes were traced through current turn/production paths without establishing a surviving invalid pointer or stale mutable cache. Practical 5033's original no-production/era-cost family used rougher hardcoded/scaled comparisons, but current descendants resolve real ERA_* values and consistently compare the intended raw XML production costs in the fallback helper. Practical 5032's cached land-heavy/naval-heavy map classification is recomputed/restored at the relevant new-game/map-generation/load lifecycle points, so no stale-map classification path was established.
  Practicals 5028 through 5015 are primarily Sevopedia additions, civ/unit/balance data and gradual settler-site tuning. Practical 5013 introduced GlobalDefines_advciv_sas.xml and contained a backwards bonus-condition mistake in the new bad-tile/site valuation; practical 5014 repairs that logic, while practical 5023 repairs the later bad FEATURE_ICE_CAP identifier. Current AIFoundValue descendants carry those fixes. No additional present-day settler valuation invariant failure was proven in this tranche.
  Practicals 5008 through 5000 are early AI production-composition and performance work. The large practical 5000 performance pass was mechanically separated from its later descendants: its executable caching is dominated by mod-static GlobalDefine/ColorType lookups and is not the source of the later unsafe process-static NiColorA caching. That separate optimization is introduced later in practical 4939 and explicitly reverted by practical 4982. No current 5000 cache survivor was established.
  Practical 4997 adds building-enabled health/happiness synergy to AI bonus-trade valuation. A potentially broader contract mismatch was tested rather than promoted from inspection alone: the precheck asks for the building's direct prerequisite tech, not the SpecialBuilding TechPrereq used by religion families. Current data masks that generic gap because the relevant resource health/happiness synergy on SpecialBuilding families is in cathedral-like buildings that this valuation deliberately excludes; current explicit NONE building overrides that could conflict with its default-building fallback are confined to the Barbarian civilization, which does not participate in ordinary resource trading. No reachable current wrong-resource-price case was proven.
  Practical 4995's multi-war emergency-peace prechecks, 4994's strict-building-happiness helper, 4993's far-city razing tuning, 4992's attack-city anti-oscillation sanity check and 4991's doomed-city evacuation policy were traced into current source. The strict-happiness helper's reliable components/gates line up with the engine happiness sources it intends to value; potentially large NoUnhappiness nominal values are capped by actual angryPopulation where cured citizens matter. The remaining aggressive choices are heuristic policy, not a demonstrated legality/invariant defect.
  Practical 4990 is important provenance for already-counted KI#183 rather than a new issue. Its original faraway-war rejection could also execute inside UWAI's recursive peace-scenario evaluation, allowing the hard reject on the hypothetical peace side to turn `war - peace` into an absurdly positive utility. Practical 6036 explicitly repairs that architecture by excluding the peace scenario before the distance rejection, with later descendants further refining the distance policy. The 4990 birth is therefore recorded under existing repaired KI#183 and not re-counted.
  Certified cursor advances through practical 4990 / cbd6eb7f95 as runtime-source SHA 983 / 1,317. Confirmed totals remain 117 = 40 inherited + 77 SAS-specific. Next runtime-source SHA: practical 4987 / 41a7bd8b7f after non-runtime practicals 4989/4988; practical 4987 is intentionally left unopened at this checkpoint.


SAS-C140 - practical 4987 through 4980 release-polish/Gunship/Worker-revert/NiColor/crash-repair lineage audit - COMPLETE; no new defect
  Reviewed 8 runtime-source SHAs: practical 4987 / 41a7bd8b7f, 4986 / 8d6bf6e0f6, 4985 / b9e253a3ce, 4984 / 4c7fc2356d, 4983 / 2a56f33057, 4982 / 846227765d, 4981 / c0190d89af and 4980 / 6296ef16d8. Practicals 4989/4988 are non-runtime-source immediately above this tranche. No current defect was established.
  Practicals 4987/4986 are overwhelmingly documentation/comment/strategy-text cleanup around release preparation. Mechanical inspection of their C++/Python hunks finds no executable token change; the XML changes are deliberate content/comment/strategy cleanup rather than a hidden runtime algorithm change. Practical 4985 coherently renames Chichen Itza's building identity to Temple of Kukulcan across class/building/art/movie/text references and deliberately gives Gunship both bCanMoveImpassable and bCanMoveAllTerrain. Current Unit XML retains both movement flags, and current Sevopedia/engine movement handling audited at C139 is consistent with them; this is intentional helicopter/all-terrain behavior rather than a mismatch.
  Practical 4984 is a deliberate rollback of the recently experimental WorkerMove city-demand logic while retaining earlier simpler countImprovedTiles behavior. Practical 4981 separately rolls back the recent bonus-roading/orphan-city/AI_nextCityToImprove routing rewrite to the older, less aggressive path after stability problems. Those two reverts are historical Worker-policy stabilization; later SAS Worker architecture is extensively rewritten and no current code path depends on the removed implementations.
  Practical 4982 is a genuine historical safety repair. It removes process-static cached NiColorA values/references from CvGameInterface's nuke/patrol border rendering after concern about a weird UI/menu crash, restoring live color retrieval. Current source retains the safe architecture: ColorTypes IDs may be cached, but NiColorA values are fetched/copied from live info when rendering. The unsafe NiColor caching itself originates earlier at practical 4939 and will be recorded there as repaired lineage, not re-counted here.
  Practical 4980 adds conservative guards and routing changes after a rare reproducible AI_nextCityToImprove crash: BuildTypes bounds/NO_BUILD checks, same-area validation, target-city ID rehydration and a different two-leg route/mission-data sequence. Much of that experimental routing is intentionally removed again by 4981/4984 because Worker behavior became unstable, and the current AI_nextCityToImprove implementation has since been substantially rewritten around direct actionability/reservation/path scoring. Therefore the T283 crash-fix code is historical lineage, not a surviving present-day root. The old documentation's proposed dangling-city-pointer and mission-payload explanations are hypotheses rather than independently proven root causes, so they are not promoted as archaeology findings merely because the crash disappeared.
  Practical 4983 is civ-specific unit/promotion balance data. No malformed UnitClass/UnitCombat mapping or current unresolved identifier was established in the changed data.
  Certified cursor advances through practical 4980 / 6296ef16d8 as runtime-source SHA 991 / 1,317. Confirmed totals remain 117 = 40 inherited + 77 SAS-specific. Next runtime-source SHA: practical 4979 / 8656a8b51f.


SAS-C141 - practical 4979 through 4962 early-tech-rework/Worker-crash-contract lineage audit - COMPLETE; no new defect
  Reviewed 17 runtime-source SHAs: practical 4979 / 8656a8b51f, 4978 / ce3a118eab, 4976 / c332858b4a, 4975 / e8199e74b3, 4974 / 7e5feb72ab, 4973 / 0c97546299, 4972 / b8b258c859, 4971 / 5e16104b83, 4970 / e2dbc313c7, 4969 / 6e40f0004c, 4968 / ca1e3bdee8, 4967 / 455a85d30a, 4966 / 3d9364ac90, 4965 / c5ed88db27, 4964 / fc87dcc301, 4963 / 3a8be4517c and 4962 / 2749d6e0ea. Practical 4977 is documentation-only and is not counted as runtime source. No current defect was established.
  Practicals 4979/4978 and 4976 through 4966 are the first-release game-speed/starting-tech/tech-tree identity/data tranche plus associated event/audio/building/unit retargeting. Current descendants are coherent: the removed TECH_EDUCATION/TECH_RIFLING/TECH_SCIMETHOD identifiers have no live current runtime/XML references, and remaining mentions of removed TECH_GUILDS/TECH_PRIESTHOOD/TECH_ARCHERY/TECH_SCIENTIFIC_METHOD are explanatory comments rather than active prerequisites. The replacement tech identities are present in current TechInfos. Practical 4979's first Normal/Quick/Epic/Marathon timelines are also superseded by the later Game-Speed lineage already audited at C127, whose current eight timelines close exactly at their intended calendar endpoint/turn counts.
  One genuine but fully historical tech-rework regression is recorded at practical 4967. Repurposing Scientific Method into Water Wheel mechanically changed two live building `<ObsoleteTech>TECH_SCIENTIFIC_METHOD</ObsoleteTech>` entries to `<ObsoleteTech>TECH_WATER_WHEEL</ObsoleteTech>`, carrying an unrelated late-tech obsolescence onto the new early Water Wheel identity. Practical 5027 explicitly repairs both surviving Water-Wheel obsolescences, replacing one with NONE and the other with TECH_NATURALISM. This is repaired lineage, not a present-day KI.
  Practical 4965 is the birth of the water-food-building/Harbor emergency-priority fast path. Later descendants add the coastal-city gate, tune the food threshold and place it behind the regular-building optimization toggle; current code therefore does not merely preserve the raw birth rule. The current Harbor family was already checked in the later low-food-building lineage; no independent 4965 survivor is backdated here. The simultaneous Barbarian and promotion edits are deliberate balance/data work.
  Practicals 4962 through 4984 form a much more clearly SAS-specific Worker-crash/rewrite lineage than their contemporary Known-Issue prose sometimes guessed. In particular, immediately before practical 4962 the rewritten AI_bestCityBuild evaluated `!canBuild(kPlot, eBestSupposedBuild) || eBestSupposedBuild == NO_BUILD`; because C++ short-circuits left-to-right, that can call canBuild with the NO_BUILD sentinel before testing the sentinel. Practical 4962 repairs the ordering, initializes caller output pointers to NULL/NO_BUILD on entry, uses temporary candidate locals, and publishes a plot/build pair only when a candidate is actually accepted. These are concrete output/enum-contract repairs in SAS-rewritten code.
  Practical 4963 then removes the old AI_betterPlotBuild postprocessing after a reproducible crash and, more importantly for current behavior, avoids replacing the exact build selected by the custom evaluator. Practical 4964 empirically suppressed a later turn-156 crash by removing the inherited `isSamePlotGroup(*pBestPlot, getOwner())` route-selection predicate, but the historical evidence does not establish that inherited predicate as the root defect: practical 4981 deliberately restores that exact predicate when the experimental routing rewrite is rolled back, and current SAS still uses it after explicit NULL/NO_BUILD candidate guards and path validation. The old “most likely base AdvCiv/Civ4” attribution is therefore retained only as historical speculation, not archaeology provenance.
  Practicals 4972/4973 continue that experimental Worker routing phase: 4972 rewrites the move/route branch around a remembered target-city ID and a two-leg route pattern; 4973 adds city-demand/orphan routing and replaces that stable city ID with a raw CvCityAI pointer. Practical 4980 later reverses the raw-pointer choice while investigating another reproducible crash, and practical 4981/4984 intentionally roll most of the experimental routing/demand work back. Current AI_nextCityToImprove has since been extensively rewritten around per-city AI_bestCityBuild outputs, one-worker reservations, direct path scoring and explicit output guards. No surviving invalid pointer, NO_BUILD dereference or inherited same-plot-group crash path was established.
  Practical 4962 also disables AIFoundValue::adjustToCivSurroundings after the observed camel/desert settlement case and replaces only selected surrounding-city logic inline. That remains a deliberate current design choice; the historical empirical observation proves that the old adjustment changed that save's site ranking, but does not by itself prove a specific invalid operation or invariant violation inside the inherited function. It is therefore not manufactured into a new KI from archaeology alone.
  Certified cursor advances through practical 4962 / 2749d6e0ea as runtime-source SHA 1,008 / 1,317. Confirmed totals remain 117 = 40 inherited + 77 SAS-specific. Next runtime-source SHA: practical 4961 / f330e11dea.


SAS-C142 - practical 4961 through 4930 early-production/scrapping/Worker-policy/save-compatibility audit - COMPLETE; KI#213 broadened, no new defect
  Reviewed 30 runtime-source SHAs: practical 4961 / f330e11dea, 4960 / 47d1e871a4, 4959 / 40da02da6f, 4958 / 342c91dcbd, 4957 / fe58e13463, 4956 / b9d3434aae, 4955 / 518c0f099a, 4954 / 261aaba47e, 4953 / 0704fd52d2, 4952 / 1312c3ccd6, 4950 / e7181173b7, 4949 / 0910e8acee, 4948 / f699f0b166, 4947 / 9cb440007d, 4946 / 98a29279d3, 4945 / f70627ec6f, 4944 / 4a6ecaa861, 4943 / 87a57bf06f, 4942 / ad58ea3c5b, 4941 / 294af9e516, 4940 / 5dfb74d7bc, 4939 / 00866a0741, 4938 / eb01565ae9, 4937 / b5e38055a8, 4936 / 8797e7f461, 4934 / 56bd62671a, 4933 / b8fb51f514, 4932 / 6a226d0fba, 4931 / bda7d2fa2e and 4930 / 6ac4d9729f. Practicals 4951 and 4935 do not touch primary runtime C++/Python and are not counted. No new current defect root was established; existing KI#213 is materially broadened and its architectural birth is now pinned to 4930.
  Practicals 4961/4960 are early tech-tree and Wonder-production/value policy. Their changed prerequisites, obsolescence/value gates and Heroic-Epic/Wonder tuning are intentional balance work and later descendants substantially retune the same systems. No malformed current identifier, illegal production state or independent present-day valuation invariant was established.
  Practical 4959 is the birth of an already-repaired no-production fallback weakness: the first hard fallback explicitly treated a head Process as though the production queue were invalid (`bNeedFallback = bQueueEmpty || bHeadInvalid || bHeadProcess`). A deliberate Process could therefore be replaced by forced military production and bypass ordinary spending policy. Practical 6086 explicitly removes `bHeadProcess` because Process is valid production. The current helper retains that repair, so this is historical provenance rather than another KI. Practicals 4955 and 4958 are earlier no-production/bonus-roading experiments whose rough routing and fallback behavior is replaced or rolled back by the later 4959 and 4981/4984 lineages.
  Practicals 4956/4957 are the first broad SAS attempts to stop AI overproduction/scrapping by adding role/era/cost gates to `canScrap` and production choice. Their architecture is superseded by the later obsolete-only/recommended-scrapping system already audited around 5477/5945/5979 and the modern centralized `canScrap` protections. They are distinct from practical 4933: 4933 removes the inherited direct `AI_workerSeaMove` block that could produce a Work Boat and immediately scrap it when `AI_neededSeaWorkers()==0` (or when treated as ice-locked), causing repeated produce/scrap loops. That direct sea-Worker scrap path remains gone in current SAS; 4933 is a genuine inherited/base repair lineage, not a SAS regression.
  Practical 4954 is an early Worker-retreat repair that wakes certain safely-returned Workers and stops retreating owned-territory Workers merely because of the broader outside-border threat logic. Current Worker movement has since been heavily rewritten. Practical 4953's early weaker-city-garrison gate scales its Normal-speed turn threshold by TrainPercent; the current eight game speeds' TrainPercents scale with their turn totals, so the retained 120-Normal-turn boundary is not a hidden speed-unit bug.
  Practicals 4950/4952 contain a clean historical SAS regression/repair pair. The giant 4950 normal-AI building prefilter was accidentally born under `if (bBarbarian && !bMinor)`, so the intended major-AI prefilter effectively ran for Barbarians rather than ordinary major AIs. Practical 4952 explicitly flips this to `if (!bBarbarian && !bMinor)` and repairs adjacent prefilter details such as the Stable-class comparison. Current source retains the corrected major-AI gate. The original rollout's empirical documentation therefore should not be read as evidence that every new prefilter branch was already affecting normal major AIs in 4950.
  Practical 4949 deliberately introduces hard promotion-selection policy (forbidden situational promotions and strong first choices for strict city-attack/city-defense UnitAIs) while also consolidating promotion identities such as Navigator and collateral-damage tiers. The current XML/runtime identities used by the surviving rules exist, absent optional lookup IDs safely resolve to NO_PROMOTION rather than matching a real candidate, and the remaining choices are strategy policy rather than a legality/invariant failure. No current defect was established from the hard rules.
  Practicals 4948 through 4940 intentionally remove old `uiFlag` save-format compatibility branches across strength-memory/game/init/map/team/unit/area/plot/city/player serializers. The retained code corresponds to the then-current/latest serialization path while the project explicitly declares cross-version savegame compatibility unsupported. No current same-version read/write mismatch was established; deleted fallback branches are therefore intentional compatibility debt removal rather than runtime defects.
  Practical 4939 is the birth of the unsafe rendering cache already repaired at 4982: it caches live `NiColorA` values/references as process-statics in CvGameInterface. Practical 4982 deliberately removes those live-object statics after the UI/menu-crash concern; current rendering only caches stable ColorTypes IDs where useful and fetches live color values. The other 4939 static additions inspected are configuration/enum IDs rather than equivalent live `NiColorA` references. No new current root is counted.
  Practicals 4938 through 4936 are early specialist-growth and first-city/site-selection policy. Their low-pop specialist guards, coastal/low-food first-city heuristics and free initial site-search window are deliberate AI tuning and have later descendants/repairs in the already-audited first-city and specialist lineages. No current invalid enum, impossible site or deterministic starvation/placement failure was established from these birth forms.
  Practical 4934 partially repairs the land-build-cache split introduced at 4930 by restoring `AI_getImprovementValue` for water plots after the fully-disabled evaluator left Work Boats parked; it also changes `AI_neededSeaWorkers` lookahead from 5 to 0 so future/unbuildable resource technology does not demand premature boats. The later/current duplicate-primary/second-water-area problem is existing inherited KI#216 and was fixed at practical 6170; it is not re-counted here.
  Practicals 4932/4931 are a dense first-generation AIFoundValue/unit-production/Worker-policy rewrite. Their map-heavy offensive/defensive UnitAI weighting, settlement-on-resource/terrain scoring and per-city Worker selection are primarily explicit heuristics; later 4962-4984 and much newer Worker/site-selection architectures repeatedly guard, revert or replace the unstable portions. The exact NO_BUILD-before-sentinel crash contract audited at C141 is not born here: reverse-diff search pins that specific bad condition to practical 4922, which remains below this checkpoint and will be audited explicitly there.
  KI#213 is broadened at practical 4930, and 4930 is now pinned as its architectural birth. That commit functionally disables `CvCityAI::AI_getImprovementValue` entirely so the custom `CvUnitAI::AI_bestCityBuild` owns land-improvement choices; 4934 restores the legacy evaluator only for water. Current source therefore still deliberately returns 0/NO_BUILD for every land plot in the legacy city best-build cache. C006/KI#213 had already proven one live consequence in Worker-demand accounting (worked unimproved Flood Plains can be a valid custom Worker job while the legacy cache says NO_BUILD) and already identified the root as multiple non-unit systems consuming an intentionally-empty land cache.
  A second deterministic supported consequence is now source-proven: current `CvPlayerAI::AI_advancedStartPlaceCity` explicitly calls `pCity->AI_updateBestBuild()` and then purchases land improvements only from `AI_getBestBuildValue`/`AI_getBestBuild`, rejecting NO_BUILD and non-improvement builds. Since every ordinary land improvement is zero/NO_BUILD by design, AI Advanced Start cannot purchase any new land improvements through this loop. The same loop only buys the associated population after successful improvement purchases, so that economic-development/population branch is also suppressed; remaining Advanced Start points can instead be spent elsewhere and are ultimately converted to gold on exit. This is reachable normal supported gameplay: current SAS retains `GAMEOPTION_ADVANCED_START`, and nonhuman players with Advanced Start points automatically execute `AI_doAdvancedStart()` when their turn activates. The already-recorded KI#213 repair direction—one shared city/plot land-build-policy evaluator consumed by both the city cache and Worker selector while keeping unit pathing/mission execution in CvUnitAI—would repair both Worker-demand and Advanced-Start symptoms at their common root. KI#213 is therefore broadened, not duplicated with a new number.
  Certified cursor advances through practical 4930 / 6ac4d9729f as runtime-source SHA 1,038 / 1,317. Confirmed archaeology totals remain 117 = 40 inherited + 77 SAS-specific; KI#213 broadening does not change the count. Next runtime-source SHA: practical 4929 / 523b965b49.

SAS-C143 - practical 4929 through 4920 Worker/citizen/site/production audit - COMPLETE; KI#320 FOUND
  Reviewed 10 runtime-source SHAs: practical 4929 / 523b965b49, 4928 / beb304dcd5, 4927 / 1ce83aadff, 4926 / 4148c4ace7, 4925 / c4ffe69511, 4924 / a6525a8b7c, 4923 / 0b1701c016, 4922 / 7e7a475897, 4921 / cc59407b55 and 4920 / d02f38ffd3. One new current SAS-specific defect, KI#320, is confirmed. Several genuine early Worker regressions are also pinned to their later repairs rather than re-counted.
  KI#320 (SAS-specific) is confirmed at practical 4928 in CvCityAI::AI_yieldValue. The KI#40 growth policy deliberately has two components when a city has spare happiness and is not currently converting food into production: multiply food value by the happiness surplus, and at 3+ surplus reduce hammer/production value so growth can beat a strong mine/hill. The food half works. The hammer half is dead because the function completely normalizes and adds `iProductionValue` to `iValue` before computing `iHappySurplus`; only afterward does the new block divide the now-local `iProductionValue`, and that variable is never used again before return. Current CvCityAI.cpp retains this exact order. Later descendants touching the block preserve the executable order, while current Main Changes Guide and KI#40 documentation still explicitly promise that high happiness 'deemphasize[s] hammers' / reduces hammer-production value. This is therefore an implementation/order-of-operations defect, not merely stale prose or a balance preference. Surgical repair direction: compute/apply the happiness-based `iProductionValue` reduction before production is normalized and added to `iValue`, while keeping the existing food multiplier and `!isFoodProduction()` exception. This is separate from KI#40 itself: KI#40 is the intended policy/feature; KI#320 is the dead second half of its implementation.
  Practical 4929's bonus-roading/Worker-food changes do not establish another current root. One surviving `AI_betterPlotBuild` comment/branch is semantically misleading: `doesImprovementConnectBonus` means an improvement is a valid trade-enabling improvement for that bonus/tech, not that the improved plot needs no route, so its local `bNeedsRoad` naming/test is conceptually inverted. Current active resource-connection paths nevertheless compensate: `AI_improveBonus` and the early Worker-on-improved-bonus check explicitly detect a proper improved but disconnected bonus and call route/connect logic, while normal `AI_improveCity` executes the custom selected build directly rather than passing it through this branch. No deterministic present missed-resource connection was proven, so this remains an unnumbered robustness/comment issue rather than a manufactured KI.
  Practical 4927 introduces a real short-lived SAS Worker regression: its new move-away condition calls `AI_nextCityToImprove` when the current city's `foodDifference(false) <= 0`, so a Worker can be told to leave precisely because its city is stagnant/starving. Practical 4931 removes that backwards food condition and keeps only a bounded unhappiness case. Current Worker architecture is much newer and does not retain this trigger; this is repaired historical lineage.
  Practical 4923 is the birth of another repaired Worker/citizen-allocation weakness. Its starvation gate rejects a proposed reassignment whenever the post-swap city would still have negative food, even when the new job substantially improves food and reduces the deficit. Practical 5996 explicitly repairs this exact overstrict condition by only blocking a still-starving reassignment when `iNextFood < iCurrentFood`, allowing partial food recovery. The contemporary Bursa example/documentation confirms the failure mode (high-food plots remained unused while Artists were retained during severe starvation). This is historical lineage related to the already-known KI#34 family, not a current new root.
  Practical 4922 is now pinned as the exact birth of the Worker sentinel/output-contract bug reconstructed from the newer side at C141. The new `AI_bestCityBuild` tests `!canBuild(kPlot, eBestSupposedBuild) || eBestSupposedBuild == NO_BUILD`, so C++ can evaluate `canBuild` with the NO_BUILD sentinel before short-circuiting to the sentinel test. Practical 4962 reverses the validation order, separately rejects NO_BUILD before calling `canBuild`, initializes output pointers/values defensively and only publishes a valid plot/build candidate after acceptance. Current source retains the hardened architecture. Thus the old reproducible Worker-crash lineage is concretely SAS-state/output-contract provenance, not evidence that the inherited `isSamePlotGroup` predicate itself was intrinsically broken. The large hardcoded bonus->Build table introduced around this rewrite is also superseded by the current data-driven `CvPlot::SAS_getBonusSpecificBuild` cache built from live Build/Improvement/Bonus XML.
  Practical 4925's broad sea-unit devaluation and Settler experiment is mostly temporary policy lineage. Practical 4932 explicitly restores the K-Mod military sea-unit scoring/multipliers after the early experiment proved too broad. The Settler UnitClass one-player-instance limit survives intentionally and is documented as the KI#37 workaround/rebalance, not an accidental legality defect. Practical 4924's nearest-city site-distance shaping is likewise policy ancestry superseded by the current bounded CitySiteEvaluator distance logic.
  Practicals 4926, 4921 and 4920 are early iterations of the custom Worker selector and plot-build rewrite. Their one-worker-per-target preference, feature-removal/route handling and bonus-priority concepts are repeatedly rewritten by 4922-4984 and much later Worker infrastructure; no additional present-day invalid build, duplicate mission or deterministic stranded-worker path was established from their birth forms. Practical 4926's accompanying technology/building/leader XML changes are intentional data/balance edits whose current identifiers resolve normally.
  Certified cursor advances through practical 4920 / d02f38ffd3 as runtime-source SHA 1,048 / 1,317. Confirmed archaeology totals become 118 = 40 inherited + 78 SAS-specific. Next runtime-source SHA: practical 4919 / fcf32edcd9.

SAS-C144 - practical 4919 through 4900 Citizen fallback/Sevopedia/Future-unit/collateral audit - COMPLETE; KI#44.5 broadened
  Reviewed 19 runtime-source SHAs: practical 4919 / fcf32edcd9, 4918 / 44ffa78d43, 4917 / 0efaba32ca, 4916 / a12a30bc7e, 4915 / faa3eb2669, 4914 / 7ef477d55d, 4913 / 54d110994c, 4912 / 9ac030d829, 4911 / 92fc29e26e, 4910 / eba1162c66, 4909 / b2560ac6b6, 4908 / 01a4d1bcbd, 4907 / 7a910521a8, 4906 / fb6e6b5395, 4905 / 6c08e62699, 4904 / 9c62c3103e, 4903 / 42c6513b0d, 4902 / eb42b210a7 and 4900 / 56ea046e0d. Practical 4901 is main-menu image/docs only and is excluded from the runtime-source denominator. No new defect number is added in this tranche; existing KI#44.5 is materially broadened and several temporary data/UI regressions are pinned to explicit later repairs.
  Practical 4919's original Citizen-specialist guard is safer than its commit title first suggests. `AI_specialistValue` reaches `AI_jobChangeValue`, and the birth guard returns 1 for the default `SPECIALIST_CITIZEN`. `AI_addBestCitizen` initializes `iBestValue` to -1, so Citizen is strongly disfavored compared with productive plots/specialists but remains the engine's guaranteed unlimited last-resort specialist when no ordinary job is available. That matches the KI#44.5 title's intended "unless absolutely necessary" contract.
  Descendant tracing from 4919 exposes the current KI#44.5 failure precisely. Practical 4938 changes the Citizen result from the safe last-resort value 1 to the global hard-forbidden value `SAS_AI_JOB_CHANGE_VALUE_AI_JOB_FORBIDDEN` (currently -100000). Current `CvCity::isSpecialistValid` explicitly keeps the default specialist valid regardless of ordinary specialist-slot caps, but `AI_addBestCitizen` only accepts candidates whose value is greater than its -1 no-candidate sentinel. Therefore a -100000 Citizen cannot be selected even when every workable plot and ordinary specialist slot is exhausted. `AI_assignWorkingPlots` has no final fallback: if `AI_addBestCitizen` returns false while `extraPopulation()` or `extraSpecialists()` is positive, it emits "failed to assign extra population" / "failed to assign extra specialist", breaks and then reaches the extra-population assertion with real productive population unassigned. Current `SPECIALIST_CITIZEN` is not a zero-output dummy (0 food, +1 production, +1 commerce), so this is a gameplay loss as well as a debug-assert cascade. KI#44.5 already records exactly this cascade and explicitly wondered whether it was caused by Citizen being unavailable as fallback; the source now proves that relationship. KI#44.5 is therefore broadened rather than duplicated as KI#321. Preferred root-level repair: preserve the hard strategic deprioritization during normal comparisons, but if `AI_addBestCitizen` finds no plot/ordinary specialist candidate, explicitly assign the valid default Citizen as guaranteed last resort. This also preserves the analogous human convenience policy without making a city fail assignment merely because all preferred jobs are exhausted.
  Practicals 4918 through 4914 are principally Sevopedia/concept/improvement presentation and intentional terrain/balance data. The new Rivers/Road/Railroad concept-routing and improvement Terrain/Feature-valid panels use the exposed current getters and current RouteInfos (Road/Railroad) consistently. Practical 4916's ominous "fix inaccurate code" title is only a small text-spacing/centering correction in `SevoPediaImprovement.py`. Practical 4914's broad Python surface is overwhelmingly comment wording plus the Improvement-page refactor; the live Workshop/Tundra and Tundra-commerce XML edits are explicit balance changes rather than accidental identifier drift.
  Practicals 4912 and 4911 do contain a real historical unit-AI data mismatch. When Robotic Recon Drone and the reworked land-domain Airship are converted from DOMAIN_AIR to DOMAIN_LAND/recon, their `DefaultUnitAI` is accidentally left as `UNITAI_ATTACK_AIR` even though their permitted UnitAI list is Explorer-oriented. Practical 4949 explicitly changes both to `UNITAI_EXPLORE`; current XML retains the repaired values. The other new Future-Era units in 4910/4909/4903 have domain/combat/default-UnitAI combinations consistent with their current land roles and their referenced classes/technologies resolve. No present orphan class, bad default role or impossible prerequisite was established.
  Practical 4908 disables the experimental repeatable-tech flag that 4907 had enabled one commit earlier. Practical 4907's `buildBRepeatString` therefore survives as dormant presentation infrastructure: current CIV4TechInfos has no `<bRepeat>1</bRepeat>` technology. Its player-context `isHasTech` suppression could be questionable for a hypothetical live repeatable tech after the first acquisition, but there is no current SAS surface on which it can misdescribe a tech, so no current KI is manufactured from dormant infrastructure.
  Practicals 4906/4905 close clean-current around their Pedia additions and resource/feature data. The 4905 Feature remove-production/time validator only raises when a later positive value is greater than the first one, so as generic validation code it would miss a lower differing value encountered later; however an exhaustive current CIV4BuildInfos check finds no Feature whose positive removal-production or removal-time values differ among Builds. Thus current SAS data satisfies the stricter intended invariant and the asymmetry has no present consequence. The one-line CitySiteEvaluator hunk is comment clarification only; Gemstones/Flood-Plains legality is handled by the accompanying intentional XML changes and existing non-hill bonus-site penalty.
  Practical 4904's military-support Pedia line and Privateer support flag are direct XML/help alignment. Practical 4902's no-unit-combat early return prevents the Unit promotion panel from indexing a nonexistent combat class and remains appropriate for current noncombat units.
  Practical 4900 is the birth of the expanded collateral-damage presentation: base collateral, limit and max-defender data can be shown even when base collateral damage is zero, and Promotion applicability rows are repaired by retaining the required append-row side effect. Several later commits refine this family. In particular the actual-unit hover eventually acquired an overstrict combat gate that hid functional Bomber/Stealth Bomber collateral and was repaired under existing KI#235; current actual-unit help accepts either land/base combat or air combat, while basic/Sevopedia help uses the same equivalent combat-capability distinction. The current XML collateral limits and displayed percentage conversions are internally consistent. No second current root is backdated to 4900.
  Certified cursor advances through practical 4900 / 56ea046e0d as runtime-source SHA 1,067 / 1,317. Confirmed archaeology totals remain 118 = 40 inherited + 78 SAS-specific; KI#44.5 broadening does not change the count. Next runtime-source SHA: practical 4899 / 8662b6bb21.

SAS-C145 - practical 4899 through 4860 Future-era/settlement/Fort/Sevopedia/mounted-split audit - COMPLETE
  Reviewed 28 runtime-source SHAs: practical 4899 / 8662b6bb21, 4898 / 9daa176dfc, 4897 / 07b48542c9, 4894 / 0c8c5d8906, 4893 / 3816ed06c1, 4892 / b09ba3d510, 4891 / df113f66c8, 4890 / d16c95379e, 4889 / 1cc70ae3be, 4888 / 9fcf571e9a, 4887 / 96aa082a99, 4886 / c20688b2c2, 4885 / 95347a520d, 4884 / 6d82d51fe1, 4883 / f6b992b469, 4882 / 6c860ed1eb, 4878 / 632293eb54, 4877 / 67a01372c8, 4876 / 4622d268ac, 4875 / 0a47e63d61, 4874 / 4516bcdd59, 4873 / 3997ab6d2b, 4872 / 45f29176c6, 4871 / 378e9fd759, 4869 / f7729f4168, 4868 / a538d84e14, 4867 / 06bf9fa587 and 4866 / b58da78c99. Practicals 4896/4895, 4881/4880, 4879/4870 and 4865 through 4860 do not touch primary game-executed C++/Python; practical 4860 changes only developer/test Python plus data and therefore does not consume the denominator. No new current defect is promoted in this tranche; one real 4875 sibling-widget regression is pinned to its explicit 5003 repair.
  Practicals 4899-4897 are presentation/audio/main-menu and Unit Chart evolution. The changed runtime Python is asset-path/layout/help wiring; current art keys resolve and the later collateral-display lineage already culminates in the separately audited KI#235 repair. Practical 4894's Python changes accompany intentional civ-specific unit/leader/data rework and do not establish an executable mismatch surviving current source.
  Practicals 4893 through 4885 are iterative `CitySiteEvaluator` policy tuning. The non-bonus terrain shaping and high-production/high-commerce resource-site adjustments remain explicit heuristic policy rather than legality gates: they add/subtract bounded found-value terms and do not make invalid city sites valid or vice versa. Current terrain/feature assumptions used by the optimized branches still hold for SAS data (notably Flood Plains remains Desert-compatible and the special high-food/low-food distinctions match the current bonus placement rules). No deterministic wrong-site or out-of-bounds/data-sentinel failure was established from this sequence.
  Practical 4889's Barbarian World Wonder guard survives current source and does what its stated policy requires: after `AI_bestBuildingThreshold` selects a building, a Barbarian owner rejects it if it is a World Wonder. Returning false lets the surrounding production chooser continue to other production categories rather than constructing the forbidden Wonder. The code does not alter wonder legality for normal players and does not dereference an invalid building because it runs only after `eBestBuilding != NO_BUILDING`.
  Practical 4884's aggressive Fort de-prioritization was checked against the current resource-connection selector rather than accepted from comments. `AIFoundValue::getBonusImprovement` still skips `isActsAsCity()` improvements when estimating a resource's normal improved yield, but current Bonus/Improvement data has no resource for which every valid trade-enabling improvement is ActsAsCity, so no current resource is falsely treated as inherently unimprovable. In `AI_connectBonusCost`, the simplified current return is raw Build time. Every ordinary land resource improvement currently builds faster than Fort (Fort = 1000), except Well ties at 1000; Well is enumerated before Fort and the selector only replaces its best Build on a strictly smaller cost, so Fort cannot steal the Oil connection on the tie. Other standard resource improvements are 400-600 and therefore beat Fort directly. The removed `iMissingWorkersInArea` multiplier was common to ordinary candidates and did not change their relative order; Fort-specific defense/sea-connection bonuses were intentionally removed. No present Fort-selection inversion is established.
  Practicals 4883/4882 are broad Future-era/Sevopedia/data additions. Their executable Pedia changes use live exposed getters and the later centralized helpers; current BuildingClass-needed, collateral, terrain/feature and unit relations resolve against current XML. No orphan runtime identifier was found from the renamed Japanese/other content in this birth layer.
  Practicals 4878 through 4871 are mostly art/data renames plus rapid Sevopedia terrain/feature/unit panel iteration. The initially ad-hoc Hill pseudo-terrain helper added at 4873 is normalized at 4874/4875 to the real pseudo TerrainInfo ID/button path, and current code no longer depends on the removed scan helper. The Jungle removal-production values and dedicated removal Builds later audited at C094 remain data-consistent. No current panel relation was found to index a missing Terrain/Feature/Unit entry in this tranche.
  Practical 4875 contains a real historical fix-plus-regression lineage. The obsolete-bonus Tech Chooser widget is constructed as `WIDGET_HELP_OBSOLETE_BONUS, data1=bonusID, data2=-1`, so changing its AltAction redirect to `iData1` correctly fixes the inherited broken obsolete-bonus Pedia jump. But the same switch block initially leaves `WIDGET_HELP_BONUS_REVEAL` falling through to that new `iData1` redirect even though revealed-bonus widgets are constructed as `data1=techID, data2=bonusID`. Ordinary revealed bonuses therefore temporarily jump using a tech index as a Bonus index. Practical 5003 explicitly diagnoses this exact regression and separates the cases again: `WIDGET_HELP_BONUS_REVEAL` returns to `m_iData2`, while `WIDGET_HELP_OBSOLETE_BONUS` retains `iData1`. Current `CvDLLWidgetData.cpp` and `CvTechChooser.py` have those complementary contracts, so this is repaired historical SAS lineage rather than a current KI.
  Practical 4866's mounted-combat split updates the engine Feat enum, Python enum exposure, advisor Feat popup handling and Barbarian mounted-terrain heuristic together. Current Unit Naming options/config use `MOUNTED_MELEE`/`MOUNTED_RANGED`, and no primary runtime reference to the removed unsplit `FEAT_UNITCOMBAT_MOUNTED` remains. Numeric enum movement is not treated as a cross-version save-compatibility defect because SAS explicitly does not promise cross-version save compatibility. The surrounding promotion/unit-combat data changes are intentional balance architecture.
  Certified cursor advances through practical 4860 / 9fdf775f56 as runtime-source SHA 1,095 / 1,317. Confirmed archaeology totals remain 118 = 40 inherited + 78 SAS-specific. Next runtime-source SHA: practical 4859 / 72f6b76010.

SAS-C146 - practical 4859 through 4840 leader/data/Python-linter/impassable-help audit - COMPLETE
  Reviewed 14 runtime-source SHAs: practical 4859 / 72f6b76010, 4858 / 55bf638ee0, 4857 / 7a5eadc235, 4856 / 6bbdbd1595, 4855 / d68d111389, 4850 / 28c746618f, 4848 / 441d5de9cf, 4847 / 2973ba48f3, 4846 / f7e27409d3, 4845 / bc799686df, 4844 / 363dca74cf, 4843 / eb1f0f2974, 4842 / 666603e380 and 4840 / 46b1576a91. Practicals 4854-4851, 4849/4841, and 4839/4838 do not touch primary game-executed C++/Python. No new current defect is promoted; two concrete 4847 linter regressions are distinguished as immediate-repair versus unreachable-obsolete-helper lineage.
  Practicals 4859-4855 and 4850 are mainly leader/content/tech-tree/Sevopedia wiring. The Noble Knights event rename in 4858 deliberately accepts either the Horse-Knight or Camel-Knight class meeting the world-size target (`if horse < target and camel < target: return false`), so the conjunction is not a missing-count bug. Current renamed tech/unit/class identifiers referenced by the runtime Python resolve after later data evolution. Practical 4855's BugEventManager runtime hunk is comment-only.
  Practical 4847 is a very broad Ruff/Pylance cleanup and was mechanically separated from presentation-only edits. Most executable transformations are Python-2.4-equivalent: `== None`/`!= None` become identity checks, `<>` becomes `!=`, explicit `== True/False` on actual boolean-returning engine calls becomes direct truth tests, line-continuation/collection formatting is preserved, and `string.split`/`string.zfill` forms later normalized around native string methods keep the same current behavior. Replacing bare `except:` with `except Exception:` intentionally stops swallowing control-flow/base exceptions and no current Civ4 extension exception path requiring a non-Exception catch was established.
  One 4847 regression is source-proven and repaired immediately at 4848. `SpyUtil.getSpending(playerOrID, ActiveplayerOrID=None)` originally includes all teams when no active-player visibility filter is supplied and otherwise includes only met teams. The linter commit accidentally changes the loop condition from `ActiveplayerOrID == None` to `ActiveplayerOrID is not None`, which reverses that first branch and can also evaluate `pActiveTeam` when no active player initialized it. Practical 4848 explicitly restores the correct `ActiveplayerOrID is None or pActiveTeam.isHasMet(...)` condition. Current source is repaired.
  A second 4847 semantic change survives textually but is not a current runtime defect. `UnitUtil.getCanTrainUnits(..., military=None)` changes explicit tri-state checks (`military == True` / `military == False`) to truthiness (`military` / `not military`). Under the documented API, `None` means no military/civilian filter, whereas the new expression treats `None` like `False` and filters out military units. Current source still contains this mismatch, but the function is explicitly marked `*** OBSOLETE ***` and a repository-wide current runtime search finds no call outside its own definition. It is recorded as latent obsolete-helper semantics rather than promoted to a KI. If the helper is ever revived, the original explicit `is True` / `is False` tri-state contract should be restored.
  Practical 4846's removal of unused imports was checked at its substantive replacements: `string.digits` becomes the identical ASCII digit literal used by these parsers, `string.split` becomes `str.split`, PLE `string.zfill` becomes `str.zfill`, and the `string.capwords` replacement in RandomNameUtils uses split/capitalize/join semantics matching capwords' whitespace-normalizing behavior. No current NameError/import side effect was established. Practical 4845 removes an accidental always-on Leader debug attribute dump; 4844 and 4843 continue Pedia/data cleanup without a surviving runtime contract break.
  Practical 4842 defers Sevopedia Leader cache construction until the Leader category is actually entered. Later cache architecture is substantially rewritten, but the birth change itself does not cache player/game-state-dependent data before XML/game context is ready and no stale-language/current-leader mismatch survives from this implementation.
  Practical 4840 exposes `CvUnit` forwarding helpers for raw terrain/feature impassability and adds actual-unit help lines. The forwarding methods return the same immutable UnitInfo fields used by basic help. As independently checked in the later 5062/5061 Terrain/Ice audit, current data does not contain a unit whose raw Ice/terrain impassability is simultaneously overridden by a current pass-tech in a way that would make these lines self-contradictory; units intended to bypass impassability use the engine's `canMoveImpassable` path. The basic-help reordering of invisibility versus impassability blocks is presentation-only.
  Certified cursor advances through practical 4840 / 46b1576a91 as runtime-source SHA 1,109 / 1,317. Confirmed archaeology totals remain 118 = 40 inherited + 78 SAS-specific. Next runtime-source SHA: practical 4837 / ee7bd44521.

SAS-C147 - practical 4837 through 4804 AI-Personality/Camel/docstring-performance lineage audit - COMPLETE
  Reviewed 15 runtime-source SHAs: practical 4837 / 5412593345, 4836 / 76ab6670a5, 4835 / 41755fedd2, 4834 / 1d08e9a28b, 4833 / d4d69a9dc8, 4832 / 44cb420869, 4830 / 1569533198, 4827 / 82cef5e53c, 4826 / 17509a8b68, 4821 / a5a449bc70, 4819 / c0c34de5ec, 4808 / 064af454fb, 4806 / 2d758a9be7, 4805 / 1db0a8084a and 4804 / 0480b0f317. Intervening practicals without primary game-executed C++/Python are not counted. No new current KI is established; the strongest defects in this tranche are short-lived historical regressions repaired immediately or by later descendants.
  Practical 4836's unusually broad Python performance sweep was not certified merely from its title. Current runtime Python has no `__doc__`, `inspect.getdoc`, pydoc-style or equivalent docstring consumer, so the converted function/class/module docstrings are not callback metadata or gameplay configuration. Removed large triple-quoted blocks were already intentionally disabled code/examples. The non-docstring executable edits isolated from the 68 runtime Python files are indentation/whitespace repairs plus small explicit UI/config changes rather than hidden reflection/discovery behavior. In particular configobj's comment-writing helpers are reindented without changing their branches/values, and the Options/Domestic-Advisor edits normalize malformed indentation around already-existing statements. No current behavior depends on the removed `__doc__` values, and no descendant mass syntax/body repair points to a bodyless-function regression.
  Practicals 4832 -> 4833 contain two concrete one-commit SAS regressions. First, the Camel-resource adaptation accidentally inserts a bare `BONUS_CAMEL` identifier into `CvRandomEventInterface.doWiningMonks2()` immediately after fetching the event plot. No Python variable by that name is defined there, so firing that event would raise NameError before the improvement is placed. Practical 4833 removes exactly the stray line. Second, 4832 leaves a large LeaderHead debug dump (`printObjAttrs` plus the flavor/NoWar/contact/memory/BBAI helper) unconditionally inside `SevoPediaLeader.placeLeaderHeadPane()`, meaning every Leader page would emit the expensive debug output regardless of the intended debug flag. Practical 4833 removes those calls as well. The helper's hardcoded debug loop limits therefore do not become normal long-term panel logic. Current source contains neither regression.
  Practicals 4835/4837 are the transition from the generated `leaders_data.py` runtime dependency to direct `CvLeaderHeadInfo` getter-driven AI-Personality data. Later descendants repeatedly refactor the cache/category/normalization pipeline (including the separately certified 5299 family), and current source no longer depends on the removed runtime leaders_data module. The getter-existence guards and excluded-leader handling operate on real current LeaderHeadInfo methods; no stale generated-data or missing-key current root was established from this birth layer. Practical 4837's generated-script key renaming is external tooling/document consistency rather than runtime Pedia state.
  Practical 4832 also exposes the BBAI victory weights and the two missing attitude-threshold getters to Python while introducing Camel content. The C++/Boost-Python bindings are direct forwards to CvLeaderHeadInfo fields and remain the intended source used by later AI-Personality work. The temporary debug exploration of Contact/Memory ranges is removed/gated before becoming production logic. The Camel/Movies event-data replacement contains the above Winning-Monks typo but otherwise updates the relevant runtime event callbacks and XML identifiers together; later resource/map audits cover the current Camel distribution/usage architecture.
  Practicals 4830/4826 are water-tech/building/event transition work; their sole runtime Python edits track the corresponding renamed/reordered tech/unit/event identifiers and current keys resolve. Practicals 4821/4819/4808/4806/4805/4804 are primarily Sevopedia presentation/config additions around Great-People modifiers, civ/building/unit panels and optional category icons, plus matching data balance changes. Their surviving UI descendants have since been centralized/refactored and no wrong info-ID/widget target or missing runtime identifier attributable to this birth tranche survived review.
  Certified cursor advances through practical 4804 / 0480b0f317 as runtime-source SHA 1,124 / 1,317. Confirmed archaeology totals remain 118 = 40 inherited + 78 SAS-specific. Next runtime-source SHA: practical 4795 / 002bf7413c.

SAS-C148 - practical 4795 through 4755 first-generation Sevopedia relationship-panels / Grapes-Elephants migration audit - COMPLETE; KI#321 FOUND
  Reviewed 18 runtime-source SHAs: practical 4795 / 002bf7413c, 4794 / e5c1d1e2e7, 4793 / bc615d9813, 4792 / 3cb4f6faf9, 4791 / de2f392218, 4790 / 709efac6e5, 4786 / c61f80654d, 4784 / cfb67ec343, 4776 / b841968682, 4775 / 4cbe67a509, 4769 / 10b06b59d6, 4768 / ea75187aa4, 4765 / 33fc0afda9, 4764 / e82115dc5, 4763 / 279ab5cd10, 4760 / 244a5a501d, 4756 / a948fbc95e and 4755 / adf53965fa. Intervening practicals without primary game-executed C++/Python are not counted. One shared current Sevopedia misinformation root is promoted; the remaining relationship/layout/rename work closes clean-current or as explicit presentation policy.
  KI#321 is a SAS-specific Sevopedia exact-unit/building civilization-availability misinformation defect. Practical 4755 introduces SevoPediaUnit.placeExclusiveCivs with an explicit branch saying that when the selected unit is the class default, it should show "Available to all civilizations" instead of enumerating civilization mappings. Practical 4764 copies the same architecture to Buildings. Current descendants are named placeCivilizations but retain the same default-vs-unique split and current localization renders the default branch simply as "All". That is false for the exact item displayed on the page: CvPlayer::canTrain rejects a UnitTypes whenever getCivilization().getUnit(unitClass) != that exact unit, and CvPlayer::canConstruct analogously rejects a BuildingTypes whenever the civilization's class mapping is not that exact building. Current CivilizationInfos contain 135 mappings that differ from the class default or are NONE. Concrete ordinary examples include Rome mapping UNITCLASS_SWORDSMAN to UNIT_ROMAN_LEGIONARY instead of UNIT_SWORDSMAN and America mapping BUILDINGCLASS_COURTHOUSE to BUILDING_AMERICAN_FEDERAL_COURTHOUSE instead of BUILDING_COURTHOUSE. Thus the base Swordsman/Courthouse pages can say "Civilizations: All" even though those civilizations cannot train/construct the exact base item. Barbarian NONE mappings make the literal "All" claim still broader. Preferred repair is to use the same exact class-mapping enumeration for default and unique items; display civ buttons whose getCivilizationUnits/getCivilizationBuildings mapping equals the page item, and only display "All" when every civilization in the intended display population truly maps to that exact item. Unit and Building are one shared root rather than two KIs because the later Building code is a direct copy of the same panel contract.
  Practical 4784's Free With relationship logic was exhaustively compared against current CvPlayer::processBuilding semantics and current Civilization/BuildingClass mappings. For every current provider/recipient pair, the panel's default-vs-unique visibility rule matches the exact building the engine grants from the provider's FreeBuildingClass; no generic/unique-building mismatch was found. Current free-building providers include Stonehenge->Monument and the SAS Barbarian provider buildings, and the UI relation matches their actual civilization-resolved recipients.
  Practicals 4790/4791's first-generation Building Stats grid was reproduced against every current BuildingInfo. Despite the old warning that the fixed nine-cell layout could overflow, the current maximum is six displayed Stats cells (Barbarian Palace), so no current capacity failure exists. The direct yield, yield-modifier/power, commerce, happiness/health and Great-Person cells follow their intended Pedia data sources; later descendants substantially beautify/refactor the layout without revealing a hidden current omission attributable to this birth tranche.
  Practical 4775 correctly repairs SevoPediaUnit's prerequisite-religion button from a help-only religion widget to WIDGET_PEDIA_JUMP_TO_RELIGION; current routing retains the proper Pedia-jump contract. Practicals 4769/4768's Grapes/Winery and Elephant/Ivory identifier migrations were checked against current runtime/XML references: no live BONUS_WINE, IMPROVEMENT_WINERY or BONUS_IVORY identifier remains (the old WINERY wording survives only inside historical/localization text where applicable). The Wining Monks event is migrated to BONUS_GRAPES/FARM coherently; its later Camel one-commit typo is the separately recorded 4832->4833 lineage.
  Practical 4756 comments out the older CvGameTextMgr unit-help "Replaces" text because the new dedicated Sevopedia relationship panels now carry that information. This deliberately reduces generic setUnitHelp prose but does not alter trainability/upgrades and current Sevopedia has explicit Replaces/Replaced By panels; it is retained as presentation policy rather than promoted as a correctness defect. Practical 4793's temporary button-path validator and the surrounding helper moves/inlinings are later refactored away and do not leave a current callback or fatal-path survivor.
  Certified cursor advances through practical 4755 / adf53965fa as runtime-source SHA 1,142 / 1,317. Confirmed archaeology totals become 119 = 40 inherited + 79 SAS-specific. Next runtime-source SHA is below practical 4755 and will be identified before the next tranche is opened.

SAS-C149 - practical 4751 through 4740 Great-Person identifier / early Sevopedia Civilization-Unit-Religion / Daoism-Paganism migration audit - COMPLETE; no new defect
  Reviewed 9 runtime-source SHAs: practical 4751 / 277746f415, 4750 / 701e2e3127, 4747 / 547f102273, 4746 / 5db8b73f51, 4745 / f02bf0b2fa, 4744 / bb3459e524, 4743 / 72338fcc2b, 4742 / 7b3d0a3906 and 4740 / 50e7c8749e. Intervening practicals 4749/4748/4741 are XML/art/data-only in the frozen runtime-source denominator and 4739 is documentation-only. No new current KI is established; the tranche is coherent current architecture plus data migrations, with later descendants covering the few historical presentation/compatibility surfaces.
  Practicals 4751/4750 complete the Great-Person naming normalization across GPUtil, UnitAITypes, UnitClass/Unit XML, C++ AI switches and Python enum exposure. Current executable/runtime XML contains no stale UNITAI_PROPHET/ARTIST/SCIENTIST/GENERAL/MERCHANT/ENGINEER or old Great-Person UnitClass identifier; enum order is retained while names change, so serialized numeric UnitAI state is not reordered by this migration. The current Great-Person defaults/allowed UnitAIs, C++ dispatch/value/disband/domain handling and GPUtil constants all use the renamed UNITAI_GREAT_* family consistently. No partial rename survivor was established.
  Practical 4747 births the Unit Pedia Replaced By panel. Its base-unit branch lists other non-graphical UnitInfos sharing the selected UnitClass, while a unique-unit page points back to that class's default unit. This was checked separately from KI#321: current XML has no non-default UnitInfo whose class replacement is unmapped by every civilization, so the panel does not currently advertise a class replacement that no civilization actually uses. Its class relationship is therefore coherent with current data. The commit's removal of duplicate generic setUnitHelp Replaces/Replaced-By prose is intentional presentation migration to the dedicated panel, not trainability logic.
  Practicals 4746/4745/4744 are first-generation Sevopedia layout/civilization presentation work and close clean-current. 4745 prevents excluded generated-data leaders (Barbarian/Defaults) from indexing the old AI-Personality cache and therefore repairs the observed KeyError rather than creating a current exclusion leak; the modern AI-Personality pipeline no longer depends on that generated leaders_data architecture. 4744's Cities panel enumerates the civilization's own CityNames data and later UI descendants retain/refine the feature. No wrong entity/widget routing or current data overflow attributable to these commits was established.
  Practical 4742 replaces the older Religion Buildings panel and adds a Religion Units panel. Current descendants still relate a Religion page to BuildingInfo prerequisite/religion/state-religion fields and UnitInfo prerequisite/religion/state-religion fields. The imported code reads GlobalReligionCommerce but historically does not use it; current data masks that apparent omission because every current shrine with GlobalReligionCommerce also carries matching ReligionType/PrereqReligion and is therefore already included. No current religion-linked building/unit omission was proven. The panel's jump widgets use the corresponding Building/Unit IDs.
  Practicals 4740/4743 perform Taoism->Daoism and Confucianism->Paganism plus Moctezuma identifier migrations across the live XML/art/audio/text/C++ surfaces. Current runtime source/XML has no stale executable RELIGION_TAOISM/TAOIST, CONFUCIAN religion/building/unit, or MONTEZUMA identifier; old strings survive only in explanatory comments where expected. The then-CvCity::read shrine-name compatibility list is renamed in lockstep and is later removed from current SAS, so it does not leave a present lookup failure. Cross-version save compatibility is not a supported SAS contract in any case. The migrations therefore close as intentional data replacement rather than a current missing-reference root.
  Certified cursor advances through practical 4740 / 50e7c8749e as runtime-source SHA 1,151 / 1,317. Confirmed archaeology totals remain 119 = 40 inherited + 79 SAS-specific. Next runtime-source SHA: practical 4738 / 7e7ab111c1.

SAS-C150 - practical 4738 through 4732 first-generation AIP normalization/config/generated-cache consolidation audit - COMPLETE; KI#322 FOUND retrospectively in current shared predump/runtime descendant
  Reviewed 7 runtime-source SHAs: practical 4738 / 7e7ab111c1, 4737 / ca48d0b889, 4736 / 64a633b346, 4735 / 000be3f76c, 4734 / 01d6b62db5, 4733 / a7bba2fed9 and 4732 / 50f7983166. Practical 4731 is documentation-only and is not counted. The historical normalization/shifting/config consolidation itself closes as replaced/refactored lineage; one current cross-runtime AIP normalization defect was discovered while tracing its descendant contract and is promoted as KI#322.
  Practicals 4734/4735 simplify the early normalizer to the mathematically equivalent shift `(0, value-min, max-min)` before scaling to 0..100. Their sanity tests and generated expected-data samples preserve the intended mapping, and that shift helper survives conceptually in current AIP code. Practical 4736 primarily reorganizes displayed attitude fields/headers and fixes explanatory normalization comments; 4737 adds the few varying attitude-change-limit fields with per-field inversion where lower negative limits represent stronger aversion. Later AIP revisions substantially replace these early display configurations and direct getter exposure, so no current omitted/duplicated field root is attributed to the 4736/4737 presentation state.
  Practical 4732 is the first large consolidation into shared AIP helpers plus generated `leaders_data.py`; 4733 removes/deprecates several inaccurate/obsolete aggregate experiments. That generated-data architecture is later replaced by direct CvLeaderHeadInfo getter providers (notably the practical-4835 lineage) and then by the current shared runtime/workflow provider architecture. Current independent `aip_predump_values.py` validation on the supplied tree compares 7,810 effective displayed tuples against the committed predump with 0 ordinary mismatches and 0 missing/unparsed entries. Thus stale generated leaders_data or early aggregate formulas do not survive as the current issue.
  KI#322 (SAS-specific, retrospective current descendant) is a Python-version-dependent AIP normalization rounding defect. Current `ai_utils_shared_with_civ4.py` deliberately serves both the Civ4 runtime and outside-Civ4 workflow Python, but `round_current_runtime(value)` is implemented as `int(round(value))`. Civ4's documented embedded runtime is CPython 2.4, while the predump/checker script is Python 3. Python 2.x rounds exact half ties away from zero; Python 3 changed exact half ties to nearest-even. Therefore the helper does not have shared semantics at `.5` ties even though both consumers call the same source function.
  The defect is directly reachable through the supported manual/runtime fallback: current `SAS_SEVOPEDIA_LEADER_AI_PERSONALITY_CACHE_USE_PREDUMPED=0` bypasses the committed predump and calls the shared cache builder inside Civ4; documentation also retains this as the manual in-game regeneration path. Rebuilding the exact current XML+UWAI cache with the normal Python-3 rounder versus Python-2-style half-away rounding changes 30 displayed tuples: 21 leaders' `iAggregatedContactProbReligionPressure` normalize from committed/predump 52 to in-game-runtime 53 (raw 52 over range 10..90 gives exactly 52.5), and 9 leaders' inverted `getLimitedWarRand` normalize from 88 to 87. The current checker reports 7,810/7,810 matches because it and the committed predump both execute on the Python-3 side, so it cannot detect this runtime-fallback divergence.
  The cross-runtime mismatch crystallizes with practical 5973, which adds the outside-Civ4 Python-3 predump checker/writer and refactors the shared normalizer. Its comment explicitly preserves the committed contact value 52 rather than half-away 53 by using built-in `round()`, but that assumption describes Python-3 behavior, not the Civ4 Python-2.4 runtime. Preferred repair is to define the chosen tie rule explicitly rather than call version-dependent built-in round. To preserve current committed predump values and minimize UI churn, implement deterministic ties-to-even in a Python-2.4-safe helper used by both runtime and workflow; alternatively choose half-away everywhere and intentionally regenerate the predump, but do not mix the two contracts.
  Certified cursor advances through practical 4732 / 50f7983166 as runtime-source SHA 1,158 / 1,317. Confirmed archaeology totals become 120 = 40 inherited + 80 SAS-specific. Next runtime-source SHA: practical 4730 / 3101a1efff.

SAS-C151 - practical 4730 through 4700 early AI-Personality aggregate/percentile/cache experiment audit - COMPLETE; no additional current defect
  Reviewed 29 runtime-source SHAs: practical 4730 / 3101a1efff, 4729 / c1d261ce9b, 4728 / edbb097f4d, 4727 / b6fcb3d5a2, 4726 / be4eaacb8e, 4725 / 18f9df6ec6, 4724 / f0497a4f77, 4723 / 266d7590cb, 4722 / f4f0eab2eb, 4721 / 0387bf15f1, 4720 / 0f70e37a6a, 4719 / 65357eb9e9, 4718 / a087e8f794, 4717 / 49cc307ad4, 4716 / bf8764cb33, 4714 / 4e5a8076c7, 4713 / 9b7a6735ce, 4712 / 6985294c5d, 4711 / e5ede647e1, 4710 / 1c1bd356d6, 4709 / 98548c0bed, 4707 / 04c2d5b3d3, 4706 / 654441ef71, 4705 / 5257f49065, 4704 / c9fcdad590, 4703 / c59c8dc78a, 4702 / 29d54e506d, 4701 / 6cd58d51cd and 4700 / 448b0c440d. Practical 4715 and 4708 are documentation-only. No additional present-day defect root survives this experimental lineage beyond KI#322 already promoted at C150.
  Practical 4703 births the first synthetic AI-Personality “aggregate” traits by min/max-normalizing selected raw LeaderHead fields and averaging them. The following commits rapidly iterate labels, symbols, layout, weights/inversion, percentile/median calculation and caching. These early synthetic traits are explicitly experimental: 4724 removes broad/global inversion in favor of per-field/per-aggregate semantics, 4719 switches one generation to median-based distribution, and 4720/4721 then substantially rework the aggregate calculation/config split. Practical 4732 above this tranche replaces/consolidates the lineage into generated-data/shared-helper architecture and 4733 explicitly deprecates inaccurate/obsolete aggregate files. The old Aggressive/Sneaky/Builder/etc. formulas therefore are not a second hidden current execution path.
  Practical 4723 contains a genuine historical robustness repair: old percentile/aggregate arithmetic could produce values slightly above the intended 0..100 display range (the commit's concrete example is 100.4 rounding to 101), so mean/median output is clamped to 100. That implementation is later superseded; current normalize_to_100 performs explicit range checks and creates 0..100 values from bounded min/max inputs. No current >100 aggregate survivor is established.
  Practicals 4716 and 4713 introduce progressively broader aggregate/min-max caching to avoid recomputing all-leader distributions during each Leader-page render. Leader XML/personality inputs are static during a running game, so the cache lifetime is compatible with the data contract; the architecture is later superseded by generated/predumped cache systems and finally the current provider/shared-helper pipeline. Practicals 4707/4706 similarly move constant category/aggregate structures out of render-time functions without changing their then-current values.
  Practicals 4729/4728/4714/4712/4711/4710/4709/4702 and neighboring commits are mainly scale, label, spacing, category and legend evolution. Their current descendants have already been audited from the newer side through the later resolution-aware AIP layout/category work; no stale panel field, overflowing old scale or active duplicate category attributable to these first-generation layouts survives current source.
  The current AIP direct-value/predump layer is not certified merely because these ancestors were experimental: C150 separately validated the current 7,810-tuple predump against effective XML/UWAI values and promoted KI#322 for the one proven Python-2.4/Python-3 normalization divergence that the same-runtime checker cannot see. No second survivor was established from the obsolete early aggregate formulas themselves.
  Certified cursor advances through practical 4700 / 448b0c440d as runtime-source SHA 1,187 / 1,317. Confirmed archaeology totals remain 120 = 40 inherited + 80 SAS-specific. Next runtime-source SHA: practical 4699 / bd42f5ad49.

SAS-C152 - practical 4699 through 4679 AI-Personality-panel birth / early two-table and Sevopedia-width audit - COMPLETE; no new current defect
  Reviewed 15 runtime-source SHAs: practical 4699 / bd42f5ad49, 4698 / 422cbb2459, 4697 / 9cfa2033ac, 4696 / 8f9facf7c6, 4692 / 4ae72388bf, 4691 / 826110e635, 4690 / 7ae0669ba4, 4689 / c740bcfb1f, 4688 / f7ddc0e669, 4687 / e1b7136cbd, 4685 / 18a12dbfa4, 4684 / 9682cf294a, 4683 / e498384f36, 4680 / 21cadeb89a and 4679 / ab03b41277. Practicals 4695/4694/4693/4686/4682/4681 do not touch primary runtime C++/Python and are not counted. This closes the executable birth of the Leader AI Personality panel itself; no new survivor beyond the later KI#322 normalization descendant is established.
  Practical 4679 introduces the first functioning Leader-page AI personality block, and 4680/4683 rapidly expand the raw LeaderHead fields shown. Practicals 4684/4685 move from simple plus markers into colored/table presentation, with 4685 explicitly losing scrolling; 4696/4697 answer that limitation by splitting the data into two panels/tables and 4698/4699 add spacing/category structures. These are direct presentation iterations rather than independent current code paths. Current AIP layout/category descendants were separately audited from the newer side (including the 5562-5567 and 5633-5635 resolution/font/category lineages), so the historical no-scroll/trim/spacing limitations do not survive current UI.
  Practicals 4687/4688 heavily reorder/refactor SevoPediaLeader and touch neighboring Feature/Terrain/UnitChart presentation while preserving the then-current data sources. Subsequent commits above this tranche replace the ad-hoc raw-field/category structures with cached/generated/shared AIP configuration and later direct getter providers. Current AIP effective values are independently covered by the 7,810-tuple predump check recorded at C150; no stale first-generation index/getter or active duplicate panel list from 4679-4699 was found.
  Practicals 4690/4691/4692 adjust Sevopedia category/item widths to reclaim horizontal room for the larger Leader page. Later centralized/font-aware width systems supersede these fixed values; newer archaeology already covers their current descendants and the later wide-screen cutoff defect separately. No current category clipping or mismatched Leader-only coordinate invariant can be traced uniquely to these initial width commits.
  This birth tranche is therefore historically useful primarily as provenance: 4679 is the AI Personality panel's executable origin, 4703 above it births the first synthetic aggregates, 4732 consolidates the generated/shared architecture, 4835 later replaces generated leaders_data with direct getter-based cache construction, and 5973 creates the current outside-Civ4 predump/checker sharing that exposes KI#322's Python-version rounding mismatch. The panel origin itself adds no second surviving correctness root.
  Certified cursor advances through practical 4679 / ab03b41277 as runtime-source SHA 1,202 / 1,317. Confirmed archaeology totals remain 120 = 40 inherited + 80 SAS-specific. Practicals 4678 through 4648 are data/art/docs-only in the frozen denominator; next runtime-source SHA: practical 4647 / 171b6d8f31.

SAS-C153 - practical 4647 through 4628 Unit-Chart / imported Terrain-Feature Pedia / NewConcept repair audit - COMPLETE; KI#323 and KI#324 FOUND
  Bookkeeping correction before the semantic results: C152's final next-SHA line accidentally wrote practical 4647 / 171b6d8f31. The local reachable-history mirror and diff header identify the actual canonical commit as practical 4647 / baa641079b. This is only a short-SHA handoff typo; the practical cursor and 1,202/1,317 count were correct, and no commit was skipped or double-counted.
  Reviewed 14 runtime-source SHAs: practical 4647 / baa641079b, 4646 / f9d9c595d7, 4645 / ac4bc48449, 4644 / 99d3aae70e, 4643 / afb1a9044f, 4642 / 6c107d6fd8, 4641 / 18dc45fc3b, 4640 / f0f55128ea, 4639 / 10c14b0736, 4638 / 2dd4efa3da, 4637 / ce3f71f509, 4634 / 201500d338, 4633 / 3982fb2cde and 4628 / 1bb92cbeee. Practicals 4636/4635 and 4632 through 4629 are XML/text/music/docs/art only and are not counted. Two current Sevopedia Unit Chart misinformation roots are confirmed: inherited RFC/DoC KI#323 and retrospective SAS-specific KI#324.
  KI#323 (inherited third-party, RFC Dawn of Civilization) is the Unit Chart's incomplete First Strike column. Practical 4637 imports RFC Dawn of Civilization's CvPediaUnitChart.py nearly verbatim; the upstream RFC source itself tests and prints only UnitInfo.getFirstStrikes() and never considers getChanceFirstStrikes(). SAS practical 4639 splits First Strike into its own column, 4644 moves the logic into placeTableFirstStrike, and later descendants preserve the same guaranteed-only test. Current source still renders a value only when getFirstStrikes() > 0. This conflicts with Civ4's actual first-strike contract: CvGameTextMgr combines getFirstStrikes()+getChanceFirstStrikes() and renders a guaranteed-to-maximum range whenever chance first strikes exist. Current SAS XML has 14 units with first-strike data. Samurai and Cacador have 0 guaranteed + 1 chance first strike, so the Unit Chart currently shows a blank First Strike cell despite real first-strike capability; Archer, Longbowman, Skirmisher, Bowman and Zhuge Nu have 1 guaranteed + 1 chance and the chart shows only 1 instead of the real 1-2 range. Preferred repair: blank only when guaranteed+chance is zero; otherwise show the guaranteed count when chance is zero and a guaranteed-(guaranteed+chance) range when chance is nonzero, matching the DLL help semantics.
  KI#324 (SAS-specific, retrospective descendant born above this historical cursor) is the Unit Chart's false Collateral display gate. The RFC/4637 import correctly shows collateral only when getCollateralDamage() > 0. Practical 4900 / 56ea046e0d changes that gate to `getCollateralDamage() > 0 or getCollateralDamageLimit()` while changing the cell format from damage-limit to damage/limit. In current Unit XML, iCollateralDamageLimit defaults to 200 even for units with no collateral attack. Across chart-visible units with a real UnitCombat type, 76 have CollateralDamage=0 but a nonzero 200 limit, while only 18 have actual nonzero CollateralDamage. Consequently ordinary units such as Warrior, Swordsman, Samurai, Rifleman, Tank, Frigate and Fighter can be shown as `0%/200% (...)` collateral units even though they inflict no collateral damage. The engine's operative predicate is the nonzero damage value; the surgical repair is simply to restore the original `getCollateralDamage() > 0` display gate while retaining the newer useful damage/limit/max formatting for genuine collateral units. C144 previously certified practical 4900 without noticing this chart/data interaction; C153 records the retrospective correction openly rather than hiding it.
  The remaining 4647-4637 Unit Chart evolution is presentation/refactoring around the imported table: separate Moves/Air Range, Bombard, Evasion and Interception columns, dynamic air/non-air widths, icons, background and per-cell helper extraction. Current Combat uses AirCombat when appropriate, Bombard distinguishes BombardRate/BombRate, air columns use their dedicated getters, Withdrawal uses WithdrawalProbability, and no additional current data/column contract failure was established after checking these paths. KI#323 and KI#324 are independent because one concerns missing chance-first-strike information in the inherited chart and the other is a later SAS-only collateral gate regression.
  Practicals 4634 and 4633 import RFC/DoC Feature and Terrain Sevopedia pages. Their birth relationship panels use FeatureInfo.isTerrain, ImprovementInfo.getFeatureMakesValid/getTerrainMakesValid/isHillsMakesValid and BonusInfo isFeature/isTerrain/isHills predicates consistently with the XML relationship data. Much newer descendants substantially expand these pages (build/remove production, build-capable units, resources, improvements and terrain/feature combat/traversal panels) and were already audited from the newer side around practicals 5459 and 5062/5061. No separate surviving birth-import defect was established here beyond those already-accounted later lineages.
  Practical 4628 is itself a repair: the earlier Sevopedia navigation assumed NewConceptInfo entries belonged to the ConceptInfo list/category ordering. 4628 restores the dedicated NewConcept list/category route. Current centralized category/list infrastructure supersedes this first repair and no stale Concept-vs-NewConcept index mismatch attributable to it survives.
  Certified cursor advances through practical 4628 / 1bb92cbeee as runtime-source SHA 1,216 / 1,317. Confirmed archaeology totals become 122 = 41 inherited + 81 SAS-specific. Practicals 4627 through 4610 are XML/text/docs/data-only for the primary runtime denominator; next runtime-source SHA: practical 4609 / 3cba4085d2.

SAS-C154 - practical 4609 through 4455 military-overhaul merge / first-generation Bonus-Improvement Sevopedia layout audit - COMPLETE; no new current defect
  Reviewed 38 runtime-source SHAs: practical 4609 / 3cba4085d2, 4608 / 612c8ec52f, 4606 / 8a03454195, 4605 / 4f283385cc, 4603 / 233a2d479b, 4602 / 280ba37fb9, 4592 / 87485edcea, 4590 / c2488bd788, 4518 / 81cfb62987, 4517 / af567fabc9, 4516 / c8c74ed621, 4515 / d426be718b, 4514 / 1e866ea3a2, 4513 / 3ff3402656, 4512 / 391913e624, 4511 / ef1bd835b0, 4510 / bca4087af6, 4509 / 3a6753c687, 4508 / 77c55bf355, 4507 / 1d6ee19d2c, 4506 / 9174b0b69f, 4505 / 3aada79aa5, 4504 / 73ddf5f120, 4503 / 6b2e69f9cb, 4502 / dc1f7c9812, 4500 / 61daaaf773, 4499 / 78ac5098e6, 4496 / 550dfd1095, 4494 / 7be870dd18, 4491 / 1384c48419, 4489 / 919a0918f4, 4488 / 010c414e03, 4487 / 64486bdcf8, 4471 / c1b1160372, 4470 / 6937a460a8, 4458 / 743858f79e, 4456 / c8661f96fb and 4455 / f96e016aa0. The large practical-number gaps are XML/data/art/docs-only commits and do not consume the primary runtime-source denominator.
  Practical 4609 is a merge integration point rather than a second implementation of the military-overhaul branch. Git metadata gives parents 440b08e86f and 612c8ec52f, and the merge tree SHA is exactly the same f61338449c tree as second parent practical 4608. Thus there are no hidden merge-resolution source edits beyond the already-audited side branch; the large first-parent diff is simply the branch content entering mainline. Practical 4608 and 4603 change executable-file comments/formatting only. Practicals 4602/4605/4606 progressively split the old Archer feat into Bow Short, Bow Long and Crossbow feat enums and keep CvEnums.h, CyEnumsInterface, CvAdvisorUtils popup creation and CvScreensInterface click routing synchronized. Current feat enum ordering, Python exposure and UnitCombat XML remain aligned. The repeated >= BowShort / >= BowLong / >= Crossbow click predicates are redundant because the first range already covers later feat IDs, but they do not alter routing or create a correctness failure.
  Practicals 4592 and 4590 are early Sevopedia fixed-margin/category-label tuning. 4592 removes top/bottom/horizontal margins to reclaim the whole screen; 4590 changes Concept category labels and wrapping thresholds. The current resolution/font-aware centralized Sevopedia layout and category infrastructure supersede these literal values. No current clipping, category-ID or navigation failure is uniquely inherited from these early settings.
  Practicals 4518 through 4470 are predominantly history-text height/padding and the first large Resource/Bonus page layout rework. The semantic panel split itself is coherent in its final 4512+ form: Revealed By uses BonusInfo.getTechReveal; Tradeable Since/Enables uses getTechCityTrade; allowed Units test AND/OR prerequisite bonuses plus SpeedBonuses; Buildings/Projects use their bonus prerequisite/modifier relations; the Effects panel delegates to CyGameTextMgr bonus help. Practical 4511 has a real one-commit presentation mistake: the newly duplicated Enables panel initially repeats both TechReveal and TechCityTrade, so Revealed/Trade technologies appear twice. Practical 4512 immediately removes TechReveal from the new panel and relabels the two halves, and 4513/4515 finish the Revealed By naming. Current source retains the separated contracts, so this is repaired historical lineage rather than a current KI.
  The early Bonus/Improvement yield panels were followed into their current descendants instead of certifying their old labels literally. Current SevoPediaBonus.placeImprovements intentionally displays the total base Improvement yield plus the selected Bonus-specific extra only for improvements with a nonzero ImprovementBonusYield relation, matching its current Total Base and Extra Improvements Yield Changes label. Current SevoPediaImprovement.placeBonusYields intentionally displays only the Bonus-specific extra yield, matching its own Bonus Yields role. The 4500-4505 label/formula churn therefore does not leave a current double-count/missing-base mismatch. No current bonus/improvement relationship predicate was found to omit a live XML relationship.
  Practicals 4489/4488/4487/4471/4470 are presentation padding/history-header changes; later font-aware layout descendants supersede the literal coordinates. Practical 4456 deliberately removes the old Sevopedia civilization-list filter that hid non-playable/minor civilizations; current SAS intentionally exposes minor/hidden civilization data for Pedia/debug usefulness, so this is not mistaken trainability logic (exact-item availability is separately covered by KI#321). Practical 4455 deliberately hides Strategy prose from several Pedia pages because those historical strings are often stale; it does not alter game mechanics or the dedicated effect/relationship panels.
  No new present-day defect is established in this 38-SHA tranche. Certified cursor advances through practical 4455 / f96e016aa0 as runtime-source SHA 1,254 / 1,317. Confirmed archaeology totals remain 122 = 41 inherited + 81 SAS-specific. Practicals 4454 through 4429 do not touch primary runtime C++/Python; next runtime-source SHA: practical 4428 / 511c696fcd.


SAS-C155 - denominator reconciliation + practical 4428 through 4403 upstream-1.12 reconciliation audit - COMPLETE; no new current defect
  Bookkeeping correction: the original 1,317 primary-runtime denominator is superseded. A fresh mechanical classification of all 1,825 SASBranch SHAs reachable from the frozen starting boundary (practical 6168 / cf1272d7fe) using this tracker's stated source scope yields exactly 1,292 primary game-executed source SHAs, 20 developer/CI/helper-Python-only SHAs, and 513 other non-primary-runtime SHAs. The historical archaeology itself is not discarded: overcounted XML/data-only commits were still inspected, but they must not consume the primary source progress meter. Older checkpoint counters remain useful historical annotations but are superseded by the mechanically recomputed current cursor.
  The same inventory gives exactly 65 primary-runtime SHAs below and including practical 4455. Therefore C154's semantic endpoint at practical 4455 corresponds to corrected source cursor 1,227 / 1,292, not 1,254 / 1,317. Seventeen additional primary-runtime SHAs are certified here from practical 4428 through practical 4403, advancing the corrected cursor to 1,244 / 1,292 and leaving exactly 48 primary-runtime SHAs below 4403.
  Reviewed 17 primary-runtime SHAs: practical 4428 / 511c696fcd; 4419 / b358fae053; 4418 / 8a99ee0dc0; 4417 / 39eab36784; 4416 / 153bab2691; 4414 / 8e07e207e1; 4413 / ed82efc3b2; 4412 / ff73bdf88e; branch-side practical 4412 / 0d81edb644; 4409 / 3cee04f381; branch-side 4408 / 797c0200f5; 4408 / 607837e3b1; branch-side 4407 / e16e275da5; 4407 / ff0662a208; 4406 / aa81ff32c2; 4405 / 7b9eeda893; and 4403 / 34522d313b.
  Practical 4428 closes the short-lived SAS free-tech-at-all-eras experiment. The branch had commented out AdvCiv's later-era-free-tech gate; 4428 removes that comment block and restores `getTechEra > getStartEra -> continue`. Current initFreeCivState retains the restored era gate, so the experiment is historical policy lineage rather than a survivor.
  Practicals 4419, 4418, 4416, 4414, 4413 and 4412/ff73 are upstream repairs carried safely into current source: 4419 corrects the reversed production-rank comparison in K-Mod Wonder evaluation; 4418 restores the missing `bCultureBuilding` gate around the emergency culture-building choice; 4416 uses upward-rounded divisions in AI_ejectBestDefender to prevent low effective-strength candidates collapsing to zero through integer truncation; 4414 removes an impossible NULL check inside the unit iterator while preserving the actual selected/non-settler predicate; 4413 removes the BtS Go-To founding-border update path that leaked rival-city information through Fog of War; and 4412/ff73 counts alive Minor/Barbarian players in shared water-area Work Boat totals. Current AdvCiv 1.14 and current SAS retain these corrected forms.
  Practical 4417 is deliberate upstream production tuning rather than an invariant repair: when floating defenders are below target, it now respects the unit-spending cap unless the player is at land war, has fallen below 72% of the required floating-defender count, or remains within the allowed spending margin. The outer `total < needed` condition guarantees a positive need; no divide-by-zero/index/legality path is introduced, and current AdvCiv 1.14 retains the same policy.
  Branch-side practicals 4407/e16e275da5, 4408/797c0200f5 and 4412/0d81edb644 carry the same upstream-1.12 runtime patch through successive merge/update points. After removing archive coverage metadata, their primary runtime hunks are byte-identical. Auditing them once by executable content therefore covers all three ancestry nodes without pretending that the repeated first-parent patch is three independent implementations. Their changes are upstream 1.12 fixes/refactors later represented by the final 4403 reconciliation and by current AdvCiv descendants.
  Practical 4409 increases reparations demanded from a human who can afford substantially more than the initially computed peace value. The branch is reached only for positive `r`, recomputes a capped maximum payable amount, and applies a bounded multiplier; the same formula survives current AdvCiv 1.14. No zero-divide, sign reversal or trade-legality bypass was established.
  Practical 4408/607837e3 changes PerfectMongoose comments only. Practical 4406 replaces repeated GetIndex/GetCirclePoint lookups with an expanded smoothing loop and list-backed temporary arrays, while practical 4407 introduces the later prefix-sum Smooth implementation and leaves the former method as Smooth_Legacy. These are historical performance lineage: the current smoothing implementation was changed again by the later upstream 1.14 work already independently audited at practical 6162. The distinct PerfectMongoose meteor-order defect KI#218 was born much later in another helper and was fixed at 6171; it is not duplicated here.
  Practical 4405 refreshes Globe-view option visibility when score strings change even if GlobeInfo itself is not dirty, preventing unit-layer options from overlapping displayed scores. It changes redraw triggering only and current UI descendants retain the corrected behavior.
  Practical 4403 / 34522d313b is a genuine two-parent `Merge 1.12 into 1.13`, not a tree-identical fast-forward-style merge. Its parents are the 1.13 side and updated 1.12 side. Comparison against the updated 1.12 parent shows only the DLL version-resource file differs; the game-executed source result therefore equals the already-audited updated-1.12 branch for this merge. Relative to the 1.13 parent, the merge imports the upstream repair bundle and a few targeted Python/layout fixes (including configobj's missing `self` and RectLayout.upperLeft returning PointLayout). No unique gameplay conflict-resolution defect was established.
  Confirmed archaeology totals remain 122 = 41 inherited + 81 SAS-specific. Corrected certified source cursor: 1,244 / 1,292 through practical 4403 / 34522d313b. Exactly 48 primary-runtime SHAs remain below this point. Next runtime-source SHA: practical 4399 / 30d8c34b80.

SAS-C156 - practical 4399 through 4345 final upstream/runtime tail + completion reconciliation - COMPLETE; KI#325 FOUND; primary SAS runtime-source sweep COMPLETE
  Reviewed the final 48 primary-runtime SHAs, exactly matching the mechanically reconciled remainder after C155: practical 4399 / 30d8c34b80, 4398 / 7f966ec9fb, 4397 / 6d00b3d838, 4396 / 5ae87c37c6, 4395 / e5b22895b0, 4394 / 1a3aea9ad0, 4393 / 13a7fe526c, 4388 / 0be952e406, 4386 / c2605f98d8, 4385 / 7476af975e, 4384 / 6078dcd29c, 4383 / 24dbe0256c, 4382 / cfa9e3cd2e, 4381 / 0cfbe91b71, 4379 / 9dbf3edafe, 4378 / 14d233db44, 4377 / 92556c71ff, 4376 / 4a31a6ffbc, 4373 / a61a76dc22, 4372 / 9be28b73b3, 4372 / 1fcdfcb6ef, 4371 / a64fd6fda3, 4370 / 9ac96050f6, 4369 / 8c6daa0e26, 4368 / c594f4c2ba, 4367 / cd021f7087, 4367 / a781a79362, 4366 / 0e59d066f9, 4366 / f6742abc2b, 4365 / 299d90a56a, 4364 / c9506f86f0, 4363 / 384d74b7ed, 4362 / 4392e52130, 4361 / 6c90a5a9cd, 4360 / 3e4d2ce5d3, 4359 / 6c4ac8f9ca, 4359 / b2708af7bf, 4358 / dd243872ff, 4358 / 30a79579f7, 4357 / 27bde47c8a, 4356 / 508a0a6b78, 4355 / 5a5a3551da, 4354 / c74d1d4f8e, 4353 / 0993575b56, 4348 / 5e269c6dd7, 4347 / 19208a3ce9, 4346 / 1c79c7c1fb and 4345 / e490c254d0. This exhausts the 1,292-SHA primary game-executed source inventory frozen at practical 6168.
  KI#325 is an inherited BtS city-name identity defect exposed cleanly while auditing practical 4345 / e490c254d0; practical 4345 broadens the defect but does not introduce its underlying root. Vanilla BtS `CvPlayer::getCivilizationCityName` already translates each canonical civilization city-name key through `gDLL->getText(...)` before calling `isCityNameValid`, and vanilla BtS `isCityNameValid` compares that translated `CvWString` directly against living cities' stored `getName()` strings. The earliest locally mirrored K-Mod boundary preserves the same contract, as do AdvCiv and current SAS. Current Options Python exposes live `CyGame().setCurrentLanguage(iValue)`. Therefore a canonical generated city name can translate to a different comparison string after a language switch and evade the living-city duplicate check even before the practical-4345 extension.
  Practical 4345 generalizes destroyed-city-name tracking into a global past-city-name list and adds a city's prior name through `GC.getGame().addPastCityName(getName())` before a rename. That stores the same display/localized identity in the new historical registry, while future automatic candidates continue to be translated afresh in the current language. The inherited language-sensitive identity defect is therefore extended from living-city uniqueness to the new promise that all former city names will no longer be reused.
  Reachability is proven with unchanged base data rather than hypothetical localization. Current SAS Germany still includes `TXT_KEY_CITY_NAME_AACHEN`; preserved BtS localization gives that exact key English `Aachen` and French `Aix-la-Chapelle`. A city created as Aachen in English can therefore remain stored as `Aachen` while, after switching to French, the same canonical candidate becomes `Aix-la-Chapelle` and no longer compares equal. The same mismatch affects both living-city duplicate prevention and, after practical 4345, past-city-name reuse prevention. This also illustrates why unchanged vanilla BtS source/XML/localization is useful as a separate provenance-control archive without belonging in the normal SAS light-source ZIP.
  Preferred repair direction for KI#325 is to separate stable generated-name identity from presentation. Preserve a canonical city-name key/identity when a city originates from an automatic civilization name and compare that identity for reuse prevention; retain literal strings for genuinely custom/player-entered names. A translation-dependent display string should not serve as the sole persistent identity.
  The remaining final-tail commits close as upstream repairs, historical policy or refactoring rather than additional survivors. Practical 4399 fixes Always-War willingness to talk incorrectly treating same-team players as permanent-war opponents. Practical 4398 orders upgrades by post-upgrade experience/discount rather than raw XP. Practical 4397 fixes queued Worker build activation after failed Route-To/Move-To so the build is not executed on the wrong plot. Practical 4396 suspends Culture-victory pursuit when an already-launched Space victory will arrive sooner. Practical 4395 fixes K-Mod Great Person mission movement bookkeeping. Practical 4385 guards team-leader reassignment when elimination leaves the would-be leader dead. Current descendants retain these repairs.
  Practical 4394 is important ancestry for existing KI#172: it corrects city-asset percentile interpretation to the direct contract that a larger percentile corresponds to a more valuable city. This is a provenance/contract refinement, not another defect count. Practical 4378's Advanced-Start border-expansion fallback is coarse in general form but exact for current data: Creative is the only trait with the relevant culture-commerce pattern and has the explicit free-city-culture effect, so no current trait loses intended expansion. Practical 4377 fixes three unused Random Event Python bindings. Practical 4372 / 1fcdfcb6ef fixes AI grouping across incompatible impassables. Practical 4371 fixes the ReligionCommerce parser target. Practicals 4370, 4369 and 4368 fix separate UWAI nuclear/cavalry/naval-forecast mistakes and survive in the supplied/current AdvCiv descendant.
  PerfectMongoose merge ancestry is now mechanically bounded. Practical 4373 / a61a76dc22 is the true upstream birth of existing KI#218: it changes the ordered meteor-circle boundary collection into an identity `set`, losing the endpoint pairing order the rasterizer requires. The primary-runtime PerfectMongoose patch carried at practical 4381 is byte-identical after diff metadata removal. Practical 5851 is therefore the SAS integration point, not the absolute birth; KI#218 remains a single inherited defect and was fixed at practical 6171. Practical 4382 is a separate map-edge hardening change and establishes no additional current failure.
  The Oxidized/repair merge topology also contains no hidden extra implementation. Practical 4366 / 0e59d066f9 is exactly the union of the individually visible 4355-through-4363 primary-runtime fixes, with zero additional executable changes. Practical 4376 / 4a31a6ffbc carries that repair family forward plus the two 4364 CvCityAI cleanup/destructor lines. This lets the individual parser/UWAI/city-evaluation repairs be audited once by executable content instead of double-counting merge copies. The branch-side practical 4367 free-tech-at-all-eras experiment is likewise historical only; its merge copy carries the same runtime patch and practical 4428 explicitly restored the normal era gate.
  The lower repair family remains clean-current: practical 4367 / a781a79362 fixes the ineffective jungle-removal candidate multiplier; 4366 / f6742abc2b restores low-priority nuke odds; 4365 fixes a wrong rival/owner variable in UN-building evaluation; 4364 closes a CvCityAI allocation leak; 4363 fixes nuclear-war party discovery; 4362 stops percentileRank from scrambling city asset inputs; 4361/4358 finish the UWAI border-dispute arithmetic repair; 4360 fixes first-colony state shadowing; 4359 restores away-supply cost and separately performs the std11->sequtil refactor; 4357 fixes ExtraSpecialistCommerce tech evaluation; 4356 fixes BBAI strategy-vs-victory-stage flag confusion; and 4355 fixes the Apostolic Palace building-variable mixup. Practicals 4354/4353/4348/4346 are comment/style/debug-local/future-proofing changes without a current correctness failure, and practical 4347 correctly repairs No-Tech-Brokering AI handling.
  Practical 4345 is also useful conceptual ancestry for existing KI#161.2: it is where the game broadens destroyed-name bookkeeping into all past city names; the much later 5981 placeholder/acquisition hardening addresses a different failure within that same broader naming lifecycle. KI#325 is independent because it concerns persistent identity across languages, not the acquisition-placeholder state.
  Final completion bookkeeping: primary SAS runtime-source sweep 1,292 / 1,292 through practical 4345 / e490c254d0. The original 1,317 denominator is permanently superseded by the C155 mechanical inventory reconciliation. The C156 handoff alone reached 128 = 43 inherited/pre-SAS + 85 SAS-specific; reconciling the three parallel KI#293.2/KI#294.2/KI#298.2 findings, rejecting KI#324 and adding the deeper KI#324.2 produces the current total of 131 = 43 inherited/pre-SAS + 88 SAS-specific. Current status after rejecting KI#321's first UI approach and fixing KI#323/KI#324.2/KI#315-KI#317: 89 fixed, 0 prepared, 1 broader repair intentionally pending (KI#213), 41 surgical repairs deferred. No primary runtime-source SHA remains OPEN. Optional follow-up work (the excluded 20 developer/CI/helper-Python SHAs, broader vanilla BtS/K-Mod provenance controls, or implementation/review of deferred findings) is outside the completed primary SAS source sweep and should use a separate counter if undertaken.

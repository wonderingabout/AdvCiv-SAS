Checkpoint 14 changelog state

Writing base: checkpoint 13, still comparing only last stable AdvCiv-SAS 6020 -> current release candidate.

New in checkpoint 14:
- Performed a complete marker-position audit over every post-6020 `Requires AdvCiv-SAS X+` Main Changes Guide bullet instead of treating the Worker example as unique. There are 12 post-6020 markers embedded mid-bullet, plus a separate post-6020 update bullet that intentionally reuses an older bold heading. Each was checked against the exact 6020 guide/diff.
- Corrected the BBAI logging summary: the dedicated Settler log category itself already existed in stable 6020; this release expands it with parking reasons, current danger context and city escort pools. Great General, war/UWAI, overseas-transport and military-production dedicated categories are genuinely new post-6020.
- Corrected the Foreign Advisor summary: the Active -> Treaties rename, icon beautification, and disconnected-network GPT display already existed in stable 6020. The genuinely new KI#241 change is that mixed resource/treaty or resource/GPT rows show every term affected by cancellation, while resource-only deals remain filtered.
- Clarified Great General wording so the changelog says the *existing* Military Instructor-first policy is now era-tunable; the older policy itself is not presented as a new release feature.
- Confirmed the other mid-marker cases were already represented correctly as stable-to-current deltas (future-BFC duplicate-resource valuation, Settler safety correction, Worker current-plot danger, long irrigation replacement, Great General safety/gating, terrain/feature rebalancing, and net civ-specific building/unit changes).
- Extended README_Release_Process.md with a required final mechanical marker audit, including marker-at-start `Update:` bullets that reuse an older heading.


New in checkpoint 13:
- Corrected the release-boundary extraction heuristic after user review: a `(Requires AdvCiv-SAS X+)` marker inserted mid-bullet normally marks only the later appended/edited clause, not the whole pre-existing bullet. Use commit diffs/stable-boundary file comparison to isolate the new text.
- Removed the broad `AI_bestCityBuild` work-selection-overhaul paragraph from the release changelog because that overhaul predates stable 6020; only genuinely post-6020 Worker changes remain (e.g. the long irrigation-route fix, Worker floor/logistics/danger/production changes).
- Added historical source archaeology near the top of the root README introduction, not only in the later detailed section, because it is now a significant project effort.
- Updated README_Release_Process.md with the marker-position heuristic so future LLM changelog extraction does not mistake an old edited bullet for a wholly new release feature.

New in checkpoint 12:
- Audited the AI body against the post-6020 Main Changes Guide rather than expanding it by instinct.
- Added a compact empirical victory-denial result: weak Lincoln previously won Space at T292 while at peace; after the fixes Persia/Egypt/France/Babylon declared T267-T269 and America was eliminated T272. Another confirming branch stopped four launched spaceships by capital capture while the strongest fifth contender still won, showing serious counterplay rather than guaranteed dogpiling.
- Added the actual soon-connectable-bonus city-site valuation rule: replace Base AdvCiv's blunt roughly 70% / 33% tiers with a tunable 15%-per-era penalty and no penalty when connectable in the current era.
- Expanded Worker danger evidence: comparable T200 Worker deaths in save 450 fell 40 -> 17 while current-plot danger remained respected.
- Added the missing post-6020 AI_bestCityBuild substance: cross-city best-job comparison, assigned-worker/one-worker-per-plot anti-crowding, more coherent bonus/feature-follow-up work, replacement-churn reduction, and Medieval-by-default lower-food replacement guard.
- Expanded long irrigation routing with the Mayapan example (dry Maize T121-T312 before; five-link route complete by T162 after).
- Added the isolated-city civilian-production safety gate and the food-production allocator's 45-turn Huamanga vs 3-turn Cuzco example plus full-run medians.
- Reframed the release overview archaeology paragraph as a first shipped tranche from a completed investigation, not as if all discovered defects were implemented already.
- Added a shorter player/modder-facing Historical source archaeology section to the root README.md and linked it from the root contents list, while keeping the more detailed methodology in Modding_Ressources/README.md.
- Added a dedicated Historical source archaeology section to Modding_Ressources/README.md. It links the tracker, explains the commit-diff-centered method, records current completed-sweep totals (131 = 43 inherited/pre-SAS + 88 SAS-specific; 84 fixed; KI#213 broader pending; 46 deferred), and distinguishes ChatGPT-5.6-Sol investigation from user + GPT-5.6-Sol (Codex) implementation/review/validation.
- Updated README_Release_Process.md so future release notes distinguish archaeology investigation completion from implementation completion and do not imply every finding was fixed.

Stable-to-release rule remains critical:
- Do not narrate temporary development states in the public changelog.
- Intermediate names/designs/values are archaeological evidence only.
- Describe a feature as NEW if it did not exist in 6020, even if renamed/reworked during development.
- For replacements/rebalances, compare only the 6020 shipped state with the final current state.

Current writing/publishing preference:
- The user may prefer to write or substantially rewrite the final release overview/introduction personally.
- LLM effort is most valuable on the body: recover meaningful shipped changes, final numerical values, documented rationale, AI evidence, fixes, UI/maps/tooling changes, and compact stable-to-current wording.
- Preserve useful substance first; the user can remove items later.
- CFC currently limits one post to about 50,000 characters; ModDB to about 100,000. Splitting or later trimming is acceptable.
- Final CFC-oriented copies should have no blank lines between individual items inside a section; the user's web HTML/list formatting supplies visual separation.
